# Widget behavior

This is a deliberately small, non-phase portability fixture for the generic verification engine.

## Public contract

Accepted names are `alpha` and `beta`. An unknown name returns `unsupported` without changing the
stored value.

## Internal notes

The representation is deliberately unspecified.

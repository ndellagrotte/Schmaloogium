# Phase 1 — architecture review 36

**Date:** 2026-09-08. **Frozen document:** `docs/phase1/v14/PHASE_1_DOC.md` SHA-256 `75402381a48acb6dc9c870dcab20384836d101746eef67cfcfb690f4985e5a21`. **Verdict:** PASS-WITH-CORRECTIONS (2 corrections, 2 notes, 0 blocking). **§5 impact:** neither correction touches a §5 export, a facade verb, or any receiver-consumed contract — both are documentation-ledger defects (a missing session-of-record for the newest bytes, and two stale RESEARCH.md line citations), so no downstream phase's consumed surface moves.

---

## 1. Scope, identity and authority actually verified

- **Hash recomputed before anything else.** `sha256sum docs/phase1/v14/PHASE_1_DOC.md` → `75402381a48acb6dc9c870dcab20384836d101746eef67cfcfb690f4985e5a21`, equal to the frozen assignment hash; `wc -l` → 6995 lines, matching the stated length. The frozen bytes were reviewed.
- **Governing design confirmed at the doc header.** The doc names RC2 as its authority (`docs/phase1/v14/PHASE_1_DOC.md:1542` "RC2 remains this phase's governing design", restated `:1642`). I read `docs/design/v2.0-RC2/DESIGN.md` Part I lines 76–946 in full (including §G1.2 verify protocol, §G2 D-1..D-10, §G4 contracts, §G9 template) and the Phase 1 spec at lines 957–1067, and checked the doc against both.
- **Complete reading list, actually read:** (a) the assigned phase doc, all 6995 lines in contiguous ranges; (b) the governing-design Part I and Phase 1 spec above; (c) `docs/research/v1/RESEARCH.md` lines 11–107 (§0–§1), plus targeted ranges cited by the conformance map for the spot-check (§3.1 `:204-233`, §4.1 step 1 `:476-478`, §3.5 feature flags `:424-433`, App A.1/A.3 `:1099-1173`, App F.6/F.7 `:1493-1524`); (d) dependency/consumer docs for interface honesty — `docs/phase5/v1/PHASE_5_DOC.md` §5 (`:2701-3033`), `docs/phase7/v1/PHASE_7_DOC.md` §5 rows and the H-ALPHA/H-BLEND catalog, `docs/phase10/v1/PHASE_10_DOC.md` §5 (`:1685-1909`), `docs/phase13/v1/PHASE_13_DOC.md` §5 (`:1655-1993`), `docs/phase14/v1/PHASE_14_DOC.md` §5 (`:1650-2046`), and `docs/phase2/v2/PHASE_2_DOC.md` consumption rows; (e) §0 headers and §11 section maps of the five phase docs via section-index grep.
- **Boundaries respected.** No `reference-src/**`, no `docs/**/chatlogs/`, no repository-root `*.txt`, no `PHASE_*_REVIEW_*.md` of any round, no `docs/PHASE_INTEGRATION_REVIEW.md`, no `docs/build/**` was opened. No build, test, gradle, GL, capture, or network execution was performed; this review is documentary and every finding below is a claim about bytes, recomputed against current files at the cited lines.

## 2. Scope and §5 audit

**Doc-gate criteria (DESIGN Phase 1 spec, ll. 1056–1067) — met literally.** (a) Module/package layout finalized with dependency rules as testable constraints: §2.1 closed package tables, §4.3's C-1..C-4 in classpath+bytecode form, §8.1's five seam tests, §12 items 11–15. (b) Every D-1..D-10 either satisfied or deferred with an owner named: §11.2 carries all ten rows with dispositions (D-7 `:6195`, D-8 `:6196`, D-9 `:6197`, D-10 `:6198`, and D-1..D-6 above them); none is contradicted anywhere I read — D-5's ~25–30 injection budget is argued with at `:6468`, D-8's double prohibition at `:4766-4771`, D-9's no-core-rewrite posture in §3 `:1883-1884`. (c) Pin table complete with re-verification procedure: §4.2.6/§4.2.6a, §10.1, checklist item 29. (d) REV1's PD §2 glue-seam completeness check present (§4.12; it is credited with surfacing the App B.3 units-0/1 gap, `:6614-6616`). (e) Bootstrap sequence adopted with deviations justified per stage (§4.13; the required/recommended/deviated triad is restated for P7 at `:6454-6475`).

**Conformance-map audit.** §3 (`:1855-1922`) bounds P1's in-scope contract rows honestly ("Phase 1 owns almost no pack-facing contract surface", `:1857-1859`) and I found zero unmapped in-scope rows. Spot-checks against current RESEARCH.md bytes: the four startup probes + GL-3.0 mipmap gate match `RESEARCH.md:476-478` exactly (rows `:1863-1867`); the ARB-form row's citations verify (`:214-215` layout-qualifiers-or-ARB, `:1161` A.3 legacy configuration row, against row `:1882`); the App A.1 terminal rows verify — external `<none>` `:1111`, absent `shadow` → fixed pipeline `:1112`, absent `gbuffers_basic` → *(none)* `:1115`, absent `final` → passthrough copy `:1141` (rows `:1880-1881`); the REV2 citation split for the extension set is recorded, not smoothed over (§3.1, `:1895-1920`). Two line coordinates in the `blend.<prog>` row do not recompute — correction C2 below; the cited content itself is faithful.

**Interface honesty — exports fully specified, receivers recorded.** P1's §5.1/§5.2 expose the module/seam/package grants, the complete facade surface (seven services + `GLDevice` + `vertexInputs()` + `DebugService`), the recorder/replay machinery, and the schema23/`MaterializedSource-v23`/`projectionVersion1` admission rows (`:5484-5496`), each with consumers named. Receiver sweep against the consuming docs' current §5 regions: **P5** §5.2 (`PHASE_5_DOC.md:2770-2790`) consumes exactly the borrowed-depth matrix, first-copy/steady depth verbs, `prepareUnitBindings`, D-P1-66 target maxima, D-P1-62 demotion, D-P1-67 typed clear and its recorder events — all matching P1's rows; **P7** records the R32/D-P1-57 adoption this doc demands at `:6632-6636` (P7 D-P7-43 at `PHASE_7_DOC.md:4518`, the nine HEAD/six RETURN sites at `:1771-1807`, `lockAlphaBlend` receipt at `:3792-3798`), plus R26 fullscreenQuad (`:2524-2527`, `:3654`), D-P7-66 (D-P1-63) and D-P7-71 (D-P1-67); **P10** records R10-1 (D-P1-50) receiver adoption with the exact `VertexInputService` signatures (`PHASE_10_DOC.md:1844-1898`), the R10-9 receipt of D-P1-68/70 (`:1842`, `:1709-1710`), the R27-1/NS-1 receipt of D-P1-55 (`:1893-1903`), and the exact `borrowedVertexList(String label, VertexLayout layout, VertexInputPlan capturePlan)` fixture D-P1-70 mandates (`:1879-1886`, matching P1 `:5492`); **P13** adopts the R3 package trio by ID (`PHASE_13_DOC.md:1753`, `:1929-1930`), D-P13-41 (D-P1-66, `:1754`), D-P13-36 (P1 §4.7.7a/D-P1-63, `:1703`), D-P13-37 schema23 (`:1761-1765`); **P14** owns and restates the label law (below) and marks R-P14→P1-1/2/3 "Fulfilled/adopted, unverified" with the exact grants (`PHASE_14_DOC.md:1873-1875`), matching P1's §4.7.8 grant text `:4696-4724`; **P2** (v2, the current consumer doc) records D-P2-70 for the D-P1-71/D-P14-40/41 lifecycle, D-P2-61 (D-P1-66), D-P2-65/66 (D-P1-67/68/70), R4A (D-P1-42) and D-P2-46 (D-P1-57) (`PHASE_2_DOC.md:2626-2632`, `:2727-2732`, `:3257`). Every promised export I traced has a receiver-side record; none is silently widened.

**Recent-change identity D-P1-72 — present at all four sites, tree form everywhere.** §4.3's softer layer `:2521-2523` ("outside `com.schmaloogium.mod.glue` and its subpackages"); the C-3 restatement `:2535-2536`; §8.1's `SeamLwjglConfinementTest` row `:5692` (same tree wording); §5.1's seam row `:5444` ("C-3's confinement is the package tree"). The decision row `:6179` states the rationale and the enforcement target, and the five granted `mod.glue.*` subpackage homes it protects all exist as grants: `frame` (D-P1-41, `:6497-6500`), `shadow` (`:6447-6452`), `vertex` (D-P1-50, `:6638-6643`), `textures` (D-P1-43, `:6666-6674`), `gl` (R-P14→P1-2, `:4715-4719`).

**Recent-change identity D-P1-73 — present at all four sites, receiver-aligned.** §4.7.3 `:3127-3133` states the full limb (legal/recorded-only; no GL, no native-label event, no queued `GLError`; retained value applied exactly once by the first admitted materialization, after exact-target materialization and before storage work; dropped if deleted unmaterialized; foreign/borrowed unaffected). §4.7.4 instructs label-then-materialize at the creation site `:3819-3824`. §4.7.8 restates the limb and its relation to the activity gate `:4703-4711`. §5.2's D-P1-71 row carries it for receivers `:5493`. Decision row `:6180` matches. Ownership/consumption: **P14 owns** the law (D-P14-40/41; `PHASE_14_DOC.md:1665-1666` — "retains labels at logical create/update and emits only after exact-target native materialization … never on a reserved nonobject") and P1 receives it without expanding create inputs, exactly as D-P1-71 `:6178` says. **P5** consumes via D-P5-43 (P14 D-P14-31/P1 D-P1-64, `PHASE_5_DOC.md:2734`) and drain-bracketed clear work (`:2777`, `:2786`). **P13** consumes via D-P13-33 (D-P1-62) and D-P13-38 (D-P1-64) (`PHASE_13_DOC.md:2017-2026`, `:2340`). **P2** consumes natively: D-P2-70 distinguishes logical create, materialization, label and storage events, with never-allocated deletion emitting no native delete (`PHASE_2_DOC.md:2632`). Aligned on every side I could reach.

**Current-identity discipline.** Cited-as-current identities are current: schema23 + nested-ID + inspection equality and `MaterializedSource-v23` (`:5489`, `:6172`), with schema22/v22 **explicitly demoted to historical** (`:5485` — the correct pattern, not a stale cite); `projectionVersion1` (`:5489`); nine metadata-only trees. `RegistryFingerprint`/`profile-selection-v3`, `ShadowHookHealth/flattened-v3` (66 rows), `TextureHookHealth/application-v2`, `capture-plan/4` + `run-manifest/4`, and `phase13.parameters/v2` + `phase13.sidecar/v1` are correctly **absent** from P1 — P1 consumes none of them (it generates no registry fingerprints, `:6414-6415`; the receivers carry those identities: P13 application-v2 at `PHASE_13_DOC.md:1663`, P2 capture-plan/4 + run-manifest/4 at `PHASE_2_DOC.md:1062`, `:1143`, `:2425-2426`, P5 `phase13.parameters/v2` at `PHASE_5_DOC.md:2879`). No stale identity is cited as current anywhere in the frozen bytes.

**Scope discipline.** §1's in/out lists (`:1648-1703`) match the DESIGN Phase 1 spec's scope; nothing foreign was found in §§2–12. The three maintainer-commissioned additions (native legacy geometry D-P1-44/57, jcpp pin D-P1-49, bounded vertex input D-P1-50) all stay inside the facade/build/pin surface §1 owns, are marked unverified, and — notably — the doc **surfaces rather than buries** the RC2-vs-RC3 ARB-interpretation tension, declining to resolve it silently (`:6786-6796`). Nothing in-scope is silently dropped: §4.7.4's declined-verbs table gives reasons (e.g., no `glGetTexImage`-shaped readback, `:6692-6696`), and the one known hand-off gap (App B.3 units 0/1) is closed by `ForeignTextureProvider` with checklist item 22b making it a v0.1 deliverable (`:3141-3146`, `:6866`).

**Template completeness.** Thirteen §G9 sections, all substantive: §0 header/history, §1 scope, §2 architecture, §3 conformance map, §4 detailed design, §5 cross-phase, §6 failure, §7 threading, §8 testability, §9 milestones, §10 OQs, §11 decisions, §12 checklist. Assigned OQs carry full spike specs with success criteria (§10.1 pin re-verification, §10.2 the §4.8.4 review criterion, §10.3 the four-step backend-swap drill with its `v0.3` step-4 gating at `:6910`, §10.4 the OQ-21 runtime checks, `:6911`).

**Licensing/Pintonium compliance.** The glsl-transformer prohibition now has both addresses — Iris's bundled AGPL library and Pintonium's `org.taumc:glsl-transformation-lib` fork, "never copy … never adopt … including transitively" (`:4766-4771`, `:4842-4846`); the vendored `kroppeb/stareval` is flagged license-unverified rather than assumed; `org.anarres:jcpp` is Apache-2.0, verified; Pintonium's mixed-license fat jar is recorded as *not a precedent* (`:4787-4789`); the session's honest "[V:web] … did not independently audit" limit is stated at the site (`:4850-4851`). OptiFine stays behavioral-observation-only (D-8 row `:6196`). Provenance tags (`[V:observed]`, `[V:doc]`, `[V:design]`, `[V:repo]`, `[V:mcp]`, `[V:web]`, and the Pintonium-observed form at `:6471`) are present on derived claims throughout §3/§4. No AGPL or glsl-transformer trace appears as a dependency or adopted mechanism anywhere in the doc.

## 3. Required corrections

**C1 — the attempt-8/R35 fix-up has no session-of-record in the doc's own change ledger.**
- **Sites:** §0 addendum sequence ends at §0.34 "Attempt-7 clear/recorder/lifetime corrections — 2026-09-08" (`docs/phase1/v14/PHASE_1_DOC.md:1637-1646`), and §1 begins at `:1648`; there is no §0.35. The closing-history paragraph (`:6923-6977`) enumerates sessions only through the twentieth fix-up/round 24, and the trailing "Current §G1.3 status" blocks stop at §0.26/§0.28 (`:6987-6995`) without covering §0.29–§0.34 — let alone the R35 bytes. Meanwhile D-P1-72/D-P1-73 exist in §11.1 (`:6179-6180`) and their edits are spread across §4.3, §8.1, §5.1, §4.7.3, §4.7.4, §4.7.8 and §5.2.
- **Failure path:** every prior fix-up session — including one-character ones — carries a §0.x addendum (§0.4–§0.34), and round 17's sole correction was precisely a closing-history repair (`:1305-1308`). An auditor diffing §0 against §11.1 finds the doc's newest binding changes (C-3's tree restatement and the label limb, both receiver-facing) with no recorded session, inputs, or no-execution claim — breaking the traceability convention the document itself enforces, exactly at the bytes a fresh reviewer must trust most.
- **Owner+receivers:** P1 owns the ledger; the receivers of the affected surfaces (P14 owner of the label law; P5/P13/P2 consumers; all phases inheriting C-3) read corrected bytes whose provenance is unrecorded.
- **Minimal resolution:** add a `### 0.35 Attempt-8 confinement/label corrections — 2026-09-08` addendum naming the session's inputs (R35, RC2 Phase 1 authority), the four C-3 tree sites (`:2521-2523`, `:2535-2536`, `:5692`, `:5444`), the D-P1-73 sites (`:3127-3133`, `:3819-3824`, `:4703-4711`, `:5493`), the receiver deltas (P14 ownership; P5/P13/P2 consumption), and the standard no-build/no-test/no-PASS claim; extend the closing history by one clause and append a fresh trailing status block that at least acknowledges §0.29–§0.35.

**C2 — two RESEARCH.md line citations in §3's `blend.<prog>` row no longer recompute against current bytes.**
- **Sites:** `docs/phase1/v14/PHASE_1_DOC.md:1890` — (i) "App F.7's form is `blend.<prog>=off|<src> <dst> [<srcA> <dstA>]` (RESEARCH.md l. 1511)" — current `docs/research/v1/RESEARCH.md:1511` is F.6's precipitation rule; the quoted form sits at `RESEARCH.md:1517`. (ii) "`PER_BUFFER_BLENDING` feature flag (l. 429)" — the flag is at `RESEARCH.md:430` (`:429` ends at `ENTITY_TRANSLUCENT`).
- **Failure path:** a reader re-deriving the row against current RESEARCH bytes lands on the wrong appendix text — in case (i) a different feature entirely — which is the same class of defect round 13's V13-4 policed, here purely coordinate drift with the semantics intact.
- **Owner+receivers:** P1 owns §3; consumers are any phase re-checking App F.7 routing through this row (P3/P4/P7 per the row's own routing text).
- **Minimal resolution:** recompute the two coordinates to `RESEARCH.md:1517` and `:430`. No prose changes needed — the quoted forms match the current text verbatim.

## 4. Notes

- **N1 — checklist shorthand for C-3.** Checklist item 14 states the failure condition as "fails on an `org.lwjgl` reference outside `mod.glue`" (`:6849`) without the "and its subpackages" qualifier D-P1-72 established at the four normative sites. Item 14 is a work item, not one of those sites, so this is not a D-P1-72 violation — but the row is where a future implementer first meets the test, and the shorthand invites re-deriving exact-package matching from it. One clause ("outside `mod.glue` **and its subpackages**") closes it.
- **N2 — receiver-by-ID uniformity for the label limb (observations, not P1 defects).** P13 consumes the materialization/label law through D-P13-33/D-P13-38 (`PHASE_13_DOC.md:2017-2026`, `:2340`) and P14's §5.1 lifetime-law row naming P5/P13 as resource owners (`PHASE_14_DOC.md:1666`), but nowhere cites D-P1-71/73 by ID, unlike P2's explicit D-P2-70 (`PHASE_2_DOC.md:2632`); a by-ID receipt in P13 §5 would make the P5/P13/P2 consumption triad uniform. Separately, P1 §11.4's R32 owner receipt (`:6632-6636`) demanded P7 integration of the HEAD interceptions; P7 has since recorded that adoption (D-P7-43 with the nine HEAD/six RETURN sites), so that loop is closed as of this review — recorded here so the next P1 fix-up can cite it rather than re-demand it.

## 5. Areas audited and found sound

- **D-P1-72 enforcement surface.** All four sites carry the package-tree form; the five granted subpackage homes are each backed by an explicit grant; the decision's rationale (exact-package equality breaks on legal tenants; unstated prefix match widens silently) matches what the sites now say.
- **D-P1-73 semantics and ownership.** The limb is internally consistent across §4.7.3/§4.7.4/§4.7.8/§5.2 (retention, single application point ordered after exact-target materialization and before storage work, deletion drop, foreign/borrowed no-op), matches P14's owner text, and gives P5/P13 exactly the "no queued `GLError` in the drain" guarantee their bracketing relies on; P2's native-evidence split distinguishes the same five events.
- **The P1→receiver grant ledger.** P5 (framebuffer/depth matrix, typed clear, sampler normalization, capability maxima), P7 (fullscreen route, lock law, sync values, debug activity, frame/conformance packages), P10 (vertex-input seam, conventional participation, list fixture, isolation law, MOD-plugin split), P13 (package trio, target maxima, sync upload, schema23), P14 (R-P14→P1-1/2/3 granted with P14's own adoption recorded, P1-4 correctly left as a separate request), P2 (recorder events, replay evidence, tagged routes, diagnostic types) — every consumed row I opened exists on the receiving side with matching shapes, and every "receiver-adopted/unverified" qualifier is present on both ends.
- **Conformance-map fidelity (modulo C2's coordinates).** Probes, extension-set split, ARB form, App A.1 terminals, App D ivec2/ivec4 rows, App F.7 verb rows, vocabulary row, lwjglx and facade-only-GL rows all verified against RESEARCH/DESIGN text.
- **Decision ledger D-1..D-10** — all ten dispositioned, none contradicted; the doc's explicit refusals (no UBO entry point, no instanced verb, no raw-GL-name adoption, no unasked `drainErrors` forcing verb) are consistent with D-2/D-9's postures.
- **Licensing posture** as detailed in §2, including the honest web-evidence limits and the do-not-inherit treatment of Pintonium's fat jar.
- **Failure/threading narrative coherence** at the spots the newest bytes touch: the D-P1-73 limb does not alter §4.7.4's drain cadence or §7's render-thread rule; the recorded-only label issues no GL and therefore cannot appear in a `GLCallLog`, which §5.2's row states and P2's D-P2-70 respects.

## 6. Verdict

**PASS-WITH-CORRECTIONS** — 2 corrections (C1 change-ledger gap for the attempt-8/R35 fix-up; C2 two stale RESEARCH.md line citations in §3), 2 notes, 0 blocking. The frozen document's substance — gate criteria, conformance fidelity, interface honesty with receiver adoption on all five consumer docs plus P2, current-identity discipline, scope, template, D-1..D-10, and licensing — holds; both corrections are fixable documentation defects that do not rebuild any section and move no §5 contract. This review is documentary: no build, test, gradle, GL, capture or network execution was performed, and no file other than this report was created or modified.

## Resolutions

2026-09-08 documentary fix-up; the report body above remains byte-identical. Both corrections
applied to `docs/phase1/v14/PHASE_1_DOC.md` (now 7023 lines); no other file was touched.

- **C1 — applied.** Appended `### 0.35 Attempt-8 confinement/label corrections — 2026-09-08`
  (`PHASE_1_DOC.md:1648-1663`) naming the session's inputs (`PHASE_1_REVIEW_35.md` plus the RC2
  Phase 1 authority), the four C-3 tree sites (§4.3 softer layer `:2537-2539`, the C-3
  restatement `:2551-2552`, §8.1's `SeamLwjglConfinementTest` row `:5708`, §5.1's seam row
  `:5460`), the D-P1-73 sites (§4.7.3 `:3143-3149`, §4.7.4 `:3836-3840`, §4.7.8 `:4720-4727`,
  §5.2's materialization row `:5509`), the receiver deltas (P14 owns the label law via
  D-P14-40/41; P5/P13/P2 consume the no-queued-`GLError` drain guarantee; every phase
  inheriting C-3 reads the tree form), and the standard no-build/no-test/no-PASS claim.
  Extended the closing history by one attempt-8 clause (`:6990-6992`) and appended a fresh
  trailing `Current §G1.3 status — §0.35` block (`:7015-7023`) acknowledging §0.29–§0.35.
  Every anchor written into the addendum was recomputed against the post-edit bytes (+16 lines
  from the insertion, all cited sites lying after it) and verified in place; the §5 text is
  unchanged, and no new decision IDs were needed — D-P1-72/73 already sit in §11.1 (now
  `:6195-6196`), so the ledger's max stays D-P1-73.
- **C2 — applied.** The §3 `blend.<prog>` row (`PHASE_1_DOC.md:1906`) now cites
  `RESEARCH.md l. 1517` for App F.7's `blend.<prog>=off|<src> <dst> [<srcA> <dstA>]` form and
  `l. 430` for the `PER_BUFFER_BLENDING` feature flag. Both coordinates were verified against
  current RESEARCH.md bytes before repointing: `:1511` is F.6's precipitation rule and the
  quoted form sits at `:1517`; `:429` ends at `ENTITY_TRANSLUCENT` and the flag sits at `:430`.
  No prose changes — the quoted forms match the current text verbatim, exactly as this report
  states.

N1 and N2 are notes and were deliberately not applied. No build, test, formatter, linter, GL,
capture or network execution was performed; fresh whole-document review and final integration
remain required.

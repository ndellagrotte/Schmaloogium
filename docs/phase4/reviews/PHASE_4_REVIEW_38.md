# Phase 4 — architecture review 38

**Date:** 2026-09-08. **Frozen document:** `docs/phase4/v1/PHASE_4_DOC.md` SHA-256 `b9ae109b0d1b78fe3da22fd1502e92c2f684d83561f30ec7a732d3f92911316d`. **Verdict:** PASS (0 corrections, 2 notes). **§5 impact:** None — no interface correction is requested. Current §5 grants are sufficient for the specified architecture: every consumed P1/P3/P5/P6/P7/P10/P2 surface has a current producer declaration (P1 native configure/linked-input/sampler-init/duration-lock; P3 schema23/MaterializedSource-v23 exact-pair evaluation and direct projections; P5 appB3 policy/resolver/initializationAssignments; P6 participants/retire; P7 D-P7-67 profileSelection forwarding; P10 table/geometry enum; P2 absent-selection inspection). The remaining open items are honestly-deferred verification/authority gates (fresh whole-document reviews, IR-01, jcpp pin/closure, real-context native-submission conformance), not missing interfaces.

---

# Phase 4 Whole-Owner Architecture Review — R38 (attempt8, review38)

## 1. Frozen identity and review posture

- **Owner:** `docs/phase4/v1/PHASE_4_DOC.md`, complete §§0–12 (3125 lines), read in full.
- **Verified SHA-256:** `b9ae109b0d1b78fe3da22fd1502e92c2f684d83561f30ec7a732d3f92911316d` — recomputed this session and equal to the frozen identity in `docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_8.json` (phase 4, reviewRound 38). The bytes are unchanged since R37's literal PASS (`docs/phase4/reviews/PHASE_4_REVIEW_37.md`); R37 and its appended history were treated as immutable evidence, never as current certification.
- **Round:** R38 on the attempt8 frozen set. Read-only: no file was edited, no build/test/linter/formatter/validation command was run.

## 2. Governing authority and inputs

- The header selects **RC3**: governing assignment `docs/design/v2.0-RC3/DESIGN.md:1471-1570` (confirmed at the actual bytes: the Phase 4 specification ends at line 1570), matching the shared authority's mapping (P3–P9 → RC3).
- Research evidence was checked at the cited coordinates: App A.1/A.2/A.3 (`docs/research/v1/RESEARCH.md:1100-1188`), the 60-slot count (`:1142`), modern stage shape and `.csh` companion exclusion (`§3.6.1/:336-362`, incl. "every program except gbuffers"), lifecycle/compile/use/uninit behavior (`:482-513`), core-object opportunity (`:763-775`), dual-form geometry (`:210-216`), fixed-unit map (`:1227-1261`), and same-unit custom-texture semantics (`:1481-1495`). The shipped author table (`reference-src/schlorbium-HD_U_G6_pre1/doc/shaders.txt:61-108`) independently confirms every catalog row and fallback edge; the count recomputes to exactly 60 named slots (3 shadow + 22 gbuffers + 17 deferred incl. pre + 17 composite incl. pre + final). The geometry pair is at `shaders.txt:349-356`.
- Current integration state was read from `docs/PHASE_INTEGRATION_REVIEW.md`, including the Resolutions and both 2026-09-07/09-08 follow-ons: IR-03/04/07/18/27/29 remediations and the IR-18 maintainer ruling (adjacent prepared submissions, N total, IDs 0…N−1, v0.5) match the owner's §11.5 contract verbatim; IR-01 remains open and no PASS/implementation clearance exists.
- The narrow approved maintainer rulings were read: `docs/decisions/GEOMETRY_PRIMITIVE_COMPATIBILITY.md` (conditional conversion; P7/P10 own the adapter; "P4 owns effective linked-program/provider metadata" — exactly the R10-5/D-P4-28 grant) and the U1 correction recorded in §5.4.

## 3. Scope audit — whole owner

All thirteen sections were audited: §0 addenda (0.1–0.41, historical addenda preserved, supersessions explicit), §1 ownership table (no vanilla/GL type enters `engine.registry`), §2.2 public shape and invariants, §2.3 relationship map, §3.1–3.4 conformance/Pintonium disposition, §4.1–4.12 algorithms, §5.1–5.7 interfaces, §6 failure rungs, §7 threading, §8.1–8.6 test plan, §9 milestones, §10/§11 decisions and handoffs, §12 checklist. Internal consistency verified across: the exhaustive §4.6 classification matrix (total over ownBuild × walk outcomes, masking semantics, §4.6:1269-1331); §4.12's projection-eligible stage list vs §4.6 step 4; the `PublicationFailureKind` pre/post-release partition (`:2091-2096`) vs §4.11's Rejected/RecoveredOff branches (15 kinds through `BOOTSTRAP_CANDIDATE` → pre-release; 5 release/post-release kinds → RecoveredOff); the barrier order in §4.10 steps 1–7 vs the §5.1 row (authenticate → force-shadow → sole resolution → P5 binds → invalidate token → close predecessor lease → bind → acquire lease → effective blend before built-ins → sampler/built-in/custom → Activated); the §2.2 `SparseArray`/prelude invariants vs §4.1/§4.2 construction rules and §8.1's pre/0/5/99, pre-only, absent-pre and illegal-membership cases; and the §9/§12 staging against §5/§8. The attempt8 structural checks (section sequence, balanced fences, unique decision IDs) hold on these frozen bytes.

## 4. §5 audit — consumed contracts and grant sufficiency

Every consumed dependency surface was verified against the dependency's **current** bytes:

- **P1** (`docs/phase1/v14/PHASE_1_DOC.md`): all five consumed operations exist with matching semantics — `configureLegacyGeometry(p,input,output,maxVerticesOut)` with closed TRIANGLES/TRIANGLE_STRIP domains and the mandatory drain-configure-drain-abort transaction (`:3273-3276`, `:3824-3834`, §5 row `:5450`); `linkedGeometryInput(p)` → `Optional<LinkedGeometryInputPrimitive>` with the exact five-value enum and cached-successful-link lifetime (`:3278`, `:3907-3945`, §5 row `:5476`); `initializeSamplerUnits` D-P1-59 with "after successful link/linked-input checks and before validation" (`:3279-3280`, `:3484-3525`) — exactly P4's `:1462-1467` ordering; `useFixedFunction()`/`shaders.useFixedFunction` zero-argument event (`:3283`, `:4176-4181`); `lockAlphaBlend` D-P1-57 one-lease/suppression semantics (`:3416-3418`, `:3538-3541`); `ScriptedResponses.linkedGeometryInput` (`:4159-4160`) used by P2 per §5.6. D-P4-42's fixed-terminal receipt matches P1's `:3513-3515` all-unit sampler clearing.
- **P3** (`docs/phase3/v1/PHASE_3_DOC.md`): `CURRENT_SCHEMA_VERSION = 23` (`:704`) and `MaterializedSource-v23` (`:3302`) match the D-P4-41 receipt (§5.3); `PackConfiguration.evaluateProgramStates(Optional<ProfileName>,DiagnosticReporter)` (`:866-869`, `:2965-2968`, `:3675-3678`) with null-wrapper `InvalidState(NULL_INPUT)`; `GeometrySourceRequest` exactly `None|PreserveNative` (`:1510-1513`) and `GeometrySourceForm` triple (`:3831`); direct `resources().programs()` lookup by `ProgramRequirementKey(dimension, programName)` with base-when-world-absent and `mipmappedAfterPass()`/`vertices()` grants (`:2726-2729`, `:3745-3760`) matching P4 §4.7 steps 2/4/9 and §5.3 rows (`:2247-2248`); inspection `inspect(PackLoadRequest)` with `projectionVersion=1` unchanged.
- **P5** (`docs/phase5/v1/PHASE_5_DOC.md`): `FixedSamplerPolicies.appB3()` implements P4's `FixedSamplerLayoutPolicy` including D-P5-38 `initializationAssignments` reusing the resolver across all validated bands with inconsistency throwing to INVALID_SAMPLER_POLICY (`:2437-2446`); `applyVirtualTransition(frameId,pass)` accepts only the exact planned VIRTUAL_FLIP_CONTROL descriptor (`:777`, `:1614-1618`, §5 row `:2717`); `BindingPurpose {SHADER, FIXED_FUNCTION_PASSTHROUGH, NONE}` (`:1071`); `samplerPolicyFingerprint()` compared even for empty registries (`:730-731`); candidate-view consumption mirrors P4 §5.1 (`:2962`).
- **P6** (`docs/phase6/v1/PHASE_6_DOC.md`): `BoundProgramUniformAccess`/`ProgramUniformCacheKey`/`ProgramBindingParticipant.afterBind` are reproduced verbatim from P4 §4.10 (`:1279-1299`); `retire(UNPUBLISHED_ABORT|REPLACEMENT|SHUTDOWN)` with `Retired|AlreadyRetired|Rejected(WRONG_THREAD|ACTIVE_CALLBACK)` and no CLOSE alias (`:477`, `:1704-1750`) — matching P4 §5.4's R7-10/11 receipt; the shared-resolver participant uses the effective `binding.samplerLayout()` (`:1226-1249`).
- **P7** (`docs/phase7/v1/PHASE_7_DOC.md`): D-P7-67 forwards the exact accepted frozen `profileSelection` in every `RegistryBuildRequest`, even for equal-option profiles (`:3298-3302`, `:3726-3729`, `:3886`); R7-4 copies `resolutions()` directly (`:3708`); required `programs.<n>.ownBuild` copied to `/4` without inference (`:2038-2039`, `:3709`, `:4572-4577`); virtual prelude forwarded once to `applyVirtualTransition` before ascending raster traversal (`:1247-1250`, `:1382-1386`); R10-5 handoff adopted — `Activated.binding().state().geometryInput()` only under the matching successful selection, cleared on FixedFunction (`:2484-2490`).
- **P10** (`docs/phase10/v1/PHASE_10_DOC.md`): `VertexGeometryInput` is the exact six-value enum (`:276-278`); `VertexInputPlan.expectedGeometryInput` compared against cached actual linked metadata before native work (`:272-274`, `:920-924`, `:1886-1888`); the fixed table consumes only P4's 10/11/12 (`:735-738`, pointer rows `:376-386`); LIST_CAPTURE is explicit capture metadata with count expansion forbidden (`:843-852`).
- **P2** (`docs/phase2/v2/PHASE_2_DOC.md`): D-P2-64 passes explicit `Optional.empty()` as `profileSelection` with same-request association, handles `Ready(candidate).view().resolutions()` with finally-close, treats `ShadersOff` as incomplete evidence, and special-cases `INVALID_PROGRAM_STATE` without fabricated rows (`:2130-2143`); `run-manifest/4` carries `programs{slot,status,from,sourcePresent,ownBuild,driverLog}` (`:1140-1168`, `:2457`).
- **P8/P13/P14** (non-§5-consumers of record): P4 §5.4's R7-12/13 receipt matches current P8 bytes — five-argument sixteen-row `shadowBindings` (`docs/phase8/v1/PHASE_8_DOC.md:1873`, `:2141-2142`) and `ShadowPlanInput(policy,hookHealth,requested)` (`:734-736`); the P13 lease's third opaque `AtlasBindingEvidence` argument is P8/P5-side (`:2012-2016`) and P4 correctly specifies no lease arity ("no texture handle enters Phase 4"). D-P8-36 receives P4's `profile-selection-v3` domain. §5.7 keeps R-P14→P4-1 pending, matching the IR-27 disposition; the synchronous compiler remains the only callable baseline.

**§5 grants are sufficient.** Every contract P4 consumes now has a current producer declaration (P1 native configure/linked-input/sampler-init/duration-lock; P3 schema23 native source, exact-pair evaluation, direct projections; P5 policy/resolver/initializationAssignments; P6 participants/retire; P7 forwarding; P10 table/geometry enum; P2 inspection route). No outstanding §5 request is load-bearing for the specified architecture: the remaining open items are verification/authority gates the document honestly defers (fresh whole-document reviews, IR-01, jcpp pin/closure, conditional native-submission real-context conformance), not missing interfaces. One status-framing observation, not a correction: §0.2's "round twenty's literal PASS is the current dependency gate" predates P1's later grants that P4 now consumes; the operative §5.2/§5.4 ledger marks every post-R20 surface "owner-granted/unverified, receiver-adopted/unverified," and the closing status declares the document unverified, so the current posture is honest.

## 5. Focus: explicit profileSelection identity and closed failure

The request carries required non-null `Optional<ProfileName> profileSelection` immediately after `configuration` (`docs/phase4/v1/PHASE_4_DOC.md:791-792`, §5.1 row `:2126`). Planning evaluates exactly once `evaluateProgramStates(request.profileSelection(), request.diagnostics())` before availability, materialization, callback planning, compilation or GL allocation (`:1336-1343`); null wrapper rejects rather than normalizing; `InvalidState` reduces to registry-wide `INVALID_PROGRAM_STATE` with empty programFailures, no projection and zero GL (`:1340`, `:2078-2081`); absent/unknown-profile semantics match P3's binding algorithm. `RegistryFingerprint/profile-selection-v3` (D-P4-43, `:1937-1941`) commits selection, evaluated state, positional routing and required ownBuild, distinct from every earlier domain and from generation. Receiver adoption is real: P7 D-P7-67, P2 D-P2-64, P8 D-P8-36. §8.1's `profileSelection_*` boundary set (equal-option/different-disable, absent/unknown/invalid, inspection association) and §12 item 5 cover the contract. No defect.

## 6. Focus: native geometry contracts

§4.8 consumes P3's current algebra exactly (`GeometrySourceRequest` None/PreserveNative; forms None/CoreLayout/NativeLegacy with source-over-API precedence); the native API triple is fixed TRIANGLES→TRIANGLE_STRIP with the unchanged positive count, configured once before first link under the drain-configure-drain-abort protocol; the linked-input comparison runs after link and before validate/READY with `GEOMETRY_INPUT_MISMATCH` as a LINK-stage whole-candidate failure (`:1461-1467`, `:1530-1540`, `:2037`); the published `geometryInput` is required/non-null and frozen only after agreement (`:609-614`, `:759-766`). Fullscreen stays P1-owned (R26: TRIANGLES uses the strip even on QUADS-capable profiles); native submitted quads go only through the maintainer-approved P7/P10 conditional adapter (D-P4-29), whose decision text matches. The P1 five-enum domain, P10 `VertexGeometryInput` mapping, and P7 R10-5 consumption align end-to-end. No defect.

## 7. Focus: compile/link/publication lifetimes

The §4.7 state machine is complete and matches P1's transaction contracts: PLANNED (uniform merge + all-band policy validation + frozen initialization assignments before any GL) → create → per-stage compile/attach → declared-only 10/11/12 binds → optional native configure → link → cached linked-input comparison → `initializeSamplerUnits` (Completed required; restored failure is candidate-local VALIDATE evidence; unrestored poisons and aborts the registry) → validate → shader-handle deletes → label → READY, with a local ownership ledger deleting every unpublished handle on every exit and idempotent candidate teardown. Publication (§4.11) validates the complete closed pre-release set before disturbing the old barrier, handles the authenticated absent-old-barrier branch with zero old-barrier GL work, atomically replaces and increments exactly once per accepted Ready/off or forced RecoveredOff, partitions pre-release `Rejected` from post-release `RecoveredOff` exactly along the `PublicationFailureKind` split, and transfers candidate ownership only on `Accepted`. P7's compensation and P6's retirement ordering (`REPLACEMENT` after old-barrier invalidation; `SHUTDOWN` before atomic P4 teardown) are consumed consistently. Generation accounting matches the IR-07 remediation (one drained request ≠ one increment; Ready-then-compensating-Off may increment twice). No defect.

## 8. Corrections

None. No Phase-4-owned contract, algorithm, §5 grant or receiver seam exposes a necessary actionable correctness or authority fix on these frozen bytes.

## 9. Notes

**N38-1 — historical Pintonium pin remains unverifiable in the current inventory (verification limit; no §5 change).** The owner's evidence tags cite `reference-src/pintonium-9c2fcc1/...`, which is absent; the available checkout is `reference-src/Pintonium-main`. I independently corroborated every load-bearing mechanism there: `ProgramFallbackResolver.java:28-44` (memoized recursive fallback), `ProgramCreator.java:20-26` (11/11/12/13/14 — the negative fixture), `PipelineManager.java:78-88` (destroy path/generation). The root `Pintonium-main` LICENSE is GPL-3.0 while individual files carry LGPLv3-derived notices; preserve that distinction before any reuse. This carries N37-1 forward; no registry behavior claim depends on the absent checkout.

**N38-2 — disabled-dimension branch has no intentional-off `RegistryFailureKind` (optional suggestion).** `docs/phase4/v1/PHASE_4_DOC.md:1355` sends an intentionally disabled dimension to a "shaders-off plan," i.e. `RegistryBuildResult.ShadersOff(RegistryBuildFailure)`, but the closed enum at `:2068-2071` (NO_REQUIRED_TERMINAL, CAPABILITY, INVALID_PROGRAM_STATE, INVALID_SAMPLER_POLICY, UNSAFE_STATE, UNEXPECTED_BACKEND) contains no intentional-off member, so an empty world-folder-disabled dimension (a documented legitimate pack feature) would be labeled with a misfitting failure kind in user diagnostics. No inspected consumer keys behavior on the kind (P2 branches on the Ready/ShadersOff variant and reserves INVALID_PROGRAM_STATE for P3 InvalidState), so the impact is diagnostic labeling only; a dedicated kind or an explicit kind mapping for this branch would close it. Not a required correction.

## 10. Limitations and unexecuted evidence

Architecture-only. No test, §8 case, §8.6 hook, conformance tier, build, runtime, GL behavior, native geometry parity, pack tier, dependency verification, license clearance, G6 parity, optional async grant or final integration PASS was executed or certified; planned checks are planned, not run. Documentary/model checks are not native or runtime proof. Forbidden paths (Oculus `pipeline/transform`, `libs/`, `glsl-relocated/`, glsl-transformer, chatlogs, root transcripts, OptiFine decompile) were not read. IR-01 remains open; this review grants no implementation clearance.

## 11. Final verdict

**PASS.** The complete current owner — algorithms, public declarations, §5 grants, §8 cases and §12 checklists, judged across its consumed P1/P3/P5/P6/P7/P10/P2/P8 contracts — contains no remaining required correction. Corrections: 0. Notes: 2. §5 impact: none.

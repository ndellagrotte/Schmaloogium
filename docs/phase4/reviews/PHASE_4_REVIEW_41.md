# Phase 4 — architecture review 41

**Date:** 2026-09-08 · **Frozen artifact:** `docs/phase4/v1/PHASE_4_DOC.md` — SHA-256 `c165be30da48a23bee807fd81646c43659375d35ef110abf6adc33f6b1c587b1`, recomputed at wave open and identical to `docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_11.json` · **Verdict: PASS-WITH-CORRECTIONS** — 0 blocking, 2 corrections, 2 notes · **§5 impact: none** (both corrections are documentary; no exported or consumed contract changes).

---

## 1. Scope, method, and hash gate

- Hash gate: `sha256sum docs/phase4/v1/PHASE_4_DOC.md` → `c165be30…c587b1`, matching the attempt-11 freeze. Proceeded.
- Complete read of all 3144 lines of the phase document. Governing inputs read: RC3 `docs/design/v2.0-RC3/DESIGN.md` Part I (§G0–§G12, including §G1.2/§G1.3 protocol, §G4.2/§G4.4/§G4.6, §G5.1–§G5.3, §G7, §G9–§G11) and the full Phase 4 specification (`DESIGN.md:1471-1568`); `docs/research/v1/RESEARCH.md` ll. 11–107 (§0–§1) plus the Appendix A/B.3/F.5/F.7/§3.6.1/§4.1/§4.2/§6.2/§7.3 regions the conformance map cites. Dependency docs read at §0 header/§1/§5/§11: `docs/phase1/v14/PHASE_1_DOC.md` and `docs/phase3/v1/PHASE_3_DOC.md` (the §G5.1 declared set for Phase 4 is {1, 3}); downstream anchors spot-checked in `docs/phase6/v1/PHASE_6_DOC.md` and `docs/phase2/v2/PHASE_2_DOC.md` where P4 cites them.
- Constraints honored: no `reference-src/**` read (Pintonium/Oculus/schlorbium source claims were checked for provenance-tag presence and internal consistency only, never against tree bytes); no other `PHASE_*_REVIEW_*.md` read; no chatlogs/root transcripts; no build, test, Gradle, GL, or network execution of any kind. This review is documentary; the only file written is this one.

## 2. Doc gate and template completeness

Every literal criterion of the RC3 Phase 4 doc gate is met:

- **Both configurations shown.** §4.2 tabulates the G6 configuration (six schedule steps, five identities, arrays populated through 15) and the full nine-identity superset (sparse 0…99, virtual preludes) against §2.2's `StageId`/`StageBand`/`SparseArray` types; §8.1 carries the structural tests for both. This satisfies "instantiates both the G6 table and the §3.6.1 superset without structural change."
- **Every App A.1 row mapped.** §3.2's table covers all 60 named rows (3 shadow + 22 gbuffers + 17 deferred-including-pre + 17 composite-including-pre + 1 final, excluding `<none>`), each fallback edge re-checked against `RESEARCH.md:1106-1145` — all edges match, and the 60 summary agrees with `RESEARCH.md:1143-1145`.
- **Barrier contract fully specified as an interface.** §4.10 defines the barrier API, context issuance/epoch/kind rules, select-once authentication, participant order and isolation, and every result variant.
- **Backup-chain table cross-validated against PD §3.1.** §3.2 records the narrower Pintonium cross-validation with `[V:observed — Pintonium …ProgramId.java:11-13, :24-26, :45-46]` tags; §3.4 disposes each PD mechanism. Tag presence and decision citations verified; tree bytes unread per wave constraints.
- **Version-counter invalidation exposed in §5.** §5.1's `PublishedRegistry.generation` row names Phase 12 reload paths and every derived cache, with the equality protocol in §2.2/§4.11.

Template: all thirteen §G9 sections (§0–§12) present and substantive. §10 legitimately records that the spec assigns no OQ (RC3 Phase 4: "OQs: —") and correctly fences OQ-15/R-P14→P4-1 as pending-owner-amendment territory (§5.7). Scope discipline: every RC3 scope-in bullet is designed (registry, program registry, backup chains, compile/link flow with 10/11/12 pre-link binds, dual-form geometry §4.8, failure handling §6, use-program barrier, version-counter invalidation, DRAWBUFFERS/scale/flip/`countInstances` semantics §4.9/§11.5), and §1.2's ownership table assigns everything the spec's scope-out list names (FBOs→P5, uniform values→P6, execution→P7, shadow camera→P8) plus the adjacent concerns (P10, P11, P12, P13, G8/S1/S2, P14).

## 3. Conformance-map audit and anchor spot-checks

Spot-checked mapped rows against the cited RESEARCH/appendix text at the stated coordinates; all of the following land with semantic fidelity:

- Modern order and pass arrays: `RESEARCH.md:336-355`, `:340-353` (§3.1 rows 1–2); G6 order `:220-224`; §7.3 read/write/flip rows `:835-841`; `.csh`-except-gbuffers `:357-361`; init/uninit `:483-491`; compile/link/failure/barrier evidence `:497-507`; core-facade `[U]` opportunity `:766-770`; dual-form geometry `:213-216`; App A.1 names/edges/count `:1106-1145`; B.3 fixed units incl. conditional shadow 4/5 and authoritative depthtex1=11 `:1228-1255`; F.5 per-program custom textures and one-type-per-unit `:1484-1490`; F.7 program-state keys `:1516-1521`; A.3 attributes/countInstances/geometry rows (`:287-288`, `:1160-1161`). DESIGN anchors `:1486-1491` (compute placeholders), `:1511-1514` (compile sequence), `:1537-1538` (`countInstances` exposure, cited again at §11.5 as `RC3:1537-1538`) all land.
- Dependency-doc anchors sampled: P1 `:3324` (`useFixedFunction()` with the exact no-null/magic-handle comment), `:4220-4226` (zero-arg `shaders.useFixedFunction` recorder event), §4.7.4a/§5.2 D-P1-48 cached `linkedGeometryInput` row (whole-candidate failure on mismatch — matches §4.8's consumption), §5.2 D-P1-59 sampler-initialization transaction and D-P1-57 duration lock — all landed. P3 pins `:1499-1515` (`DeclaredGlslType` closed algebra), `:1560-1562` (`GeometrySourceRequest.None/PreserveNative`), `:2769-2783` (`ProgramRequirementKey`, `ResourceRequirements.programs` with `mipmappedAfterPass`/`vertices` — the §0.57 grant P4 §5.3 consumes), `:916-918` (`evaluateProgramStates(Optional<ProfileName>, DiagnosticReporter)` exact signature), `:3379`/`:1591-1594` (materialization route-distinctness and source-attributed unavailability — supports the §3.1 declarations row), `:4048-4055` (`PackInspectionResult` exactly as §5.6 quotes) — all landed. P6 `:1279-1299` (`BoundProgramUniformAccess`/`ProgramUniformCacheKey` shape, inside P6 §4.9) and `:1187-1226` (shared fixed-sampler policy, B.3 authority) — landed.
- Internal consistency probes: the §4.6 evidence matrix rows match the prose classification and each other; the `PublicationFailureKind` pre-release/post-release split matches the enum's member partition; `RegistryBuildRequest`'s eight components match §5.1's stated order; §4.2's `SparseArray(99,15,…)` G6 rows satisfy §2.2's `-1 ≤ highestPopulatedIndex ≤ highestLegalIndex == 99` invariant; §8.1's test names track §4.1/§4.6/§4.7 contracts. No unmapped in-scope row and no semantic-fidelity error found in §3.1–§3.4.
- One anchor miss found (F-2 below): the P3 "canonical framing" pin.

## 4. Interface honesty and dependency posture

- **Consumed interfaces exist.** Every P1 §5.2 contract §5.2 consumes is published there: `ShaderService.use/useFixedFunction`, `UniformService.locate`, `StateService` (+ D-P1-57 `lockAlphaBlend`/opaque `AlphaBlendOverride`/`effectiveBlend`), `DebugService`, `UniformLocation` with `isAbsent()`, `GLCapabilityProfile` + text serialization, opaque handle lifetime, never-throwing `CompileResult`/`LinkResult`/`ValidateResult`, D-P1-59 `initializeSamplerUnits`, `configureLegacyGeometry` (§0.25 row, consumer 4), `linkedGeometryInput` (D-P1-48 row), `RecordingGLDevice`/`ScriptedResponses`/`GLCallLog`/`ReplayAssertions`, `DiagnosticReporter`, SPDX/`THIRD-PARTY` mechanism. Every P3 §5.1 contract §5.3 consumes is published there (table above), and §5.3's schema gate matches P3's current bytes: `CURRENT_SCHEMA_VERSION = 23` (P3 `:754`), `MaterializedSource-v23` (P3 `:3359`), `projectionVersion=1`.
- **Exports specified with receiver adoption.** §5.1 names concrete consumers matching the §G5.1 dependent set (5, 6, 7, 8, 10-via-7, 12, 13); the P2 inspection-enrichment export (§5.6) and the P5 policy/resolver, P6 R7-10/11, P8 R7-12/13 coordinated consumptions are each explicitly marked owner-granted or receiver-adopted and unverified, with the §G1.3 gates restated. §5.7 records R-P14→P4-1 as received/pending, not adopted — no API self-grant.
- **Current-identity discipline.** schema23/`MaterializedSource-v23` is the only current P3 identity asserted (D-P4-41); schema17–22 receipts and `positional-route-v2`/`own-build-v1` appear only as superseded history; current registry domain is `RegistryFingerprint/profile-selection-v3` (§4.11). `capture-plan/4` + `run-manifest/4` with "No `/1`–`/3` compatibility default" (§4.6) matches P2's current canonical schemas (`PHASE_2_DOC.md:1063`, `:1144`); §11.4.1's "golden/1" matches P2's golden schema1. No D-P2 receipt is cited at all (D-P2-55's territory is P2's own doc), and no ShadowHookHealth/TextureHookHealth/phase13-parameters/sidecar version identity is invoked, so no stale instance exists. P6's "§5.2 owner-review gate open, P6 currently unverified" is accurate against P6 `:575`. P4 cites no P2 document path, so the P2-v2 fact is not at risk here.
- One dependency-posture claim is stale (F-1 below).

## 5. Binding decisions, licensing, and reference compliance

- **D-1…D-10:** §11.2's disposition table is accurate against the design: no loader type enters `engine.registry` (D-1); registry/compiler only (D-2); matrix rows in §3/§8 (D-3); the modern-superset registry is the central invariant with both configurations (D-4); no hook authored — P7/P8 call the interfaces (D-5); all policy pure, GL only via facade/recorder (D-6, §2.1 invariant 8, §7.1); GPL posture and D-8 reuse rules honored (D-7/D-8); compatibility profile retained — fixed-function terminal, native `GL_ARB_geometry_shader4` form, no core rewrite (D-9); conformance tiers T0–T3 wired in §8.5 (D-10). No contradiction found.
- **Licensing/Pintonium (§G7/§G11):** §0.4's posture follows D-7/D-8; shaders.txt is treated as contract evidence; the schlorbium digest supplies restated behavior only (TRIANGLES→TRIANGLE_STRIP topology, §4.8) with no decompile identifiers copied; every Pintonium-derived claim carries a `[V:observed — Pintonium <path>]` tag; adoptions ride recorded decisions with contract checks (D-P4-4/5/8/10/18/19, §3.4 table); PD §18's pre-decided rows (locations 11/11/12/13/14, dimension caches, generated heuristics) are rejected under D-P4-6/D-P4-4, and §3.4 closes with "No PD §17 dead/stub feature or PD §18 divergence is inherited." No trace of the transformation library, vendored stareval, or Pintonium's stale DESIGN.md. §11.5's web/licensed checks (OptiFine published docs, Angelica/Iris bounded negative reads) are disclosed with limits and grant no parity claim.
- **Decision log:** D-P4-1…D-P4-43 each carry rationale/contract checks; the historical supersession chains (schema receipts, `/3`→`/4` ownBuild wire, positional-route-v2→profile-selection-v3) are consistently marked historical in §4.6/§4.11/§11.1.

## 6. Findings and verdict

**F-1 (correction) — stale P3 dependency-verdict posture cited as current.**
Location: `docs/phase4/v1/PHASE_4_DOC.md:77-81` (§0.2) and `:328-330` (§0.35).
Claim: "Current `docs/phase3/v1/PHASE_3_DOC.md` has Review36 applied resolutions and changed §5 without a fresh PASS; it is provisional …, not freshly verified" / "Phase3's Review36 applied resolutions remain provisional with changed §5 and no fresh PASS."
Evidence: P3's current bytes record Round 62 as the latest state: §0.67 (`docs/phase3/v1/PHASE_3_DOC.md:621-643`) and the closing block (`:5577-5581`) state R62's PASS-WITH-CORRECTIONS "resolves to verified under §G1.3 with all corrections resolved and no §5 change outstanding." Citing P3's Round-36 state as current is the exact defect class the attempt-11 fix-up wave cured in P12 (stale "Round 36 leaves a §5 change outstanding" repointed to P3 `:5577-5581`) and that P4's own §0.42 cured for P1 in §0.2/§11.5 — the P3 sentence was missed. That P3 awaits this wave's fresh whole-owner review remains true; the specific round identity and "changed §5 without a fresh PASS" characterization are stale as current-state claims. Severity: correction (documentary posture prose; no §5 impact — §5.3's schema gate and §5.4's grant/verification framing are unaffected).

**F-2 (correction) — P3 canonical-framing pin does not land on the framing text.**
Location: `docs/phase4/v1/PHASE_4_DOC.md:322-327` (§0.35: "canonical framing at `docs/phase3/v1/PHASE_3_DOC.md:532-666,1389-1410,3233-3249`") and `:1454-1457` (§4.7: "Phase3 §4.10's length-prefixed scalar/string/list framing (`docs/phase3/v1/PHASE_3_DOC.md:3233-3249`)").
Claim: the cited P3 range contains the length-prefixed framing P4's sampler/policy digest design reuses.
Evidence: at current P3 bytes, ll. 3233-3249 contain the `MappingOrigin` closure sentence and §4.10's three validation levels — no framing. The `atom`/`seq` length-prefixed scalar/string/list framing is defined at `docs/phase3/v1/PHASE_3_DOC.md:3262-3275` (introduced "Define `atom(s)` … define `seq(v…)`") and applied to the `MaterializedSource-v23` payload at `:3356-3371`. The content is absent from the stated coordinates, so per anchor-hygiene rules this is a correction: repoint both citations (≈ `3258-3285` captures definition plus first applications). §0.42's "one pin verified already landed" record and the wave's Phase-C re-derivation both passed over it. Severity: correction (evidence anchor only; no §5 impact).

**N-1 (note) — §9 milestone condition wording predates §5.4's closure.**
Location: `docs/phase4/v1/PHASE_4_DOC.md:2746` ("Synchronous materialize/compile/link/validate transaction … implemented after the §5.4 legacy-geometry dependency gap closes"). §5.4 (`:2273-2287`) now records the API gap as closed ("no outstanding accessor request … remains"; "Source and linked-metadata API absence are closed, not verification or implementation clearance"), leaving fresh verification as the gate — the condition §12 item 7 (`:3086`) states correctly ("After fresh owner/receiver verification"). Reword the §9 row to the verification gate for consistency. Severity: note.

**N-2 (note) — §5.5 design-graph note omits the P2 enrichment edge.**
Location: `docs/phase4/v1/PHASE_4_DOC.md:2324-2328` (§5.5) vs §5.1/§5.6. §G5.1 lists Phase 2 as depending on Phase 1 only, yet P2 consumes `candidate.view().resolutions()` enrichment (§5.6, sanctioned by IR-29/§0.36 and §0.22's R10 grant). §5.5 requests the §G5.1 correction for the Phase 12 generation edge but not this one; the consumer-side declaration belongs to P2's doc and the seam was integration-reconciled upstream, so this is recorded as a note only.

Positive record: hash gate passed; doc gate met literally; 13/13 §G9 sections; zero unmapped conformance rows; consumed-interface existence verified in P1 §5.2 and P3 §5.1 at exact signatures; exports specified with marked receiver adoption; current-identity set (schema23/`MaterializedSource-v23`, `RegistryFingerprint/profile-selection-v3`, `capture-plan/4`+`run-manifest/4`, `projectionVersion=1`) verified against owner bytes; D-1…D-10 and §G7/§G11 compliance clean; PD §17/§18 non-inheritance explicit. No blocking finding: both corrections are prose/anchor repairs inside §0 and §4/§9 that touch no contract, decision ID, or interface.

**Verdict: PASS-WITH-CORRECTIONS** (0 blocking / 2 corrections / 2 notes; no §5 change outstanding).

No build, test, Gradle, GL, capture, or network command was executed for this review; verification was documentary, by hash recomputation and byte-level reads at the cited coordinates.

## Resolutions
**Date:** 2026-09-08 · Attempt-11 fix-up wave (§G1.3). Certified-bytes SHA-256 (pre-fix-up): c165be30da48a23bee807fd81646c43659375d35ef110abf6adc33f6b1c587b1. Post-fix-up SHA-256: 91b267648c435ec7fe752e69ad1f212b8de6d2ac098486e18c06e1e677cb1042.

- **F-1 (correction) — resolved.** The stale P3 dependency-verdict posture is reworded in place at both cited sites, zero line-count change: `docs/phase4/v1/PHASE_4_DOC.md:78-80` (§0.2, within the reviewed `:77-81` block) and `:328-330` (§0.35) now state the wave-completion truth — P3's attempt-11 whole-owner review (R63) returned PASS-WITH-CORRECTIONS (0 blocking); the 2026-09-08 fix-up wave applied all corrections and recorded resolutions; no §5 change outstanding — verified per §G1.3. The "Review36 applied resolutions … changed §5 without a fresh PASS / provisional" sentence is gone from both sites; §0.2 keeps its Review20 historical-read lead-in and the preserved-contracts sentence, and §0.35 keeps the RC3-governing lead. Verified by re-reading the edited bytes; document line count unchanged at 3144.
- **F-2 (correction) — resolved.** Both P3 canonical-framing pins repointed to the reviewer's suggested `≈:3258-3285`: `docs/phase4/v1/PHASE_4_DOC.md:322` (§0.35, now `docs/phase3/v1/PHASE_3_DOC.md:532-666,1389-1410,3258-3285`) and `:1457` (§4.7, now `docs/phase3/v1/PHASE_3_DOC.md:3258-3285`). Verified against current P3 bytes before repointing: the `atom(s)`/`seq(v…)` length-prefixed scalar/string/list framing is defined at `docs/phase3/v1/PHASE_3_DOC.md:3264-3275` inside the new range, with the `MaterializedSource-v23` payload application at `:3356-3371` (`seq(atom("MaterializedSource-v23"), …)` at `:3359-3362`). Both replacements are same-width (`3233-3249` → `3258-3285`), so zero line shift.
  A full-document grep confirms the only remaining `3233-3249` occurrence is `docs/phase4/v1/PHASE_4_DOC.md:409` inside §0.42's dated Review-40 addendum — a historical record of what that wave recorded as verified, not a live pin, and not an R41-ordered site; per fix-up-wave constraints dated historical addenda are edited only where the review explicitly orders, so it is left byte-identical and this resolution supersedes it going forward.
- **N-1 (note) — disposition.** Applied. `docs/phase4/v1/PHASE_4_DOC.md:2746` (§9 milestone table) reworded in place from "implemented after the §5.4 legacy-geometry dependency gap closes" to "implemented after fresh owner/receiver verification; §5.4 legacy-geometry gap closed", consistent with §12 item 7 (`:3086`, "After fresh owner/receiver verification") and §5.4's closed-gap record.
- **N-2 (note) — disposition.** Recorded only. The site is §5.5 (`docs/phase4/v1/PHASE_4_DOC.md:2324-2328`), which this wave does not edit under the absolute §5 freeze; the missing Phase-2-enrichment edge request for §G5.1 (P2 consumes `candidate.view().resolutions()` enrichment per §5.6/IR-29/§0.36) is routed to the final §G5.3 item-4 integration review, alongside the §5.5 Phase-12 generation edge already requested. No bytes changed.

All corrections applied; §5 untouched (edited lines: 77-81, 322, 328-330, 1457, 2746, 3144 — none in §5); closing status updated at `:3144`; document remains 3144 lines. Per §G1.3 the phase document is **verified**.

# Phase 14 - architecture review 8

**Date:** 2026-09-08.
**Frozen target:** `docs/phase14/v1/PHASE_14_DOC.md` — SHA-256 `b185b50dea626f77eeee38c962c8236ee3d18134b703d384d4186bb9d5f699c5`, recomputed at wave open against disk; match, so the review proceeded (3,003 lines, read in full).
**Verdict: PASS-WITH-CORRECTIONS** — 0 blocking, 2 corrections (C-1, C-2), 1 note (N-1).
**§5 impact:** none — both corrections are anchor-precision fixes inside §3/§4.2.2; no consumed contract, grant, receiver obligation, or exported surface changes.

No build, test, Gradle, GL, or network execution was performed; this review is documentary and read-only apart from this file. `reference-src/**`, `docs/**/chatlogs/`, root `*.txt` transcripts, and all other `PHASE_*_REVIEW_*.md` files were not read.

---

## 1. Scope and method

Independent adversarial verification of the Attempt-11 frozen Phase 14 document under §G1.2, refutation posture, no author context. Inputs actually read:

- `docs/phase14/v1/PHASE_14_DOC.md` — complete (§§0–12, ll. 1–3003).
- Governing design: `docs/design/v3/DESIGN.md` Part I sections cited by the doc and load-bearing for its gate — §G0.3/§G0.4, §G1.1, §G2.3–§G2.5, §G3.2/§G3.3, §G4.1–§G4.6, §G5.1 graph, §G6, §G9 template, §G10 assignments, §G11.2/§G11.4/§G11.5, §G12.6, and the Phase 14 spec (`:2514`–`:2588`, read in full).
- `docs/research/v1/RESEARCH.md` ll. 11–107 (front matter, mission, non-goals, decision log D-1..D-10), plus the §4.8/§6.2/§6.3/§8/§9/§11 rows the doc cites (`:524-526`, `:558`, `:621-645`, `:741-798`, `:951`, `:999-1029`, `:1366`, `:1380-1381`, `:1401`).
- Dependency docs at §0 header / §1 / §5 / §11 depth, following incorporated sections where cited: `docs/phase5/v1/PHASE_5_DOC.md`, `docs/phase6/v1/PHASE_6_DOC.md`, `docs/phase7/v1/PHASE_7_DOC.md`, `docs/phase13/v1/PHASE_13_DOC.md`; narrow non-dependency reads recorded by the doc's own §0.2 discipline: `docs/phase1/v14/PHASE_1_DOC.md` (§1.2, §5, §7, D-P1-52/54/63/64/65), `docs/phase4/v1/PHASE_4_DOC.md` (§5.1, §5.7, §11), `docs/phase11/v1/PHASE_11_DOC.md` (§5.1, §10.1, §11), `docs/phase2/v2/PHASE_2_DOC.md` (§4.5.4, §5, §11) for the §5.1 negative claim.

Anchor hygiene: a sample of ~45 dependency citations was re-derived at stated coordinates (results in §3). Conformance-map rows were spot-checked against the cited RESEARCH/appendix text (results in §3).

## 2. Doc-gate and template verification

- **Hash gate:** recomputed digest equals the frozen value. Proceeded.
- **§G9 template:** all 13 sections present and ordered 0–12 (`docs/phase14/v1/PHASE_14_DOC.md:3`, `:256`, `:349`, `:461`, `:606`, `:1682`, `:2078`, `:2125`, `:2237`, `:2365`, `:2433`, `:2625`, `:2945`), matching `docs/design/v3/DESIGN.md:819`–`:853` ("sections may be short, never absent"). §12 is an ordered, milestone-tagged, test-hooked checklist as required.
- **Header discipline (§G9 item 0):** declared dependencies 5/6/7/13 match §G5.1 (`docs/design/v3/DESIGN.md:626`); assigned OQs OQ-15/OQ-22 match §G10 (`:878`, `:885`). §0.2 records the narrow non-dependency reads with reasons, per the §G1.1 gap-recording rule (`docs/design/v3/DESIGN.md:243`–`:245`).
- **OQ spike completeness (§G4.4):** §10.1 (OQ-15) carries the question verbatim from `docs/research/v1/RESEARCH.md:1021`, a concrete shared-context procedure, numbered success criteria S1–S6, explicit failure criteria, and the mandatory synchronous fallback (`docs/phase14/v1/PHASE_14_DOC.md:2237`), matching `docs/design/v3/DESIGN.md:575`–`:580`. §10.2 (OQ-22) carries the verbatim question (`docs/research/v1/RESEARCH.md:1028`), the P11-owned method (≥2 platforms, fixed scripted inputs, allocation-profile separation — consistent with `docs/phase11/v1/PHASE_11_DOC.md:1617`–`:1626`), thresholds, and the retain-interpreter fallback.
- **Failure modes (§6):** maps §G2.4's ladder case by case (`docs/design/v3/DESIGN.md:419`–`:449`); A-tiers degrade to `FORCE_OFF`/synchronous without touching contract-visible behavior; no row degrades by crashing or by silently adopting an ungranted optional mechanism.
- **Licensing/Pintonium compliance:** §7.3 quotes §G11.2 rule 2 verbatim ("treat as AGPL-3.0 — never copy, never adopt as a dependency (D-8)", `docs/design/v3/DESIGN.md:915`–`:916` at the cited `:915`–`:919`); no glsl-transformation-lib use anywhere; PD §18 pre-decided rejections honored (dynamic unit allocation rejected at `docs/phase14/v1/PHASE_14_DOC.md:511` citing `docs/design/v3/DESIGN.md:953`–`:954`, verified verbatim); contract-visible adoptions (A3 GPU candidate, DSA tiering, sampler objects, KHR_debug pattern) each carry a PD citation plus §G11.4 decision IDs, and A3's adoption is contract-checked against Phase 6's published SPI rather than PD §6.3 alone. D-7/D-8, §G7 and §G11.5 trust tiers are respected; no do-not-copy item is copied.
- **D-1..D-10 conformance:** scope stays inside the pipeline (D-1/D-2: every optimization is internal, §3 row at `:491` + §4.8 evidence discipline); matrix targets D-3; milestone tags v0.1–v0.5/post-v0.5 without implementation claims (D-4); §4.8 Keep/Adapt/Skip budget rows (D-5); headless-first §8.1 against the facade/recorded profiles (D-6, `docs/design/v3/DESIGN.md:705`–`:708` verified); GPL-3.0-or-later + AGPL prohibition (D-7/D-8); every A-tier is a compat-context mechanism with no core-profile rewrite (D-9); §8.2 procedures are Phase 2 harness runs with no new oracle (D-10).
- **Current-identity spot-checks:** D-P14-33 receives schema23/MaterializedSource-v23 as current and demotes D-P14-29/25/24/19 to historical — consistent with the registry's current-identity state and with P3-side receivers; D-P14-39 receives P4 D-P4-43 `RegistryFingerprint/profile-selection-v3` as current with D-P4-31 own-build-v1 and D-P4-33 positional-route-v2 historical (verified against `docs/phase4/v1/PHASE_4_DOC.md:1953`–`:1954`, `:2804`, `:2806`, `:2816`); §5.5 consumes `phase13.parameters/v2` as current with "never relabel v1" (`docs/phase14/v1/PHASE_14_DOC.md:1825`–`:1826`) and P13's v2 identity is confirmed current in `docs/phase13/v1/PHASE_13_DOC.md:196`; projectionVersion1 and the nine source-free trees are kept unchanged (`:2016`, `:2035`); no run-manifest or capture-plan schema version is cited anywhere, so no stale `/3` identity occurs; P2 is not pinned by versioned path at all. No stale identity cited as current was found.

## 3. Conformance-map and anchor audit

Spot-checked rows and coordinates (all verified unless listed in §5):

- §3 row "centerDepthSmooth" → `docs/research/v1/RESEARCH.md:642`, `:773` and Phase 6's selected Candidate A at `docs/phase6/v1/PHASE_6_DOC.md:1145`–`:1153` — exact ("Once per frame, synchronously read the center depth through `FramebufferService.readDepthPixel`", `:1149`–`:1150`).
- §3 row "Frame-begin sampling completes before resize/clear" → REV1 constraint `docs/design/v3/DESIGN.md:526`–`:528` verbatim; P6 beginFrame step 6 at `docs/phase6/v1/PHASE_6_DOC.md:1065`–`:1066` verbatim. The row's second P6 pin is correction C-2.
- §3 row "Everything refreshes on program switch" → `docs/research/v1/RESEARCH.md:1380` via `docs/phase6/v1/PHASE_6_DOC.md:604`–`:605` (activation-policy definition, exact) — and P6 itself reconciles the phrase at `:607`–`:609`.
- §3 rows "Stage semantics" → `docs/research/v1/RESEARCH.md:626`; "companion atlases / `0xFF7F7FFF`" → `:638` + `docs/design/v3/DESIGN.md:2498`–`:2500` (memory posture, exact); "DSA tiering", "sampler objects", "bindTextureToUnit rejection", "MULTI_BIND proposal", "KHR_debug" → `[V:observed]` Pintonium evidence tags present with PD §15 anchors; the §G4.6 rejection quote at `docs/design/v3/DESIGN.md:595`–`:596` is verbatim.
- §4.1.4 → P5 `textureBindings`/`shadowBindings`/`TextureBindingResult` semantics (`docs/phase5/v1/PHASE_5_DOC.md:2576`, `:2617`–`:2621`, `:2737`: Bound-only lease transfer, zero-bind Degraded/Rejected, BackendFailed-may-follow-partial-GL — all verbatim); BufferResize eight-reason ordered list matches `:2738` exactly, and that row's consumer column reads "Phases 13 and 14; G8/S2" as the doc claims.
- §4.1.5/D-P14-36/37 → P1 `DebugService` at `docs/phase1/v14/PHASE_1_DOC.md:3490`–`:3495` (`isActive()` with the no-debug-context-prerequisite comment), D-P1-54 gate text at `:6191`/`:4730`–`:4736`, per-call `glGetError` cadence at `:4993`–`:4995`, `RecordingGLDevice` bounded ring at `:4982` — all exact.
- §4.2.2 → P6 `CenterDepthSource` sealed interface inside `docs/phase6/v1/PHASE_6_DOC.md:716`–`:736` (actual block `:722`–`:734`); `CenterDepthResult.Sample/Unavailable`; §6 degradation row at `:2036`; half-lives at `:657`; `centerDepthMacroContributor()` empty at `:1826`; "does not hide the cost with an uncontracted one-frame queue" at `:2098`. One pin is correction C-1.
- §4.4.1/§5.4 → P7 ten-step transaction §4.1 steps 1–10 (`docs/phase7/v1/PHASE_7_DOC.md:917`–`:988`): step 3 compiles Phase 4 and plans/creates Phase 5 (`:951`–`:955`), step 7 is the synchronous resize delivery (`:972`–`:977`), step 8 builds Phase 13 after accepted generations ("Build after estate acceptance avoids guessed generations", `:983`), step 10 compensation (`:906`–`:908`) — the doc's ten-step summary at `docs/phase14/v1/PHASE_14_DOC.md:1771` matches point for point. `FrameExitKind`/`FrameAbortReason` five values exact at `docs/phase7/v1/PHASE_7_DOC.md:2652`–`:2655`; `FrameDriver`/`FrameHookSink` enter/exit sentence exact at `:623`–`:625` and `:2631`–`:2641`; reload types within `:2952`–`:2974` (`DriverReloadRequest` at `:2959`); "Phase 7 adds no readback" at `:4226` and OQ-3 "Make no context-flag change" at `:4470`–`:4473` verbatim; JFR posture "permits JFR attribution of its synchronous driver, not a public per-pass elapsed API" at `:577`, reinforced by IR-26 at `:4714`.
- §4.5.4/D-P14-18 → P7 debug posture "Debug hook timing uses preallocated counters and is off by default" at `docs/phase7/v1/PHASE_7_DOC.md:4229`, exact; `RUN_SCENE_SELFCHECK` repeated-frames row at `:4311`.
- §4.1.4/§4.2.1 → P1 `readDepthPixel` export at `docs/phase1/v14/PHASE_1_DOC.md:3422`; D-P1-52/63/64/65 present and scoped exactly as consumed (`:4527`–`:4616`, `:4618`–`:4620`, `:4712`–`:4715`, `:6194`–`:6205`); "bind-and-label-only" boundary at `:3234`–`:3235`.
- §4.3.5/§5.5 → P13 `TextureParameterSpec(MinFilter,MagFilter,WrapMode)` and closed domains at `docs/phase13/v1/PHASE_13_DOC.md:484`–`:490`, fingerprint at `:491`, params/v2 currency at `:196`/`:336`–`:337`, three-argument lease with `currentBaseBinding` and `INVALID_BASE_BINDING`/`STALE_BASE_BINDING` rejections at `:899`–`:902`/`:1484`–`:1492`, `isCurrent()` base-serial currentness at `:619`/`:1435`/`:1778`–`:1779`, four-argument legacy form removed at `:733`, wrap `S/T` for 2D/RECT and `RECTANGLE → CLAMP_TO_EDGE` + non-mipmap minification at `:2494`–`:2495`/`:1274`–`:1277`, reverse-order owned cleanup at `:1168`/`:1498`, `PARAMETERIZATION_UNSUPPORTED` at `:2136` — all exact.
- §5.8 → P11 `ExpressionMetricsSink.record(ExpressionMetrics)` and the ten-field immutable record at `docs/phase11/v1/PHASE_11_DOC.md:1064`–`:1067` — field-for-field identical to the doc's recitation; synchronous render-thread delivery, no-op default, sink-failure-disables-metrics-only all match P11 §5.
- §5.9/§5.10 recipient dispositions → P4 records R-P14→P4-1 as pending/ungranted (`docs/phase4/v1/PHASE_4_DOC.md:2385`, `:2451`–`:2453`, `:2797`, `:2897`) and keeps §5.1 `compile(RegistryBuildRequest) → Ready | ShadersOff` synchronous (`:802`–`:804`, `:2142`); P5 records R-P14→P5-2 as received/ungranted (`docs/phase5/v1/PHASE_5_DOC.md:3035`–`:3038`, `:3519`–`:3520`); P7's D-P7-64 records D-P14-32 as pending (`docs/phase7/v1/PHASE_7_DOC.md:4578`) and D-P7-55/63 receive the sampler-normalization dispatch and complete-baseline receipt (`:4570`, `:4577`).
- §5.1 negative claim → verified: `docs/phase2/v2/PHASE_2_DOC.md` (current v2) defines no modernization/environment-fact manifest field and records no such granted request; its `schmaloogium.run-manifest/4` field inventory (`:1144`–`:1149`) and §5 consumer rows (`:2425`–`:2426`, where 14 appears only for the `GLCapabilityProfile` fixture set) support "proposed only, not adopted".
- §5.6 → P1 §5 consumer columns name 14 for both `GLCapabilityProfile` (`docs/phase1/v14/PHASE_1_DOC.md:5510`, tail: "…, **7**, **8**, 14") and the GL-error surface (`:5508`, tail: "**6** …, 4, 5, 14"); the scope-out row is at `:1726` verbatim; the §7 invitation quote at `:5626` is verbatim.
- §G5.1 leaf claim → `docs/design/v3/DESIGN.md:611`–`:626`: no phase lists 14 in "Depends on"; P14's row is `5, 6, 7, 13`. The §3.5 Wave-5 contradiction (P14 in Wave 5 beside an unverified P13) is reported, not smoothed, with a requested upstream fix in §11.4 — correct §G1.1 behavior.

## 4. Interface honesty, identities, and dependency discipline

- **Consumed interfaces exist in owner §5s:** every §5 consumption was found at the cited owner coordinates (§3 above). No interface is consumed that the owner does not publish; where the owner changed (P5 §§2.4/4.12, P6 §5 resolver adoption, P7 §§4.1/5.3, P13 §§2.3/4.3.5/4.5/4.7/5), the doc says so and marks the change unverified rather than leaning on old PASS evidence.
- **Exports carry receiver adoption state:** `GlModernizationPlan` to Phase 2 is "proposed only" and the negative claim holds (§3); `CompileExecutor` is "proposal only" with no caller permitted to infer an async grant; `SamplerKey` is justified as the A1 equivalence surface; the `CenterDepthSource` implementation and `DebugService` implementation rows are correctly framed as `mod.glue` implementations of existing owner types, not new grants.
- **Ungranted requests are labeled and consequential:** R-P14→P1-1..4 (P1 records them; the thread-safety invitation is quoted verbatim), R-P14→P4-1 (pending, with the exact-schema owner-amendment requirement), R-P14→P5-2 (`MULTI_BIND` stays disabled without it), R-P14→P6-1 (`PBO_FENCE` stays `FORCE_OFF` if declined — the honest permission blocker), R-P14→P7-1/2 (synchronous operation remains without them), D-P14-32 upload inventory (pending P13/P7 adoption; P7 D-P7-64 confirms). No request is silently assumed granted.
- **Scope discipline:** §1.1/§1.2 ownership lines match §G5.1 and the dependency docs' own §1.2 rows (P1's "Owned by Phase 14" row verified; P7's ":576" ownership row verified). A4 does not choose/order/fall back programs (Phase 4's); A1/A5 touch no uniform upload or barrier participant; A3 is the only readback path; the §1 scope-out of staging/render-scale/instancing mechanics matches P13/P7 ownership.
- **Identity discipline:** current receipts (schema23, profile-selection-v3, params/v2) and demotions (schema19/20/21/22, positional-route-v2, own-build-v1, params/v1) are all correctly labeled; historical §0.3–§0.11 receipts are consistently marked as history, not active premises.

## 5. Findings

Severity scale: blocking (structural rebuild required) / correction (false or drifting statement to fix in place) / note (record-only).

- **C-1 — correction — §4.2.2 seam pin misses `CenterDepthRequest`.**
  Location: `docs/phase14/v1/PHASE_14_DOC.md:969` — "consumes it exactly as it exists (`docs/phase6/v1/PHASE_6_DOC.md:716`–`:736`, `:740`–`:749`)".
  Claim: the cited ranges cover the `CenterDepthSource`/`CenterDepthRequest` seam quoted in the following code block.
  Evidence: `docs/phase6/v1/PHASE_6_DOC.md:722`–`:734` holds the `CenterDepthSource` block (inside `:716`–`:736`), but `CenterDepthRequest` is declared at `:751`–`:753`; `:740`–`:750` contains `OnceUniformSample`/`FrameSampleRequest`/`FrameUniformSample`. Content is present and quoted accurately; only the second range lands ~2–5 lines early. Non-blocking; fix the pin to include `:751`–`:753`.
- **C-2 — correction — §3 row pin lands on the neighboring P6 §5 row.**
  Location: `docs/phase14/v1/PHASE_14_DOC.md:482` — "exported ordering contract at `docs/phase6/v1/PHASE_6_DOC.md:1816`".
  Claim: the cited line carries Phase 6's exported frame-begin ordering contract.
  Evidence: that contract row ("beginFrame completes sampling, previous snapshots and center-depth read before any Phase 5 resize/clear…") is at `docs/phase6/v1/PHASE_6_DOC.md:1818`; `:1816` is the `FrameBeginInput`/`FrameBeginResult` row. Content present, off by 2 lines. Non-blocking; repin to `:1818`.
- **N-1 — note — §0.2 historical pins predate Phase 1's fix-up growth.**
  Location: `docs/phase14/v1/PHASE_14_DOC.md` §0.2 — Phase 1 §1 scope-out row cited at `:1708` and §7 thread-ownership invitation at `:5608`.
  Evidence: in current `docs/phase1/v14/PHASE_1_DOC.md` bytes the scope-out row is at `:1726` (`:1707`–`:1709` is the §1.2 heading) and the invitation at `:5626`. §0.2 is explicitly the historical record of initial-build reads, and the live sections (§1.2, §5.6) pin `:1726`/`:5626` correctly, so no current-identity claim is wrong. Recorded for anchor hygiene only; no action required.

No blocking findings. No fabricated quotations were found: every quote spot-checked (RESEARCH, DESIGN, P1, P4, P5, P6, P7, P11, P13) matched its source verbatim. No stale identity is asserted as current. The known RESEARCH `:596`–`:597` attribution question is a Phase 13 matter; Phase 14 does not cite those lines.

## 6. Verdict

**PASS-WITH-CORRECTIONS** — 0 blocking, 2 corrections (C-1, C-2), 1 note (N-1). The document is structurally complete against §G9, its conformance map is honest and spot-checkable, all consumed interfaces exist in the owners' current §5s at the cited substance (two line-pin drifts corrected above), exports and requests carry truthful receiver-adoption state, current identities are current and historical ones are labeled historical, and licensing/Pintonium rules are honored. The two corrections are anchor-precision repins; neither changes a contract, a grant, a receiver obligation, or the §5 surface. Fresh whole-document review remains required after any fix-up that touches §§3/5 bytes; no implementation, test/validation result, OQ closure, or fresh PASS is claimed by this review. No build, test, Gradle, GL, or network command was executed.

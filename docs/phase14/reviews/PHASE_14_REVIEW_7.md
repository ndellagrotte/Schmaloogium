# Phase 14 — architecture review 7

**Date:** 2026-09-08. **Frozen document:** `docs/phase14/v1/PHASE_14_DOC.md` SHA-256 `fc774f7ce1e08d72d3ae3ece2ac032374fa38405180a19573878370435c73b96`. **Verdict:** PASS-WITH-CORRECTIONS (12 corrections, 5 notes, 0 blocking). **§5 impact:** none of the corrections changes a consumed contract shape, grant, or receiver obligation — they are citation re-anchors, duplicate-row removal, and garbled/orphaned-text repair; the duplicated §5.3 row is byte-identical content, so its removal leaves the consumed cross-phase contract set unchanged.

---

## 1. Scope, identity and authority actually verified

- Gate: `sha256sum docs/phase14/v1/PHASE_14_DOC.md` recomputed = `fc774f7ce1e08d72d3ae3ece2ac032374fa38405180a19573878370435c73b96`, equal to the frozen hash. The complete 3 004-line document was read (all sections §0–§12 plus trailer).
- Governing inputs read: `docs/design/v3/DESIGN.md` Part I §G0–§G12 (`:125`–`:1135`, heading-derived) and the Phase 14 specification (`:2514`–`:2589`, located by heading); `docs/research/v1/RESEARCH.md` ll. 11–107 plus every RESEARCH coordinate the §3 conformance map and §7.5 coverage check cite; the four declared dependencies' §0 header, §1, §5, and §11 of `docs/phase5/v1/PHASE_5_DOC.md`, `docs/phase6/v1/PHASE_6_DOC.md`, `docs/phase7/v1/PHASE_7_DOC.md`, `docs/phase13/v1/PHASE_13_DOC.md`; narrow current-byte checks in `docs/phase1/v14/PHASE_1_DOC.md` (§5 consumer rows, D-P1-52/54/63/64) and `docs/phase4/v1/PHASE_4_DOC.md` (D-P4-43, R-P14→P4-1 disposition) because the reviewed doc makes current-byte claims about them; `docs/phase2/v2/PHASE_2_DOC.md` (run-manifest/4, D-P2-53/57) for the §5.1 proposed-only claim; `docs/reference/pintonium/v1.0/PINTONIUM_DESIGN.md` §§6.3/15/16/17/18 at the cited coordinates.
- Authority chain verified: the doc declares v3 governance via its own §0 (no `verification/targets/` file exists; `docs/MOVES.md` rule honored as described). Every §G coordinate this review re-derived from v3's own bytes matched the doc's pins (Part I `:125`–`:1135`, Phase 14 spec `:2514`–`:2589`, §G9 template `:817`–`:854`, §G5.1 row `:626`, §G10 `:878`/`:885`).
- Current-identity ground truths all hold in the doc: schema23/`MaterializedSource-v23` current (D-P14-33, §5.9 ll. 1997–2019, §8.1 ll. 2273–2274, checklist item 36); D-P14-24/25/29 schema20/21/22 receipts explicitly historical (ll. 1999–2000, 2021–2023, 2663–2668); D-P14-39 carries P4 D-P4-43 `RegistryFingerprint/profile-selection-v3` (verified current at `docs/phase4/v1/PHASE_4_DOC.md:1937`–`:1938`); `phase13.parameters/v2` consumed, "never relabel v1" (l. 1825; v2-supersedes-v1 confirmed at `docs/phase13/v1/PHASE_13_DOC.md:1376`); projectionVersion=1 nine-tree boundary unchanged (ll. 2016, 2035); no `docs/phase2/v1/` path and no D-P2-32/38 citation anywhere in the doc.
- Not independently verified, by constraint: the §0.1–§0.3 historical citations to other phases' review files (`PHASE_5_REVIEW_37/38`, `PHASE_6_REVIEW_24`, `PHASE_7_REVIEW_32`) and their verdicts — other `PHASE_*_REVIEW_*.md` files are forbidden inputs for this round. The doc's own current posture (old PASS verdicts are historical and do not certify current bytes; §0.4, §5.2–§5.5 headers, trailer) is consistent with each dependency doc's own §0 status line and needs no review-file support.
- No build, test, gradle, GL, or network execution was performed; this review is documentary and read-only except this report file.

## 2. Scope and §5 audit

- **Scope discipline: sound.** The seven owned rows A1–A7 match the spec's Scope — in (`docs/design/v3/DESIGN.md:2528`–`:2561`), including the REV1 additions (DSA tiering `:2533`–`:2535`, conditional PBO `:2536`–`:2542`, PD-pattern-not-wiring KHR_debug `:2548`–`:2551`). Scope — out (`:2563`–`:2565`) is honored row by row in §1.2 with per-owner "Owned by Phase Y" lines; D-P14-18 adds no Mixin and no vanilla hook; §1.3 correctly establishes that the phase owns no contract-visible component, and nothing in §4 authors pack-observable semantics.
- **Conformance map audit: sound in substance.** All sampled rows resolve semantically against current cited text: fixed unit map/App B.3 (`docs/research/v1/RESEARCH.md:628`; PD §18 `docs/reference/pintonium/v1.0/PINTONIUM_DESIGN.md:808`; pre-decided rejection `docs/design/v3/DESIGN.md:953`–`:954`), ping-pong/flip (`RESEARCH.md:627`, P5 ownership), filter/wrap policy (`docs/phase5/v1/PHASE_5_DOC.md:1431`–`:1432`), shadow filters (`RESEARCH.md:524`–`:526`, P5 `:623`–`:624`), `centerDepthSmooth` (`RESEARCH.md:642`, `:773`; Phase 6 design `docs/phase6/v1/PHASE_6_DOC.md:1143`–`:1152`), frame-begin ordering (`docs/design/v3/DESIGN.md:526`–`:528`; P6 `:1816`), program set (`RESEARCH.md:626`), `_n`/`_s` defaults (`RESEARCH.md:638`; P13 spec `docs/design/v3/DESIGN.md:2451`–`:2454`), compat/`GL_QUADS` (`RESEARCH.md:758`, `:103`), no-UBO (`:759`), own-pipeline-only (`:72`), clean-code-first (`:784`). Zero unmapped in-scope rows found; the invariance-map framing (§3.1) is a correct application of §G9 to a phase with no contract-visible component.
- **§5 interface honesty: sound.** Every consumed interface exists in the owning dependency's current §5: P5 `textureBindings`/five-arg `shadowBindings`/`TextureBindingSnapshot` (Bound-only lease transfer; `docs/phase5/v1/PHASE_5_DOC.md:789`, `:1090`, `:1116`, `:2145`, `:2567`–`:2568`, §5 rows `:2723`, `:2729`), `TextureService.prepareUnitBindings(int)` with exactly the claimed placement (D-P5-39 at `:2780`), the eight ordered resize reasons (`:545`–`:552`, §5 row `:2730`), `ConsumerFailed` installed-off ownership and `deliveredCount` (`:722`–`:723`, `:2363`, `:2709`); P6 `CenterDepthSource`/`CenterDepthRequest`/`CenterDepthResult` with exact validation (`docs/phase6/v1/PHASE_6_DOC.md:716`–`:786`), D-P6-1 and the not-obviated sentence (`:2228`, `:1182`–`:1185`), the 2a degradation row (`:2034`), resolver adoption (`:1811`, `:1932`, `Ready(List<ResolvedSamplerBinding>,…)` at `:1202`), half-lives (`:655`), macro-empty (`:1824`), render-thread confinement (`:2051`–`:2052`); P7 `FrameDriver`/`FrameHookSink` (`docs/phase7/v1/PHASE_7_DOC.md:602`–`:603`, `:2610`–`:2618`), `FrameExitKind` plus the five current `FrameAbortReason` values exactly at `:2631`–`:2633`, `ReloadStatus{Queued,Building,Active,Off,Failed,Unknown}` (`:2930`–`:2952`), ten-step transaction retained (`:390`), "Phase 7 adds no readback" (`:4204`), OQ-3 default plan (`:4448`–`:4451`); P13 `TextureParameterSpec`/`TextureParameterFingerprint` (`docs/phase13/v1/PHASE_13_DOC.md:463`, `:470`), `TextureSystemFactory.create` (`:361`, `:1704`), three-argument `TextureLeaseSource.lease(..., AtlasBindingEvidence)` (`:368`–`:370`), D-P13-43 closed rejection set incl. `INVALID_BASE_BINDING`/`STALE_BASE_BINDING` (`:385`, `:1408`), base-serial `isCurrent()` (`:1414`, `:1476`), D-P13-36 (`:1109`), D-P13-27 baselines (`:1211`), `PARAMETERIZATION_UNSUPPORTED` (`:476`, `:1260`), `TextureHookHealth/application-v2` receipt (§5.1 head). Exposed contracts are specified with receiver adoption status: proposals and requests are marked ungranted/proposed-only, and the one checkable recipient claim is accurate — P4 records R-P14→P4-1 "received, pending/not adopted" (`docs/phase4/v1/PHASE_4_DOC.md:2369`, D-P4-24 at `:2781`).
- **The §5.1 P2 run-manifest claim is accurate against current bytes:** `docs/phase2/v2/PHASE_2_DOC.md` defines `schmaloogium.run-manifest/4` whose `environment.*` block carries os/jvm/versions/hashes only — no modernization-tier field — and its D-P2-53/D-P2-57 are P2-side evidence receipts, not manifest-field grants; "proposed only … no such manifest field … no granted request" (l. 1691) is therefore correct, and no `phase2/v1` path is cited.

## 3. Required corrections

Each finding: **location / claim / evidence / severity = correction.** All are anchor or editorial defects; none requires a doc rebuild.

1. **`docs/phase14/v1/PHASE_14_DOC.md:1669` (§4.7.3) / P6 anchor drift.** The quote *"Sampler integers usually skip after the first activation of a linked program"* is pinned to `docs/phase6/v1/PHASE_6_DOC.md:1542`–`:1543`. Current P6 bytes carry that sentence at `:2080`; `:1542`–`:1543` are a code-block close and a blank line. Evidence: `grep -n "usually skip after the first activation"` → single hit `docs/phase6/v1/PHASE_6_DOC.md:2080`. Severity: correction.
2. **`docs/phase14/v1/PHASE_14_DOC.md:482` (§3.1) and `:986`–`:988` (§4.3.1) / P6 anchor drift.** The quoted beginFrame step-6 text *"read the previous frame's main depth attachment at (floor(priorFramebufferWidth/2), floor(priorFramebufferHeight/2))"* and the step-7 smoother advance are pinned to `docs/phase6/v1/PHASE_6_DOC.md:578`, which is a §2.3 data-flow diagram line; the quoted numbered steps are at `:1063`–`:1065`. Quote substance verified verbatim at `:1063`–`:1064`; step 7 paraphrase at `:1065`. Severity: correction.
3. **`docs/phase14/v1/PHASE_14_DOC.md:1094`–`:1097` (§4.3.4) / P7 anchor drift.** The quote *"On an active frame, Phase 6 samples the prior depth first; Phase 7 then abandons that shader frame …"* is pinned to `docs/phase7/v1/PHASE_7_DOC.md:1046`–`:1051` (per-frame machine diagram intro); the sentence is at `:1546`. Severity: correction.
4. **`docs/phase14/v1/PHASE_14_DOC.md:487` (§3.1) and `:1468`–`:1470` (§4.5.5) / P7 anchor drift, internally inconsistent.** *"only if it is sanctioned by Cleanroom, preserves legacy fixed-function behavior, and passes client startup/resize on every spike platform"* is pinned to `docs/phase7/v1/PHASE_7_DOC.md:2317`–`:2319` (actual `:4445`), and *"Make **no** context-flag change"* to `:2321` (actual `:4448`) — while §5.4's own table row (l. 1775) already cites `:4448`–`:4451` correctly. The §0.11 re-derivation was partial. Severity: correction.
5. **`docs/phase14/v1/PHASE_14_DOC.md:292`–`:294` (§1.2) / P5 anchor drift.** The Phase-5 ownership list is pinned to `docs/phase5/v1/PHASE_5_DOC.md:339`–`:356`, which now contains §0.44/§0.45 addendum text ("…RC3 remains the governing revision…"), and the hand-over quote *"sampler objects, DSA modernization, asynchronous transfers, persistent staging, and performance tuning"* is pinned to `:384`–`:386`, now the "### 0.45 Attempt-5 …" heading; the quoted text is at `:465`–`:466`. Evidence: sed of both ranges; `grep -n "sampler objects, DSA modernization"` → `docs/phase5/v1/PHASE_5_DOC.md:465`. Severity: correction.
6. **`docs/phase14/v1/PHASE_14_DOC.md:622`–`:623` (§4.1.1) / P5 anchor drift.** "Phase 5 allocates a main/alt pair per logical colortex and applies filter/wrap to both sides" is pinned to `docs/phase5/v1/PHASE_5_DOC.md:848` (a `DrawBuffersNoneCloseResult` record) and `:1042`–`:1045` (a `TextureCandidateEntry` record block); the supporting content is at `:421`, `:1177`, and the filter policy text at `:1431`–`:1432` (which §3.1/§8.1 already cite correctly). Severity: correction.
7. **`docs/phase14/v1/PHASE_14_DOC.md:890` (§4.2.2) / P5 anchor drift.** *"All GL state cached by `GlStateManager` is changed through Phase 1's backend discipline"* is pinned to `docs/phase5/v1/PHASE_5_DOC.md:2163` (a `FramebufferHandle` field); the sentence is at `:3104`. Severity: correction.
8. **`docs/phase14/v1/PHASE_14_DOC.md:1733` and `:1735` (§5.3); `:2170` and `:2172` (§7.3); `:2383` and `:2385` (§9.1) / duplicated table rows with divergent anchors.** §5.3 repeats the identical `CenterDepthSource` SPI contract row (pins `:716`–`:749` vs `:716`–`:786`); §7.3 repeats the identical hot-path row (pins `:2093` — correct — vs `:2090` — stale, a blank/heading line); §9.1 repeats the identical "Synchronous `CenterDepthSource` (A3 fallback)" milestone row (pins `:2204` vs `:2192`, both individually valid rows). Merge leftovers from the fix-up rounds; in §5 the duplicate sits in the contract section itself. Severity: correction.
9. **`docs/phase14/v1/PHASE_14_DOC.md:1379` (§4.5.1) / orphaned fragment.** The line `only at Phase 14**"* (\`docs/phase5/v1/PHASE_5_DOC.md:3108\`).` floats between the `DebugService` code block and §4.5.2 with no opening sentence — the tail of P5's "`DebugService` labels are present from v0.1 and activate only at Phase 14." (`:3108` verified) whose head was lost in an edit. Severity: correction.
10. **`docs/phase14/v1/PHASE_14_DOC.md:2717`–`:2720` (§11.2, C-3) / garbled duplicated passage.** The clause "Phase 7's OQ-3 fallback — its **default plan** — is *"Make" appears twice (ll. 2717 and 2719) and the intervening text is scrambled ("Under both, no debug / (`docs/phase1/v14/PHASE_1_DOC.md:5601`). …"). The intended sentence is reconstructable but the recorded contradiction is currently unreadable at the point a reader needs it. Severity: correction.
11. **`docs/phase14/v1/PHASE_14_DOC.md:354`–`:355` (§2.1) / quotation not verbatim in the governing document.** *"`mod.glue` is the only place LWJGL is called"* is presented as a quotation of `docs/design/v3/DESIGN.md:488`/`:598`; no such sentence exists anywhere in v3 (`grep -n "only place"` → no hits). The rule itself is real but worded differently ("All engine GL goes through the `engine.gl` facade — no direct LWJGL calls outside `mod.glue`'s facade implementation", §G4.6 region `:593`–`:596`; §G3.1 `:488` names the module row). The claim is true; the quotation and its anchors are not. Severity: correction.
12. **`docs/phase14/v1/PHASE_14_DOC.md:1621`–`:1622` (§4.7.1) and `:2210` (§7.5 L-7) / P1 anchor drift.** "bind-and-label-only" is pinned to `docs/phase1/v14/PHASE_1_DOC.md:1622` (an addendum sentence fragment); current support is at `:1337`, `:1858` (§5 row), `:3216`, `:5473`. L-7's "`supportsMipmapGeneration()` … as `atLeast(3,0)`" is pinned to `:1647` (blank at current bytes); current support is `:2972` and the §5 row at `:1883`. Severity: correction.

## 4. Notes

1. **Location:** §0.1–§0.3 (`docs/phase14/v1/PHASE_14_DOC.md:38`–`:167`). **Claim:** historical citations to other phases' review files and verdicts. **Evidence:** those files are forbidden inputs for this review round and were not opened; the doc itself already demotes every old PASS to historical/non-certifying (§0.4, §5.2/§5.3/§5.4 headers, trailer ll. 2998–3004), which matches each dependency doc's own §0 status lines. **Severity:** note.
2. **Location:** §4.5.1 l. 1375 and §5.6 l. 1904. **Claim:** the `isActive()` comment is quoted as "installed supported GL4.3/KHR backend && glLabels (P1 D-P1-54)". **Evidence:** P1's current bytes read "installed supported backend && glLabels; no debug-context prerequisite (§4.7.8)" (`docs/phase1/v14/PHASE_1_DOC.md:3472`–`:3477`); substance identical, wording paraphrased inside a code quote. **Severity:** note.
3. **Location:** §11.6 item 6 (ll. 2940–2942). **Claim:** no RESEARCH appendix was read; the verify session should check §3 for unperturbable-omissions. **Evidence:** sampled the §3.1 rows against the reachable RESEARCH §3/App text (`:626`–`:628`, `:638`, `:641`–`:645`, `:524`–`:526`, `:758`–`:759`, `:1380`–`:1381`, App B.3 unit 11 via PD §18 `:808`) — no perturbable contract row is missing from the map. **Severity:** note.
4. **Location:** `docs/design/v3/DESIGN.md:2586` vs `docs/phase14/v1/PHASE_14_DOC.md:162`–`:165`, `:2798`–`:2802`. **Claim:** the spec's "≈ 34k tokens mandatory reading" remains materially wrong (dependency docs alone exceed 130k lines-sum). **Evidence:** confirmed at `:2586`; the doc reports it upstream (§11.4 item 2) instead of absorbing it. **Severity:** note (upstream, not this doc's defect).
5. **Location:** §7.5 coverage check (ll. 2227–2234). **Claim:** §6.2 has ten rows and §6.3 five, all accounted. **Evidence:** verified by reading `docs/research/v1/RESEARCH.md:769`–`:778` (ten rows) and `:784`–`:788` (five rows); the ledger/not-ledgered partition (including `:771`, `:776`, `:777`, `:788`) is accurate row by row. **Severity:** note.

## 5. Areas audited and found sound

- **Doc gate (spec `docs/design/v3/DESIGN.md:2578`–`:2581`), literally met.** Each Adapt row has design + fallback + ledger entry: A1 (§4.1/§4.1.7/L-2), A2 (§4.2/§4.2.5/L-6, with the missing-RESEARCH-row gap made a ledger row and an upstream request, C-6), A3 (§4.3/§4.3.7/L-3), A4 (§4.4/§4.4.6/L-4), A5 (§4.5/§4.5.6/L-5), A6 (§4.6/§4.6.4/L-8), A7 (§4.7/§4.7.4, feeding L-2/L-8). OQ-15 spike complete (§10.1: verbatim question `docs/research/v1/RESEARCH.md:1021` verified, procedure, K1–K5, fallback designed now). Imperceptibility verification specified concretely (§4.3.6, criteria C1–C4). The PBO conditional is resolved against Phase 6's recorded decision, verified at current bytes (`docs/phase6/v1/PHASE_6_DOC.md:2228`, `:1182`–`:1185`, `:636`). DSA tiering is integrated as a facade-internal strategy row (§4.2, D-P14-5).
- **Template completeness:** all thirteen §G9 sections present and substantive (§0–§12 per the skeleton at `docs/design/v3/DESIGN.md:817`–`:855`); both assigned OQs carry full four-part spike specs (§10.1, §10.2 incl. S-22-1…S-22-6), matching §G4.4 (`:575`–`:580`); §G10 ownership matches (`:878`, `:885` both name P14).
- **D-1…D-10 honored** (§11.3, spot-checked): D-9 compat baseline (every tier is compat-legal; the only context-flag touch is OQ-3-gated), D-6 seam (`:engine` pure values only, all LWJGL in `mod.glue`), D-2 non-goals made structural (D-P14-16, delta-only reporting), D-5 no new Mixin (D-P14-18), no-UBO respected (§3.1), no decision contradicts the decision log.
- **Pintonium compliance:** every PD-derived claim sampled carries a `[V:observed]` provenance tag and resolves at the cited source lines (PD `:312`–`:332`, `:737`–`:749`, `:756`–`:779`, `:793`, `:795`, `:797`, `:808`); the §G11.4 pre-decided rejection is honored (D-P14-3 at `docs/design/v3/DESIGN.md:953`–`:954`); standing do-not-inherit rows B7/B9/B11 are demonstrably handled in §3.3 with balance-by-construction (D-P14-13/37), single-naming-path labels (D-P14-40), and no new state save/restore; no AGPL `glsl-transformation-lib` trace (§3.3; §0.2 records it was never opened); no PD §17 stub re-inherited.
- **§G5.3/Wave-5 contradiction (C-4)** is reported with ruling and provenance, not smoothed over, and the ruling follows the design's own subordination clause (`docs/design/v3/DESIGN.md:659`–`:671`, verified verbatim).
- **Failure/threading posture:** §6 maps every failure to §G2.4 rungs with no false safe-restoration claims; §7.1 confines everything to the render thread except the §G2.3-sanctioned worker (`docs/design/v3/DESIGN.md:412`–`:415`, quote verified verbatim); the D-P14-32 dual-fence upload ordering and §5.9 quarantine/ownership accounting are internally consistent across §4.4.2, §5.9, §8.2, §10.1.
- **Current-identity discipline:** clean (see §1) — no stale identity is cited as current anywhere the doc speaks in the present tense.

## 6. Verdict

**PASS-WITH-CORRECTIONS** — 12 corrections, 5 notes, 0 blocking.

The document's architecture, scope discipline, interface honesty, template completeness, decision compliance, licensing posture, and current-identity handling are sound; every defect found is a stale citation anchor shifted by same-day dependency addenda (the known drift class the §0.11 re-derivation partially missed), duplicated table rows from the fix-up rounds, an orphaned fragment, one garbled passage, and one governing-doc quotation that is a paraphrase presented verbatim. None requires rebuilding the document; all are fixable in a §G1.3 fix-up session, and none changes a cross-phase contract. No build, test, gradle, GL, or network command was executed by this review.

## Resolutions

2026-09-08 fix-up (attempt-10 wave):

Basis: the reviewed doc's bytes had already moved past this review's frozen hash before this session (fc774f7c… → 979c4e2e…, a pre-sweep anchor re-derivation wave), so every site was re-located by searching current bytes (never by review line number) and every cross-document claim re-verified against current sibling bytes. Dependency docs are under concurrent fix-up, so per the drift-avoidance contract no cross-document line pin was re-pointed here; pure re-anchor corrections are recorded below and left for the dedicated citation re-derivation pass. No new D-P14 decision IDs were needed; no §0 addendum was required.

- **Correction 1:** note — current bytes already carry the corrected pin `docs/phase6/v1/PHASE_6_DOC.md:2080`–`:2081`; quote verified at P6:2080–2081 (sole grep hit). No edit required.
- **Correction 2:** note — pure re-anchor, delegated to the dedicated pass. Verified: step-6 quote verbatim at P6:1063–1064, step 7 at `:1065`; P6:578 is a line of the frame-begin `text` code block. Current doc still pins `:578` (§3.1) and `:1147`–`:1148` (§4.3.1); both left standing for the pass.
- **Correction 3:** note — current bytes already cite P7:1545–1548; sentence verified at P7:1546. No edit required.
- **Correction 4:** note — current bytes already cite `:4448`/`:4448`–`:4451` at all three sites (§3.1, §4.5.5, §5.4); quotes verified at P7:4445–4446 and `:4448`–`:4451`. The §4.5.5 start-pin `:4444` (quote begins `:4445`) left for the pass.
- **Correction 5:** note — current bytes already cite P5:418–435 and `:465`–`:467`; quote verified at P5:465–466; P5:339–356 confirmed to now hold §0.44-era rebuild prose. No edit required.
- **Correction 6:** note — current bytes already cite P5:1177 and `:1431`–`:1432`; support verified at P5:1177 (main/alt pair row, filter applied to both sides). No edit required.
- **Correction 7:** note — current bytes already cite P5:3104; sentence verified verbatim at P5:3104. No edit required.
- **Correction 8:** applied — §5.3 kept the `:716`–`:786` validation-span row and deleted the `:716`–`:749` twin; §7.3 kept a single `:2093` hot-path row (both copies had converged to the correct pin; one removed); §9.1 kept the `:2204` row and deleted the `:2192` twin. Consumed contract set unchanged, as the review anticipated.
- **Correction 9:** applied — orphaned tail repaired into the full sentence "Phase 5 records the same posture: *"`DebugService` labels are present from v0.1 and activate **only at Phase 14**"* (`docs/phase5/v1/PHASE_5_DOC.md:3108`)." Pin `:3108` reused unchanged (verified at P5:3108).
- **Correction 10:** applied — §11.2 C-3 rebuilt from the surviving fragments: the duplicated "Phase 7's OQ-3 fallback…" splice was removed and the sentence completed ("Under both, no debug context would exist on any shipping configuration, leaving `DebugService` … — dead code."). P7:4448–4451 and DESIGN:587–589 pins reused unchanged; the dead P1 line pins went with the rewrite (see Sweep 1).
- **Correction 11:** applied — pseudo-quotation replaced with the verbatim §G4.6 rule ("All engine GL goes through the `engine.gl` facade — no direct LWJGL calls outside `mod.glue`'s facade implementation", verified at DESIGN:593–594) under an explicit §G4.6 anchor; old pins `:488`/`:598` reused unchanged; grep confirms "only place" occurs nowhere in v3.
- **Correction 12:** note — current bytes already cite P1:3216–3217 (§4.7.1) and P1:2972 (L-7); both verified at current P1 bytes, with the review's additional support rows (:1337, :1858, :1883, :5473) confirmed present. No edit required.
- **Note 1:** note — no action; the doc already demotes every historical PASS to non-certifying, and this round likewise opened no other phase's review file.
- **Note 2:** note — no action; the code-comment paraphrase is substance-identical to the current D-P1-54 gate (P1:3476 "installed supported backend && glLabels; no debug-context prerequisite (§4.7.8)").
- **Note 3:** note — no action; §11.6 item 6's instruction to the verify session stands as written.
- **Note 4:** note — upstream defect (design spec `:2586` "≈34k tokens" claim); already reported upstream by this doc's §11.4; no receiver-side edit belongs in this document.
- **Note 5:** note — no action; the §6.2/§6.3 coverage partition was verified accurate by the review; nothing to change.
- **Sweep 1:** applied — both named sites reconciled to the current D-P1-54 contract: §4.5.5 now labels the old quote "pre-D-P1-54 wording; P1 §4.7.8's current gate has no debug-context prerequisite" (dead P1:3064 pin dropped), and §11.2 C-3 carries the same reconciliation inside the rebuilt passage (dead P1:4714–4715/:5601 pins replaced by decision/section anchors, since current P1:4714–4715 hold the superseding §4.7.8 gate and P1:5601 holds unrelated sealed-type javadoc). Verified: "false unless a debug context" occurs nowhere in current P1; the current gate text verified at P1 §4.7.8/:3476.
- **Sweep 2:** applied — same reconstruction as Correction 10; the orphaned P1:5601 cite was released rather than re-pinned (a concurrent P1 fix-up would re-stale any new pin).
- **Sweep 3:** applied — §0 gate narrative now reads "At gate time `docs/phase13/reviews/` contained only `.gitkeep` — zero review rounds then; PHASE_13_REVIEW_1–10 have since landed.", and §11.2 C-1's matching claim was updated the same way; both rulings' gate-time basis preserved (PHASE_13_REVIEW_1–10 existence verified by directory listing).
- **Session note (doc integrity, not a review finding):** the first §9.1 duplicate-row cut silently removed the adjacent legitimate "Async `_n`/`_s` atlas upload (A4)" row (the edit tool applied it while returning a stale content window); the §9-span audit caught the one-line loss and the row was restored verbatim. §9.1 again carries exactly one `CenterDepthSource` row, the restored A4 row, and every other original row; section/fence/line-count arithmetic balances against every intended edit.

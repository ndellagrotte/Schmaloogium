# Phase 10 — architecture review 5

**Date:** 2026-09-08. **Frozen document:** `docs/phase10/v1/PHASE_10_DOC.md` SHA-256 `67d7ade2b1fb2ba769ab1abdef2b695055f9798ece758e91bad1f6fd34ee4b4e`. **Verdict:** PASS-WITH-CORRECTIONS (2 corrections, 2 notes). **§5 impact:** YES — both corrections amend §5 ledger text (the §5.2 registry-fingerprint domain name and the §5.1/§5.3 receipt-status cells); no grant, interface or semantic rebuild is needed.

---

# Phase 10 Architecture Review — R5 / Frozen Attempt 8

**Owner:** `docs/phase10/v1/PHASE_10_DOC.md`
**Frozen SHA256:** `67d7ade2b1fb2ba769ab1abdef2b695055f9798ece758e91bad1f6fd34ee4b4e` — verified by checksum before review; matches `docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_8.json` phase 10, reviewRound 5 (that attempt recorded only an infrastructure failure; no prior attempt-8 report prejudices this one).
**Corrections:** 2. **Notes:** 2. **§5 impact:** YES (two ledger-text amendments inside §5; no grant, semantic or structural rebuild).

## 1. Scope and selected authority

This was a fresh independent whole-owner review of §§0–12, not merely D-P10-30. I read the complete owner document verbatim, its header-selected governing `docs/design/v3/DESIGN.md` Part I assignment (Phase 10 specification, mandatory template and document gate), `docs/research/v1/RESEARCH.md` §4.6 and Appendix C in full, the current Resolutions of `docs/PHASE_INTEGRATION_REVIEW.md` (including the attempt-5/6/7 correction waves), the historical `docs/phase10/reviews/PHASE_10_REVIEW_4.md` with appended Resolutions (treated as evidence only; its C1 resolution was independently re-derived, not trusted), and the frozen attempt-8 manifest. Dependency review covered the actual current headers and §5/§4 surfaces of P1 (v2.0-RC2 confirmed), P3/P4/P7/P9 (v2.0-RC3 confirmed) and P2 (v3 confirmed), matching the attempt-8 design-revision allocation; P10's own header selects v3 as required.

## 2. Central scrutiny areas — independently verified

1. **D-P10-30 ingress mechanics against primary native evidence.** I fetched the vanilla 1.12.2 `BufferBuilder`, `BlockModelRenderer`, `DefaultVertexFormats`, `TexturedQuad` and `BlockFluidRenderer` sources and read the Cleanroom 0.6.12-alpha `BlockModelRenderer.java.patch` and `ForgeBlockModelRenderer.java:49–86`. Findings: (a) vanilla BLOCK is POSITION/COLOR/UV0/LIGHTMAP = 28 bytes with no normal, ITEM is POSITION/COLOR/UV0/NORMAL+padding = 28 bytes — the owner's "vanilla block storage is 28 bytes; normal at 28 is also new storage" and RESEARCH App C.1 agree exactly; (b) **both** `renderQuadsFlat` and `renderQuadsSmooth` do `addVertexData(quad.getVertexData())` then `putBrightness4(...)` per quad, so the owner's flat-and-smooth obligation coverage and T10-PARTIAL-BLOCK are correct; (c) `putBrightness4` (`func_178962_a(IIII)V`, MCP-verified readable `putBrightness4`, "Set the brightness for the previously stored quad") writes four packed ints at `(count-4)*intsPerVertex + getUvOffsetById(1)/4` with stride `size>>2` — i.e. it derives offset/stride from the builder's installed format, exactly the owner's "invokes the real writer using the projected format's UV1 offset and stride"; (d) vanilla `addVertexData` is a raw append whose `count += len/format.getIntegerSize()` is wrong for ITEM→56-byte projection, confirming the owner's mandated authenticated adapter conversion rather than raw append; (e) the trailing `putColorMultiplier`/`putPosition` calls are color/translation-only late mutations matching the owner's Late-mutation policy (translation-invariant tangent). MCP also confirmed `func_187315_a=tex(DD)`, `func_187314_a=lightmap(II)`, `func_181667_k=nextVertexFormatIndex()V`, `func_178581_b=rebuildChunk`. The scope/range/epoch obligation tracking, exceptional invalidation (partial/throwing write → whole-product INVALID + original exception propagated + finally cleanup), and pre-publication rejection at model-scope exit, outer block exit, save/sort/seal, upload/capture/draw admission or target displacement are all present, internally consistent, and safe against the interleavings vanilla can produce (a shifted last-quad target fails the exact-range check and cannot discharge the obligation).
2. **H10-BRIGHTNESS-4 CORE expected-one, full v0.3/v0.5 only.** The §4.11 main-table row, the "D-P10-30 health delta" block ("Full v0.3/v0.5 catalog/fingerprint only; the seven H10-WRITER-* rows and v0.1 base subset remain"), the v0.1 base-subset enumeration (excludes it), receiver docs (P7 D-P7-75 "full v0.3/v0.5 only … v0.1 subset is unchanged"; P2 planned evidence case at `PHASE_2_DOC.md:2957`) and §12 item 2 are mutually consistent.
3. **Cached arrays and inherited OLDMODEL unchanged.** "Cached BakedQuad arrays remain immutable", "Never mutate the caller's array/buffer contents … or stamp IDs into cached BakedQuad data", "genuinely uncolored OLDMODEL keeps COLOR/UV1 absent and inherited rather than acquiring an obligation", D-P10-30's decision text and T10-PARTIAL-BLOCK's byte-identical-ITEM-array and A/B playback assertions all cohere; D-P10-29's live-inheritance law is unchanged.
4. **Complete capture-plan fixture receipt.** P1 §4.7.6 D-P1-70 grants exactly `borrowedVertexList(String label, VertexLayout layout, VertexInputPlan capturePlan)` with private retention of the complete immutable plan before first replay and removal of the former geometry-only overload; P10 §5.3's R10-1 adoption and T10-LIST-FIXTURE-AUTHORITY match it, and P7 D-P7-75 records the same factory.

## 3. Whole-owner and §5 audit

The 13 mandatory sections are present and substantive (attempt-8 structural checks pass; my read confirms). Layout/math (§4.1/§4.3 incl. N×T handedness and the 28-byte-prefix statement), stacks/tasks, saved states, draw paths, model/sky cached products, FastTESR ranges, lighting policy, compat table, spikes, milestones and checklist were checked against RESEARCH/governing design and current dependency contracts. Every consumed grant exists and matches on the receiver side: P1 D-P1-50/53/55/68/70 (§4.7.6/§5), P7 R10-2 (§0.43, §5.1 accepted participant), D-P7-32 (R10-6, eight model rows), D-P7-53 (R10-7, seventeen sky rows — count re-verified: 3+4+3+7), D-P7-54 + P9 §4.12.1/§5.6, P4 D-P4-28 (geometryInput) with locations 10/11/12, P3 `CURRENT_SCHEMA_VERSION = 23` with D-P3-68/69, P2 H10-BRIGHTNESS-4 forwarding. **Current §5 grants are sufficient in substance**: no consumed contract is missing; both findings below are accuracy defects in P10's own §5 ledger text, not absent owner grants.

## 4. Corrections

### C1 — §5.2 cites the superseded `RegistryFingerprint/own-build-v1` domain
`docs/phase10/v1/PHASE_10_DOC.md:1819-1822` (schema23 receipt paragraph): "P4 D-P4-31's `RegistryFingerprint/own-build-v1` remains opaque identity carried normally through P7". Current P4 advanced the binding domain to `RegistryFingerprint/profile-selection-v3` (D-P4-43; `docs/phase4/v1/PHASE_4_DOC.md:1938`, `:2800` — "distinct from every earlier registry domain", prior own-build fields retained), and current P7 receives/carries exactly `profile-selection-v3` opaquely (`docs/phase7/v1/PHASE_7_DOC.md:3730`, `:3746`). Failure path: the eventual §G5.3 integration audit reconciling P10's consumed-contract row against P7's carried identity hits a versioned-domain mismatch; under this project's exact-current identity discipline (the same class as the P3 schema 19→23 chain and the /3 wire-major migration), a receiver naming a superseded domain is an unreconciled §5 surface even though the value is opaque to P10. Minimal owner fix: amend the sentence to name `RegistryFingerprint/profile-selection-v3` (P4 D-P4-43; D-P4-31's own-build fields retained within it), preserving the opaque-carrier semantics; no receiving change.

### C2 — §5.1/§5.3/§11.3 mark already-granted P7/P2 receipts as outstanding
`docs/phase10/v1/PHASE_10_DOC.md:1702` (hook-subreport row: "new receiver receipt required"), `:1840` (R10-8 gate: "Owner-specified here; Main receiver receipt and fresh applicable review required"), `:1841` (R10-9 gate: "P7 coordinated receiver integration … remain") and `:2308-2310` (§11.3: "Main must receive the seven H10-WRITER-* IDs … into P7 and forwarded P2 evidence") all present the R10-8/R10-9 receipts as pending. Current P7 already grants D-P7-62 (`PHASE_7_DOC.md:1858-1872`: all seven H10-WRITER-* rows with exact descriptors, fingerprint recompute, unchanged v0.1 subset, identical P2 forwarding), D-P7-75 (`:1874-1879`: H10-BRIGHTNESS-4 full-v0.3/v0.5-only receipt) and D-P7-70 (`:2501-2510`: conventionalInputs receipt), and P2's current planned case forwards H10-BRIGHTNESS-4 (`PHASE_2_DOC.md:2957`). This misclassifies receiver-adopted/unverified grants as genuinely missing under the IR-04 grant-ledger discipline and is asymmetric with the R10-6/R10-7 rows (and R10-9's P1 half), which cite their exact decision IDs. Failure path: an integration reader cannot close R10-8/R10-9 edge status from P10's §5 and would either re-derive it from P7 or wrongly hold the edges open; §11.4's Phase-7 handoff enumeration likewise omits H10-BRIGHTNESS-4 among the received rows (only §4.11/§5.1's D-P10-30 row/§12 item 2 name it). Minimal owner fix: update the four cells to the R10-6/R10-7 style citing D-P7-62/D-P7-75/D-P7-70 (and P2's forwarding) as receiver-adopted/unverified, keeping "fresh applicable review required" as the genuinely outstanding part, and name H10-BRIGHTNESS-4 in the §11.4 enumeration.

## 5. Notes and verification limits

1. **Documentary review only.** No build, test, formatter, linter, runtime, transformed-bytecode-cardinality or native GL evidence was run or demanded; the owner honestly defers runtime proof (§11.3), which is not held against it. My MCP checks establish mappings (func_178962_a/putBrightness4, tex, lightmap, nextVertexFormatIndex, rebuildChunk) and the fetched vanilla/Cleanroom sources corroborate the producer path; they do not certify the pinned transformed runtime.
2. **Historical provenance remains qualified.** `reference-src/cleanroom-0.6.6-alpha` and `pintonium-9c2fcc1` are absent from the checkout (R4 N1 carries over); current corroboration uses Cleanroom-0.6.12-alpha/Pintonium-main, and §0.11 explicitly qualifies this. Relatedly (optional, no change demanded): §0.1's dependency-gate table cites P4 §0.35/P7 §0.40 rebuild notices that have since advanced; it is authoring-time provenance already self-qualified by §0.1's reconciliation requirement.

## 6. Verdict

The frozen owner document is architecturally complete and correct on every scrutinized axis: the D-P10-30 partial-ingress/completion law matches real vanilla producer behavior in both lighting paths with sound failure semantics; H10-BRIGHTNESS-4 is scoped exactly as resolved (CORE, expected-one, full v0.3/v0.5 only); cached BakedQuad arrays stay immutable and OLDMODEL inheritance is unchanged; the capture-plan fixture receipt is complete on both ends; and all consumed §5 grants exist and are substantively sufficient. Two localized §5 ledger-accuracy defects remain (a superseded registry-fingerprint domain name and stale receiver-receipt status), both fixable by small owner text amendments without touching contracts. This review certifies neither sibling owners, implementation readiness, transformed hook coverage, native GL behavior, source-license clearance nor conformance parity.

**PASS-WITH-CORRECTIONS** — corrections: 2; notes: 2; §5 impact: YES.

## Resolutions

- **C1 — resolved (D-P10-31).** `docs/phase10/v1/PHASE_10_DOC.md` §5.2 schema23 receipt paragraph (frozen lines 1819-1822): the superseded `RegistryFingerprint/own-build-v1` citation now names the current `RegistryFingerprint/profile-selection-v3` domain per P4 D-P4-43, with D-P4-31's own-build fields retained within it and the opaque carriage through P7 unchanged. Decision row D-P10-31 recorded in §11.1.
- **C2 — resolved (D-P10-32).** The outstanding-receipt cells are restated as received: §5.1 hook-subreport row (frozen line 1702) and §5.3 R10-8/R10-9 gate cells (frozen lines 1840-1841) cite P7 D-P7-62 (all seven H10-WRITER-* rows, identical P2 forwarding), D-P7-75 (H10-BRIGHTNESS-4 full-v0.3/v0.5-only) and D-P7-70 (conventionalInputs) as receiver-adopted/unverified, keeping fresh applicable review as the genuinely outstanding part; §11.3's R10-8 bullet (frozen lines 2308-2310) restates the same receipts with exact references including P2's planned forwarding (`PHASE_2_DOC.md:2957`); §11.4's Phase 7 handoff enumeration now names H10-BRIGHTNESS-4. Decision row D-P10-32 recorded in §11.1. No schema, grant or interface change — textual ledger repair only.

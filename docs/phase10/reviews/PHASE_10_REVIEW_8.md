# Phase 10 — architecture review 8

**Date:** 2026-09-08
**Frozen artifact:** `docs/phase10/v1/PHASE_10_DOC.md` — SHA-256 `4b460a999a732248de47a080a9c3c4d5bf112df2b91bcad6011acf1d87790ba4`, recomputed at wave open and identical to the attempt-11 registry entry (`docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_11.json`, phase 10, reviewRound 8). Gate passed; review proceeded on the frozen bytes.
**Verdict:** PASS-WITH-CORRECTIONS — 0 blocking, 2 corrections, 1 note.
**§5 impact:** none. Both corrections are confined to the §0.1 dependency-gate table's evidence column (authoring-session snapshots presented as current); no §5 contract, decision, or consumed/exported interface requires change. No build, test, gradle, GL, or network evidence was used or needed; this is a documentary verification.

---

## 1. Scope and method

Fresh independent adversarial verification of the whole Phase 10 document against its governing sources, under the attempt-11 §G1.2 verify-session protocol. The hash gate was evaluated first and passed; every statement below is anchored to the frozen bytes.

Read in full: `docs/phase10/v1/PHASE_10_DOC.md` (2442 lines, all sections 0–12). Governing design: `docs/design/v3/DESIGN.md` Phase 10 specification (ll. 2162–2276), mandatory template §G9 (ll. 817–854), document gate (ll. 2266–2270), §G5.1 phase/dependency table (ll. 609–631), §G2.4 degradation ladder, §G10 OQ assignments, plus spot reads of §G11/G12. `docs/research/v1/RESEARCH.md` ll. 11–107 as assigned, plus the sections the phase doc cites: §3.6.5, §4.6 heading, §7.4, §8.1–8.2, §9, §11 OQ register, Appendices C (whole), D.4, E (rows 3–9 context), F.1. Dependency docs read at §0 header/§1/§5/§11 depth with incorporated detail: Phase 4 (`PHASE_4_DOC.md` §§0.35–0.42, 4.8–4.10, 5, 11), Phase 7 (§§0.40–0.52, 4.6, 4.10.1, 4.12, 5.1–5.5, 8.5, 11), Phase 9 (§§0, 2.1–2.3, 4.10, 4.12.1, 5.1–5.6), Phase 1 (§§4.7.6, 4.10, 5.1–5.3), Phase 3 (§§2.2, 3.1, 4.8, 5, 11 area). Citation targets verified at stated coordinates: `docs/MOVES.md:85–146`, `docs/phase2/v2/PHASE_2_DOC.md:2962`, `docs/decisions/GEOMETRY_PRIMITIVE_COMPATIBILITY.md`. Forbidden surfaces (reference-src/**, docs/**/chatlogs/, root *.txt, other PHASE_*_REVIEW_*.md) were not read; `shaders.txt`/`shaders.properties` claims are therefore adjudicated on internal consistency only, not primary verification.

## 2. Document-gate and template conformance

Every document-gate requirement (DESIGN ll. 2266–2270) is satisfied on the frozen bytes: Appendix C is fully mapped (§3 covers all eight C.1 rows, all C.2 population rules, and all four C.3 growth candidates with dispositions); both draw paths are specified (§4.6 client-array, VBO, and the non-VBO display-list capture/replay mode, explicitly not VBO-only); both spike specifications are complete with procedure, success, failure, and "Fallback designed now" sections (§10.1, §10.2); the growth design shows `at_midBlock` addable without consumer edits (§4.10: one descriptor, one producer, existing writers/binders iterate descriptors); the coexistence policy and message text are drafted (§4.9). The REV1 additions are present: the constant-attribute candidate is recorded as rejected with a four-point contract check (§4.7, D-P10-3); the OQ-5 detection table carries the Pintonium anchors plus the doc's own correction that the actual 1.12.2 mod id is `celeritas` (§4.9, §11.2 item 2); per-quad formulas are cross-checked against the PD §9 numeric reference with the divergence (T×N vs required N×T) demonstrated by a worked square and mirror case (§4.3, §11.2 item 1).

Template conformance (§G9): all thirteen sections 0–12 present and populated; §1.2 gives an explicit adjacent-owner boundary for every touched concern; §2.2 carries load-bearing signatures; §6 applies the §G2.4 ladder rungs (1/2, 2a, 3, 4/5, 5) matching the design's ladder exactly; §9 assigns exactly one milestone tag per row; §11.1 holds D-P10-1…D-P10-32 with the layered 2026-09-08 addenda explicitly marking superseded schema receipts historical; §12 items each carry a milestone and a named test hook. Assigned questions OQ-5/OQ-14 match §G5.1/§G10 ownership; the doc claims resolution by neither.

Licensing/Pintonium compliance: structure-only adoption is explicit and consistently applied (D-P10-2; §4.3 "What Pintonium actually proves"; §3's do-not-inherit list covering 11–14 numbering, u16 midpoint, byte tangent, opposite handedness); LGPL evidence table with notice-preservation condition (§0.2, §0.3); AGPL transformation library excluded (§0.3, §4.7, §10.1 step 3); formulas independently derived from RESEARCH with PD's over-claim flagged as an upstream request rather than silently edited (§11.5). D-1…D-10 constraints are honored where they touch this phase (D-2 scope in D-P10-8; D-5 hook ledger; D-6 engine seam, §2.1; D-7/D-8 licensing posture; D-9 in the `va*` map row; D-10 in §8.3's reuse of the Phase 2 runner).

## 3. Conformance-map and citation audit

Spot-checked rows against the cited RESEARCH text, all exact:

- C.1 layout: 56 bytes/14 ints, midpoint 2×float @32 loc 11, tangent 4×short @40 loc 12, entity data @48 loc 10 — matches RESEARCH ll. 1285–1296 verbatim. The map's qualification of C.1's "3×short (2 used)" as payload words with byte 54 as padding (map row; §4.1; §11.2 item 3) is an honest reading of a real contradiction, logged as an upstream request, not a silent deviation.
- C.2 population rules (RESEARCH ll. 1300–1306): `renderType<<16 | aliasedBlockId` stamping, `normalize((v2−v0)×(v3−v1))`, `w = sign(dot(bitangent, normal×tangent))`, UV average, stride 56 / offsets 32/40/48 — §4.3/§4.6 implement these exactly, including the two worked examples and the mirrored handedness case that distinguishes the doc's N×T from Pintonium's T×N.
- C.3 growth (ll. 1308–1315 and §3.6.5 ll. 395–402): `at_midBlock` 1/64-block center offset with Iris vec4 emission, `at_velocity` unwired, `mc_chunkFade` −1.0-only, core-profile `va*` unadvertised — §4.10 and the map match, including the explicit "no silent clamp" encoding reservation.
- D.4 (ll. 1370–1378): `entityId`/`blockEntityId`/`entityColor` are uniforms, distinct from the C.2 block triple — the §4.7 rejection argument rests on the correct contract text.
- E rows 3–9 (ll. 1403–1409): targets and SRG names (`func_178581_b`, class-level dispatcher row, `func_181668_a`/`func_181675_d`/`func_178981_a`, `func_78381_a`, `func_181679_a`, `func_181722_a`/`func_177358_a`, `func_178267_a`) all match §4.11's ledger; the doc correctly discloses that the worker/upload methods missing from E4's class-only row were MCP-resolved (§0.2), not invented. The full SRG/descriptor table (ll. 1511–1543) is internally consistent with MCP stable_39 1.12.2 names; the seven H10-WRITER-* descriptors match P7 D-P7-62's grant character-for-character modulo the doc's disclosed `B` expansion (§4.11 ll. 1547–1549).
- §7.4 `[A]` detect-and-bail (ll. 844–852) and the OQ-5/OQ-14 verbatim questions (ll. 1009, 1024 of the register) — §4.9/§10 quote both questions exactly. F.1 tri-state engine flags with video-setting priority and AO-in-color.a (ll. 1442–1448) — §4.8.1's precedence chain is consistent, with the missing numeric defaults honestly declared as local compatibility decisions (D-P10-11).
- Implementation gate (§9 v0.3, ll. 947–951): "Classic packs at T2 within tolerance on terrain scenes"; §8.3 names the three classic packs from §8.1's composition (SEUS Renewed, Chocapic13 V9, projectLUMA) and correctly refuses Pintonium as a numeric oracle.

Counting checks: the seven H10-WRITER-* rows, the seventeen H10-SKY-* rows (3 capture + 4 list + 3 VBO + 7 lifetime), and the H10-BRIGHTNESS-4 grant are enumerated consistently across §4.11, §5.1, §5.3, §9, §11.3, §11.4, and §12.

## 4. Interface honesty and dependency currency

Declared dependencies "Phases 4, 7, and 9" match §G5.1's literal edge (ll. 619, 625) and the Phase 10 spec header; the Phase 1 consumption is the assignment-mandated bail registry, documented as a narrow extra read with reasons (§0.3 item 4) and consumed only through P1's actual surface; Phase 3 is consumed through Phase 7's resolved input, not as a fourth declared dependency.

Consumed interfaces all exist on the owners' current frozen surfaces:

- P4 §5.1 fixed attribute row 10/11/12 (`PHASE_4_DOC.md:2154`); D-P4-28's `GeometryInputRequirement geometryInput` appended after `Optional<LegacyGeometryConfig> legacyGeometry` with the exact closed enum (`:2141`, §0.38 at `:351`, detail at `:775`); D-P4-31 ownBuild fields; D-P4-43 `RegistryFingerprint/profile-selection-v3` (`:1953–1954`) — the phase doc's schema/identity prose in §5.2 carries the current names, with no stale domain cited as current.
- P7 §0.43 R10-2 owner grant (`PHASE_7_DOC.md:386`); accepted R10-2 participant in §5.1 (`:2389`); `IdDependentGeometryInvalidator`/`IdPublicationChange` (`:2860–2863`); `ShaderReloadController` (`:2951`); `HookApplicationSubreport` (`:3014`); §4.6 `countInstances` boundary (`:1409`); §4.10.1 notation (`:1678`); §4.12 CompatEvaluation consumption (`:2024–2025`); ten-step placement step1/step9/step10 (`:2440`, `:2456–2465`, `:2473`); grants D-P7-32 (`:2526`), D-P7-53 (`:1922`), D-P7-54 (`:1934`), D-P7-62 (`:1950`), D-P7-70 (`:2591`), D-P7-75 (`:1965`) — every receiver-adopted receipt cited in P10 §5.3/§11.3 exists with matching scope, including "full v0.3/v0.5 only" and unchanged v0.1 subset for H10-BRIGHTNESS-4.
- P9 `AliasLookup`/`BlockStampResult` Present/Absent/Unrepresentable (`PHASE_9_DOC.md:295–308`), §4.10 alias service (`:672`), §4.12.1 FastTESR ranges (`:799`), §5.6 coordinated receiver amendments recording the P10 D-P10-26 adoption (`:1070`, `:1058`), §§5.3/5.4 adoption ledger — matching P10 §5.2/§5.3 rows.
- P1 §4.7.6 `GLDevice.vertexInputs()`/`VertexInputService`/`VertexBindMode{LIVE_DRAW,LIST_CAPTURE,LIST_REPLAY_GUARD}` (`PHASE_1_DOC.md:3287`, `:4313–4324`), D-P1-50 (`:1556`, `:4311`), D-P1-53 (`:1587`), D-P1-55 (`:1594`, `:4418`), D-P1-68 (`:4342`), D-P1-70 list fixture (`:4499`), §4.10 `EarlyCompatCheck`/`EarlyCompatContext`/`evaluateEarly` (`:5074–5086`), §5.3 consumer row for phases 10/7 (`:5576`).
- P3 `PackFrontEnd.CURRENT_SCHEMA_VERSION = 23` (`PHASE_3_DOC.md:754`), `MaterializedSource-v23` (`:589`, `:3359`), D-P3-68/69/70 (`:503`, `:1939`, `:557`), nine metadata-only trees with `projectionVersion=1` (`:561`, `:590`), `EngineFlags` tri-state record (`:1107`, separateAo at `:1123`), behavior ownership rows assigning `oldLighting`/`separateAo` to Phase 10 (`:1722`, `:1734`), §5 exposure and `oldLighting→MC_OLD_LIGHTING` projection (`:3415` area, `:3542`).
- Coordinate-cited anchors: `docs/phase2/v2/PHASE_2_DOC.md:2962` is exactly the planned `FullVertexHealthEvidenceContractTest` row covering H10-BRIGHTNESS-4 (`func_178962_a(IIII)V`, full-v0.3/v0.5, base catalog unchanged), with `CapturedPlanFixtureContractTest` (`:2960`) covering the ITEM→BLOCK completion and list-fixture authority — matching P10 D-P10-32/§11.3 precisely; the P2 reference is to v2, the current document. `docs/MOVES.md:85–146` contains the verify-loop/`verification/targets/`/`scripts/verify` retirement and the per-header authority text the doc claims. `docs/decisions/GEOMETRY_PRIMITIVE_COMPATIBILITY.md` records the dated maintainer authorization whose boundaries §4.6 mirrors (earliest affected-.gsh milestone, required topology/provoking/primitive-ID policy, no renderer rewrite).
- Header coordinates (§0): G0 at DESIGN l. 132, Part I end/Part II start at ll. 1133–1136, template ll. 817–854, Phase 10 spec ll. 2162–2276, doc gate ll. 2266–2270 — all four coordinate claims re-derived and correct.
- Current-identity discipline: schema23/MaterializedSource-v23 as current with schema19/20/21/22 receipts explicitly historical (§0.7–§0.9, D-P10-22/23/25/28, §5.2); projectionVersion=1 and the nine trees unchanged; `RegistryFingerprint/profile-selection-v3` current with D-P4-31's fields retained within it; P2 cited only at v2. No stale identity is cited as current anywhere in §§4–5.

## 5. Findings

1. **Correction — `docs/phase10/v1/PHASE_10_DOC.md:31` (§0.1 table, Phase 4 row): stale dependency status cited as current.** Claim: Phase 4's "Current artifact and evidence" is the "§0.35 coordinated rebuild, 2026-09-06", whose "closing status says §5 changed and is unverified", with `docs/phase4/reviews/PHASE_4_REVIEW_31.md` as the newest review evidence. Evidence: the frozen Phase 4 document carries addenda §0.36–§0.42 (`docs/phase4/v1/PHASE_4_DOC.md:334–402`, through 2026-09-08) and its current closing status (`:3144`) records Review 40 (2026-09-08) PASS-WITH-CORRECTIONS with all eight corrections applied, note N1 dispositioned, "no §5 change outstanding", and the §G1.3 fix-up verification condition satisfied (`docs/phase4/reviews/PHASE_4_REVIEW_40.md`). The row also paraphrases §0.35's actual heading ("Coordinated shared-unit rebuild", `:308`). Severity: correction. The operative consumption posture (provisional; reconcile against freshly verified versions before implementation; D-P10-10) is unaffected — if anything P4's standing has improved since authoring — but an evidence column labeled "Current" must not describe a superseded closing status and review round as present fact. Same defect class as the attempt-10 wave's recorded P12 §0.1–0.4 stale-claim fix.
2. **Correction — `docs/phase10/v1/PHASE_10_DOC.md:33` (§0.1 table, Phase 9 row): "no Phase 9 review artifact found" is stale as a present-tense claim.** Claim: the Phase 9 row's evidence that no review artifact exists. Evidence: `docs/phase9/reviews/` currently contains `PHASE_9_REVIEW_1.md` through `PHASE_9_REVIEW_7.md` (seven whole-owner rounds; the attempt-11 registry puts Phase 9 at reviewRound 8). Severity: correction. Phase 9's provisional-consumption status under the maintainer exception stands on its own grounds and the row's remaining facts (initial build, 2026-08-03 header — verified at `PHASE_9_DOC.md:7`) are accurate; only the review-existence statement is outdated. The maintainer authorization and §0.1's reconciliation requirement do not depend on it.
3. **Note — `docs/phase10/v1/PHASE_10_DOC.md:1805` (§5.2 table, Phase 3 row): section citation imprecise.** Claim: "Phase 3 §§4.8/5 engine flags and codec". Evidence: the engine-flag tri-state behavior/ownership table is P3 §3.1 (`PHASE_3_DOC.md:1710–1741`), the `EngineFlags` record is §2.2 (`:1107–1124`), P3 §4.8 is `ShaderPropertiesModel` (`:2859`), and §5 (`:3393`) carries the exposure row and the `oldLighting→MC_OLD_LIGHTING` macro projection (`:3542`). The consumed surface exists exactly as described and is owner-assigned to Phase 10 (`:1722`, `:1734`); only the §-coordinates under-cite §3.1/§2.2. Severity: note; no contract ambiguity results.

No blocking findings. No unmapped in-scope contract row, no fabricated anchor among those spot-verified, no scope breach, no licensing violation, and no stale current-identity citation found in the operative §§3–5.

## 6. Verdict

**PASS-WITH-CORRECTIONS** (0 blocking, 2 corrections, 1 note).

The frozen document satisfies every document-gate requirement, maps Appendix C without unmapped rows, specifies both draw paths and both spike fallbacks completely, keeps every consumed interface anchored to a real current owner surface (including all six P7 receiver grants, the P4/P1/P3/P9 grants, and the P2 evidence anchor at the stated line), maintains current-identity discipline in its operative contract sections, and stays inside its scope and licensing boundaries with the maintainer-authorized topology exception properly recorded. The two corrections are confined to the §0.1 dependency-gate table's evidence column, which still describes the Phase 4/Phase 9 verification landscape as it stood at the 2026-09-07 authoring session rather than as of this frozen wave; they alter no §5 contract and require no re-review of the interface region. Fix-up is a header evidence refresh; nothing else in the document is affected. No build, test, gradle, GL, or network activity was executed or relied upon by this review; the Phase 13 §4.2.1 RESEARCH-citation item is that phase's adjudication and does not touch this dependency edge.

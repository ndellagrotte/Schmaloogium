# Phase 10 — architecture review 6

**Date:** 2026-09-08. **Frozen document:** `docs/phase10/v1/PHASE_10_DOC.md` SHA-256 `d0f2ada709d62a38e9d5f545e1f72485b0fc43eccd14dad5519f78a2d1810be6` (recomputed before review; match). **Verdict:** PASS-WITH-CORRECTIONS (0 blocking, 2 correction, 3 notes). **§5 impact:** both corrections are prose/ledger repairs outside §5, so a §G1.3 fix-up can close this phase without re-opening §5; the document's own standing gate — integration waits on fresh verification of the provisional P4/P7/P9 surfaces it consumes — remains unchanged and already recorded.

---

## 1. Scope, identity and authority actually verified

- Hash gate: `sha256sum docs/phase10/v1/PHASE_10_DOC.md` → `d0f2ada7…10be6`, equal to the frozen value. 2441 lines read completely, in ranges, against current bytes; all cited line numbers below were recomputed against those bytes.
- Governing design: `docs/design/v3/DESIGN.md` Part I read at lines 1–1136 (§§G0–G12, including §G1.2's verify protocol, §G2 D-1..D-10, §G4.2/§G4.4, §G9 template, §G10 OQ assignments, §G11 Pintonium rules, §G12 Oculus rules) and the Phase 10 specification at lines 2162–2278.
- RESEARCH: `docs/research/v1/RESEARCH.md` lines 11–107 (§0 reading guide/tags, §1 mission/non-goals/decision log), plus the phase's contract sources re-read directly: §4.6 vertex portion (`:583–592`), §7.4 (`:844–852`), §11 OQ-5/OQ-14 rows (`:1011`, `:1020`), Appendix C in full (`:1278–1316`), Appendix E rows 3–9 and headings (`:1387–1433`), Appendix D.4 heading (`:1370`), Appendix F.1 (`:1442–1448`).
- Dependency docs consumed per assignment (§0 header, §1, §5, §11, plus cited §4 sections where the §5 rows incorporate them): P4 `docs/phase4/v1/PHASE_4_DOC.md`; P7 `docs/phase7/v1/PHASE_7_DOC.md`; P9 `docs/phase9/v1/PHASE_9_DOC.md`. Narrow verifications also ran into P1 `docs/phase1/v14/PHASE_4_DOC.md`'s sibling `PHASE_1_DOC.md` (§4.7.6, §4.10, decision table), P3's schema surface, and P2's evidence table, because the phase doc's §5 adoptions cite them as grant owners/receivers.
- Cross-checked dependencies' own currency: P3 `CURRENT_SCHEMA_VERSION = 23` (`PHASE_3_DOC.md:704`, `:4161`), with the 19→20→21→22→23 receipt chain (D-P3-68/69/70/72/73); P7 §0.43/§5.1 R10-2 owner grant and D-P7-32/53/54/62/70/75 receiver rows; P4 §5.1 fixed attribute row and D-P4-28/D-P4-43; P9 §4.10/§5.1/§5.6 and R10-3 adoption.
- Hard boundaries honored: read-only except this file; no `reference-src/**` reads, no chatlogs/root-`*.txt` inputs, no other `PHASE_*_REVIEW_*.md`, no `docs/build/**`, no build/test/gradle/GL/network execution. Consequence, recorded as Note N1: reference-tree-anchored citations inside the phase doc were accepted as declared provenance, not re-verified against the trees.

## 2. Scope and §5 audit

**Scope discipline.** Every *Scope — in* bullet of DESIGN 2162–2278 is designed: format/packing (§4.1, exact App C.1 offsets/locations), population rules (§4.3, App C.2 formulas), side channel (§4.2), entity push/pop with worker thread-safety (§4.4/§7, the PD §9 thread-local pattern adopted structurally with the contract-required stack retained), both draw paths plus the non-VBO display-list third path (§4.6), constant-attribute candidate (§4.7 — rejected with a four-point contract check and recorded D-P10-3, which the spec permits), format-swap lifecycle + OQ-14 (§4.8/§10.2), per-program enablement (§4.6), growth design with `at_midBlock` addable without consumer edits (§4.10), OQ-5 detect-and-bail with drafted message text (§4.9/§10.1), hook-site specs for App E rows 3–9 (§4.11). *Scope — out* is respected: alias computation stays with P9 (§1.2/§4.4 never infer aliases), `_n`/`_s` with P13, compile-time binding with P4. The four post-spec extensions (cached model lists NS-2, sky/star R1-1, deferred FastTESR D-P10-26, lighting policy IR-05, conditional primitive conversion) are each traceable to a recorded maintainer authorization or review finding in §0.5–§0.11 and `docs/decisions/GEOMETRY_PRIMITIVE_COMPATIBILITY.md`; none is silent sprawl.

**§5 audit — consumed interfaces all exist.** Verified against current bytes: P4 §5.1 fixed attribute row `mc_Entity=10, mc_midTexCoord=11, at_tangent=12` consumer Phase 10 (`PHASE_4_DOC.md:2138`); P4 R10-5 `GeometryInputRequirement` after `legacyGeometry` with the exact six-value enum and "Phase 7, 10" consumers (`:2125`, `:2141`); P7 §5.1 all-or-none P10 participant bundle, step1 real worker/upload drain, step4 `prepare(token,layout,lighting)`, step9 `activate(token,epoch,aliases)`, step10 `restoreVanilla` (`PHASE_7_DOC.md:2391–2447`), sink result domain `Entered|Rejected(STALE_EPOCH|NO_ACTIVE_SHADER|INVALID_DECLARATION|WRONG_THREAD)` (`:2459`, identical to P10 §5.1), `IdDependentGeometryInvalidator`/`IdPublicationChange`/`IdGeometryInvalidationRejection{STALE_GENERATION, INVALID_CHANGE, SCHEDULER_UNAVAILABLE}` (`:2822–2837` — P10 §5.1 quotes this domain exactly), non-fullscreen `countInstances` prepared submission (`:1371`), `HookApplicationSubreport` with P8/P10 consumers (`:3661`), and D-P7-62's seven writer rows with per-row descriptors, fingerprint recompute, unchanged v0.1 subset and identical P2 forwarding (`:1912–1925`) plus D-P7-75's H10-BRIGHTNESS-4 full-v0.3/v0.5-only receipt (`:1927–1932`); P9 §5.1 `AliasLookup`/`BlockStampResult` with the exact Phase 10 borrow contract (`PHASE_9_DOC.md:914–924`), §4.10's `metadata = legacyMetadata & 0xffff` and −32768..65535 domain (`:666–677` — this directly grounds P10 §4.1's "high metadata half is already zero"), §5.6's seven `H10-TE-BATCH-OWNER` subtargets + SORT + RANGES rows (`:1098–1083`), and R10-3 adopted in §5.4 (`:1047`); P1 §4.7.6 `GLDevice.vertexInputs()`/`VertexInputService` (`PHASE_1_DOC.md:3253`, `:4277–4288`), D-P1-50 package homes incl. `mod.mixin.compat.vertex` (`:1742`, `:1757`, `:1763`), D-P1-55 isolation law (`:4384`, `:6162`), D-P1-68 receipt (`:4308`, `:6175`), D-P1-70 `borrowedVertexList(String,VertexLayout,VertexInputPlan)` with old overload removed (`:4461–4468`, `:6177`), D-P1-53 early veto (`:2707`, `:6161`), §4.10 `EarlyCompatCheck`/`evaluateEarly` (`:5044–5053`, `:5074`). P10's §5.3 status column ("owner-granted/receiver-adopted, unverified; fresh review") matches each owner's own ledger.

**Conformance-map spot-checks against cited RESEARCH/appendix text.** App C.1 rows (offsets 0/12/16/24/28/32/40/48; locations 11/12/10; "3×short (2 used)") — §3 rows and §4.1 table agree; the "2 used" contradiction is ruled in §11.2 item 3 consistent with the value formula. App C.2 — normal = normalize((v2−v0)×(v3−v1)), UV-delta tangent with `w = sign(dot(bitangent, normal×tangent))`, midpoint = UV average, stride 56/offsets 32/40/48, both draw paths: §4.3 and §4.6 match semantically; both worked examples recompute correctly (square: N=(0,0,1), w=+1, tangent shorts (32767,0,0,32767); mirrored square: d=−1, w=−1, shorts (−32767,0,0,−32767)); the identity packing example (0x0003001f,2 → bytes `1f 00 03 00 02 00 00 00`) is endian-consistent with P9 §4.10's `packedWord = (renderType<<16)|lowId`. App C.3 — all four growth rows mapped with `[V:web]` provenance and post-v0.5 tags. §4.6 swap-invalidation row matches RESEARCH §4.6's OQ-14 clause; §7.4's four requirements are each mapped. OQ questions quoted verbatim (`:1011`, `:1020`). App F.1 tri-state/video-priority/AO-to-alpha matches §4.8.1's precedence.

**Currency of identities.** The doc's active gate is current: schema23 + `MaterializedSource-v23` (D-P10-28, §5.2 `:1811–1827`), `RegistryFingerprint/profile-selection-v3` (D-P10-31; P4 §4.11 `:1937–1940` confirms D-P4-31's ownBuild fields are retained inside that domain), `projectionVersion=1` and nine source-free trees (D-P3-69), schema22/21/20/19 receipts all explicitly historical. No stale `capture-plan/N` wire version, ShadowHookHealth/TextureHookHealth, or phase13 parameter identity is cited anywhere in the document.

**Template completeness.** All thirteen §G9 sections present and substantive; §9 gives exactly one milestone tag per component; §10 carries both assigned OQ spike specs each with question/procedure/success/failure/fallback/result-recording; §11 logs D-P10-1…D-P10-32 with rationales; §12 items are ordered, tagged, and hooked to named tests.

**Licensing/Pintonium compliance.** §0.3 records no AGPL/transformation-library exposure; §3's do-not-inherit block dispositions the relevant PD §17/§18 rows (locations 11–14, u16 midpoint, byte tangent, always-paid re-encode, heuristic aliasing, renderType substitution, AST transformation, undelivered blockEntityId/entityColor); the constant-attribute rejection carries the §G11.4 contract check; §G11.5 "reuse structure, re-derive values" is honored by §4.3's structure-only adoption with the numeric divergences (T×N vs N×T, byte packing, d=0 reciprocal, zero-dot→+1) explicitly rejected and the DESIGN/PD equivalence overstatement reported rather than smoothed.

## 3. Required corrections

### C1 — stale "P4 owner grant pending" contradicts the adopted R10-5 state (severity: correction)

- **Sites:** `docs/phase10/v1/PHASE_10_DOC.md:906` (§4.6, conditional-primitive block).
- **Claim:** the sentence "**P4 owner grant pending**; these names are a precise request, not assumed private access" presents R10-5 as an outstanding request, but the same document records it as granted and adopted three times: §0.6 (`:139–141` "D-P10-19 adopts P4 §§4.8/4.9/5/D-P4-28's required effective `geometryInput` … supersede the pending-owner status"), §5.3's R10-5 row (`:1837` "Owner-granted/receiver-adopted, unverified"), and the opening of the very same §4.6 paragraph (`:900` "P4 D-P4-28 grants R10-5 exactly"). The owner's ledger agrees: P4 §5.1's "Effective geometry draw handoff — R10-5" row (`PHASE_4_DOC.md:2141`).
- **Failure path:** a fix-up or integration reader treating §4.6 as the operative statement would re-derive the geometry metadata as a missing owner grant, or worse, treat the pre-grant "precise request" phrasing as license to negotiate field names, when the enum and placement are already fixed and identical on both sides. Internal inconsistency of this kind is exactly what the attempt-loop's "current-identity discipline" exists to purge.
- **Owner + receivers:** owner Phase 10 (doc text); no dependency action — P4's grant and P7's adoption are already correct and untouched.
- **Minimal resolution:** delete or rewrite the stale clause at `:906` to state the current posture, e.g. "R10-5 is owner-granted (P4 D-P4-28) and receiver-adopted (D-P10-19), unverified; these names are the owner's exact grant, not locally minted private access." No §5 edit; no renumbering beyond the paragraph.

### C2 — P2 evidence-row citation points at the wrong line (severity: correction)

- **Sites:** `docs/phase10/v1/PHASE_10_DOC.md:2251` (D-P10-32) and `:2314` (§11.3, R10-8 bullet), both citing `PHASE_2_DOC.md:2957` for "P2's planned forwarding/evidence case".
- **Claim:** under current bytes the planned H10-BRIGHTNESS-4 evidence row (`FullVertexHealthEvidenceContractTest`, "CORE expected-one H10-BRIGHTNESS-4 wrapping BufferBuilder.func_178962_a(IIII)V") is at `docs/phase2/v2/PHASE_2_DOC.md:2960`; line 2957 is `TypedClearEvidenceContractTest`, an unrelated P5 typed-color-clear evidence row. The receipt's substance is correct — the P2 row exists and grants exactly what D-P10-32 records — but the anchor is stale, and this review round (R5 C2) was precisely about restating receipts with correct coordinates.
- **Failure path:** a receiver or integrator following the citation lands on the wrong contract and concludes the P2 forwarding row is missing or different; the next attempt loop inherits a wrong anchor into an otherwise accurate receipt.
- **Owner + receivers:** owner Phase 10 (both citation sites); P2's document is correct and must not be modified.
- **Minimal resolution:** replace `2957` with `2960` at `:2251` and `:2314`. Ledger text only; no interface change.

## 4. Notes

- **N1 (verification boundary):** this session's hard boundary forbids `reference-src/**` reads, so the doc's tree-anchored citations (`[V:observed — Pintonium …:lines]`, `shaders.txt:119/346–348`, Cleanroom patch lines, the KealJones vanilla-source mirror links) were audited for provenance discipline and internal consistency but not re-verified against the trees. All RESEARCH/appendix-anchored rows were verified against current RESEARCH bytes and agree. A later session with reference-tree access should re-confirm §4.3's Pintonium line claims (`QuadUtil.java:78–117`, `VintageChunkBuildContext.java:179–220`, `ChunkVertexExtendedData.java:62–105`) before the T2 gate relies on the "what Pintonium actually proves" contrast.
- **N2 (historical ledger wording):** D-P10-12/D-P10-13's in-row phrases "current P3 schema17/schema18" read as present-tense inside a decision log whose rows are append-only history. Supersession is fully traceable (D-P10-13 supersedes D-P10-12; D-P10-17→D-P10-28 advance the schema; §5.2 carries schema23 as the sole active gate), so this is hygiene, not a currency defect: a future fix-up could prefix such rows "historical:" to prevent misreading.
- **N3 (header endpoint imprecision):** §0 cites the Phase 10 specification as lines "2162–2276"; the spec's content ends at 2275 with the separator on 2277. The heading-derived coordinates are otherwise correct and hash-gated; one-line endpoint drift is not an identity failure.

## 5. Areas audited and found sound

- **Doc gate, literally:** App C fully mapped (every C.1/C.2/C.3 row present in §3); both draw paths specified and a third (display lists) honestly bounded; both spike specs complete with fallbacks; growth design demonstrates `at_midBlock` without consumer edits; coexistence policy with drafted message text; constant-attribute decision recorded with its contract check; OQ-5 table includes the PD anchors plus the actual `celeritas` ID the assignment missed; per-quad formulas cross-checked against the PD numeric reference with the divergences itemized.
- **Interface honesty (export side):** every §5.1 exposed contract binds to §4 semantics with named consumers and recorded adoption status, including the four consumers in this review's assignment: P7 (lifecycle/sink/invalidator/owner10 bundle, R10-6/7/8/9 receipts), P2 (H10-BRIGHTNESS-4 forwarding), P12 (ShaderLightingPolicy consumption named on the §5.1 row), P1 (`VertexLayout`/`VertexInputPlan`/`borrowedVertexList` fixtures — matching D-P1-70's exact factory signature, with the former geometry-only overload removal adopted).
- **Geometry gate:** the six-value `VertexGeometryInput` enum, "unknown ≠ NONE", effective-provider-only sourcing, and the preflight comparison against actual linked state all match P4 §5.1 and P7 §5.1 exactly; the conditional QUADS→TRIANGLES adapter stays inside the maintainer's recorded authorization (topology (0,1,3),(1,2,3), last-vertex3 provoking, explicit primitive-ID restart, no parity claim).
- **D-P10-20/21 conformance:** whole-source staged ModelRenderer capture preserves original scale/incarnation; every-cached-call guard covers `render`, `renderWithRotation` and postRender-triggered compilation; paired lifetime with final-use retirement; conventional-position/generic-zero isolation per D-P1-55 adopted verbatim; P7 D-P7-32 grants exactly the expanded eight-row catalog P10 enumerates.
- **ShaderLightingPolicy (IR-05):** immutable pair, user→pack→default precedence matching App F.1, AO-to-alpha channel contract, fixed face factors honestly labeled as local compatibility decisions (D-P10-11), bake/mesh identity participation, REPUBLISH-only reload handoff, quiescent invalidation — all present in §4.8.1 with no parity claim.
- **D-P10-29/30 (R10-9, H10-BRIGHTNESS-4):** authenticated conventional participation distinct from CLASSIC_56 storage; inherited COLOR/UV1 through live draws, client/VBO and cached playback (physical CLASSIC56 unchanged); partial ITEM ingress authentication with real four-corner BLOCK brightness completion before publication; full-only H10-BRIGHTNESS-4 health (v0.1 subset unchanged) — matching P7 D-P7-70/D-P7-75 and P1 D-P1-68/D-P1-70 receipts; source-semantic writer dispatch (D-P10-27) with the seven-row R10-8 receipt verified against D-P7-62 verbatim; adjacent-repeat v0.5 integration with N native copies IDs0…N−1 and the call-local single-submission guard against N² forwarding, consistent with P7 §4.6's `countInstances` grant.
- **Threading (§G2.3/§7):** render-thread GL ownership, task-local stacks with no `InheritableThreadLocal`, worker drain before format mutation, no worker GL, honest "zero-cost = no shader work/allocation" scoping.
- **Failure philosophy (§6):** every row maps to a real G2.4 rung including 2a; no symptom-suppression fallbacks; stale-generation events never masquerade as crashes.
- **Licensing (§G7/§G11):** LGPL evidence handled with compliance posture; no AGPL trace; do-not-inherit rows demonstrably handled in §3; Oculus explicitly unused (§G12 assigns P10 no gated addition).
- **Identity currency:** schema23/MaterializedSource-v23/profile-selection-v3/projectionVersion=1 all current; every superseded numeric receipt marked historical; P9 §4.10's masked metadata grounds the §4.1 packing claim.

## 6. Verdict

**PASS-WITH-CORRECTIONS** — 0 blocking, 2 corrections (C1 stale §4.6 grant-status clause; C2 two stale P2 line anchors), 3 notes. Both corrections are local text repairs with no §5 effect; a §G1.3 fix-up session applies them and records resolutions, after which the document's standing gates (fresh verification of the provisional P4/P7/P9 surfaces, runtime/transformed evidence for OQ-5/OQ-14 and the hook ledger) remain exactly as the document itself declares. No build, test, gradle, or GL execution was performed by this review; all evidence above is documentary, recomputed against current file bytes.
---

## Resolutions

**Date:** 2026-09-08. Applied by the §G1.3 fix-up session against `docs/phase10/v1/PHASE_10_DOC.md`; the frozen hash `d0f2ada709d62a38e9d5f545e1f72485b0fc43eccd14dad5519f78a2d1810be6` was verified equal before editing. Report body above is byte-identical.

- **C1 — applied.** The stale "**P4 owner grant pending**; these names are a precise request, not assumed private access" clause in §4.6 (report site `:906`) is rewritten to the current posture: "R10-5 is owner-granted (P4 D-P4-28) and receiver-adopted (D-P10-19), unverified; these names are the owner's exact grant, not locally minted private access." D-P10-19's adopting role was verified against §11.1 before the rewrite. No §5 edit; no renumbering.
- **C2 — applied with recomputed anchors.** Both P2 citations were repointed, but to `:2962`, not this report's stated `:2960`: under current P2 bytes the quoted row — `FullVertexHealthEvidenceContractTest`, "Complete full-v0.3/v0.5 owner10 evidence includes CORE expected-one H10-BRIGHTNESS-4 wrapping BufferBuilder.func_178962_a(IIII)V …" — sits at `docs/phase2/v2/PHASE_2_DOC.md:2962`, while `:2960` is `CapturedPlanFixtureContractTest` and `:2957` is `ShadowOutlineEvidenceContractTest` (so this report's own `:2957` description had also drifted). Sites updated: §11.1's D-P10-32 row and §11.3's R10-8 bullet now cite `PHASE_2_DOC.md:2962`. Ledger text only; P2 untouched.
- **Main structural check — false positive; renumbering reverted in full.** Main's structural checker flagged `D-P10-30` as defined twice with different subjects. Re-examined against the governing document, this was a false positive: bold receipt/discussion paragraphs are not definitions, and §11.1's ledger defines `D-P10-30` exactly once, its row covering both the partial-ingress/completed-producer aspect and the exact CORE health receipt under that single ID. The renumbering was therefore reverted in full: the "and exact CORE health receipt" clause is restored to the `D-P10-30` ledger row, the interim `D-P10-33` ledger row is deleted, the health-delta paragraph's label reads "**D-P10-30 health delta:**" again, and the §0.11 summary, the §4.11 `H10-BRIGHTNESS-4` hook-row provenance, §11.4's receipt bullet and §12's checklist limb all reference `D-P10-30`. No `D-P10-33` exists anywhere in the document; §0.11, the hook row, the health-delta paragraph, §11.4's bullet and §12 item 2 are byte-identical to their pre-fix-up state. Cross-file caution resolves accordingly: the P1 references (`docs/phase1/v14/PHASE_1_DOC.md` `:4314`, `:6177`) and the P7 references (`docs/phase7/v1/PHASE_7_DOC.md` `:1927`, `:4550`) all match the single D-P10-30 decision and still resolve; no recipient edits are pending in P1 or P7.
- **Notes N1–N3 — not incorporated:** neither correction requires them; all three stand as recorded in the report body.

# Phase 8 — architecture review 12

**Date:** 2026-09-08. **Frozen document:** `docs/phase8/v1/PHASE_8_DOC.md` SHA-256 `c89daafc1dee8b844e9cc42e5bd984635bd1e25450f3426b34f6f08c3e16f17d`. **Verdict:** PASS (0 corrections, 5 notes). **§5 impact:** None — no corrections to §5 (or any section) are required. Current §5 grants are architecturally sufficient: every consumed interface (P4 selection/barrier/fingerprint, P5 five-argument shadowBindings/estate/mipmap/neutralization/BaseAtlasContext, P6 events/replay-failure, P7 eleven-field invocation context/bridge/base-binding receiver/3-arg P13 lease source, P13 lease domain, P1 package grant) exists at its owner with matching shapes and semantics, and every P8-produced value (66-row flattened-v3 health, public CelestialMath, invocation results, sort-cache restoration contract) has a confirmed current receiving branch in P7/P2/P6. The remaining blockers are the documented fresh owner/receiver verification gates and §5.5 upstream gates, which are deliberate unverified posture, not grant deficiencies.

---

# Phase 8 Architecture Review — review 12 (attempt 8)

**Owner document:** `docs/phase8/v1/PHASE_8_DOC.md` — frozen SHA-256 `c89daafc1dee8b844e9cc42e5bd984635bd1e25450f3426b34f6f08c3e16f17d` **verified by checksum before reading** (matches the attempt-8 frozen set; `docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_8.json` records the infrastructure failure of the prior worker generation and no phase verdict change).

**Verdict: PASS — zero required corrections.** Five optional notes and one separated evidence-limitation group are recorded below. Prior review R11 (`docs/phase8/reviews/PHASE_8_REVIEW_11.md`) and its appended Resolutions were treated as historical evidence only, not certification.

## 1. Governing authority confirmed

The header (`PHASE_8_DOC.md:6-11`) selects `docs/design/v2.0-RC3/DESIGN.md` Part I §G0–§G12 plus the Phase 8 assignment at `:1957-2034` — correct per the attempt-8 rule that P3–P9 use RC3. I read all of Part I (`:92-1109`) and the complete assignment. RESEARCH sections cited by §0.1 were read (§4.5 `:567-580`, App A.3, B.2–B.3, D.2, D.3, E, F.1). No chatlogs, root `*.txt`, OptiFine decompile, Oculus `pipeline/transform/`/`libs/`/`glsl-relocated/`, or glsl-transformer material was read. Source restrictions were respected; primary native/API evidence was consulted only to resolve concrete uncertainties (symbol identities, pass protocol, math conventions).

## 2. Scope audit — whole owner document read

All 2769 lines of §§0–12 were read. The thirteen G9 sections are present and substantive. Ownership boundaries (§1.2) correctly assign sfb objects to P5, programs/barrier to P4, uniforms to P6, frame/bridge to P7, IDs to P9, vertex format to P10, textures/leases to P13, `shadowcomp` to G8/S1. The doc-gate items from the assignment are all met: camera math written out (§4.5), traversal algorithm specified (§4.6–4.7), every in-scope A.3/B.2/B.3/D.2/D.3 row mapped in §3 with provenance, added hook sites in App E format (§4.13), camera/snap math cross-checked against the PD §10 reference with the absent-traversal risk acknowledged (§11.3 item 5, §10).

## 3. §5 cross-phase audit — consumed interfaces all exist with matching semantics

- **P4** (`docs/phase4/v1/PHASE_4_DOC.md`): `ProgramBindingSelections.validateSelection` (`:729`), `UseProgramRequest(selection,context)` / `releaseToFixedFunction(context)` with the closed `BarrierResult` domain exactly matching §4.2's exhaustive branches (`:1670-1681`), `RegistryFingerprint` excluding generation (`:1899-1921`, "It does not hash … generation"), and `RegistryFingerprint/profile-selection-v3` (D-P4-43, `:1937-1943`) carried opaquely per D-P8-36. ✔
- **P5** (`docs/phase5/v1/PHASE_5_DOC.md`): five-argument `shadowBindings(long,long,ShadowPassSnapshot,TextureOverlayLease,TextureOverlayPublicationId)` (`:2145-2147`), `ShadowPassSnapshot` field-for-field (`:2157-2166`), `ShadowEstateResult` tri-state with generation-bearing variants (`:2131-2135`), `ShadowDepthCopyPoint.SHADOW_PRE_TRANSLUCENT` (`:2172`), `ShadowNeutralReason` incl. the four backend reasons + `EXPLICIT_FEATURE_DISABLE` (`:2207-2213`), `degradeToNeutral`/mipmap `Neutralized(...,true)` semantics (`:2243-2251`), and D-P5-50 `BaseAtlasContext`/`baseAtlasContext()`/`baseTexture()`/`atlasContext(TextureHandleRef)` (`:1059-1070`, `:2735`) consumed unchanged by D-P8-39. ✔
- **P6** (`docs/phase6/v1/PHASE_6_DOC.md`): `updateCelestial`/`updateShadowMatrices` (`:525-526`), exact `CelestialSample`/`ShadowMatrixSample` records (`:786-790`), `phase6.replay.delivery.failed` with scope `"uniform replay evidence delivery"` (`:1429-1433`), D-P6-37 independent celestial receipt mirroring D-P8-38 (`:767-781`). ✔
- **P7** (`docs/phase7/v1/PHASE_7_DOC.md`): eleven-field `ShadowInvocationContext` in the exact incorporated order (`:2641-2654`), `ShadowExecutionView.currentBaseBinding()`/`installBaseBindingReceiver`/`ShadowBaseBindingReceiver.refreshBaseBinding(AtlasBindingEvidence)→SignalResult` (`:2665-2671`) with D-P7-73 receiving semantics matching D-P8-39 clause-for-clause — sole receiver, before first draw, fail-closed without it, close-old-binding-only then lease+same-snapshot `shadowBindings`, Rejected/Degraded suppress+latch, BackendFailed/throw contain (`:3111-3122`); `ShadowRejection {WRONG_FRAME, STALE_PUBLICATION, UNSUPPORTED}` covering §4.2's rejections (`:2706`); terminal-release receiver (`:3113-3120`); D-P7-72 pure-CelestialMath provider/H-SKY-02 routes (`:2962-2970`, `:3082-3090`). ✔
- **P13** (`docs/phase13/v1/PHASE_13_DOC.md`): three-argument `TextureLeaseSource.lease(...,AtlasBindingEvidence baseBinding)` with the exact six-reason domain incl. `INVALID_BASE_BINDING`/`STALE_BASE_BINDING` (`:350-367`, `:1693-1694`). ✔
- **P1** (`docs/phase1/v14/PHASE_1_DOC.md:1379-1382`): exact `engine.shadow`/`mod.glue.shadow`/`mod.mixin.shadow` grant backing R8-3. ✔

Cross-boundary **producers** were traced to their dispatch points: P2 D-P2-67 (`docs/phase2/v2/PHASE_2_DOC.md:1361-1364`, `:2952`, `:3275`) validates and preserves the 66-row/65-non-CLOUD flattened-v3 catalogue with the six independent sort-cache observations; P7's D-P7-72/73/74/75 delta (`docs/phase7/v1/PHASE_7_DOC.md:3613`) copies/freezes the same projection. No silent drop exists on any producing side.

## 4. Native symbol and control-flow verification

All load-bearing SRG targets were resolved through the Cleanroom mappings DB (1.12.2 stable_39) and match the ledger's descriptions exactly: `field_72740_G`=`renderEntitiesStartupCounter`, `field_72748_H`=`countEntitiesTotal`, `field_72749_I`=`countEntitiesRendered`, `field_72750_J`=`countEntitiesHidden` (the "entity-startup and rendered/hidden/total" counters of §4.4.8/§4.8.2); `field_147596_f/g/h`=`prevRenderSortX/Y/Z`; `field_147595_R`=`displayListEntitiesDirty`; `func_174979_m()V`=`setDisplayListEntitiesDirty`; `func_178577_a(I)Z`=`RenderChunk.setFrameIndex` (the visit marker); `func_174978_c(BlockPos)Set`=`getVisibleFacings`; `func_181562_a`=`getRenderChunkOffset`; `func_178495_a`=`CompiledChunk.isVisible`; `field_175612_E`=`Minecraft.renderChunksMany`; plus the five view caches, both debug fields, `chunksToUpdate`, and `renderInfos`. The current Cleanroom `RenderGlobal.java.patch` corroborates the pass-aware `shouldRenderInPass` traversal for entities **and** tile entities, the startup-counter early-out (`if (pass > 0) return;` before decrement), the `if(pass==0)`-gated outline branch keyed on `isRenderEntityOutlines() && (!list1.isEmpty() || entityOutlinesRendered)` (the retained-state case §4.8.2 suppresses), and `ForgeHooksClient.setRenderPass(:235)`/`MinecraftForgeClient.getRenderPass(:50)`.

## 5. Focus area audits (the three recently changed regions)

- **D-P8-37 (sort-cache restoration, `PHASE_8_DOC.md:1424-1443`, `:1638`, `:1690-1699`, `:1737`, `:2546`):** the diagnosis is correct — the world-loop `renderBlockLayer` overload compares the view-entity position against last-frame `prevRenderSortX/Y/Z`, so a shadow translucent call before the main translucent call would consume the movement threshold and suppress main-only sorting; the fix (receiver-bound snapshot, three independent finally restores on return and throw, preserve scheduled work, no replay, `Failed(RESTORE)` forbidding main continuation) is minimal and correct. The six GET/SET rows are independently counted with no aggregate; the catalogue arithmetic is exact: 1+1+1+13+2+2+37+1+1+7 = **66 rows, 65 non-CLOUD, RESTORE=37 (31 existing + 6 new)**, ASCII-ascending order verified character-by-character (including `H-SHADOW-OUTLINE-01` first via `-`(0x2D) < `8`(0x38), and the six sort IDs following `147595-R-SET`), `expected=1` per row, `flattened-v3` preimage well-formed (`:1752-1756`).
- **D-P8-38 (public pure `CelestialMath`, `:515-518`, `:831-846`, `:1080-1130`, `:1775`, `:2547`):** verified against the LGPL reference: `angles`'s transform `a = shadowAngle < 0.25 ? +0.75 : -0.25` is exactly the sun↔sky angle inverse used by the reference; `M0 = T(0,0,-100)·Rx(90°)·Rz(-360°·a)·Rx(sunPathRotation)` matches the reference composition order verbatim; the signed-remainder snap with `-I/2` shift and zero-disable matches including the documented negative-range quirk; `upPosition = gbufferModelView·Ry(-90°)·(0,100,0,0)` and `sun/moon/shadowLight` compositions match; the day boundary `s <= 0.5` matches `isDay()`. The claimed perspective bottom-right discrepancy is real: the reference constructor's last element is `1f` while its own embedded fov=90 expected matrix has `0.0f` — D-P8-1's re-derived `0` follows the compat-profile `glFrustum` form and the embedded expectation. Purity is airtight: no instance, plan, estate, extent, health, GL, or world prerequisite on either `angles` or `sample`, with the P7/P6 receiving sides (D-P7-72, D-P6-37) requiring no shadow demand.
- **D-P8-39 (context-bound lease/refresh, `:1556-1581`, `:1981`, `:2556`):** the registration/refresh ownership matches P7's D-P7-73 receiver exactly; the refresh closes only the prior binding's ownership, re-authenticates the triggering serial via `currentBaseBinding()`, re-leases with the retained selection/expected overlay and the actual evidence, re-runs the unchanged five-argument `shadowBindings` on the **same open snapshot/generation/frame**, permits no select/use/activation on `Bound`, and on Rejected/Degraded suppresses the undrawn remainder with a no-next-draw latch while BackendFailed/throw take Failed/off containment with independent terminal cleanup. Root activity retention and no-recursive-echo of P5's own batching are consistent with P5's D-P5-50 transaction-observer design.

Traversal (§4.7), depth split (§4.8.3 — exactly one `SHADOW_PRE_TRANSLUCENT` copy, `Applied`-gated), filtering (§4.6 plane algebra: `q=(-v)p₊+up₋` is provably light-parallel and interior-preserving; positive-vertex AABB test; conservative fallbacks), and render order (SOLID→CUTOUT_MIPPED→CUTOUT per RESEARCH over the reference's differing order, correctly flagged in §11.3 item 2) were all traced and are sound. §6's ladder maps correctly onto P5's closed result types; §4.2's terminal-release proof (`FixedFunction` alone) matches P4's domain and P7's receiver.

## 6. §5 grant sufficiency statement

**Current §5 grants are sufficient.** Every interface Phase 8 consumes exists at its owning phase with matching shapes, semantics, and failure/lifetime rules, and every interface Phase 8 exposes has a current receiving branch. Nothing in the document relies on an ungranted dependency or invents a missing interface. The `unverified` status, the required fresh whole-document owner/receiver reviews, and the separate §5.5 upstream gates (native legacy source preservation, jcpp permission, U1 suffix execution, P6/P7 consumer reverification) are explicit, deliberate blockers — not grant deficiencies — and the document consistently keeps real shadow `NotInstalled` under them.

## 7. Notes (optional, non-blocking)

1. `PHASE_8_DOC.md:1002` — §4.5.1's first sentence says "`sample` rejects …" while specifying the *angular* domain validation that belongs to `angles`; the complete rule is recoverible from the later §4.5.1 sentence, §5.1's `ShadowCelestialPolicy` row and §8.1 item 2, so this is wording imprecision only.
2. `PHASE_8_DOC.md:944-952` — the configured cloud draw's exact adjacency to entity pass 0 is bounded only by "after opaque terrain and before the split"; the contractually load-bearing fact (clouds inside the pre-split depth set) is unambiguous.
3. `PHASE_8_DOC.md:1556-1558` — the D-P8-39 receiver registration is specified in §4.10 and checklist item 18 but is not a numbered step of §4.2; the "before any shadow draw" bound is safe because no external base transition can occur between step-8 binding and registration (P5's own bind echo is non-recursive by design), but adding it to the step list would remove the latitude.
4. `PHASE_8_DOC.md:2033` — §5.5's R8-5/D-P8-37 row still reads "P7/P2 coordinated receiving edits … required" although both receivers' current bytes already contain those edits (P7 `:3613`, P2 D-P2-67); the accompanying paragraph already records P2 R8-5 as granted/adopted. Status-precision nit; reverification genuinely remains.
5. `PHASE_8_DOC.md:905-908` — synthesized-plane deduplication "within a fixed epsilon" admits a bounded near-parallel merge that could in principle cull a caster in the sliver; the §7.3 brute-force no-omission oracle is the designed containment and must stay mandatory.

## 8. Verification limits (unexecuted evidence — separate from notes)

This was a read-only documentary architecture review: no builds, tests, formatters, validation commands, runtime launches, hook applications, or native rendering were run, and none is claimed. The historical source pins named in §0.2 (`cleanroom-0.6.6-alpha`, `pintonium-9c2fcc1`) are absent from the working tree; corroboration used the current `Cleanroom-0.6.12-alpha` and `Pintonium-main` trees (method/field identities and patch behavior matched at near-identical line positions — pin drift only), which does not prove historical pin identity. All §8 fixtures (including the moved-camera translucent, outline, retained-shadow binding and traversal fixtures) are unexecuted architecture fixtures by design. Mapped-anchor health, the exactly-once rebuild witness, and the no-omission oracle remain implementation-time obligations. IR-01 and final §G5.3 remain open.

## 9. Final verdict

The frozen document is internally consistent, arithmetically exact, honest about unverified status, and fully backed by existing owner-side interfaces on every boundary; the three recently changed regions (D-P8-37/38/39) are correct against both their receiving contracts and the primary native/reference evidence. **No required correction remains: PASS.**

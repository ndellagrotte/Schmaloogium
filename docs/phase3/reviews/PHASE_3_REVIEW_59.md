# Phase 3 — Whole-owner architecture review R59

**Frozen inventory:** attempt7, `docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_7.json:18–22`.

**Owner:** `docs/phase3/v1/PHASE_3_DOC.md`, §§0–12, through line 5510.

**Inventory SHA-256:** `aa05ec63482b504f38073aafaab79adb162a96c88cf393dae2f0c1e8b7f344b6`. This is the supplied frozen inventory identity, not a checksum executed by this reviewer.

## Scope and selected authority

This is an independent review of the entire current owner: header and amendment precedence, scope, declarations, conformance maps, ingestion and preprocessing algorithms, incorporated §5 contracts, failures, threading/performance, planned tests, milestones, spikes, decisions, handoffs and implementation checklist. It is not a review limited to the schema23 range amendment or an acceptance of historical correction receipts.

The selected governing design is **RC3**, as expressly pinned at `docs/phase3/v1/PHASE_3_DOC.md:7–9`: `docs/design/v2.0-RC3/DESIGN.md:92–1109` and `:1316–1470`. The newer global design was not silently substituted. Current versioned citation paths were resolved using `docs/MOVES.md`. Governing research checks covered the relevant ingestion, source/preprocessor, standard-macro, ID-mapping, integration, milestone and OQ-7 material, Appendix A.3 and Appendix F in `docs/research/v1/RESEARCH.md`; the bounded shipped author documentation and the explicit U1/owned-sidecar decisions were used where the owner incorporates them.

The sole declared dependency was independently read at `docs/phase1/v14/PHASE_1_DOC.md:5356–5487`, with the load-bearing module/seam, jcpp admission, immutable capability-profile, logging/diagnostic and notice definitions incorporated there. Actual receiving algorithms in P2/P4/P5/P6/P7/P8/P9/P10/P11/P12/P13 were inspected as boundary evidence, not treated as certified sibling owners.

## Independent checks

1. **Ingestion, identity and durable/session state.** The factory-bundle authority, bounded discovery generations, exact durable-reference resolution, immutable input snapshot, source-root selection, archive lifecycle and selection-dependent validation were checked across owner §§2/4/5. Off short-circuits without invoking unrelated inputs; Internal uses the bounded provider and exact bundle/pack session authority; filesystem loads require matching access and discovery credentials. Preview option states cannot be substituted for finalized load state. Current P7 receiving behavior performs fresh resolution for FULL, retains authenticated selection for REPUBLISH and retains the exact configuration/assets for resource-only NONE, rather than reopening pack bytes (`docs/phase7/v1/PHASE_7_DOC.md:3242–3265`). Closed persistence outcomes preserve failure and partial-write distinctions instead of claiming a cross-file transaction.

2. **Source universe, includes, options and language decoration.** Owner `:1990–2276` keeps base/override/disabled distinctions, dimension-only usability, direct source-root rules and the include-depth/cycle boundary. Same-file switch confirmation is not replaced by WCC or whole-pack confirmation; WCC is confined to merge/ambiguity work. Safe out-of-list values remain distinct from invalid input. The normalized full locale catalog has explicit collision, absence and empty-string semantics. P12's actual model loop freezes that catalog, expands the one winning star, routes `ScreenProfileEntry()` to `ProfileCycle`, counts retained slots and performs requested-locale→en_us per-key presence fallback (`docs/phase12/v1/PHASE_12_DOC.md:550–665`). Its preview inference and separately retained explicit profile intent do not introduce a profile identity into P3's payload-free screen entry (`:750–786`).

3. **Macros and properties.** Owner `:2277–2416,2593–2622` separates GLSL preprocessing from the lossless Properties adapter. Properties receives standard A–G macros, not option macros; typed old-light resolution then supplies shader macros without a parse/materialization cycle. The required companion pair, reserved zero-only AA storage and finalized state remain load inputs rather than consumer overrides. The corresponding typed lighting receiver uses the same user→pack→true precedence and does not infer runtime policy from macro presence (`docs/phase10/v1/PHASE_10_DOC.md:1295–1303`). Current permitted Pintonium preprocessing bodies independently corroborate why blanket hash removal and unrestricted marker hoisting must not be copied.

4. **Native materialization, attribution and uniform catalogs.** Owner `:2417–2591,3301–3314,3816–3850` supplies closed None/CoreLayout/NativeLegacy results, explicit native requests, source-layout precedence, preserved native language tokens, shader intrinsics, attributable final token ranges and a nonrecursive materialization/uniform-catalog fingerprint. P4's actual materialization and merge algorithm processes each available stage, rejects catalog/fingerprint or same-name structural-type disagreement and dispatches all geometry forms (`docs/phase4/v1/PHASE_4_DOC.md:1333–1635`). The downstream successful-link input is independently cached by P1 and compared by P4 before drawable publication; an unknown query result cannot become a default no-geometry success (`docs/phase1/v14/PHASE_1_DOC.md:3865–3903`). This establishes architectural routing, not successful compilation or native draw evidence.

5. **Directive/resource publication.** The owner separately publishes global minima/scalars and program-keyed routes, attributes, instance counts, mipmap membership and geometry. The const/comment/uniform forms have explicit ownership and aggregation rather than inheriting reference TODO dispatch. Dryness and wetness remain separate fields; P6's actual smoother selects them independently and uses game-time ticks (`docs/phase6/v1/PHASE_6_DOC.md:1013–1027`). P5 derives its real allocation baseline from the declared minima, retains requested format identity and distinguishes planned from realized evidence (`docs/phase5/v1/PHASE_5_DOC.md:1258–1317`). P8 receives color-or-depth shadow demand from those same minima and routes structural invalidity, NotRequested and requested-policy failure separately (`docs/phase8/v1/PHASE_8_DOC.md:710–746`). The owner does not manufacture runtime availability from declaration presence.

6. **ID parser and schema23 range reception.** The current closed types at owner `:1302–1334` and grammar at `:2986–3177` preserve ordinary/alternate provenance, metadata interval unions, literal versus integer-interval alternatives, conjunction, source ordering, bounded endpoints and one rule per selector. The shipped examples are independently present at `reference-src/schlorbium-HD_U_G6_pre1/doc/properties_files.txt:97–122`. P9's actual matcher tests intervals against captured finite domains without expanding ranges, preserves OR-within/AND-between semantics, and rejects an invalid selector without widening it (`docs/phase9/v1/PHASE_9_DOC.md:491–686`). Its ordinary/forced selection remains isolated per contribution and applies the alternate only to the present-empty case. P2 explicitly receives the nested range variants/endpoints in the existing typed projection rather than reparsing or flattening them (`docs/phase2/v2/PHASE_2_DOC.md:2044–2054`).

7. **Owned binary assets and texture dispatch.** The same-load manifest/capability contract distinguishes available, missing and positively classified optional-sidecar-only unreadable outcomes, with original-byte identity and independent read-only heap cursors. P13's actual consumer dispatch handles PackPath, MinecraftResource, Raw and noise separately, acquires only retained typed references and treats invalid capability/metadata pairing as identity failure rather than a fallback read (`docs/phase13/v1/PHASE_13_DOC.md:888–985`). Optional-sidecar recovery is isolated from fatal primary/source/bounds failures; P13 owns the single warning and role-specific outcome. Target-specific native limits are supplied by the immutable P1 profile, not guessed by P3 or queried by the engine parser (`:989–1019`). Numeric duplicate discriminators and documented sidecars are retained without reviving the superseded unspecified suffix parser.

8. **Source-free inspection, schemas and expression boundary.** Owner `:3987–4210` preserves exactly nine metadata-only trees, explicit typed variants/absence/order, arbitrary-string hashing, separate archive provenance and exact current containing/nested/inspection schema checks. P2's actual adapter maps all nine trees losslessly, checks schema/fingerprint before projection or enrichment and checks archive bytes against its independently established fixture identity (`docs/phase2/v2/PHASE_2_DOC.md:1997–2127`). P4's separate same-request enrichment preserves ownBuild rather than inferring it from fallback resolution (`docs/phase4/v1/PHASE_4_DOC.md:2195–2253`). Expression declarations remain lossless and ordered; P11's actual adapter maps both closed enums, preserves duplicates/ordinals and fails unknown future variants instead of dropping them (`docs/phase11/v1/PHASE_11_DOC.md:454–521`). Historical schema receipts are expressly superseded by the current schema23 gate, not compatibility adapters.

9. **Gate completeness and restraint.** Owner §§6–12 contain failure dispositions, finite ownership/lifetime rules, named observable acceptance cases, staged component ownership, OQ-7 evidence requirements and explicit remaining implementation/runtime gates. None of the reviewed owner claims requires treating future tests, receiver receipts or an architecture verdict as completed conformance. No current load-bearing owner defect was independently established.

## Substantive corrections

**None.** Correction count: **0**. No §5 mutation is requested.

## Notes

### N59-1 — Informational: retain bounded provenance and license confidence

**Locations:** `docs/phase3/v1/PHASE_3_DOC.md:60–70`; `docs/reference/pintonium/v1.0/PINTONIUM_DESIGN.md:400–417,646–674`; `docs/reference/oculus/v1.0/OCULUS_DESIGN.md:132–204`.

The historical `reference-src/pintonium-9c2fcc1` and `reference-src/Oculus-1.12` paths are absent from the current inventory. The permitted current trees do corroborate specific portable observations: `reference-src/Pintonium-main/common-shaders/src/main/java/net/irisshaders/iris/shaderpack/include/IncludeGraph.java:223–242` retains the WCC stub; its `preprocessor/PropertiesPreprocessor.java:78–103` retains the destructive hash/backslash adapter; `properties/PackDirectives.java:251–255` retains the dryness misrouting; and `parsing/DispatchingDirectiveHolder.java:261–290` retains the comment/uniform TODO routes. Current Oculus's `reference-src/Oculus-1.12-1.12.2/src/main/java/net/coderbot/iris/shaderpack/preprocessor/JcppProcessor.java:7–24` corroborates the scoped LGPL lineage notice and misplaced-version caveat. These current observations do not authenticate the missing historical pins or grant blanket source-incorporation permission. In particular, current Pintonium's top-level `LICENSE:1–2` is GPLv3 and cannot by itself establish every subtree's licensing. **No observable owner contract breakage or owner edit is established by this limitation.** Retain file-specific notice/dependency review before any future incorporation; do not promote this architecture review into license certification.

## Limitations and final disposition

This review performed document/source reads only. No file was edited; no checksum, formatter, linter, build, test suite, runtime experiment, GL operation, archive execution or matrix run was performed. No forbidden transcript, Oculus transformation boundary, glsl-transformer implementation or OptiFine decompiled implementation was opened. The permitted mining digests were read as bounded evidence, not as authority to adopt prohibited implementation structure. Runtime behavior, jcpp implementation admission, original fixture execution, native geometry correctness on drivers, license compliance and sibling-owner correctness remain un-certified.

**Corrections:** 0. **Notes:** 1. **Section 5 impact:** no.

**Verdict: PASS**

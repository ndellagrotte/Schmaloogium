# Phase 3 — architecture review 61

**Date:** 2026-09-08. **Frozen document:** `docs/phase3/v1/PHASE_3_DOC.md` SHA-256 `0ccb3861a20424893c022696fa715f66bed694479f50c90eea9a9f375c363acf`. **Verdict:** PASS-WITH-CORRECTIONS (0 blocking / 3 corrections / 3 notes). **§5 impact:** none of the three corrections touches §5 or any published contract surface — they are a §0-ledger omission, one stale §3.5 dependency citation, and one §11.4 receiver-wording overstatement — so they add no new §5-driven re-verification trigger beyond the outstanding whole-document verification this round is part of.

This review is documentary only: no build, test, Gradle, GL, capture, or network execution was performed, and no file other than this report was created or modified.

---

## 1. Scope, identity and authority actually verified

- **Hash recomputation (first action):** `sha256sum docs/phase3/v1/PHASE_3_DOC.md` returned `0ccb3861a20424893c022696fa715f66bed694479f50c90eea9a9f375c363acf`, identical to the frozen assignment hash. The document is 5521 lines; review proceeded against those exact bytes.
- **Governing design confirmed at the doc header:** `docs/phase3/v1/PHASE_3_DOC.md:7-9` names `docs/design/v2.0-RC3/DESIGN.md` Part I §G0–§G12 plus the Phase 3 specification only, with RC3 governing this phase and Phase 1/2 pins untouched. Verified against `docs/design/v2.0-RC3/DESIGN.md:1316-1470` (Phase 3 spec) — matches.
- **Complete reading list actually read (in ranges, this session):**
  - The entire assigned phase document, `docs/phase3/v1/PHASE_3_DOC.md:1-5521` (§0 addenda, §1–§12, decision log D-P3-1..D-P3-74, closing italics).
  - Governing design Part I: `docs/design/v2.0-RC3/DESIGN.md:198-363` (§G1.1/§G1.2/§G1.3 protocol, §G2.1 decision log D-1..D-10), `:600-660` (§G5 waves/invariants, §G5.3 integration checklist), `:790-860` (§G9 mandatory template, §G10 OQ table incl. OQ-7), `:862-935` (§G11 rules incl. §G11.5 trust/do-not-inherit tiers), and the Phase 3 spec `:1316-1470`.
  - `docs/research/v1/RESEARCH.md:11-107` (§0 authority, §1.1 mission, §1.2 non-goals, §1.3 D-1..D-10), plus conformance spot-check ranges: §3.6.8 (`:439-462`), §3.7 (`:464-474`), App A.3 rows (`:1160-1177`), App F inventory lines (`:240-266`, `:1444-1464`, `:1518`), unit-map rows (`:1230`), virtual-pre rows (`:221`, `:350`, `:1135-1139`).
  - Dependency P1 `docs/phase1/v14/PHASE_1_DOC.md`: §0 header (`:5-31`), §1 (`:1648-1704`), §4.2.4b D-P1-49 jcpp admission (`:2292-2320`), pin-table context (`:2349-2363`), §5 (`:5420-5552`, incl. the D-P1-49 row `:5450`, GLCapabilityProfile row `:5476`, legacy-geometry row `:5468`, schema23 row D-P1-65 `:5489`), §4.9.2 channel rows (`:4906-4908`).
  - Consumer receipts recomputed on the receiving side: P4 (`docs/phase4/v1/PHASE_4_DOC.md:373-379`, `:1365`, `:1379`, `:1932`, `:2200-2266`, `:2358`, `:2798`, `:2907-2917`, D-P4-31 at `:365`, `:745`), P5 (`:2796-2833`, D-P5-44 `:3390`), P6 (`:1822`, `:1852-1871`, D-P6-36 `:2260`), P7 (`:886-888`, `:3421`, `:3461`, `:3697-3733`, `:4021`, `:4071`, D-P7-33 `:403`, `:4508`, D-P7-57 `:3714`, `:4532`), P9 (`:932-986`, `:1045`, D-P9-22 `:1313`), P11 (§5.2 `:1164-1234`, D-P11-25 `:1658`, D-P11-27 `:525-535`), P12 (`:565-580`, `:670`, `:968-990`, `:1078`, `:1441-1473`, D-P12-22 `:2075`, D-P12-36 `:2089`), P13 (`:207`, `:227`, `:296`, `:993`, `:1750-1762`, `:1922`, `:2201`, D-P13-37 `:2339`, D-P13-44 `:2346`), P8 (D-P8-33 `:2048-2062`), P14 (D-P14-33 `:1966-1967`, `:2641`), P2 (`docs/phase2/v1/PHASE_2_DOC.md` — see Required correction 3).
- **Fresh-eyes boundaries honored:** no `reference-src/**` read; no `docs/**/chatlogs/`, no root `*.txt`, and no other `PHASE_*_REVIEW_*.md` file opened. Only this report file was written.

## 2. Scope and §5 audit

- **Doc gate (spec `DESIGN.md:1451-1457`), all seven criteria met literally:**
  1. Conformance map with zero unassigned rows — §3.1 (`PHASE_3_DOC.md:1660-1690`) maps all 17 App F.1 flags; §3.2 (`:1692-1739`) covers F.2–F.8 keys; §3.3 (`:1741-1785`) covers the App A.3 directive table; §3.5 (`:1808-1823`) closes the remaining §3 families.
  2. Flag-ownership map complete — §3.1 assigns every flag; owners verified live: P9 owns `oldHandLight`/`dynamicHandLight` (`PHASE_9_DOC.md:684`, `:703`), P10 owns `oldLighting`/`separateAo` (`PHASE_10_DOC.md:1328`, `:1341`), P7 owns `frustumCulling` (`PHASE_7_DOC.md:809`, `:1783`).
  3. Identity set option-3-shaped, decision open — `PHASE_3_DOC.md:2291-2300`, `:2362-2363`; OQ-7 left to G8/S3 (`:5197`).
  4. Pure-core placement confirmed — `:627-629` (`:engine` only, no MC/Forge/Cleanroom/Mixin/LWJGL types).
  5. jcpp adoption stated — D-P3-1 (`:5006`), §4.5 (`:2424-2428`), receiver adoption of D-P1-49 in §5.4 (`:4291-4304`).
  6. B1/B2/B3/B12 each trace to a conformance row **and** a named test — §3.4 rows `:1794-1797`; §8.1 gates `:4797-4801`.
  7. centerDepthSmooth injection point reserved — `:2288`, `:2304-2305`, `:4652`, `:4929`; P6 side supplies `MacroContribution.Empty` under D-P6-1 (`PHASE_6_DOC.md:1822`).
- **Conformance-map spot-checks against RESEARCH/appendix text (semantic fidelity):** shadow/shadowcolor/depthtex allocation minima 1–2/1–2/1–3 (`:1754-1756`) match RESEARCH `:1164-1166`; `gdepth` mandatory RGBA→RGBA32F upgrade (`:1758`, `:5031`) matches `:1167`; half-life ticks distinctness incl. the B1 split (`:1769-1770`, `:1787-1788`) matches `:1177`/`:1330`; `scale.<prog>` finite 0–1 executable-only and `deferred_pre`/`composite_pre` flip-only (`:1732-1733`) match `:1518`/`:1135-1139`/`:350`; texture stage expansion (`:1723`) matches the `:1230` unit table split; `clouds` four-state (`:1664`) matches `:263`; ID-map A–G/no-option-macros and layer/solid-opaque rules (`:1823`, `:3003-3006`) match §3.7 `:464-474`; the D-P3-47 claim that §3.6.8 supplies only the generic shim + entries-beat-tags priority (`:1820`, `:5298-5300`) matches §3.6.8 `:456`. One citation defect found (Required correction 2).
- **Interface honesty — consumed from P1 exists in P1's current §5:** C-1..C-4 seam/module layout (`:5443-5445`), `GLCapabilityProfile` as the whole GL-side macro input (`:5476`), channels `schmaloogium.pack`/`.preprocess`/`.config` (`:4906-4908`), `EngineDiagnostic`/`DiagnosticReporter` (`:5536`), `schmaloogium.debug.saveSources` (`:5537`), SPDX/THIRD-PARTY mechanism (`:5544`), `configureLegacyGeometry` (`:5468`), and the D-P1-49 jcpp grant (`:2292-2320`, `:5450`) whose exact terms (one verified pin, no ranges/snapshots, unchanged C-1, once-only `contain` packaging, no public jcpp types) are mirrored by P3 §5.4 `:4291-4304`. P3 §5.2's "No GL service or handle is consumed" holds.
- **Interface honesty — promises to dependents specified; receiver adoption of recent grants recorded on the receiving side:** schema23 receipts exist and are dated on every receiving side P3 §5.3 `:4219-4224` names: P1 (D-P1-65, `PHASE_1_DOC.md:5489`), P4 (D-P4-41; materialization with no `OptionState` argument `PHASE_4_DOC.md:1365`, direct `mipmappedAfterPass()`/`vertices()` `:1379`, `:2248`), P5 (D-P5-44), P6 (D-P6-36; `MacroContribution.Empty` `:1822`), P7 (D-P7-57; `CompanionOptionMacros` supply `:886-888`, `InternalOptionCommitter` `:3421`, `:3461`; D-P7-33 assets retention `:3733`, `:4508`), P9 (D-P9-22; nested `IdMappingInput` gate `:932-946`), P11 (D-P11-25; `customExpressions()` algebra match §5.2 `:1164-1200`), P12 (D-P12-36; `localizedDecorations()` sole authority `:1462`, `:1674`; durable `FilesystemCandidateReference` D-P12-22 `:2075`, `:1441`), P13 (D-P13-37/44; `assets()`/`PackAssetSnapshot` consumption `:1752`, `:993`; D-P13-30 `:207`), P8 (D-P8-33), P14 (D-P14-33). P3 honestly frames these as "producer handoffs, not assertions that receipts or fresh reviews are complete" (`:4225`) — except the single overstatement in Required correction 3. D-P3-71's P4 ownBuild enrichment receipt exists on both sides (P3 `:5075`; P4 D-P4-31 `:365`, `:745`, `:1275-1276`).
- **Current-identity discipline:** `PackFrontEnd.CURRENT_SCHEMA_VERSION = 23` declared at `:704` and `:4161`; `MaterializedSource-v23` at `:3309`, `:4212`; inspection `projectionVersion=1` at `:4008`, `:4186`, `:4215`; D-P3-69 assets with Acquired/Missing/Unreadable/InvalidReference and read-only cursors `:1889-1988`; locale-indexed `localizedDecorations()` with requested→en_us→fallback `:4352-4357`; old-light exact `default|true|false`, user→pack→true, `MC_OLD_*` emitted before shader jcpp `:2342-2349`, `:1612-1622`; U1/D-P3-67 no-suffix-parser posture `:3586-3612`, `:4319-4334`; session-only Internal capture D-P3-65 `:2114-2124`. Historical schema19/20/21/22 receipts are consistently marked historical (`:4191-4206`). No stale identity is cited as current; P3 cites none of the P7/P13 health/capture identities, so nothing to falsify there.
- **Scope discipline:** §1.1/§1.2 (`:595-651`) match the spec's Scope—in/out (`DESIGN.md:1333-1432`): no compilation, allocation, uniform values, expression evaluation, GUI, texture loading, or alias resolution is designed; the P4 mipmap/vertex surface is data publication through existing resource accessors (`:5064`, `:5144-5149`), which the spec's "you produce sizing requirements" explicitly permits. Nothing in-scope is silently dropped (§9's P3-C01..C23 register covers every §2.2 subsystem).
- **Template completeness:** all thirteen §G9 sections (0–12) present and substantive; the one assigned OQ (OQ-7) carries a full spike spec — verbatim question, procedure (6 steps), success criteria, failure criteria, and fallback (`:4952-4998`), with the PD §7.6 evidence caveat (`:4997-4998`).
- **Binding decisions D-1..D-10:** §11.2 (`:5085-5092`) preserves all ten; verified in substance — D-6 via pure `:engine` placement, D-8 via the no-AST-transformer ruling D-P3-16 (`:5020`) and §0.3 (`:68-69`), D-9 via profile-describing macros without GL calls (`:2541-2543`), D-10 via §8.2/§11.2 hand-off to Phase 2.

## 3. Required corrections

1. **D-P3-74 (R60 C1) is absent from the §0 amendment ledger, the header revision pointer, and the closing review-history italics.**
   - **Sites:** `docs/phase3/v1/PHASE_3_DOC.md:6` ("Last revised: 2026-09-08 (§0.65)"); §0 ends at §0.65 (`:576-593`); closing italics end at the §0.65/D-P3-73 record (`:5518-5521`); the change itself lives only in §4.4 (`:2306-2328`), §5.1's MacroConfiguration row (`:3362`), §8.1 (`:4703-4707`), and the D-P3-74 table row (`:5078`).
   - **Failure path:** every sibling amendment (§0.62–§0.65) carries a dated §0 entry stating inputs read and its no-verification posture, and the closing italics narrate each; D-P3-74 — which resolved the round-60 blocklist/whitelist conflict — has none, so a fix-up session or the final §G5.3 integration review reading §0 as the change ledger will conclude §4.4's whitelist text and the §8.1 test description are unratified by the recorded history, and the header's "(§0.65)" pointer under-counts the revisions the document incorporates.
   - **Owner+receivers:** Phase 3 owner; no receiver adoption required (no §5 or schema change — D-P3-74 itself records "no `PackConfiguration` component, meaning, fingerprint or schema change").
   - **Minimal resolution:** append a dated `§0.66` addendum recording R60 C1 (inputs read, whitelist-authoritative statement, explicit no-implementation/no-PASS posture), update the header parenthetical to `(§0.66)`, and add one closing-italic sentence for §0.66 mirroring the §0.63/§0.65 pattern.
2. **§3.5's companion-macro conformance row cites Pintonium-independent Phase 13 text that no longer exists at the cited lines, quoting phrases not present in the dependency's current bytes.**
   - **Sites:** `docs/phase3/v1/PHASE_3_DOC.md:1816` cites `docs/phase13/v1/PHASE_13_DOC.md:1343-1347` for the quotations "every same-build materialization" and "Include both booleans".
   - **Failure path:** recomputed against current P13 bytes, lines 1343-1347 are the canonical-fingerprint framing (`phase13.texturePublication/v1`/`phase13.source/v1`/`phase13.parameters/v2` domains) — not the companion-macro request; "every same-build materialization" survives only in altered form at `PHASE_13_DOC.md:831`, `:1921`, `:2408`, and "Include both booleans" appears nowhere verbatim. A consumer auditing the row against the cited text (exactly the §G1.2 spot-check this review performs) finds the quotations fabricated-by-drift, weakening an otherwise sound D-P3-58 grant that P13 has in fact adopted (`:1921`, R1 row `:2408`).
   - **Owner+receivers:** Phase 3 owner; Phase 13 is the cited dependency (its R1/D-P13-38 adoption is already recorded and unaffected).
   - **Minimal resolution:** repoint the citation to P13's current companion-macro rows (e.g. `PHASE_13_DOC.md:1921` and the §11.5 R1 row `:2408`), and quote only text that those lines verbatim contain, or paraphrase without quotation marks.
3. **The §11.4 TS-1 bullet asserts a Phase 2 receiver receipt (D-P2-32) that does not exist in Phase 2's current document, contradicting the document's own pending-amendment posture.**
   - **Sites:** `docs/phase3/v1/PHASE_3_DOC.md:5232-5238` — "P2 D-P2-32 preserves all nine source-free projection trees, with assets mapped to `[properties] owner.assets` … These are current receiving contracts"; contrast `:5272-5279`, which correctly states Main must amend P2 and that required receiving amendments are "pending Main integration".
   - **Failure path:** recomputed against current bytes, `docs/phase2/v1/PHASE_2_DOC.md`'s decision log tops out at D-P2-23 (no D-P2-32 exists anywhere except P3's citation), and P2's current document contains no schema23/`CURRENT_SCHEMA_VERSION` admission, no `projectionVersion`, no nine-tree inspection projection, and no `owner.assets` mapping. A dependent (or the integration review) treating the TS-1 bullet as evidence of recorded P2 adoption would rely on a receipt that exists on neither side — precisely the interface-honesty failure §G1.2 exists to catch; the P13 (D-P13-30) and P7 (D-P7-33) legs of the same TS-1 triad are genuinely recorded, which makes the P2 leg stand out as unverified assertion.
   - **Owner+receivers:** Phase 3 owner; Phase 2 is the affected receiver (its amendment is owned by Main per the D-P3-73 hand-off `:5272-5274`); P7/P13 receipts unaffected.
   - **Minimal resolution:** reword the TS-1 bullet's P2 clause to pending-amendment form consistent with `:5272-5279` — e.g. "Main must amend P2's source-free projection so it preserves all nine trees with assets mapped to `[properties] owner.assets`; no P2 receipt is recorded yet" — or drop the D-P2-32 identifier until P2's document actually records it.

## 4. Notes

- **Shadow-FOV domain provenance:** §3.3 (`:1761`) and the `directive_shadowFovDomain` test (`:4653-4655`) assert "finite degrees strictly between 0 and 180, otherwise malformed", but RESEARCH's App A.3 row (`RESEARCH.md:1169`) states no numeric domain, and the row carries no provenance pointer for the boundary choice (unlike neighboring decisions such as D-P3-55's tagged behavioral observation). Consistent with the shipped OptiFine documentation family this phase cites elsewhere; a one-token provenance tag in a future fix-up would close the gap. Not a conformance deviation — RESEARCH is silent, not contrary.
- **P1 dependency status is a recorded input, not re-verified:** §0.1 item 4 (`:23-26`) relies on `docs/phase1/reviews/PHASE_1_REVIEW_15.md` being literal PASS. Per this review's fresh-eyes constraints no historical review file was opened; P1's §5 was verified directly instead, and P1 separately records its own schema23 receipt (D-P1-65).
- **Identity sweep clean:** every recent-change identity in the assignment is cited at its current value — schema23 configuration/nested-ID/inspection state (`:704`, `:4205-4217`), `MaterializedSource-v23` (`:4212`), nine source-free projection trees incl. the assets manifest and `TextHash`/`IntegerValue` leaf discipline (`:5272-5274`), `InternalOptionSnapshot`/`captureInternalOptions` session-only semantics D-P3-63/65 (`:2114-2124`, `:5192-5196`), locale-indexed `localizedDecorations()` with per-key requested→en_us→fallback (`:4352-4357`), old-light wire values and pre-jcpp emission D-P3-64 (`:2342-2349`, `:5304-5308`), U1/D-P3-67 (`:5287-5297`), D-P3-69 assets outcomes and read-only cursors (`:1897-1932`), D-P3-71 (`:5075`), and D-P3-74's whitelist (`:2306-2328`).

## 5. Areas audited and found sound

- **Doc gate**, all seven criteria (see §2) — including the B1/B2/B3/B12 pitfall chain from §3.4 rows to §8.1 named gates, and the reserved Phase 6 macro-header slot adopted end-to-end (P3 `:2288`/`:4652`; P6 `MacroContribution.Empty` under D-P6-1).
- **Conformance-map coverage and fidelity:** zero unmapped in-scope rows found in §3.1/§3.2/§3.3/§3.5; every spot-checked row (allocation minima, `gdepth` upgrade, half-life ticks, scale arity/domain, virtual-pre flip-only, stage mapping, `clouds` states, ID-map A–G rule, layer/solid-opaque, §3.6.8 scoping) matched RESEARCH text semantically. The single defect is the stale citation of Required correction 2, not a semantic error.
- **Interface honesty toward P1:** every consumed contract exists in P1's current §5 with matching terms; the D-P1-49 receiver adoption in §5.4 mirrors P1 §4.2.4b clause-for-clause (exact pin, no ranges/latest/snapshot, unchanged C-1 scans, no public jcpp types, once-only `contain` packaging, LICENSE/NOTICE preservation, implementation-blocking on failed closure).
- **Receiver adoption of recent grants:** dated schema23 receipts verified on P1, P4, P5, P6, P7, P8, P9, P11, P12, P13, P14; D-P3-69 assets adoption recorded at P13 (D-P13-30) and P7 (D-P7-33); D-P3-71 ownBuild receipt recorded at P4 (D-P4-31); P4's §5.3 binding table matches P3's §5.1 shapes (no `OptionState` argument, direct projections, `evaluateProgramStates(profileSelection, diagnostics)`). The one unrecorded leg (P2/D-P2-32) is Required correction 3.
- **Scope discipline:** §1.2 anti-sprawl table and §9's component register cover the full spec Scope—in without foreign design; post-v0.5 items (`RENDERTARGETS` P3-C22, compute roots P3-C19, OQ-7 spike P3-C09) are staged, not smuggled into v0.1.
- **Binding decisions and licensing/Pintonium compliance:** D-1..D-10 preserved; §0.3 posture plus per-claim `[V:observed — …]` tags on all PD-derived rows sampled; no glsl-transformation-lib trace (D-P3-16 refuses any AST transformer); §G11.5 do-not-inherit rows demonstrably handled (dimension model rejected D-P3-8, enumerate-all extensions rejected D-P3-9, FE-01 properties split rejected D-P3-15, B12 `#`-stripping prohibited §4.6, PD §17 dead features gated by named tests); Oculus consumed through its gated report only.
- **Internal consistency of the recent corrections:** D-P3-72's dual-era BLOCK grant, D-P3-73's typed interval/property-constraint algebra, and D-P3-74's whitelist ruling are stated identically in §4.9/§4.4, §5.1's binding rows, §8.1's test plans, and §11.4's receiver hand-offs, with historical receipts consistently downgraded to historical.

## 6. Verdict

**PASS-WITH-CORRECTIONS** — 0 blocking, 3 corrections, 3 notes.

The document's contract surface, conformance coverage, dependency honesty, scope discipline, template completeness, decision compliance, and licensing posture are sound as written; the three corrections are bounded documentation repairs (a missing §0 ledger entry, one stale dependency citation, one over-asserted receiver bullet) that a fix-up session can apply without touching §5, any published record shape, or the schema-23 cutover. Per §G1.3, a fresh fix-up session should apply these corrections and record resolutions; this review itself changed nothing, ran no build/test/GL/capture, and claims no verification, PASS, or implementation clearance for any implementation session.

## Resolutions

Applied 2026-09-08 by the Phase 3 fix-up session against `docs/phase3/v1/PHASE_3_DOC.md` (frozen at 5521 lines; now 5547). The report body above is unchanged; only this section was appended.

- **C1 (D-P3-74 missing from §0 ledger / header pointer / closing italics) — applied.** Appended the dated `§0.66 R60 override-target whitelist correction — 2026-09-08` addendum at the end of §0 (`PHASE_3_DOC.md:595-612`, immediately before `## 1.`), recording the whitelist-authoritative ruling with its inputs read and the explicit no-build/no-implementation/no-PASS posture, in the style of §0.63/§0.65; updated the header revision pointer (`:6`) from `(§0.65)` to `(§0.66)`; appended one closing-italic paragraph for §0.66/D-P3-74 after the §0.65 record (`:5542-5547`). No receiver adoption was added, matching the review's owner-only scope.
- **C2 (stale P13 §3.5 citation with drifted quotations) — applied with recomputed anchors.** Both quotations are confirmed absent from P13's current 2506-line bytes (FixP13 amended P13 mid-wave; anchors below are recomputed against those settled bytes): neither "every same-build materialization" nor "Include both booleans" occurs verbatim anywhere, and `:1343-1347` now holds the canonical `phase13.texturePublication/v1` fingerprint framing, exactly as this review found. Replaced the quotations with an unquoted accurate paraphrase and repointed the §3.5 companion-macro row (now `PHASE_3_DOC.md:1835`) to P13's verified current locations: the preliminary-demand paragraph `docs/phase13/v1/PHASE_13_DOC.md:846-852` (required `CompanionOptionMacros(boolean,boolean)` pair before load/jcpp, retained by Phase 3 §5.1 in `MacroConfiguration`, same-build materialization and both fingerprints), the R1 adoption record `:1940-1944`, and the §11.5 R1 row `:2430`. Note on the steering hint: the "D-P13-44/45 region" does not host the preliminary-demand text in current bytes — D-P13-44/45 sit at `:2367-2368` as the R8 acquisition-gate and anchor-repair rows — so the paraphrase anchors at the verified demand/adoption rows instead.
- **C3 (§11.4 TS-1 P2 receipt) — applied as a repoint; the review's "does not exist" claim is rejected as factually wrong, and the staleness concern is confirmed.** Evidence: P2's current document is `docs/phase2/v2/PHASE_2_DOC.md` (v1 tops out at D-P2-23, which explains the reviewer's recomputation), and its decision log defines `D-P2-32` at `:3245` as the "Historical 2026-09-08 schema20/ninth-asset receipt, schema superseded by D-P2-38", with the historical receipt prose at `:2087-2095` and `:2760-2766` (ninth section maps in full to `[properties] owner.assets`). Repointed the TS-1 bullet's P2 clause (now `PHASE_3_DOC.md:5255`) from D-P2-32 to D-P2-38 — per Main-verified ground truth the schema21/nine-tree/assets adoption receipt ("nine source-free trees" at `:3251`; "current D-P2-38 adopts schema21, nested IDs, same-load assets and all nine inspection trees" at `:3389-3390`, retaining D-P2-32's owner.assets mapping) — leaving the rest of the bullet byte-identical. For completeness: P2's later D-P2-55 row (`:3268`, `:2431`) supersedes schema21 *admission* with schema23 while retaining the nine trees and asset contracts, so the repointed claim ("preserves all nine source-free projection trees, with assets mapped to `[properties] owner.assets`") remains consistent with P2's current bytes.

New decision IDs: none — C1 records the existing D-P3-74; C2/C3 are anchor/prose repairs with no new grant, so §11.1 is untouched. §5 section text: unchanged. Files touched: `docs/phase3/v1/PHASE_3_DOC.md` and this review file only; no build/test/Gradle/GL/capture/network execution was performed.

**Follow-up correction, 2026-09-08 (Main-coordinated, pre-attempt-10-freeze):** the C3 repoint
landed on D-P2-38, which P2's own ledger records as the schema21-era receipt; P2's current
schema23 receiver receipt — retaining the nine source-free trees/projectionVersion1 and the
`owner.assets` mapping — is **D-P2-55** (`docs/phase2/v2/PHASE_2_DOC.md:2431`, `:3268`).
The TS-1 bullet's P2 clause at `PHASE_3_DOC.md:5255` was corrected from D-P2-38 to D-P2-55
before the attempt-10 freeze; the C3 evidence above stands as the record of the original repair.

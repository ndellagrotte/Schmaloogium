# Phase 6 — architecture review 33

**Date:** 2026-09-08. **Frozen document:** `docs/phase6/v1/PHASE_6_DOC.md` SHA-256 `21b59a00d1c77802b6feaaa4357d28d76041b2282a1ecfbfd4440f2b1ce73e62` (recomputed before review; matches the Attempt-9 registry entry, `reviewRound: 33`). **Verdict:** PASS-WITH-CORRECTIONS (blocking=0; corrections=2; notes=2). **§5 impact:** none of the required corrections touches a §5-incorporated contract — they repair stale `/3` hand-off text in §11.3 and stale evidence line anchors — so the document's already-owed fresh whole-document review is the only verification gate these findings add nothing to.

---

## 1. Scope, identity and authority actually verified

- Hash gate: `sha256sum docs/phase6/v1/PHASE_6_DOC.md` recomputed first; equal to the frozen value above. No mismatch; review proceeded.
- Read in full: the assigned phase document (all 2445 lines, including the long-line §5.1 rows re-read raw where display truncation elided them).
- Governing design: `docs/design/v2.0-RC3/DESIGN.md` Part I lines 1–1109 (§G0–§G12, including §G1.2 protocol, §G2 D-1..D-10 digest, §G4.2/§G4.4, §G9 template, §G11 Pintonium rules, §G12 Oculus rules) and the Phase 6 spec at lines 1689–1804.
- `docs/research/v1/RESEARCH.md` lines 1–110 (§0 reading guide/tags, §1 mission/non-goals/decision log D-1..D-10), plus targeted re-reads of every RESEARCH anchor the conformance map and body cite: §0.3 `:47`, §3.2 `:290`, §4.4 flow `:533–:537`, `:558`, App A.3 `:1166`, App B.3 `:1228–:1255`, App D `:1319–:1383`, App E row 16 `:1416`, §3.4/F.6 `:1494–:1505`.
- Dependency docs, §0 headers / §1 / §5 / §11 surfaces and the specific sections P6 incorporates: P1 (`docs/phase1/v14/PHASE_1_DOC.md` — readDepthPixel verb, ReplayAwareGLError/D-P1-42 law, D-P1-57/57 lease rows, RecordingGLDevice/ScriptedResponses, rung-2 sweep-reuse precondition), P3 (`docs/phase3/v1/PHASE_3_DOC.md` — §0.63–§0.65 schema21/22/23 chain, `CURRENT_SCHEMA_VERSION = 23` at `:704`, `MaterializedSource-v23` at `:3309`, SmoothingConstants `:1770`/`:2750`, DeclaredUniformCatalog `:1435`, macro placement `:1536–1537`, §0.17 catalog grant), P4 (`docs/phase4/v1/PHASE_4_DOC.md` — §0.35 unverified status at `:329`, D-P4-31/34/35/43, §4.10 activation order `:1757–:1792`, `BoundProgramUniformAccess` `:1646–:1656`, three positions `:1751–:1755`, token/lease invalidation `:1957/:1961`, `RegistryFingerprint/profile-selection-v3` `:1937–:1938`), P5 (`docs/phase5/v1/PHASE_5_DOC.md` — §5 `FixedSamplerPolicies` row `:2726`, shared table/schema `:2441`, gbuffers-band map `:2478`, Invalid-evidence `:2499–2502`, `ResolvedSamplerBinding` `:1021`, §5.5 eight-argument factory receipt `:3002–:3012`).
- Consumer docs at the cited hand-off surfaces: P7 (`docs/phase7/v1/PHASE_7_DOC.md` — R7-10 text now at `:4030`, R7-11 at `:4040–4042`, D-P7-34/35 `:4509–:4510`, D-P7-41 `/4` cutover `:4516`, §0.46/§0.48 status, ShadowHookHealth/flattened-v3 `:1861`/`:3667`, TextureHookHealth/application-v2 `:1945`), P8 (`docs/phase8/v1/PHASE_8_DOC.md` — `CelestialMath.angles(float)`/`sample(ShadowFrameView, CameraSnapshot, float sunPathRotationDegrees)` `:514–:519`, D-P8-37/38, D-P8-24 `/4` cutover), P9 (`docs/phase9/v1/PHASE_9_DOC.md` — held-light tuple law `:713`, H9-HELD-01 `:732`), P11 (`docs/phase11/v1/PHASE_11_DOC.md` — seven-name rule `:446–:448`, Bool1 `:820`/`:1258`, CLOSE mapping `:1119`/`:1247–1249`), P14 (`docs/phase14/v1/PHASE_14_DOC.md` — CenterDepthSource reuse `:392`, beginFrame step-6 ordering `:449`, no sample-age grant implied by `:342`).
- P2 current owner bytes: `docs/phase2/v2/PHASE_2_DOC.md` (the Attempt-9 registry's pinned P2 path, hash-verified) — `capture-plan/4` `:1062–1063`, `run-manifest/4` `:1143–1145`, `/1–/3` unsupported history `:2425–2426`, "current `/4` gl_errors" `:2436`, D-P2-39 historical `/3` receipt `:3250`.
- `docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_9.json` read to confirm the phase-6/round-33 assignment and the frozen path set.
- Pintonium/Oculus pinned source files under `reference-src/**` were NOT re-opened, per this review's hard boundary; PD/OD-derived rows were audited for internal consistency, provenance tagging, and gated-adoption form only.
- No build, test, gradle, GL, or network execution was performed. Read-only except this report file.

## 2. Scope and §5 audit

- **Doc gate (spec `DESIGN.md:1791–1795`):** met. Every App D row appears in §4.4.1–§4.4.4 with type, exact value, provider/acquisition, activation, and milestone; I diffed the D.1–D.4 tables name-by-name against §4.4 and found zero missing and zero invented rows. Smoothing formulas are written out in closed form (§4.5, `d = 2^(−Δt/h)` with six edge rules). Barrier fulfillment is traced point by point (§4.10 table), and the trace matches P4's current §4.10 steps 5–6 exactly (invalidate previous token and close previous lease → bind → acquire `lockAlphaBlend` lease → sample `effectiveBlend` through P7's notification before the built-in participant → sampler/built-in/custom in order → publish Activated only after all succeed) — D-P6-33's claimed alignment is real. The centerDepthSmooth decision is recorded with a four-point contract check and `[D-P6-1]` (§4.8); the notifier→producer audit table is present (§4.12, 14 rows); the frame-begin ordering constraint is exported in §5.1 ("Frame-begin ordering contract" row).
- **Conformance-map audit:** zero unmapped in-scope rows. Semantics spot-checked against current RESEARCH bytes all hold: B.3 fixed map `:1228`, depthtex1-unit-11 ruling `:1254`, wraps 720720/3600 (`:1346`/`:1348`) with the correct off-by-one boundary mechanics, near/far `:1360`, eyeBrightness 0–240 `:1332`, D.4 exclusion heading `:1370`, cadence sentence `:1380`, F.6 seven-name union `:1501–1505` (including "does not override or narrow D.4"), declaration grammar `:1495`, App E row 16 `:1416`. The map's RESEARCH line numbers are systematically stale by one to two lines (correction 2), but no semantic infidelity of the depthtex1-unit class exists.
- **Interface honesty:** every consumed contract exists in the owner's current §5: P4 `afterBind`/`BoundProgramUniformAccess`/activity token (`:1646–:1656`, `:1825–:1833`), P5 sole resolver row (`:2726`), P1 readDepthPixel and replay law (`:3388`, `:5475`, `:5496`), P3 catalog/schema23 (`:1435`, `:704`). Every export promised to dependents is specified with receiver adoption status in §5.1. Consumer-side symmetry verified: P9's `(max(main,off), off)` old-mode tuple matches §4.2; P11's seven-name exclusion, `Bool1` GL encoding, and never-forward-CLOSE mapping match §4.13/§4.14/§11.3; P14's reuse of `CenterDepthSource.readCenter` inside beginFrame step 6 matches §4.6, and P6 publishes no sample-age field, matching P14's "age/behavior grants still required" note; P8's `CelestialMath` signatures quoted in D-P6-37 match P8's bytes verbatim, including "no Ready plan, estate, extent, hook health" independence.
- **Recent-change identities:** D-P6-36 (schema23 / `MaterializedSource-v23` / nine trees / projectionVersion=1) is current against P3 §0.65/D-P3-73; schema19/20/21/22 receipts are each explicitly labeled historical. D-P6-31/D-P6-38 place the replay/timing rows on the current `/4` envelope, which matches P2 v2 (`capture-plan/4`, `run-manifest/4`, `/1–/3` unsupported) and P7 D-P7-41/P8 D-P8-24 — except two residual `/3` sentences in §11.3 (correction 1). The eight-argument factory (resolver immediately after configuration, required P7-owned `UniformReplayErrorSink` after diagnostics), original-drain order, delivery before `afterBind`/event return, and non-complete containment all match P7's §5.4 receipt text and P2 v2 §5.1.2/P5 §5.5 receiver rows. D-P6-29's N-total/0…N−1 law including N=1 is consistently published in §4.4.4, §8.2, and §11.3.
- **Scope discipline:** §1.2's ownership lines match the spec's Scope — out (`DESIGN.md:1776–1777`): custom expressions to P11, invoking hooks to P7/P8, alias-derived ids to P9, `atlasSize` source to P13, PBO replacement to P14. The Phase 5 dependency widening (R7-10) is not silent scope creep: it is a maintainer-authorized amendment recorded in §0.23 with explicit unverified-grant gates in §5.2.
- **Template:** all thirteen G9 sections present and substantive; §10 correctly documents that no OQ is assigned (`DESIGN.md:1691`, "OQs: —") so no spike spec is owed.
- **Binding decisions:** no D-1..D-10 contradiction found. D-6 is honored structurally (`:engine` sees only JDK plus published Phase 1/3/4/5 interfaces; Minecraft/GL names stop at `mod.glue`). D-9 compat cooperation intact. D-10 covered by §8.2 tiers including the required camera-path motion family.
- **Licensing/Pintonium compliance:** every PD-derived conformance row carries a provenance tag and disposition; B1 (wetness/dryness field swap), B6 (nullable notifier), and B10 (sampler overload landmine) are each explicitly not inherited with cited source rows; the §18 dynamic-unit allocation is a pre-decided rejection; the AGPL transformer is cited only as prohibited (§4.8 point 4); D-P6-34's license-label qualification is conservative (no reuse claimed without per-file licensing). No AGPL trace, no chatlog/root-`*.txt` input claimed, no Oculus content used (§0.2).

## 3. Required corrections

### Correction 1 — §11.3 still publishes the retired `/3` envelope as P2's current wire

- **Sites:** `docs/phase6/v1/PHASE_6_DOC.md:2313` — "P2 owns `/3` fields and comparability" (R39-2 handoff); `:2320` — "P2 retains all entries in existing `/3` gl_errors with every error failing T0" (R25-1/P8 receiving handoff).
- **Claim:** both sentences are present-tense statements of current P2 ownership, yet this same document's §0.28, D-P6-31, §4.11 ("P2 flattens the values into current `/4` `gl_errors`"), §5.1 (D-P6-38-repaired replay row), and D-P6-38 ("replaces the retired `/3` clause") establish `/4` as the only current envelope. The current owner bytes agree: `docs/phase2/v2/PHASE_2_DOC.md:1062–1063` and `:1143–1145` pin `capture-plan/4`/`run-manifest/4` with "`/1`–`/3` unsupported history" (`:2425–2426`), and P2's current receiving row says "current `/4` gl_errors" (`:2436`). D-P6-38 repaired the §5.1 ledger row but missed these two §11.3 hand-off sentences written the same day.
- **Failure path:** an implementer consuming §11.3's hand-offs (they are the doc's directed work items to P7/P2) serializes replay evidence or timing comparability into the retired `/3` envelope; P2 v2 §5.1.1 preflight rejects it, or worse, a receiver treats the two sentences as license to keep `/3` support, contradicting P7 D-P7-41's "/4 only, rejecting /3 and earlier".
- **Owner + receivers:** Phase 6 owns the fix (a fix-up session; §11.3 is not §5, so no re-verify trigger is created by this correction itself). Receivers of the corrected text: P7 (collector/comparability), P2 (envelope law), P8 (report route).
- **Minimal resolution:** in both §11.3 sentences, replace `/3` with `/4` (or rephrase to "the current `/4` envelope per D-P6-31/D-P6-38") and mark the D-P2-39-era `/3` wording historical; no other byte of §11.3 needs to change.

### Correction 2 — dependency and authority line anchors are stale against current owner bytes (evidence discipline)

- **Sites (cited → actual, all verified by re-read):**
  - RESEARCH.md: `:533`→`:534` ("frame start"), `:536`→`:537` (snapshot line), `:1166`→`:1167` (centerDepthSmooth directive), `:1318`→`:1319` (App D heading), `:1325`→`:1326` (D.1 table), `:1331`→`:1332` (eyeBrightness), `:1345`→`:1346`, `:1347`→`:1348` (wraps), `:1359`→`:1360` (near/far), `:1365`→`:1366` (centerDepthSmooth row), `:1369`→`:1370` (D.4 heading), `:1379`→`:1380` (refresh-on-switch), `:1382`→`:1383` (custom after built-ins), `:1415`→`:1416` (App E row 16), `:1494`→`:1495` (F.6 declaration grammar), `:1500–:1501`→`:1501–:1505` (F.6 exclusion union). (`:47`, `:100`, `:290`, `:558`, `:1228`, `:1254` are exact.)
  - P1 `docs/phase1/v14/PHASE_1_DOC.md`: `:2920` (readDepthPixel verb) → `:3388` (signature; §5 rows `:5496`/`:5525`); `:4119` ("reuses the values already computed for this sweep") → the binding law now at `:3785`/`:5565`/`:6139` (D-P1-32, wording "uses the values already computed for this sweep"); `:4146` ("recorded-GL run") → `:3019`/`:5516`/`:5756`; `:4220–:4221` (attributed-replay contract) → `:5475` (D-P1-42 row).
  - P3 `docs/phase3/v1/PHASE_3_DOC.md`: `:403` (macro before first restored pack line) → `:1536–1537`; `:918` (`SmoothingConstants.drynessHalfLifeTicks`) → `:1770` (record at `:2750`); `:1112` ("single validated downstream truth") → `:3356`; `:1115` (`DeclaredUniformCatalog`) → `:1435` (grant narrative §0.17 at `:128`).
  - P4 `docs/phase4/v1/PHASE_4_DOC.md`: `:682` ("one immutable compiled binding") → `:1016`; `:1181–:1184` (participants run when the same handle stays active) → `:1813`; `:1203–:1210` (callback scoping/retention, invalidation before every later activation) → `:1825–:1833` and `:1836–1840`; `:1362–:1372` (`afterBind`/`locate`) → `:1646–:1656` (`afterBind` at `:1653`); `:1373–:1375` (merged layout grant) → §5 rows `:2131`/`:1910`; `:1467–:1471` ("The three positions are") → `:1751–:1755`; `:1627–:1631` (invalidate old token before close old registry) → `:1957`/`:1961`; `:1789–:1790` ("calls three participants") → the verbatim quote no longer exists; current text is `:1787–:1788` "invoke sampler, built-in, custom in that order".
  - P5 `docs/phase5/v1/PHASE_5_DOC.md`: `:908–:917` (`ResolvedSamplerBinding`) → `:1021`; `:2116–:2120` ("They share one table/schema") → `:2441`; `:2127–:2148` ("Both gbuffers bands share this map") → `:2478`; `:2150–:2172` ("Invalid retains the complete validation evidence") → `:2499–2502`; `:2360` ("no second map or free-unit allocation", cited twice) → `:2726`.
  - P7 `docs/phase7/v1/PHASE_7_DOC.md`: `:2309–:2320` (R7-10 "binding.samplerLayout and context") → `:4030`; `:2322–:2330` and `:2325–:2327` (R7-11 `retire(...)` / "after final callback and before borrowed services disappear") → `:4040–:4042`.
- **Claim:** every quoted contract text still exists in its owner's current bytes and is semantically faithful — I located and read each one; no consumed interface has changed shape. But roughly thirty citations resolve to unrelated lines (and two quoted strings no longer match verbatim), which fails §G1.2's evidence rule that citations be recomputed against current bytes; the same drift class that D-P6-38 fixed for content is still open for coordinates.
- **Failure path:** a fix-up or implementation session that trusts a cited anchor lands on unrelated text (e.g., P4 `:1789–1790` is now the fixed-function/publish steps), wastes a verification cycle re-deriving the real location, or in the worst case edits against the wrong paragraph believing it cites authority.
- **Owner + receivers:** Phase 6 owns its own citation column (fix-up session; §5's incorporated contracts are untouched, so this is not of itself a §5 change). No receiver action is required; owner docs are mid-churn and each is re-pinned by its own loop.
- **Minimal resolution:** sweep §0.1/§0.6/§2.2/§3/§4.3–§4.11/§4.13/§5.2/§5.3/§11.2 for the anchors listed above and repoint each to the current line (or switch RESEARCH/dependency citations to section-anchored references immune to owner-doc churn); restore the two drifted quotes to their owners' verbatim wording.

## 4. Notes

1. **P7 round reference aging (§0.27, §5.2):** "P7's receiving change requires R38" was written before P7's same-day §0.46/§0.48 (R38 executed with a terminal correction; R39/R55 fix-ups followed). The substantive posture — R37's PASS is historical and fresh whole-document verification is owed — is correct and unchanged; at the next fix-up, refresh the round number to P7's current owed review so the gate names a live requirement.
2. **`instanceId` vs RESEARCH D.4 wording:** RESEARCH.md `:1378` paraphrases `instanceId` as "0 original, 1..N instanced copies", while the adopted law (D-P6-29, R25-2) is N total values `0…N−1` with N=1 a single draw at ID0. The adopted law is internally consistent across §4.4.4/§8.2/§11.3 and matches the sequence owner's per-slot model, but the literal D.4 reading (N+1 values) is nowhere reconciled in §11.2's contradiction list. Record the disposition (D.4 paraphrase vs countInstances total) so §G0.1's no-silent-resolution rule stays airtight; if the literal reading were ever deemed contract, that is a RESEARCH-maintainer question, not a Phase 6 unilateral fix.

## 5. Areas audited and found sound

- **Identity chain of the coordinated amendments:** schema23/`MaterializedSource-v23` receipt current; schema19/20/21/22 receipts correctly demoted to historical with dates; `/4` envelope posture in §0.28/§4.11/§5.1/D-P6-31/D-P6-38 aligned with P2 v2, P7 D-P7-41, P8 D-P8-24 (the two §11.3 stragglers are correction 1); `phase13.sidecar`/`parameters` mentioned only generically, matching P13's current `phase13.parameters/v2`+`phase13.sidecar/v1`; `docs/decisions/U1_TEXTURE_SAMPLING.md` exists as cited.
- **D-P6-33 barrier alignment:** P6's §2.3 flow, §4.10 trace, and §4.12 blend paragraphs match P4's current §4.10 step ordering (close predecessor lease and invalidate token → bind → acquire effective lease → publish/sample effective blend before built-ins → three participants → publish Activated), including failed-acquisition dispatch containment and no predictive `BlendSpec` overlay.
- **Replay evidence machinery (D-P6-27):** eight-argument factory, required non-null P7-owned sink, one `UniformReplayReport` per nonempty triggering drain with per-original-entry cardinality and drain-order preservation, honest false verdicts, cleanup drains retained as all-false reports, delivery before `afterBind`/event return, sink-failure latch (`phase6.replay.delivery.failed`) with non-complete containment — all internally consistent and matching P7 §5.4's receipt, P1's D-P1-42 law, P2 v2 §5.1.2, and P5's §5.5 arity receipt.
- **Actual accepted-frame timing (D-P6-25):** render-thread-confined query, empty-set semantics for stale/reset/adopted/retired states, report copied from the accepted input plus post-update counter cells, allocation only on query, detached records non-authoritative — consistent with P7 D-P7-34's harness-only clock ownership and P6's own "P6 neither reads a replacement clock nor knows the capture plan" (§4.5).
- **Sampler sole-policy consumption (R7-10):** §4.9's eight-step `afterBind` algorithm consumes P5's current `resolve` algebra verbatim (Ready/Invalid, ascending-unit then fixed-name order, shared bands, virtual/fixed-function empty Ready, no fullscreen fallback for unsupported domains — P5 `:2499–2502`), keeps `depthtex1`=11 and no unit-12 gbuffers sampler as owner-policy consequences, and adds no fourth participant, allocator, or texture bind.
- **Retirement algebra (R7-11):** CLOSED reason set, precedence (wrong thread → already-retired → active callback → terminal), non-GL cleanup, permanent capability guards table, reason-specific ordering (unpublished abort / replacement after actual old-barrier invalidation / shutdown before Phase 4 atomic teardown), and P11's map-CLOSE-to-`retire` consumption all cohere across §2.2/§4.14/§5.1/§11.3.
- **centerDepthSmooth decision (D-P6-1):** the four-point contract check (declaration-rewriting macro, fixed-unit exhaustiveness, bit-identity, AGPL prohibition) is accurate against P3's macro placement (`:1536–1537`), App B.3, and RC3's own inconclusive-check default; §5.4's upstream clarification request is properly flagged, not silently applied.
- **Smoothing math and boundaries:** closed-form tick-domain EMA with correct wrap semantics (720720 counter, 3600 seconds), truncation-toward-zero `ivec2` quantization explicitly ruled as a Phase 6 decision bounded by T2, B1 avoided by independently carried half-lives.
- **Held-light, celestial, and atlas receipts:** P9 tuple law, P8 `CelestialMath` signatures and shadow-independence, and the authenticated atlas adapter's evidence/invalidation rules each match the owning docs' current bytes.
- **Threading, failure ladder, testability:** render-thread confinement table complete including the delivery sink; §6 ladder rows map to rungs 1/2/2a/3/4/5 plus explicit 5-guards without inventing policy; §8.1's 30 named tests cover every exported behavior including the retirement, replay-evidence, and capture-timing boundaries; §8.3's gate requires two recorded profiles and owner-gate satisfaction.
- **Milestone staging and checklist:** §9 tags agree with the per-uniform milestone columns; §12's 29 items are ordered, each with one test hook, and correctly gate consumption behind §5.2's owner reviews.

## 6. Verdict

**PASS-WITH-CORRECTIONS** — blocking=0; corrections=2; notes=2.

The document's architecture, contract fidelity, cross-phase interfaces, licensing posture, and current-identity receipts are sound; the two corrections are a same-day §11.3 envelope repair and a mechanical repointing of stale evidence anchors, both executable by a single fix-up session without reopening any §5-incorporated contract. Per §G1.3 the document remains unverified until the already-required fresh whole-document review returns literal PASS with zero blocking findings and zero corrections.

No build, test, gradle, GL, or network execution was performed; no file other than `docs/phase6/reviews/PHASE_6_REVIEW_33.md` was written.

## Resolutions

**Date:** 2026-09-08 (fix-up session for this report's corrections 1–2). Applied to `docs/phase6/v1/PHASE_6_DOC.md` only; the report body above is byte-identical to the reviewed text. Every target anchor was recomputed against the cited document's current bytes and the quoted/claimed content verified to sit there before repointing. No build, test, gradle, GL, or network execution was performed.

- **Correction 1 — applied.** §11.3 no longer publishes the retired `/3` envelope as current. The R39-2 handoff (`:2316–:2318`) now reads "P2 owns the current `/4` fields and comparability per D-P6-31/D-P6-38 (the D-P2-39-era `/3` wording is historical)", and the R25-1/P8 receiving handoff (`:2323–:2325`) now reads "P2 retains all entries in current `/4` gl_errors with every error failing T0 (the D-P2-39-era `/3` envelope is retired history)". No other byte of §11.3 changed. Both sentences now match §0.28, §4.11, §5.1's D-P6-38-repaired row, and P2 v2's `/4`-only law; no re-verify trigger is created (§11.3 is not §5).
- **Correction 2 — applied.** All stale dependency/authority anchors repointed after per-site verification against current owner bytes: RESEARCH.md `:1166`→`:1167` (§0.2 `:95`), `:1369`→`:1370` (§0.6 `:206`, §4.4.4 `:977`), `:1500`–`:1501`→`:1501`–`:1505` (§0.6 `:208`; union verified to span those lines, including "does not override or narrow Appendix D.4"), `:1379`→`:1380` (§2.4 `:606`, §3 `:616`), `:1318`→`:1319` (§3 `:615`), `:1494`→`:1495` and `:1382`→`:1383` (§3 `:621`), `:533`→`:534` (§3 `:625`), `:536`→`:537` (§3 `:627`), `:1415`→`:1416` (§3 `:631`), `:1325`→`:1326` (§4.4.1 `:900`), `:1331`→`:1332` (§4.4.1 `:902`), `:1345`→`:1346` and `:1347`→`:1348` (§4.4.2 `:923`/`:924`), `:1359`→`:1360` (§4.4.3 `:961`), `:1365`→`:1366` (§4.8 `:1151`). P1 `:2920`→`:3388` (§4.6 `:1081`), `:4119`→`:6139` (D-P1-32; `:3785`/`:5565`) with the drifted quote restored to the owner's verbatim "the re-upload uses the values **already computed for this sweep**" (§4.11 `:1366`), `:4146`→`:3019`/`:5516`/`:5756` (§8.1 `:2141`), `:4220`–`:4221`→`:5475` (§3 `:633`). P3 `:403`→`:1536-1537` (§4.8 `:1165`), `:918`→`:1770` (§4.1 `:677`), `:1112`→`:3356` (§4.1 `:667`), `:1115`→`:1435` (§3 `:618`, §5.3 `:1982`). P4 `:682`→`:1016` (§4.3 `:859`), `:1181`–`:1184`→`:1813` (§4.10 `:1328`), `:1203`–`:1210`→`:1825`–`:1833` + `:1835`–`:1842` (§4.10 `:1310`; the token paragraph spans `:1835–:1842`, `isCurrent` at `:1836`, the invalidation-moment list at `:1838–:1840`), `:1362-1372`→`:1646-1656` (§2.2 `:572`, §4.10 `:1277`; `afterBind` at `:1653`), `:1373`→`:1910` and `:1374`–`:1375`→`:2131` (§3 `:618`/`:619`), `:1373`–`:1375`→`:1910`/`:2131` (§5.3 `:1991`), `:1467-1471`→`:1751-1755` (§2.2 `:570`), `:1627-1631`→`:1957`/`:1961` (§4.14 `:1788`), `:1789-1790`→`:1787-1788` with the drifted quote restored to the owner's verbatim "invoke sampler, built-in, custom in that order" (§5.2 `:1916`). P5 `:908-917`→`:1021` (§4.9 `:1210`), `:2116-2120`→`:2441` (§2.2 `:552`), `:2127-2148`→`:2478` (§4.9 `:1223`), `:2150-2172`→`:2499-2502` (§4.9 `:1217`), `:2360`→`:2726` at both sites (§4.9 `:1192`, §5.2 `:1936`; both quoted phrases verified verbatim on P5 `:2726`). P7 `:2309-2320`→`:4030` (§0.23 `:242`), `:2316-2320`→`:4033` (§3 `:624`; recomputed for that site's own quote "No second map", which sits at `:4033`, not `:4030`), `:2325-2327`→`:4042` (§4.14 `:1770`; the quote sits verbatim on `:4042`).
- **Correction 2 deviation, recorded with evidence:** the report's claim that every quoted contract text still exists in its owner's bytes does not hold for the P7 quote "its owner must reconcile that rule, not add a conflicting alias" (§0.24 and §5.2 R7-11 row). A search of the current `docs/phase7/v1/PHASE_7_DOC.md` finds no "reconcile that rule" anywhere; the current R7-11 receipt spans `:4038–:4049` and states at `:4043–:4044` "Phase 6 removed reset(CLOSE); Phase 11's own terminal controller CLOSE is distinct and precedes this retirement after final custom use." Both P6 sites were therefore repointed to the current receipt (`:4038-4044` at §0.24 `:275–:278`; `:4038-4049` at §5.2 `:1953–:1955`), the adoption-time quote is marked historical in place, and the owner's current verbatim clause replaces it. `retire(UniformRetirementReason)` itself remains verbatim at P7 `:4038`.
- **Notes 1 and 2 — not applied** (neither is incorporated by a correction): the "including P7 R38" round reference (§11.3 `:2327` region) and the RESEARCH D.4 `instanceId` disposition remain as recorded.
- **Ledger:** no new D-P6 decision IDs and no §0 addendum — correction 1 enforces already-adopted D-P6-31/D-P6-38 posture and correction 2 is evidence hygiene, so both fall under the no-new-ID rule. Top-level `## 0.`–`## 12.` sequence, code-fence balance (26 fences), and decision-ID uniqueness verified intact post-edit; the document is 2450 lines.
- **§5 impact:** no §5-incorporated contract row changed. §5.1 is untouched. §5.2 changed only in prose citation coordinates and the historical-quote marker (`:1916`, `:1936`, `:1953–:1955`); §5.3 changed only its two citation columns (`:1982`, `:1991`).

# Phase 6 — Fresh Whole-Document Architecture Review R29

## Frozen owner and disposition

- **Owner:** `docs/phase6/v1/PHASE_6_DOC.md`.
- **Frozen SHA256:** `cc75b963b3395d52b9b27c579422e104fe77cda61752835166f6d9f50004c725`.
- **Inventory:** `docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_5.json:35-39` records Phase 6 and review round 29. The checksum is the supplied frozen identity corroborated by that inventory; no separate hash-validation command was run.
- **Scope:** the entire current owner, sections 0–12, including its historical addenda, current incorporated interfaces, failure/lifecycle semantics, staging, testability plan and implementation checklist. This was not a D-P6-35-only check.
- **Result:** zero required corrections and zero finding notes. No required §5 repair was identified. No repository file was edited.

## Authority and independent method

The owner's header selects **`docs/design/v2.0-RC3/DESIGN.md`**, not global newest v3. I read its Part I and complete Phase 6 assignment (`:1689-1801`), with RESEARCH taking precedence. The review checked `docs/research/v1/RESEARCH.md` §§0–1, §3.4, §4.2, §4.4, the relevant declaration trigger, Appendix B.3 and the complete Appendix D inventory; current §3.4/F.6 was also checked for the expression-exclusion ruling. `AGENTS.md` and `docs/MOVES.md` supplied repository and moved-citation restrictions.

Current dependency contracts and their load-bearing incorporated declarations were independently compared across P1, P3, P4 and P5. Receiving dispatch, data copying and final-use ownership were traced in P7 and P11, with focused reciprocal P2/P8/P13/P14 checks where Phase 6's current interface crosses those boundaries. Prior PASS statements in the owner were treated as historical records, not proof of present correctness or sibling certification.

## Independent checks and evidence

### 1. D-P6-35 accepts the actual current producer schema

`docs/phase6/v1/PHASE_6_DOC.md:1849-1865` requires exact `PackFrontEnd.CURRENT_SCHEMA_VERSION`, equal containing/nested-ID/inspection schemas, and schema22/`MaterializedSource-v22` before derivation or reuse. Its later numeric receipts are explicitly historical. This matches `docs/phase3/v1/PHASE_3_DOC.md:4040-4106`: schema22 changes BLOCK alternate provenance while retaining same-load assets, nine source-free trees, projectionVersion1 and existing native/option contracts. P6 does not acquire an ID parser, choose mapping eras, reconstruct assets or upgrade old materialization identities.

The current P1/P4/P5 receiving rows also use schema22 (`docs/phase1/v14/PHASE_1_DOC.md:5147`, `docs/phase4/v1/PHASE_4_DOC.md:2086-2105`, `docs/phase5/v1/PHASE_5_DOC.md:2597-2611`). These comparisons establish contract agreement only, not those owners' review outcomes.

### 2. Candidate initialization and runtime sampler ownership remain separate

P6's required borrowed resolver and existing runtime participant are defined at `docs/phase6/v1/PHASE_6_DOC.md:437-456,543-551,1202-1274`. P5's current resolver supplies complete immutable exact-name/full-shape/unit rows, ordered by unit then fixed-name declaration order; Invalid retains the full validation evidence (`docs/phase5/v1/PHASE_5_DOC.md:947-994,2276-2344`). P6 consumes the effective layout and authenticated stage/band, caches only matching policy/layout inputs, preserves aliases and unit 11, and never binds texture objects or creates another map.

The new candidate path does not require a fourth participant or P6 candidate activation. P5 separately supplies an exact-name-sorted initialization assignment list; P4 freezes it before GL and invokes P1 after link, before validation (`docs/phase5/v1/PHASE_5_DOC.md:2283-2296`; `docs/phase4/v1/PHASE_4_DOC.md:1377-1380`). P1's transaction restores the previous selection, preserves texture/sampler/alpha/blend state and dispatches no P4/P6 callbacks (`docs/phase1/v14/PHASE_1_DOC.md:3395-3414`). Consequently its separate ordering and private locations do not invalidate P6's runtime cache or ownership contract.

### 3. Locks precede observed blend values and participant dispatch

The owner's corrected trace is close predecessor → bind effective shader → acquire effective lease → publish/sample actual effective blend → sampler/built-in/custom (`docs/phase6/v1/PHASE_6_DOC.md:577-587,1311-1324,1470-1530`). It no longer predicts a later lock through a descriptor overlay.

This agrees with P4's actual activation dispatch (`docs/phase4/v1/PHASE_4_DOC.md:1691-1706`) and P1's held-aspect interception, private coherent bypass, rollback and notification law (`docs/phase1/v14/PHASE_1_DOC.md:3449-3477`). P7 explicitly installs and consumes the effective-blend route, requires Accepted, and treats rejection/throw as notification failure (`docs/phase7/v1/PHASE_7_DOC.md:2837-2868`). Suppressed setter operands do not become events. P7 also releases final leases while the old endpoint and frame identity remain live, before P6 retirement; retirement-before-atomic-teardown therefore does not require a post-retirement blend callback.

### 4. Acquisition, clocks and temporal values retain distinct semantics

Every Appendix D row has a type, acquisition cadence, upload policy, provider and milestone in `docs/phase6/v1/PHASE_6_DOC.md:866-977`. The cadence separation and unconditional matrix uploads remain explicit (`:838-864`). The smoothing rules distinguish tick-domain time from rendered seconds, preserve pause semantics, initialize from the first valid target, keep wet/dry half-lives independent and specify brightness quantization (`:979-1046`).

Frame acceptance rotates history and samples before resize/clear; current matrix capture happens later, once after camera setup (`:1048-1102`). The timing query returns actual accepted input plus actual post-update counter cells, not values reconstructed from frameId or capture ordinal (`:1104-1119`). P7's receiving dispatch calls that exact query on the live runtime and checks frame, generation, epoch and controlled-step identity before copying the immutable report (`docs/phase7/v1/PHASE_7_DOC.md:3043-3086`). P2 likewise requires actual owner observations rather than target echoes (`docs/phase2/v2/PHASE_2_DOC.md:2448-2473`). No second P6 clock or gameplay timing override is introduced.

### 5. Replay reports reach a concrete consumer without changing attribution

P6's required eighth factory input, immutable report and borrowed lifetime are closed at `docs/phase6/v1/PHASE_6_DOC.md:437-456,553-562`. Its batching and delivery rules preserve the original triggering errors, exact occurrence order, false cleanup evidence and one verdict per triggering error, without reevaluating providers or counting probes twice (`:1341-1439`). These agree with P1's published `ReplayAwareGLError` law (`docs/phase1/v14/PHASE_1_DOC.md:5136-5137`).

The consuming side is explicit: P7's collector appends/classifies synchronously, retains originals on downstream failure, copies ordered values into `/4`, and checks the delivery-failure diagnostic/latch before drawing or acknowledging capture (`docs/phase7/v1/PHASE_7_DOC.md:2110-2180`). P6's ordinary isolated degradation is distinct from failed evidence delivery. Immediate-event failures also reach the closed-admission recovery path rather than disappearing behind P4's participant exception containment. The current `/4` receipt at P6 §0.28 and §4.11 supersedes earlier `/3` references; it does not grant P6 wire-format ownership.

### 6. The custom schema and typed sink have matching receiver branches

`docs/phase6/v1/PHASE_6_DOC.md:1540-1678` defines the immutable load-time schema, matching runtime view, six upload variants, bool-only validation/encoding, exact-name duplicate rules, normal absence, ordered accepted prefix and authoritative three-way counters. Current RESEARCH explicitly adopts the same seven-name exclusion union (`docs/research/v1/RESEARCH.md:296-302,1494-1506`), so no historical exclusion conflict remains an active requirement.

P11's actual refresh loop branches on Accepted/SkippedAbsent/Rejected, counts each result, preserves submitted-prefix counts and maps all six declared types to the exact P6 command variants (`docs/phase11/v1/PHASE_11_DOC.md:732-778`). Its §5 receives those same schema/boolean/absence contracts (`:1188-1217`). No missing consuming branch, bool/int coercion or invented ivec3 upload grant was identified.

### 7. Retirement preserves final-use ownership and permanent invalidation

The owner distinguishes nonterminal reset/adoption from terminal `retire`, rejects wrong-thread/in-flight retirement without releasing services, permanently guards retained capabilities, and gives different candidate/replacement/shutdown ordering (`docs/phase6/v1/PHASE_6_DOC.md:1680-1785`). P7 consumes those exact reasons/results and retains the observer/services after rejection (`docs/phase7/v1/PHASE_7_DOC.md:3776-3787`). Its candidate protocol adopts the actual accepted generation on the new runtime and retires the old one only after actual barrier invalidation (`:3684-3697`). No CLOSE alias, old-runtime resurrection or accepted-candidate caller-close is needed.

### 8. Later producers and optional modernization remain bounded

P6's P8 receipt uses pure angular data through the P7 mod provider, then the actual associated post-camera snapshot (`docs/phase6/v1/PHASE_6_DOC.md:756-772`). P8's actual API and receiving contract match (`docs/phase8/v1/PHASE_8_DOC.md:473-481,969-975,1668-1689`). Atlas values likewise require authenticated current-bind evidence, not mere stitch availability (`docs/phase6/v1/PHASE_6_DOC.md:1493-1506`; `docs/phase13/v1/PHASE_13_DOC.md:1135-1139`). Entity color remains P7-owned at v0.1; alias IDs and later atlas values retain their designated milestones.

Candidate A remains synchronous CPU center-depth smoothing; Empty macro contribution avoids declaration corruption and an invented sampler unit (`docs/phase6/v1/PHASE_6_DOC.md:1155-1200`). P14's sample-age extension is still only a request, and its PBO tier stays FORCE_OFF until permission lands (`docs/phase14/v1/PHASE_14_DOC.md:1567-1598`). This review grants no optional API or latency relaxation.

## Permitted source evidence and limitations

The historical local `reference-src/pintonium-9c2fcc1` checkout is absent. That absence was not treated as proof that its cited mechanics are unavailable: exact pinned upstream sources were independently readable, including [SystemTimeUniforms](https://raw.githubusercontent.com/Xplodin/Pintonium/9c2fcc1/common-shaders/src/main/java/net/irisshaders/iris/uniforms/SystemTimeUniforms.java), [SmoothedFloat](https://raw.githubusercontent.com/Xplodin/Pintonium/9c2fcc1/common-shaders/src/main/java/net/irisshaders/iris/uniforms/transforms/SmoothedFloat.java), [ProgramSamplers](https://raw.githubusercontent.com/Xplodin/Pintonium/9c2fcc1/common-shaders/src/main/java/net/irisshaders/iris/gl/program/ProgramSamplers.java), [MatrixUniforms](https://raw.githubusercontent.com/Xplodin/Pintonium/9c2fcc1/common-shaders/src/main/java/net/irisshaders/iris/uniforms/MatrixUniforms.java), [the matrix-capture mixin](https://raw.githubusercontent.com/Xplodin/Pintonium/9c2fcc1/forge122/src/shaders/java/org/taumc/celeritas/mixin/shaders/MixinEntityRenderer_Shaders.java), and [PackDirectives](https://raw.githubusercontent.com/Xplodin/Pintonium/9c2fcc1/common-shaders/src/main/java/net/irisshaders/iris/shaderpack/properties/PackDirectives.java). These corroborate mechanics and rejected pitfalls, not runtime or pack-tier correctness. The local shipped uniform specification was read at `reference-src/schlorbium-HD_U_G6_pre1/doc/shaders.txt:123-266`; no decompiled implementation was mined.

The pinned [root LICENSE](https://raw.githubusercontent.com/Xplodin/Pintonium/9c2fcc1/LICENSE) is GPL version 3, consistent with D-P6-34's qualification. The historical LGPL label is not a per-file reuse grant. No implementation was copied or licensed by this review.

No forbidden chatlog, root transcript, Oculus blocked subtree, glsl-transformer implementation or unresolved transformation dependency was read. No build, test, lint, formatter, runtime, native-GL or pack-tier validation was performed. Planned §8 tests and §12 work are architecture requirements, not executed evidence.

## Corrections, notes and final verdict

- **Required corrections:** none.
- **Finding notes:** none.
- **Structural blockers requiring owner rebuild:** none identified.
- **§5 impact:** no required interface or semantic correction.

**PASS**

Disposition: R29 passes the supplied frozen Phase 6 architecture as a whole. This verdict does not certify sibling documents, grant optional APIs, establish implementation/native/pack-tier correctness, or close final integration. Integration and any mutation of the frozen owner set remain Main's separate responsibility.

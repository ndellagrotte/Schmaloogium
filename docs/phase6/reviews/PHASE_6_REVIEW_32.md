# Phase 6 — architecture review 32

**Date:** 2026-09-08. **Frozen document:** `docs/phase6/v1/PHASE_6_DOC.md` SHA-256 `0ebee7f40a2ab1e55d6bce5910e0f1f1f03395a10f8e7ebac7bb7f598e5bbe9a`. **Verdict:** PASS-WITH-CORRECTIONS (1 corrections, 3 notes). **§5 impact:** Current §5 grants are content-sufficient: every consumed dependency surface (P1 upload/readback/replay law, P3 schema23/MaterializedSource-v23, P4 layout/access/token/barrier order, P5 sole resolver, P7 eight-argument factory + replay observer + frameTiming + adapters, P8 pure CelestialMath, P11 bridge/SkippedAbsent, P13 atlasSize association, P2 /4 evidence/timing fields) exists in current dependency bytes with matching semantics and explicit unverified-status gates. One binding §5.1 row (line 1825) still names the retired P2 /3 envelope and must be synchronized with §4.11/D-P6-31 (/4); implementation consumption additionally remains gated on the recorded fresh P3/P4/P5/P7 whole-document verifications.

---

# Phase 6 Architecture Review — Attempt 8 (review32)

## 1. Frozen input and governing authority

1. Reviewed artifact: `docs/phase6/v1/PHASE_6_DOC.md`, verified SHA256 `0ebee7f40a2ab1e55d6bce5910e0f1f1f03395a10f8e7ebac7bb7f598e5bbe9a` (matches the attempt8 freeze in `docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_8.json`; that file records only the infrastructure failure of all fourteen workers, so no prior attempt8 report exists to supersede).
2. Governing design confirmed from the §0 header: `docs/design/v2.0-RC3/DESIGN.md` — consistent with the attempt8 contract's P3–P9 mapping. Document revision is v1, maintained through §0.29; declared dependencies Phases 1/3/4 plus Phase 5's pure resolver under R7-10. The header's §G1.3 status correctly states the current bytes are **not verified**; the last literal PASS (R31, attempt7) covered earlier bytes and is historical evidence only.
3. Read-only review: the complete document (§§0–12, 2444 lines), including verbatim re-reads of the full §5.1 table. No edits, builds, tests, formatters, or validation commands were run; no chatlogs or prohibited sources were read.

## 2. Scope of audit

1. Whole uniform/sampler/provider lifecycle: construction (§2.2 eight-argument factory, required resolver/observer inputs), frame-begin ordering and temporal snapshots (§4.6), cadence/upload-cache (§§4.3–4.4), smoothing (§4.5), matrices (§4.7), center-depth decision (§4.8), shared sampler resolver participant (§4.9), Phase 4 barrier and caches (§4.10), replay batching/isolation/delivery (§4.11), notifier-to-producer audit (§4.12), custom-uniform boundary (§4.13), reset/adoption/terminal retirement (§4.14), threading (§7), tests (§8), staging (§9), decisions/gaps/handoffs (§11), checklist (§12).
2. Current dependency §5 contracts audited against the frozen current identities: P1 v14 (`GLDevice`, `UniformService`, `FramebufferService.readDepthPixel`, `drainErrors`, `ReplayAwareGLError(GLError, attributed)` — all present and sufficient); P3 v1 (schema23 / `MaterializedSource-v23` / projectionVersion1 current, matching D-P6-36; `SmoothingConstants`, `DeclaredUniformCatalog`, `CustomExpressionDecl`, reserved `phase6.centerDepthSmoothRedirect` confirmed); P4 v1 (participant signature, `ProgramUniformCacheKey`, `BoundProgramUniformAccess`/token, `BarrierContext.stage()/band()`, sampler/built-in/custom order, pre-participant lease acquisition and notification-failure no-dispatch at P4 §4.10 steps 4–7 and its §6 row — matches D-P6-33); P5 v1 (sole `FixedSamplerPolicies.resolver()`, exact `resolve(ProgramSamplerLayout, StageId, StageBand)` algebra, watershadow-conditional unit 5/else 4, depthtex1=11, no gbuffers/shadow unit 12, Ready row ordering, Invalid evidence); P7 v1 (R7-10/R7-11 exact factory and retirement text, D-P7-38 replay collector matching §4.11 verbatim in substance, `frameTiming` consumption, atlas adapter, D-P7-72 H-SKY-02, prepared-submission instance loop); P8 v1 (`CelestialMath.angles`/`.sample` signatures and rejection rules, no-instance/no-Ready-plan independence, pre-camera `shadowAngle`, same-association reuse, real-shadow preactivation delivery); P11 v1 (§§5.3–5.4 consumption of the bridge, `SkippedAbsent` normality, `Bool1` encoding ownership, D-P11-27 converted-memo/sole-P6-upload split); P13 v1 (§4.4 observer/issuer association and §4.5.2 three-argument lease with INVALID_BASE_BINDING/STALE_BASE_BINDING — the lease path is P5/P13-owned and correctly not routed through P6); P2 v2 (`/4` run-manifest/capture-plan current, `/1`–`/3` unsupported history, D-P2-50 copies all eight `UniformFrameTiming` fields).
3. Current integration Resolutions in `docs/PHASE_INTEGRATION_REVIEW.md` (attempt7 wave) consulted; the previous numbered Phase 6 reviews were treated as immutable historical evidence, not certification.

## 3. Scrutinized recent changes — verified sound

1. **D-P6-37 (shadow-independent main celestial vectors).** §4.2's receipt matches P8's current public math exactly: `CelestialMath.angles(float sunAngle) -> ShadowCelestialAngles` before camera capture for the frame `shadowAngle`, and `CelestialMath.sample(ShadowFrameView, CameraSnapshot, float sunPathRotationDegrees) -> CelestialSample` at P7's H-SKY-02 for the main vectors, with no Ready plan/target/extent/publication/hook-health prerequisite, same-association exact-sample reuse, and real shadow still delivering before activation (P8 §§2.2/4.3/4.5.1; P7 §0.49/D-P7-72/§5.1). P6 adds no provider/frame/celestial field; `CelestialSample` and every event payload in §2.2/§4.2 are unchanged; P6's world/nonregressing-frame/finite admission, immediate-if-active upload, and every-switch semantics are preserved; §8.1 (`CelestialPolicyProviderBoundary`, `CelestialWithoutShadowDemand`), §8.2 v0.2 scenes, §12 item 23, §5.1 receipt row, and D-P6-37 in §11.1 are synchronized.
2. **P11 converted uniform memo / sole P6 uploads / SkippedAbsent.** P6 §4.13's six-variant command grammar, definition-order submission, Accepted/SkippedAbsent/Rejected dispositions with three authoritative counters, bool 0/1 encoding owned by P6, counter-contract whole-batch discard, and accepted-prefix commit match P11's §§5.3–5.4 consumption and D-P11-27's "only UNIFORM designates upload … P6 remains sole upload owner"; variables memoized by P11 never reach the sink, consistent with P6's counting and `NoCustoms`.
3. **Current P13 atlas association.** D-P6-20's adapter (opaque evidence minted only after actual vanilla or P5-Bound bind/restoration; issuer/thread/epoch/serial authentication; Known → `updateAtlasSize(Int2(w,h))`; Unknown/NonAtlas/Unavailable/reset → `Int2(0,0)`; stale never mutates; quiescent live-runtime reset before retirement; retired sink never called) matches P13 §4.4/D-P13-22/D-P13-43 and P7's current adapter text. The three-argument lease with INVALID_BASE_BINDING/STALE_BASE_BINDING is the P5/P13 texture-overlay lease route and correctly does not add a P6 channel.
4. **Whole lifecycle.** Construction/failure semantics, generation adoption vs direct WORLD_EPOCH reset, the terminal retirement precedence chain (wrong-thread → already-retired → active-callback → retired), the retired-capability guard table, borrowed-service retention through final use, replay failed-delivery containment (`phase6.replay.delivery.failed`, non-COMPLETE capture), `frameTiming` current-frame-only semantics, and the P4 barrier choreography (invalidate predecessor → close prior lease → bind → acquire lease + effective blend → sampler → built-in → custom) were each traced end-to-end across P6/P4/P7 bytes with no silent drop, no-op, or discard at any consumer dispatch point.

## 4. Findings

### Correction C-1 (P2) — §5.1 replay-evidence row still names the retired `/3` wire envelope

1. **Location:** `docs/phase6/v1/PHASE_6_DOC.md:1825` (§5.1 row `UniformReplayErrorSink` / `UniformReplayReport`, final clause "P2 serializes existing /3 fields").
2. **Failure path:** §5.1 declares itself binding (lines 1828–1831: every consumer-visible semantic change to the incorporated §4.11 declarations "must update the corresponding §5 row in the same document revision"). D-P6-31 (§0.28, line 333) received P2's current `/4` capture envelope for the unchanged timing and replay values, and §4.11 (line 1416) was duly updated ("P2 flattens the values into current `/4` `gl_errors`"). The §5.1 row was not: it still instructs `/3`. A P7/P2 integration consumer reading the binding row implements or validates replay-evidence serialization against the retired `/3` run-manifest schema, which P2's current contract rejects outright ("`/1`–`/3` unsupported history", no compatibility reader), yielding a non-conformant receiver and a self-contradictory §5. P7's D-P7-38 independently states P2 receives flattened errors "into unchanged `/4` `gl_errors`", so the weight of current contracts contradicts the row. Unlike the dated §11.3 handoff paragraphs, this row is undated current interface text and is not covered by §0.28's "earlier dated `/3` handoffs" reconciliation.
3. **Minimal resolution (owner P6; no receiver change needed):** in the §5.1 row at line 1825, replace "P2 serializes existing /3 fields" with the §4.11-conformant wording, e.g. "P2 flattens the values into current `/4` `gl_errors`".

## 5. Optional notes (no action required)

1. **N1 — dated `/3` handoff wording (§11.3:2312, 2318–2319).** The R39-2 and R25-1 handoff paragraphs still say "/3 fields" / "/3 gl_errors". §0.28's blanket sentence already characterizes them as the prior envelope, so this is internally reconciled; an inline "(historical `/3`; superseded by D-P6-31)" annotation would remove any residual reader ambiguity.
2. **N2 — D-P6-20 evidence enumeration (§4.12:1489).** The private-field list (`Optional<AtlasId>`, `PipelineVersion`, `resourceReloadEpoch`, `bindSerial`) predates P13's D-P13-43 extension (observed opaque base handle; Atlas/NonAtlas/Unavailable association). The evidence type is P7-owned and opaque, P6's observable mapping already covers NonAtlas/Unavailable → `(0,0)`, and authentication is explicitly deferred to P7, so no P6 semantics depend on the enumeration; optionally synchronize it with the current P13/P7 description.
3. **N3 — non-§5 citation-anchor drift.** Body citations to sibling docs have drifted under maintenance while their quoted content remains present (e.g. §2.2:568 cites `PHASE_4_DOC.md:1467-1471` for "The three positions are", now at P4:1751). §5-level citations were checked for substance, not anchors; a periodic anchor refresh would reduce lookup friction.

## 6. §5 grant sufficiency

Current §5 grants are **sufficient in content** for the architecture as documented: every consumed surface (P1 upload/readback/replay law, P3 schema23 catalog/configuration, P4 layout/access/token/barrier order, P5 sole resolver, P7 factory/observer/timing/adapters, P8 pure celestial math, P11 bridge, P13 atlas association, P2 `/4` evidence/timing fields) exists in the current dependency bytes with matching semantics, and every producing-side declaration has a located consuming-side dispatch. They are **not verification-sufficient for implementation**, exactly as §5.2 itself records: the coordinated P4/P5/P7 bytes (and P3's post-R56 dependency changes) still await fresh whole-document PASS, and Phase 6's own changed §5 requires this fresh loop. One binding §5.1 row (C-1) must be corrected before the §5 text is internally consistent.

## 7. Limitations

1. This is a documentary/model audit: no native, runtime, hook-application, GL, G6-parity, conformance-tier, or implementation evidence exists in the repository and none was demanded — the document honestly defers all of it, and deferral is not a defect under this review's rules.
2. Unexecuted evidence limits: P7's installed-hook viability, real capture execution, T2 behavior comparisons, and the pending non-blocking DESIGN Candidate-B clarification (§5.4) remain future work owned elsewhere; the RESEARCH expression-exclusion discrepancy is confirmed resolved in current RESEARCH §3.4/F.6 (seven-name union, "does not override or narrow Appendix D.4").
3. Line anchors inside sibling phase documents reflect their current bytes at review time; sibling maintenance after this freeze will drift them again.

## 8. Verdict

**PASS-WITH-CORRECTIONS.** The Phase 6 architecture on the frozen attempt8 bytes is coherent, boundary-complete, and consistent with all current dependency contracts across the celestial, sampler, custom-uniform, atlas, replay, timing, and retirement boundaries, with honest unverified-status bookkeeping. One necessary correction remains: synchronize the §5.1 `UniformReplayErrorSink` row (line 1825) with the current `/4` P2 envelope per §4.11/D-P6-31. Once that single owning-side edit lands (Main coordinates), no required correction remains on the architecture itself; verification sufficiency for implementation continues to be governed by §5.2's recorded fresh-review gates, not by this report.

## Resolutions

- **C-1 — resolved (D-P6-38).** `docs/phase6/v1/PHASE_6_DOC.md` §5.1 `UniformReplayErrorSink`/`UniformReplayReport` row (frozen line 1825): the final clause now reads "P2 flattens the values into current `/4` `gl_errors`", matching §4.11's current wording and D-P6-31; the retired `/3` serialization instruction is gone. Decision row D-P6-38 recorded in §11.1. No other §5 row, §4.11 declaration, schema or receiver changed — textual §5-ledger repair only.

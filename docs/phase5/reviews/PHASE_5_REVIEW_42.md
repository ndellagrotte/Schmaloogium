# Phase 5 R42 — Attempt-5 independent whole-document architecture review

## Frozen owner and disposition

- **Owner:** `docs/phase5/v1/PHASE_5_DOC.md`.
- **Frozen owner SHA256:** `422f4c0681944abc386f08cd07e14a9f68b65565f12cb13c01b25fbb7f886dbb`, as recorded in `docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_5.json`.
- **Scope:** the complete current Phase 5 document, §§0–12, including declarations, implementation plan, binding §5 contracts, failure/lifetime rules, acceptance criteria, milestones, decisions, historical qualifications and work checklist. The focus on D-P5-38–41 did not replace whole-document review.
- **Required owner corrections:** 0. **Cross-owner notes:** 1. **Owner §5 changes required by this review:** no.

## Authority and evidence

The selected governing design is `docs/design/v2.0-RC3/DESIGN.md`, not the globally newer design. I checked its Part I constraints and Phase 5 assignment, governing `docs/research/v1/RESEARCH.md` requirements including the fixed-unit/format tables and framebuffer/world-order behavior, and `docs/reference/pintonium/v1.0/PINTONIUM_DESIGN.md` §5 and relevant bug/divergence entries. `docs/MOVES.md` was used to distinguish relocated material and retired historical citations from current authority. The explicit, dated option-only supersampling decision was treated as a narrow authorized correction, not permission to rewrite historical research.

Dependency and reciprocal checks covered the current published contracts in `docs/phase1/v14/PHASE_1_DOC.md`, `docs/phase3/v1/PHASE_3_DOC.md`, `docs/phase4/v1/PHASE_4_DOC.md`, `docs/phase6/v1/PHASE_6_DOC.md`, `docs/phase13/v1/PHASE_13_DOC.md`, and load-bearing receiver paths in Phases 2, 7, 8 and 14. Prior reviews and historical PASS language were not used as proof of the present owner.

Permitted implementation corroboration included the exact Pintonium `9c2fcc1` revision's targets directory, shadow targets, depth-copy strategy and framebuffer mixin, retrieved from the upstream repository because the historical local checkout is absent. The pinned shadow accessor and flip implementation actually select alternate textures; the owner's qualification of the historical B4 description is therefore justified. The pinned root LICENSE identifies GPLv3, consistent with the owner's corrected provenance boundary. Representative pinned sources: [targets](https://github.com/Xplodin/Pintonium/tree/9c2fcc1/common-shaders/src/main/java/net/irisshaders/iris/targets), [shadow targets](https://github.com/Xplodin/Pintonium/blob/9c2fcc1/common-shaders/src/main/java/net/irisshaders/iris/shadows/ShadowRenderTargets.java), [framebuffer mixin](https://github.com/Xplodin/Pintonium/blob/9c2fcc1/forge122/src/shaders/java/org/taumc/celeritas/mixin/shaders/MixinFramebuffer_Shaders.java), and [license](https://github.com/Xplodin/Pintonium/blob/9c2fcc1/LICENSE). The available different local checkout was not treated as the historical pin. OptiFine evidence remained limited to shipped author documentation and the permitted behavioral digest. The Minecraft mapping tool independently corroborated `Framebuffer.createFramebuffer(II)V`, `deleteFramebuffer()V`, `framebufferClear()V`, `useDepth` and their 1.12.2 SRG identities.

## Independent contract checks

1. **Planning, allocation and evidence remain different stages.** The opaque candidate/publication ownership rules at `docs/phase5/v1/PHASE_5_DOC.md:698–735` and projection mapping at `:1265–1306` preserve equal-input PLANNED equality separately from successful candidate/accepted-estate REALIZED equality. Allocation fallback preserves each requested format while recording every realized allocation's actual origin. The current receiver explicitly enforces these distinctions at `docs/phase2/v2/PHASE_2_DOC.md:1302–1340`, `:1957–1971` and `:2130–2145`; a plan cannot become live allocation evidence and frame fog is not fabricated.

2. **Real buffer mechanics are specified rather than delegated to reference bugs.** The owner retains scan-driven inventory, the closed pack-facing format vocabulary, distinct unsized default versus explicit RGBA8, physical main/alternate ownership, real flip transitions, positional draw-route holes, both-side clear rules and forced-clear consumption. Relevant current sections are `docs/phase5/v1/PHASE_5_DOC.md:1233–1260`, `:1490–1615` and `:1638–1690`. A successful clear is not inferred from a partial batch, and a metadata rebase is not an invented copy-back operation.

3. **Main depth, copies and attachment identity have explicit lifetimes.** `docs/phase5/v1/PHASE_5_DOC.md:1735–1934` distinguishes safe-point preparation, same-extent reattachment and resize-required replacement; borrowed depth is not converted into owned texture authority. Depth copies consume weather then translucent points, and a failed copy does not publish its destination as fresh. The order is supported by `docs/research/v1/RESEARCH.md:535–564`. Stale attachment identities invalidate snapshots instead of reviving old physical bindings.

4. **Hidden snapshot failure is represented and received.** `PassSnapshotResult.Failed(...,true)` is terminal for the open frame, not a mutation-free rejection. Owner rules at `docs/phase5/v1/PHASE_5_DOC.md:903–934` and the §5 publication at `:2523–2527` are received by ordinary, nested and fullscreen acquisition/cleanup paths in `docs/phase7/v1/PHASE_7_DOC.md:1010–1075`, `:1110–1270` and `:3566–3570`. The caller suppresses further P5 terminal operations while independently owned leases, bindings and local scopes still close.

5. **D-P5-38 closes compile-time sampler assignment ownership.** `docs/phase5/v1/PHASE_5_DOC.md:2274–2290` makes the appB3 policy/resolver the sole source of immutable exact-name/unit candidate assignments and requires agreement across validated bands before GL. The consuming compile path validates/freezes those assignments before allocation and initializes them after link but before validation at `docs/phase4/v1/PHASE_4_DOC.md:1331–1341` and `:1376–1380`. `docs/phase1/v14/PHASE_1_DOC.md:3396–3415` supplies the private candidate initialization/restoration contract. Optimized-out declarations and aliases are not silently dropped, and this does not add a runtime participant or expose native program names.

6. **D-P5-39 preserves the preflight/mutation boundary.** The complete first-failure order and sixteen-row resolution at `docs/phase5/v1/PHASE_5_DOC.md:2400–2445` precede `prepareUnitBindings(mask)`. Rejected/Degraded perform no GL work or lease transfer; normalization occurs once only after successful preflight, followed by ascending BoundObject binds. Late failures are BackendFailed rather than falsely mutation-free results. P1 receives the same mask behavior at `docs/phase1/v14/PHASE_1_DOC.md:3417–3428`; P14's implementation contract clears Unused native sampler bindings at `docs/phase14/v1/PHASE_14_DOC.md:662–670`.

7. **Physical binding and closure authority cross receivers intact.** Selection authentication, overlay publication identity, full sampled-shape compatibility, greatest compatible custom ordinal and alias conflicts remain explicit at `docs/phase5/v1/PHASE_5_DOC.md:2338–2390`. Bound alone transfers the lease; completion, invalidation and off publication do not discharge closure at `:2448–2472`. The actual shadow dispatch branches on Bound, Degraded, Rejected and BackendFailed at `docs/phase8/v1/PHASE_8_DOC.md:864–897`, then independently cleans up at `:910–943`; this is not merely a receiver-table citation.

8. **Shadow failure containment retains terminal distinctions.** The owner retains bounded coherent neutral resources and distinguishes per-buffer mipmap degradation with successful filter restoration from result-level neutralization that has already aborted the snapshot (`docs/phase5/v1/PHASE_5_DOC.md:1940–2134`). The receiver explicitly stops without duplicate completion/abort/neutralization on that terminal result at `docs/phase8/v1/PHASE_8_DOC.md:902–910`. Neutral fallback is not permission to reuse a prior map or stale lease.

9. **D-P5-40 admits exactly current schema22.** The current §5 receipt at `docs/phase5/v1/PHASE_5_DOC.md:2602–2621` requires containing/nested/inspection version equality before derivation, retention, reuse and evidence, receives MaterializedSource-v22 opaquely, and explicitly makes older numeric/hash-domain receipts historical. It does not add parser, source-reopening or binary asset authority. This matches the current Phase 3 §5 publication rather than superseded schema21 prose.

10. **D-P5-41 preserves owner-authored parameters through runtime demotion.** `docs/phase5/v1/PHASE_5_DOC.md:2585–2586` receives complete authenticated owner-parameter replay before NONE admission, otherwise stale/off/rebuild containment. Its complete owner conversion at `:2660–2687` agrees with `docs/phase1/v14/PHASE_1_DOC.md:4274–4360` and the runtime demotion mechanism at `docs/phase14/v1/PHASE_14_DOC.md:706–745`. Borrowed textures gain no mutation authority; failed replay does not become successful retirement or resize. Optional optimization APIs, including the ungranted batch path, remain ungranted.

## Required corrections

None. No substantive current Phase-5-owned contract defect was established by this review.

## Notes

### N1 — Cross-owner receiver accessor mismatch; no Phase 5 API expansion

**Owner attribution:** Phase 8, not Phase 5. **Severity:** P2 receiver integration defect; non-blocking for this Phase 5 verdict. `docs/phase5/v1/PHASE_5_DOC.md:658–661` publishes `PublishedBufferEstate(generation, Optional<BufferEstateView> estate, resources)`, while `shadow()` belongs to `BufferEstateView` at `:764`. The current invocation in `docs/phase8/v1/PHASE_8_DOC.md:820–825` instead names `PublishedBufferEstate.shadow()`. Implementing that receiver literally would call an unpublished method and bypass the optional/off estate boundary. **Minimal receiver fix:** unwrap and authenticate the current accepted estate through `estate()`, handle absence/off without drawing, then call the accepted `BufferEstateView.shadow()`; synchronize the Phase 8 construction/invocation/§5 descriptions. Do not add a convenience method or widen Phase 5 to accommodate the receiver typo. This was communicated to the Phase 8 reviewer, who independently confirmed the attribution. It requires no owner correction or Phase 5 §5 change here and is not a certification of Phase 8.

## Limitations and final disposition

This is an architecture review, not implementation, runtime, GL-profile, conformance-pack, performance, or final integration certification. No repository mutation, build, test, formatter, linter or runtime execution was performed. Planned acceptance cases were assessed as architectural obligations, not claimed as executed evidence. Forbidden implementation directories and transcripts were not consulted. Missing historical local artifacts were distinguished from the exact pinned permitted sources actually retrieved; no historical verification result was recreated or inherited.

**Final literal verdict: PASS.** Phase 5's frozen current architecture has zero required owner corrections. The single note is explicitly receiver-owned. Final integration and each sibling's independent disposition remain separate.

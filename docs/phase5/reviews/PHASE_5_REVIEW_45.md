# Phase 5 — architecture review 45

**Date:** 2026-09-08. **Frozen document:** `docs/phase5/v1/PHASE_5_DOC.md` SHA-256 `84ead9d936e7fb34cea13e3d895b662d99cff9edf99f42149fa61fbbeb844aa0`. **Verdict:** PASS (0 corrections, 2 notes). **§5 impact:** None — no §5 change required. Current §5 grants are sufficient and mutually consistent: P1 D-P1-63/64/66/67/69 typed values/baseline/maxima/clear/discard, P3 schema23 + MaterializedSource-v23 + projectionVersion1, P4 profile-selection-v3 + D-P4-40 prelude + validateSelection/initializationAssignments, P13 three-argument lease with INVALID_BASE_BINDING/STALE_BASE_BINDING + phase13.parameters/v2 + sidecar/v1 + TextureHookHealth/application-v2, P8 flattened-v3 shadow health, P6 R7-10/R25-1 factory, P14 D-P14-31 baseline (R-P14→P5-2 correctly ungranted), and P7/P8 D-P5-50 refresh receivers all exist in current dependency bytes and match P5's receipts. Several grants remain adopted/unverified on their producer side, gating implementation and downstream verified consumption, not this review.

---

# Phase 5 Review 45 (attempt 8) — Independent whole-owner architecture review

## 1. Frozen identity and scope

1. Reviewed `docs/phase5/v1/PHASE_5_DOC.md` in full (3,653 lines, including all previously elided/truncated ranges). Computed SHA-256 `84ead9d936e7fb34cea13e3d895b662d99cff9edf99f42149fa61fbbeb844aa0` — exactly the frozen attempt-8 identity with `reviewRound: 45` in `docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_8.json`.
2. Governing design confirmed from the actual header (`PHASE_5_DOC.md:18-20`): `docs/design/v2.0-RC3/DESIGN.md`, assignment `:1572`–`:1685`. This matches the shared authority band (P3–P9 → RC3); no v3 override is adopted. The RC3 doc gate is met: App B.3 reproduced exactly with the unit-11 ruling (§3.3, §4.12.1, D-P5-9); clear/flip specified as testable state machines (§4.4, §8.1); every App B.1/B.2/B.4 row mapped (§3.1/§3.2/§3.4); frame-end decision D-P5-4 recorded with provenance; fog-alpha-1.0 verbatim (§4.6); depthtex0 swap traced to PD §5.2 (§4.8); shadow flip real with the B4 attribution qualified (§4.10, D-P5-8/35).
3. Read-only review: no file edited, no build/test/linter/formatter/validation command run, no implementation claimed. Historical reviews/addenda were treated as immutable evidence only; R44's correction and its D-P5-49 resolution were checked against current bytes, not taken as certification.

## 2. D-P5-49 scrutiny (attempt-7 correction receipt)

1. **Three preserved fallback triggers.** §4.7 (`:1856-1861`) lists exactly: facade allocation error attributed to a candidate color texture; non-complete pass/clear FBO status; internal-format capability rejection — one whole-estate `RGBA_COMPAT` retry, estate-wide, depth formats unchanged, warning, then shaders-off. §4.6 (`:1787-1791`) now *preserves* all three triggers ("Existing whole-estate RGBA fallback preserves all §4.7 triggers…"), exactly the minimal fix R44 C44-1 prescribed; the old completeness-exclusive clause is gone.
2. **Missing integer-clear tier stays a preallocation rejection.** §4.6 (`:1786-1787`, `:1789-1791`) keeps it "a separate preallocation hard rejection, never hidden by that single retry", with realized-format conversion recomputed after fallback. This matches P1's grant: `PHASE_1_DOC.md:4646-4647` ("Integer color formats additionally require D-P1-67's GL3 or EXT_integer clear tier; this is capability rejection, not the incomplete-framebuffer RGBA fallback") and §8.1 (`:3117-3120`, "absent integer-clear tier permits zero allocations/retries"; §8.2 "Unsupported integer tier rejects preallocation without RGBA downgrade"). The two capability notions (clear-tier capability vs internal-format capability) are kept distinct and non-contradictory.
3. **Discard receipt.** §4.6 (`:1824-1827`) incorporates P1 D-P1-69 exactly: private save of actual rasterizer-discard, disable before clearing, independent finally-restore on GL>=3.0 or EXT/NV_transform_feedback, no query/change on unsupported contexts, and disable/clear/restore failure = BACKEND_FAILED that never consumes the full-clear requirement. P1's D-P1-69 text (`PHASE_1_DOC.md:4039-4042`) agrees clause-for-clause; §8.1 cases and checklist item 39 stage it.

## 3. D-P5-50 scrutiny (attempt-7 receiver receipt)

1. **Exact accepted winning unit0 association.** §4.12.2 step 4 (`:2529-2541`): resolve the effective winning unit0 object under the same full-shape/custom rules first (declaration-order independent); unit0-undeclared uses the lease's observed `baseTexture`/`baseAtlasContext`, otherwise `overlay.atlasContext(winningUnit0Handle)`; only a compatible Companion whose atlas equals that context and kind equals the name may win; custom precedence is preserved ahead of the whole fallback branch. This matches P13's current bytes exactly: `TextureOverlayLease` accessors and `BaseAtlasContext` semantics (`PHASE_13_DOC.md:562-568,1319-1321,1739-1740`, invalid handle → Unavailable), and D-P13-43's producer rule (`:817-820,2345`).
2. **Explicit kind defaults.** `CandidateOrigin.DefaultFill(CompanionKind)` maps to `BindingOriginKind.NEUTRAL` retaining P13 source/parameter identity (§2.4); disabled kinds retain NOT_CONFIGURED with no allocated default; NonAtlas/Unavailable or no matching companion selects only the kind's compatible DefaultFill; missing/incompatible required default degrades with SUPPRESS_DRAW (`:2537-2539`). Closed and unambiguous.
3. **Base-serial currentness and before-draw refresh without activation replay or state resurrection.** §4.12.3 step 6 folds the D-P13-43 base-observation check into lease currentness (changed serial → `CLOSED_OVERLAY_LEASE` before mutation); §4.12.4 (`:2630-2641`) defines refresh as: close only the binding, acquire a fresh context-bound lease, rerun the same binder on the still-open main/shadow pass with the retained selector — no flip, snapshot completion, provider reselection, root-shadow activation or new native binder; binding close is ownership-only and never restores over the triggering base; the P5-owned transaction batches observer delivery and commits evidence/lease serial plus atlasSize only after all binds succeed. Receivers are present and reciprocal: P7 D-P7-73 (`PHASE_7_DOC.md:1190-1195,3563-3572`) and P8 D-P8-39 (`PHASE_8_DOC.md:1556+,2548`). §8.5 D-P5-50 cases (`:3273-3280`) and checklist item 40 stage all branches.

## 4. Cross-boundary §5 audit (consumed contracts)

1. **P1** (dependency): D-P1-63 §4.7.7a target-bearing values, D-P1-66 captured target maxima (`max.3DTextureSize`/`max.rectangleTextureSize` mandatory keys), D-P1-67/§4.7.4b `clearColorAttachment(f,drawBufferIndex,ColorClearValue)` with Floating/Signed/Unsigned and recorder events, D-P1-69 discard bracketing, D-P1-64 complete object baseline, D-P1-65 schema23, D-P1-60 `prepareUnitBindings(mask)` after complete preflight, D-P1-59 candidate-only sampler initialization, D-P1-71 materialization/deletion — all present in current P1 §4/§5 and consumed faithfully by P5 §§4.2/4.6/5.2.
2. **P3** (dependency): current identity is `CURRENT_SCHEMA_VERSION=23` + `MaterializedSource-v23` + `projectionVersion=1` (`PHASE_3_DOC.md:703-704,3302,4154-4206`); P5's D-P5-44 receipt matches; D-P5-40/29/28/schema19 receipts are explicitly historical.
3. **P4** (dependency): `RegistryFingerprint/profile-selection-v3` (D-P4-43, `PHASE_4_DOC.md:1937-1941`) received by D-P5-48 with positional-route-v2 marked historical; D-P4-40 `SparseArray(...,Optional<ProgramSlotId> virtualPrelude)` received by D-P5-45; `validateSelection` reason set and the documented P4→P5 rejection mapping (INVALID_ISSUER→INVALID_PROGRAM_SELECTION, STALE_GENERATION→STALE_REGISTRY_GENERATION, STALE_CONTEXT/WRONG_STAGE_BAND→PROGRAM_SELECTION_MISMATCH, PROVIDER_LAYOUT_MISMATCH→SAMPLER_LAYOUT_MISMATCH) match P4 `:1794-1802`; `initializationAssignments` consumed by D-P5-38 matches P4 D-P4-36; detached `CompiledRegistryCandidate.view()` grant intact.
4. **P13**: the lease now requires the third opaque `AtlasBindingEvidence` argument with rejection domain PUBLICATION_UNAVAILABLE / PUBLICATION_ID_MISMATCH / REGISTRY_FINGERPRINT_MISMATCH / STALE_SELECTION / INVALID_BASE_BINDING / STALE_BASE_BINDING (`PHASE_13_DOC.md:350-353,366-368,1693-1694`) — P5 §5.3.1 documents exactly this signature and domain; `phase13.parameters/v2` with `phase13.sidecar/v1` and `TextureHookHealth/application-v2` (eight rows) are current P13 identities and P5's receipts agree.
5. **P8**: `ShadowHookHealth/flattened-v3` (66 rows, 65 non-CLOUD, 37 RESTORE per D-P8-37) is not contradicted anywhere in P5; R7-12 `ShadowInvocationContext` field list and R7-13 `ShadowPlanInput(policy,hookHealth,requested)` + `create(plan,registry,...)` in P5 §5.3.1 match P8 `:392-418,1773,1776,2035`.
6. **P7**: D-P7-71 (SUCCESS-only clear admission), D-P7-45 (shared Failed(...,true) terminal), D-P7-73 (before-draw refresh receiver), mipmap-after-snapshot-before-binding placement, and the removed manual bind loop are all present in current P7 and match P5 §§4.2.1/4.4/4.6/4.12.
7. **P6/P14/P2**: the eight-argument factory with required P7-owned `UniformReplayErrorSink` (R25-1) and R7-10 resolver injection match P6 `:443-449,1809`; P14 D-P14-31 is received as D-P5-43, and R-P14→P5-2 correctly remains *received/ungranted* on both sides (P5 §5.5/§11.5; P14 `:1692-1694,1964`) with per-unit/NONE and disabled MULTI_BIND intact; P2's `/4` grammar (`evidence_stage`, `requested_format`, `clear_policy=FOG_RGB_ALPHA_ONE|CONSTANT`, REALIZED-only `allocated_format`/`allocation_origin`) and the IR-29 source-free `plan`-only inspection route match P5 §4.1.1/D-P5-25 meaning exactly.
8. **Internal algorithm checks.** The D-P5-46 saturation table was independently recomputed: R11F_G11F_B10F max 65024 (32768×127/64), B10F 64512 (32768×63/32), RGB9_E5 65408 (65536×511/512), 16F 65504 — all correct; truncation/saturation examples (1.9→1, negative unsigned→0, 2^32→4294967295) follow from the stated rules; enum/variant closures (rejections, resize reasons/registrations, FrameProtocolRejection vs ShadowProtocolRejection, BufferFailureCode with MAIN_DEPTH_RESIZE_REQUIRED as exclusive carrier) are all closed and self-consistent across §§2/4/5/6/8/12.

## 5. Findings

**No necessary correction was found.** Every boundary defect class checked (D-P5-49 trigger preservation, integer-tier separation, discard receipt; D-P5-50 unit0 association, kind defaults, serial currentness, refresh protocol; schema/registry identity cutovers; lease arity/rejection domain; resize/evidence grammar; refresh receivers) has both the owning clause and the receiving clause present in current bytes, mutually consistent. R44's single correction C44-1 is verifiably incorporated (§4.6 `:1786-1791` + §4.7 `:1856-1861`) with no superseded competing clause left active.

## 6. §5 grant sufficiency

**Current §5 grants are sufficient.** Every contract P5 consumes exists in the dependency's current §5/§4 with the current shared identity set (P3 schema23/MaterializedSource-v23; P4 profile-selection-v3; P13 lease third argument + INVALID_BASE_BINDING/STALE_BASE_BINDING + parameters/v2 + sidecar/v1 + application-v2; P8 flattened-v3; P1 typed-clear/target-value/captured-limit/discard grants; P6 R7-10/R25-1; P14 D-P14-31), and every P5-exposed contract has a present consumer branch (P2, P6, P7, P8, P13, P14). No §5 change is required by this review. Several of those grants are themselves *adopted/unverified* pending their owners' fresh reviews — a gate status P5 records honestly, which postpones verified downstream consumption and implementation, not the correctness of this architecture.

## 7. Notes and limitations (optional, non-blocking)

1. **Unexecuted-evidence limit.** All §8 cases (including the D-P5-49 three-trigger/discard matrix and D-P5-50 two-atlas/refresh cases) and §12 items are planned architectural checks; no native GL run, hook application, G6 parity capture, or runtime proof exists or was demanded — the document defers runtime evidence honestly, and per review policy that deferral does not block architecture approval.
2. **Sibling verification gates remain.** P4's coordinated rebuild, P13's R7 corrections, and the P6/P8 adoptions are current but unverified on their owner side; P5's receipts are faithful to those bytes, but final G5.3 integration and IR-01 remain open independently of this PASS, and this verdict certifies neither sibling documents nor implementation clearance.

## 8. Final verdict

**PASS** — 0 corrections, 2 notes, no §5 impact (no change required). The frozen bytes are architecturally correct within the documented evidence limits; the D-P5-49/50 attempt-7 corrections are fully and unambiguously incorporated with reciprocal producer/receiver contracts on every boundary checked.

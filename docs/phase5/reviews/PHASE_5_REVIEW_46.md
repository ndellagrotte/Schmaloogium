# Phase 5 — architecture review 46

**Date:** 2026-09-08. **Frozen document:** `docs/phase5/v1/PHASE_5_DOC.md` SHA-256 `84ead9d936e7fb34cea13e3d895b662d99cff9edf99f42149fa61fbbeb844aa0` (recomputed before review; matches the frozen assignment). **Verdict:** PASS-WITH-CORRECTIONS (0 blocking, 4 corrections covering 42 stale evidence-anchor instances, 6 notes). **§5 impact:** none — every correction is a citation re-derivation inside evidence/provenance prose; no §5 interface, identity, protocol row, or gate text changes.

**Documentary only:** no build, test, gradle, GL, capture, or network execution was performed for this review. All findings are from reading current repository bytes and recomputing line numbers against them.

---

## 1. Scope, identity and authority actually verified

- **Hash recomputation (first action):** `sha256sum docs/phase5/v1/PHASE_5_DOC.md` → `84ead9d936e7fb34cea13e3d895b662d99cff9edf99f42149fa61fbbeb844aa0`, identical to the frozen assignment. Proceeded.
- **Governing-design confirmation at the doc header:** the doc's §0 names `docs/design/v2.0-RC3/DESIGN.md` with the Phase 5 assignment starting at `DESIGN.md:1572` — verified: the spec header `### Phase 5 - Framebuffer & buffer architecture` is at `DESIGN.md:1572`, objective sentence "Contract-visible almost end to end" at `:1583`, deliverable-gate text at `:1677`–`:1685`. The doc's §0.1 read-list matches the assigned reading set.
- **Complete phase document:** all 3653 lines read, in ranges, with every truncated/elided window re-read (gaps closed at 1041–1233, 1234–1470, 1470–1833, 1834–2283, 2487–2742, 2956–3410, 3404–3653). Section skeleton recomputed: §0 Header (`:3`), §1 Scope & boundaries (`:414`), §2 Architecture overview (`:482`), §3 Contract conformance map (`:1167`), §4 Detailed design (`:1270`), §5 Cross-phase interfaces (`:2701`), §6 Failure modes (`:3034`), §7 Threading (`:3071`), §8 Testability (`:3112`), §9 Milestones (`:3308`), §10 OQ & spike (`:3332`), §11 Decisions & open items (`:3341`), §12 Implementation checklist (`:3588`) — thirteen sections, matching the §G9 mandatory template (`DESIGN.md:790`, thirteen-section gate at `:295`).
- **Governing design Part I:** `DESIGN.md:1`–`:1109` read in full (§G0–§G12, §G2 decision log D-1..D-10, §G4 contracts/gates, §G9 template, §G11 Pintonium rules, §G12 Oculus boundary), plus the Phase 5 spec `:1572`–`:1688`.
- **RESEARCH.md §0–§1:** lines 11–107 read (reading guide, mission, confidence tags `:24`–`:38` — the doc's §0.2 tag discipline cites `:24`–`:38` and that anchor is correct).
- **Dependency docs, §0/§1/§5/§11 surfaces, targeted to the consumed and granted contracts:** P1 v14 (`borrowDepthAttachment`/`attachDepthStencil`/`initializeDepthTextureFromFramebuffer`/`copyDepthToTexture` facade `:3325`–`:3344`; D-P1-52, -59, -60, -62, -63, -64, -66, -67 §4.7.4b at `:4001`–`:4022`, -69; §5 consumer rows `:5472`–`:5476`, bindDefault sentence `:5497`); P3 v1 (`CURRENT_SCHEMA_VERSION = 23` at `:704`/`:4161`, `MaterializedSource-v23` at `:3309`/`:4212`, nine trees/`projectionVersion1` `:4215`, lossless `textureDeclarations` `:1085`/`:2813`/`:3272`, `CompanionOptionMacros` in `PackLoadRequest` `:790`/`:3355`, `renderResMul`/`shadowResMul` `:2343`/`:3454`/`:3490`, U1 adoption `:468`–`:492`); P4 v1 (`CompiledRegistryCandidate.view()` `:817`–`:820`/§5.1 row `:2127`/grant `:2935`–`:2938`; `validateSelection` `:1794`–`:1796`/row `:2137`; D-P4-43 `RegistryFingerprint/profile-selection-v3` `:1937`–`:1941`/`:2800`; D-P4-40 `SparseArray(...,Optional<ProgramSlotId> virtualPrelude)` `:510`–`:511`/`:2797`; `samplerPolicyFingerprint()` `:809`–`:810`/`:2274`–`:2278`; `initializationAssignments` `:667`/D-P4-36 `:1418`–`:1422`; `DrawRoutingSlot.Attachment/None` row `:2142`; non-inference warning `:2149`–`:2151`); P6 v1 (eight-argument `UniformRuntimeFactory.create(..., UniformReplayErrorSink replayErrors)` §2.2 `:440`–`:450`/§5.1 `:1909`; §5.2 consumed-P5 row `:1930`–`:1931`; D-P6-36 schema23 receipt `:2316`); P7 v1 (D-P7-76 estate-clear ordering and D-P7-77 celestial provider, §4.4 Failed-variant consumption at `:1213`/`:1338`/`:3824`/`:4140`, §5.1 `generateMainMipmaps` ordering `:3836`, shadow glue `:1792`–`:1793`/`:1598`–`:1605`/`:2933`/`:3880`–`:3928`); P8 v1 (§0.7 five-argument sixteen-row adoption `:121`–`:127`, D-P8-37 flattened-v3 66 rows and D-P8-39 actual-base lease consumption, §11 status `:2763`–`:2766`); P13 v1 (`TextureLeaseSource.lease(expected, selection, baseBinding)` `:350`–`:353`, D-P13-44 schema23 gate `:2346`, D-P13-45 re-resolved P5 anchors `:2347`, `phase13.parameters/v2` `:196`/`:1344`); P14 v1 (D-P14-31/`D-P14-26` `:202`–`:205`, §5.2 resize row with all eight reasons in declaration order `:1682`, §4.1.4 sixteen-row preflight + Bound-only transfer `:686`–`:698`, `prepareUnitBindings` row `:2931`).
- **Reference reports:** PD §5 whole (`:215`–`:283`), §6.5 (`:342`–`:356`), §17 B13 (`:799`), §18 flip/texture-unit rows (`:808`–`:809`) — all PD anchors in the doc verified accurate against current bytes. RESEARCH appendix ranges recomputed for conformance spot-checks (B.1 `:1200`–`:1216`, B.2 `:1218`–`:1226`, B.3 `:1228`–`:1255`, B.4 `:1258`–`:1268`, §4.3 `:510`–`:527`, F.5 `:1484`–`:1490`, F.7 `:1513`–`:1524`, A.3 `:1174`–`:1188`, §3.6.3 `:369`–`:386`).
- **Not read, per hard boundaries:** `reference-src/**` (so the doc's pinned `shaders.txt`/`SHADER_ENGINE_IMPL.md`/`.java` line ranges are accepted as disclosed reads, not re-verified), `docs/**/chatlogs/`, repository-root `*.txt`, and every other `PHASE_*_REVIEW_*.md` file (the doc's citations to `PHASE_1_REVIEW_20.md:61`–`:75`, `PHASE_4_REVIEW_15.md:59`–`:72`, `PHASE_13_REVIEW_3.md`, and R45 are therefore unverified pointers, not review inputs).

## 2. Scope and §5 audit

- **Doc-gate criteria (RC3 `:1677`–`:1685`), checked literally:** flip/clear semantics externally confirmed with pinned evidence (§3.1/§3.2/§3.6, D-P5-1/2) — present; frame-end flip reconciliation recorded with provenance and an explicit App F.7 contract check (D-P5-4, §4.4.3 metadata-rebase against PD `:229`–`:236`) — present; clear-color table includes the fog-alpha-1.0 rule (§4.6 table `:1744`–`:1750`, D-P5-2) — present; depthtex0 swap design present and traced to PD §5.2 (§4.8, D-P5-5, PD `:245`–`:254` verified) — present; shadow flip semantics real, not the PD B4 stub (D-P5-8 with D-P5-35's pinned-method qualification, §4.4.2/§8.1 item 5) — present. Impl-gate coverage: headless state-machine matrix §8.1 items 1–11; recorded-GL full-estate create/destroy with `noLeakedObjects()`/`noUseAfterDelete()` §8.3 item 16 — present.
- **Conformance-map audit:** every §3 row carries a provenance tag and decision pointer; spot-checked rows against current RESEARCH bytes: colortex0 fog/white (`:1173`↔B.1 `:1204`), colortex1 conditional RGBA32F (`:1174`↔`:1205`/`:521`), colortex2–7 (`:1175`↔`:1206`–`:1208`), count 4–8 (`:1176`↔`:1210`), main/alt allocation and gbuffers-main (`:1177`–`:1179`↔`:1211`–`:1212`), depthtex0/1/2 and shadow rows (`:1190`–`:1196`↔`:1219`–`:1226`), unit-11 authority (`:1213`↔`:1250`), 37-format families (`:1227`–`:1235`↔`:1261`–`:1265`), pixel vocabulary (`:1237`↔`:1267`–`:1268`), incomplete-FBO fallback (`:1239`↔`:520`), clears-both-sides (`:1803`↔`:523`), growth identities (`:1253`↔`:371`–`:386`), shared-unit/AppF.5 (`:1221`, `:3367`↔`:1484`–`:1490`), normative unit table (`:2475`↔`:1228`–`:1255`). Semantic fidelity holds everywhere; only line anchors drifted (correction C-1). No in-scope scope item lacks a §3 row; nothing foreign appears.
- **Interface honesty — consumed:** every surface P5 consumes exists in the current dependency §5s exactly as the doc's §5.2–§5.4 rows state (verified line-by-line in §1 above): P1 borrowed-depth facade and typed-clear/limits/baseline grants; P3 schema23/MaterializedSource-v23/nine-trees/`textureDeclarations`/`CompanionOptionMacros`/resMul decoding/U1; P4 candidate `view()`, `validateSelection` reason set, profile-selection-v3, `virtualPrelude`, `samplerPolicyFingerprint()`, `initializationAssignments`, positional routing; P6's eight-argument factory with trailing `UniformReplayErrorSink` and the resolver-injection point immediately after configuration (P6 `:443`–`:444` ↔ doc §5.5 item 4 — exact match, argument order identical); P13's `lease(expected, selection, baseBinding)` with the actual-base context as third argument (P8 D-P8-39 consumes it the same way).
- **Interface honesty — granted:** everything P5 promises is adopted on the receiving side with current records: P6 §5.2 consumed-P5 row `:1930`–`:1931` (sole table/policy, pure, pre-runtime availability); P7 §5.2/§11 (D-P7-76/77 ordering; §4.4's ordinary-acquisition `Failed` consumption at four call sites; §5.1's post-snapshot/pre-bind `generateMainMipmaps` obligation); P8 §0.7/§5 (five-argument sixteen-row `shadowBindings`, result-level `Neutralized`, R7-12 not R7-13); P13 §5.2/§11.1 (lease protocol; D-P13-45's re-resolved P5 anchors `:2726`–`:2735` and `:2729` Bound-only sentence — recomputed, accurate); P14 §5.2 row `:1682` (all eight `BufferResizeReason` values in exactly the doc's declaration order) and D-P14-19 (physical binding before Bound return).
- **Recent-change identities:** `schema23`/`MaterializedSource-v23` (D-P5-44), `RegistryFingerprint/profile-selection-v3` (D-P5-48; D-P4-33 correctly marked historical), `ShadowHookHealth/flattened-v3` 66 rows and `TextureHookHealth/application-v2` (receiver-owned; P5 claims only its own sixteen-row operation and never cites stale P8 identities), `capture-plan/4` + `run-manifest/4` (§4.1.1 `/4` grammar with variant-required/forbidden fields), `projectionVersion1`, `phase13.parameters/v2` (P13 receipt) — all cited at current versions. `phase13.sidecar/v1` is not consumed by P5 and no claim about it is made — correct silence.
- **Scope discipline:** §1 in/out boundaries match the RC3 assignment's in/out lists item for item (main FBO, ping-pong/flip, clears, formats, depth textures, shadow structure, sizing, resize lifecycle, fixed units, draw prefixes; out: timing to P7, raw upload vocab to P13, sampler objects/DSA/async to P14, renderer features per D-2). Nothing in-scope is dropped: sizing incl. `BufferSizing(Extent2i mainExtent, Optional<Extent2i> shadowExtent)` with exactly two fields (D-P5-37), the eight-reason resize/notice matrix, shadow estate and neutralization, fallback, evidence projection, overlay lease, virtual transitions.
- **Binding decisions D-1..D-10:** §11.2 dispositions checked against the doc body — no contradiction (D-2 honored; D-5 single targeted mixin at §4.8; D-6 pure `engine.buffers`/opaque handles throughout §2.2; D-7/D-8 GPL posture with the §11.3 Pintonium qualification; D-9 compatibility-profile paths retained in §4.2/§4.6; D-10 tests first-class in §8).
- **Licensing/Pintonium compliance:** no `glsl-transformer` or AGPL trace anywhere in the doc (`:62`–`:63` explicitly disclaims); §11.6 web reads are license-tagged and narrowly scoped (Iris 26.1 `OptionAnnotatedSource`, LGPL-3.0, "only this file"); do-not-inherit rows handled honestly: B4 stub (D-P5-35 pins the real method behavior instead of inheriting the stale TODO), all-16 allocation (D-P5-7 rejects), dynamic units (D-P5-9 rejects, PD `:808` verified), copy-back (D-P5-4 rejects, PD `:809` verified), render-quality 1.0 gap (D-P5-15 routes it to P7's prepare).
- **Assignment's named decision clusters:** D-P5-30..35, -36/37, -46/49/50, -23/26/27, -25 each re-derived from the body: legal DefaultRgba baseline with REQUESTED/RGBA_FALLBACK provenance (`:1309`–`:1315`, `:1358`–`:1363`), owner-only main-mipmap transaction (§4.2.1), honest PLANNED/REALIZED evidence with `/4` grammar (§4.1.1), scheduled point consumption independent of copy success (D-P5-33, §4.9), policy-keyed bounded neutral cache (D-P5-34, §4.10), mutation-bearing snapshot failure `Failed(...,true)` (D-P5-36, §4.4.2 step 7), typed realized-format clears with P1 D-P1-67 records (D-P5-46), positional DRAWBUFFERS route with explicit None positions (§4.5 `0N2` example), integer-clear-tier rejection separated from the RGBA fallback (D-P5-49), actual-base lease context (D-P5-50), engine-SSAA removal with `BufferArchitectures.create()` pure exposure (D-P5-25/27, §5.5 item 3).

## 3. Required corrections

All four corrections are one defect class: the dependency documents grew through same-day amendments after this doc's evidence anchors were derived, and the affected anchors were not re-derived. Every quoted sentence, identity, and semantic claim was relocated at current coordinates and is faithful — no contract drift hides behind any stale anchor. Fix is mechanical anchor re-derivation; none of it touches §5.

### C-1 — Stale RESEARCH.md line anchors (29 instances)

- **Sites:** §0.1 `:29` (`:1180`; `:1508`–`:1515`), §0.3 `:70` and `:1181`-row `:1181c` (`:1513`–`:1514`), `:75` (`:1180`), §3.1 `:1174` (`:1204`), `:1175` (`:1205`–`:1207`), `:1176` (`:1209`), `:1177` (`:1210`), `:1181` (`:1513`–`:1514`), `:1182` (`:1212`–`:1214`), §3.2 `:1191`–`:1196` (`:1221`, `:1222`, `:1223`, `:1224`, `:1225`, `:385`), `:1197` (`:523`–`:525`), §1.3 `:478` and §3.2 `:1203` (`:526`), §3.4 `:1227`–`:1237` (`:1260` ×2, `:1261` ×2, `:1262` ×2, `:1263` ×2, `:1264`, `:1266`–`:1267`), §3.5 `:1253` (`:368`–`:385`), §4.6 `:1803` (`:520`–`:522`).
- **Failure path:** RESEARCH.md gained lines since the anchors were derived: +1 before `:520`/after `:1180`/after `:1204`, +6 before the F.7 flip sentence. Verified current locations: superSamplingLevel `:1181` (doc cites `:1180`, which is now `ambientOcclusionLevel`); F.7 flip sentence `:1519`–`:1520` (doc cites `:1513`–`:1514`); colortex1 `:1205`; colortex2–7 `:1206`–`:1208`; count row `:1210`; composite-blend `:1213`–`:1215`; depthtex1/2 and shadowtex0/1/shadowcolor0/1 `:1222`–`:1226`; PCF row `:524`–`:526`; Final-to-vanilla `:527`; clears-honor-flip `:523`; format families `:1261`–`:1265`; pixel vocabulary `:1267`–`:1268`; growth rows `:371`–`:386` (shadowcolor2–7 at `:386`, outside the cited `:368`–`:385`).
- **Owner+receivers:** Phase 5 owner; no receiver impact (evidence-only). The fix-up session already planned for the §G5.3 integration review should re-derive all sites in one sweep.
- **Minimal resolution:** re-anchor the 29 sites to the current coordinates listed above; keep every quote and tag unchanged. Note the doc is internally inconsistent today: §11.6 (`:3556`) already cites the current `:1181` while §0.1/§0.3 still cite `:1180` — evidence the last revision re-anchored only its own new section.

### C-2 — Stale Phase 3 line anchors (6 instances)

- **Sites:** `:1174` (conditional gdepth upgrade "at `docs/phase3/v1/PHASE_3_DOC.md:468`"), `:1180` (flip.* publication "`:443`–`:445`"), `:1195` (shadow algebra "`:974`–`:1001`"; binding input "`:1271`"), `:1247` (B.5 prefix canonicalization "`:487`–`:493`"), `:2512` (canonical ordering "`:1482`-`:1508`").
- **Failure path:** P3 grew through §§0.57–0.61 amendments (schema 17→23 progression). Current bytes at those anchors: `:443`–`:445` = §0.58 integration-reconciliation header; `:468` = §0.60 U1 adoption header; `:487`–`:493` = §0.60 closing prose; `:974`–`:1001` = `OptionCatalog`/`OptionState` declarations; `:1271` = §2.2 capability-identity comment; `:1482`–`:1508` = `SourceSpan`/`LegacyGeometry*`/`ShaderLanguage` declarations. The consumed P3 semantics are real and current — e.g. the ResourceRequirements row with gdepth RGBA32F force, positional DrawSlot and format distinction at `:3367`, schema-23 admission at `:4161`/`:4207`–`:4215` — but none of them sit at the cited lines.
- **Owner+receivers:** Phase 5 owner; P3 is unaffected (its identity surface is consumed correctly).
- **Minimal resolution:** re-derive the six anchors against current P3 bytes (the §4.7 ResourceRequirements rows and the conformance-table rows near `:3367`/`:3355` are the correct targets for the algebra/binding-input/canonicalization claims).

### C-3 — Stale Phase 4 line anchors (4 instances)

- **Sites:** `:1685` (non-inference quote cited `docs/phase4/v1/PHASE_4_DOC.md:1800-1802`), `:1248` (ordered routing cited `:1295-1309`), §5.4 row `:2959` (validateSelection contract cited `:1787`–`:1796`), §0.45 item 2 `:2992` (`view()` cited `:777-780`).
- **Failure path:** P4 grew through R32–R36 (D-P4-35..43). Current bytes: the exact quoted sentence "Phase 5 must not infer a resolved ping-pong side from `explicitFlips`; it owns the side state" is at P4 `:2149`–`:2151`; ordered positional routing lives in §4.9's adaptation text and §5 row `:2142`; `validateSelection`'s first-failure-wins check begins at `:1794` (the cited range's tail overlaps but its start is stale); `CompiledRegistryCandidate.view()` is declared at `:817`–`:820` with the §5.1 row at `:2127` and the grant at `:2935`–`:2938` — `:777`–`:780` now contains sampler-projection conflict-canonicalization prose.
- **Owner+receivers:** Phase 5 owner; P4 unaffected (all consumed identities current, including profile-selection-v3 via D-P5-48).
- **Minimal resolution:** re-anchor the four sites (`:2149`–`:2151`, `:2142`, `:1794`–`:1796` onward, `:817`–`:820`).

### C-4 — Stale Phase 1 line anchors (3 sites, 4 instances)

- **Sites:** §1.3 `:479` (bindDefault warning cited `docs/phase1/v14/PHASE_1_DOC.md:4009`), §4.8 `:1988` and §0.45 item 1 `:2983` (borrowed-depth §5 service rows cited `:4131`–`:4147`), §4.8 `:1992` (forged/wrong-origin service contract cited `:3038`–`:3058`).
- **Failure path:** P1 v14 grew through D-P1-52..69 amendments. Current bytes: `:4009` is D-P1-67 typed-clear prose ("Floating components must be finite..."), while the bindDefault-means-name-zero warning sits in the §5 consumer table at `:5497`; `:4131`–`:4147` is now the `GLCallLog`/`RecordingGLDevice` section, while the borrowed-depth service row is at `:5473` (facade at `:3326`); `:3038`–`:3058` now lands on the Handles section — the `BorrowedDepthAttachmentHandle` forgery comment overlaps at `:3056`–`:3060`, but the full forged/wrong-origin/wrong-device/stale service rows cited by the sentence live in §5. The claims themselves are all true of current P1 (verified at `:3325`–`:3344`, `:5473`, `:5497`).
- **Owner+receivers:** Phase 5 owner; P1 unaffected.
- **Minimal resolution:** re-anchor to `:5497`, `:5473` (+`:3326`), and the current §5 forged-use row.

## 4. Notes

- **N-1 (observation, no action here):** P14 `:419` cites `PHASE_5_DOC.md:2018` for the resize-consumer ownership sentence; on current P5 bytes that sentence is the `BufferResizeNotice` §5.1 row at `:2729`–`:2731`. The P5-side content is correct and unchanged; the stale anchor is P14's to fix in its own next session. Recorded so the integration review does not misattribute it.
- **N-2:** The doc's `reference-src/` evidence (pinned `BufferFlipper.java:25/:27`, `ClearPassCreator.java:21–78/:35–42`, `MixinFramebuffer_Shaders.java:59–100`, `DepthCopyStrategy.java:16–30`, `RenderTargets.java:148/:166`, `ShadowRenderTargets.java:43–72`, `CommonIrisRenderingPipeline.java:549–551`, `shaders.txt` rows, `SHADER_ENGINE_IMPL.md:209–213`) was not re-verified: this review is barred from `reference-src/**`. The doc's own disclosures (§0.1/§0.2), the §G11.3 checkout traps, and D-P5-35's pinned-method qualification are internally consistent and were audited at the documentation level only.
- **N-3:** Citations to prior review files (`PHASE_1_REVIEW_20.md:61`–`:75`, `PHASE_4_REVIEW_15.md:59`–`:72`, `PHASE_13_REVIEW_3.md`, R45) were not opened — historical reviews are not inputs to a fresh round by protocol. Their use in the doc is as historical verification pointers, which is the correct role.
- **N-4:** §3.3's unit-11 resolution range `RESEARCH.md:1250`–`:1254` covers the conflict note at `:1250` but stops one row short of the B.1 continuation's last row at `:1255`; folded into C-1's sweep rather than counted separately.
- **N-5:** The P6 factory receipt in §5.5 item 4 is explicitly dated and marked "unverified" pending P6's own round — that honesty marker is correct and consistent with P6's current `:2616` status row; no staleness is claimed as current anywhere.
- **N-6:** §10's "no assigned open question, no spike invented" matches the RC3 assignment ("OQs: -") and the header's "Assigned OQs: none"; the former superSamplingLevel OQ is correctly recorded as resolved by the maintainer disposition (§11.6, D-P5-26/27), not as an open spike.

## 5. Areas audited and found sound

- **Frame semantics:** the `committedMain`×`flipped` side table (§4.4.1) is exactly the observed relation; pass snapshot/transition rules (§4.4.2) including virtual-pre handling via `applyVirtualTransition` with no resolved program; frame-end metadata rebase (§4.4.3) satisfying App F.7 without copy-back; the same machine on shadowcolor with the stub attribution honestly qualified.
- **Clears:** default table with contractual fog-alpha-1.0; D-P5-46's realized-format typed conversion table (deterministic truncation/saturation/clamping, negative-zero normalization, NaN/∞ rejection); positional batch dispatch skipping route holes; all-success full-clear consumption; P1 D-P1-69 rasterizer-discard bracketing received without a public state verb.
- **depthtex0/1/2:** borrowed-depth bridge with monotonic version, `prepare`/`Pending`/extent-exact candidate gate, `refreshMainDepth` epoch advance with ordered reattachment steps and fail-closed poisoning, `ResizeRequired(MAIN_DEPTH_RESIZE_REQUIRED)` as the sole unambiguous carrier; copy tiering with function-pointer check; `DepthCopyResult` four-variant closure with scheduled-point consumption.
- **Shadow estate:** planning gate on either shadow minimum; bounded policy-keyed neutral cache initialized before readiness; five-argument sixteen-row `shadowBindings` with Bound-only transfer; result-level `Neutralized` on filter-restore failure; idempotent `degradeToNeutral`; exact `ShadowProtocolRejection` set.
- **Fixed unit table:** sixteen rows, unit-11 authority, watershadow 4→5 conditional shared with P6's resolver, AppF.5 per-program type selection with greatest-compatible-ordinal precedence (D-P5-21), full §8.5 coordinated hook matrix with correct owner splits.
- **Lifecycle:** ownership-ledger creation with single estate-wide RGBA_COMPAT retry and truthful per-row provenance; publication/consumer-acknowledgement resize protocol (registration rejection set, ordered delivery, `deliveredCount` semantics) matching P14's eight reasons and P13/P14 receivers; candidate-inspection/estate REALIZED equality.
- **Identity currency:** every shared identity the doc cites as current is current (schema23, MaterializedSource-v23, profile-selection-v3, flattened-v3 receiver-side, capture-plan/4 grammar, projectionVersion1, parameters/v2); every superseded identity it retains is labeled historical (D-P4-33, D-P5-10/16/20/23/28/29/40).
- **Test plan realism:** §8.1–§8.3 enumerate exactly the failure branches the §4 mechanisms define (per-position mipmap failure, lease open/close/abort matrix, resize-consumer failure positions, neutral-cache policy distinctness), and §8.4's motion-scene requirement guards the stale-flip capture trap.
- **Evidence discipline beyond the stale anchors:** confidence tags are used per §0.2's definitions; `[V:observed]` is confined to behavior restatements; D-P5-13's allocation-path reproduction is labeled as observation, not structure copying; §11.6's web table is license-scoped.

## 6. Verdict

**PASS-WITH-CORRECTIONS** — 0 blocking, 4 corrections (42 stale evidence-anchor instances across RESEARCH.md, P3, P4, P1), 6 notes. The document's architecture, contracts, identities, scope coverage, template completeness, and licensing posture are sound; all consumer and receiver surfaces match current dependency bytes, including the post-R45 P6/P7/P13 boundary changes named in the assignment. The corrections are confined to re-deriving evidence line anchors against current bytes and do not touch §5 or any gate; a follow-up anchor sweep can be performed in the already-planned fix-up session before the §G5.3 integration review. IR-01 remains open and untouched by this round. Documentary only: no build, test, GL, or capture was executed.

---

## Resolutions

**Date:** 2026-09-08. Applied by the Phase 5 fix-up session. All four required corrections are **applied**; none rejected. Every replacement anchor was re-derived against the current bytes of the cited document and the quoted content verified in place before repointing; every quote, identity, and confidence tag is unchanged, and no contract wording changed. The report body above is byte-identical to the reviewed text. No new `D-P5-*` decision IDs were created (all four corrections are pure evidence-anchor repoints; the §11.1 ledger is untouched and its maximum remains D-P5-50), and no §0 fix-up addendum was appended because no correction demanded one. Doc structure re-verified after the sweep: 3653 lines, thirteen-section `## 0.`–`## 12.` sequence intact, 34 code fences balanced.

- **C-1 (stale RESEARCH.md anchors) — applied: 33 anchor substitutions across 32 doc sites.** §0.1 `:29` (App A.3 `:1180`→`:1181`; App F.7 `:1508`–`:1515`→`:1514`–`:1521`, F.7 heading recomputed at `:1514`, content through `:1521`); §0.3 `:70` and §3.1 `:1181` (`:1513`–`:1514`→`:1519`–`:1520`, flip sentence verified there); §0.3 `:75` (`:1180`→`:1181`); §1.3 `:478` and §3.2 `:1203` (`:526`→`:527`); §3.1 `:1174` (`:1204`→`:1205`), `:1175` (`:1205`–`:1207`→`:1206`–`:1208`), `:1176` (`:1209`→`:1210`), `:1177` (`:1210`→`:1211`), `:1182` (`:1212`–`:1214`→`:1213`–`:1215`); §3.2 `:1191`–`:1195` (`:1221`→`:1222`, `:1222`→`:1223`, `:1223`→`:1224`, `:1224`→`:1225`, `:1225`→`:1226`), `:1196` (`:1225`→`:1226`; growth source `:385`→`:386`), `:1197` (`:523`–`:525`→`:524`–`:526`); §3.4 `:1227`/`:1228` (`:1260`→`:1261` ×2), `:1229`/`:1230` (`:1261`→`:1262` ×2), `:1231`/`:1232` (`:1262`→`:1263` ×2), `:1233`/`:1234` (`:1263`→`:1264` ×2), `:1235` (`:1264`→`:1265`), `:1237` (`:1266`–`:1267`→`:1267`–`:1268`); §3.5 `:1253` (`:368`–`:385`→`:371`–`:386`, the `shadowcolor2-7` row at `:386` now inside); §4.6 `:1803` (`:520`–`:522`→`:521`–`:523`). **N-4 folded in as directed:** §3.3 `:1213` unit-11 resolution `:1250`–`:1254`→`:1251`–`:1255` (blockquote recomputed at current bytes — the note spans `:1251`–`:1255`). Left untouched per the report's own soundness spot-checks: `:53`, `:346`, `:1173`, `:1178`–`:1179`, `:1190`, `:1208`, `:1221`, `:1239`, `:2475`, `:3367`, `:3556`. Post-edit grep confirms zero remaining stale RESEARCH anchors.
- **C-2 (stale Phase 3 anchors) — applied: 6 anchors, recomputed against current P3 bytes.** §3.1 `:1174` conditional-upgrade publication `:468`→`docs/phase3/v1/PHASE_3_DOC.md:1758` (§3.3 `uniform … gdepth` row: every active declaration mandates `colortex1` format `Explicit(RGBA32F)`); `:1180` flip publication `:443`–`:445`→`:1733` (§3.3 `flip.<prog>.<buf>` row: executable plus `deferred_pre`/`composite_pre` eligible, index-backed `FlipBufferKey`); `:1195` sfb algebra `:974`–`:1001`→`:2711`–`:2724` (§4.7 "Its public immutable algebra is:" through `BufferMinima(int colorBuffers, int mainDepthTextures, int shadowDepthBuffers, int shadowColorBuffers)`) and binding input `:1271`→`:3367` (§5.1 `ResourceRequirements` conformance row with positional DrawSlot, explicit/unsized format distinction, gdepth RGBA32F force; consumers include Phase 5 sizing/formats/clear/mipmap and Phase 8 shadow); §3.5 `:1247` B.5-prefix canonicalization `:487`–`:493`→`:2651`–`:2658` (§4.7 `normalizeColorBufferName`: the sixteen exact spellings mapped to indices 0–7); §4.12 `:2512` canonical ordering `:1482-1508`→`:3660`–`:3663` (stage declaration order → sampler unsigned-UTF-8 byte order → discriminator absent before `0..9` → source kind, with last valid property occurrence winning before ordering).
- **C-3 (stale Phase 4 anchors) — applied: 4 anchors, at the report's coordinates after byte verification.** §4.4 `:1685` non-inference quote `:1800-1802`→`:2149`–`:2151` (quote verified verbatim at current P4 bytes); §3.5 `:1248` ordered routing `:1295-1309`→`:2142` (§5 `DrawRouting.Explicit(List<DrawRoutingSlot>)`/`Attachment(BufferRef)`/`None()` row); §5.4 `:2959` validateSelection contract `:1787–1796`→`:1794`–`:1800` ("`ProgramBindingSelections.validateSelection(selection,context)` is a pure private-credential check … First failure wins" through "otherwise Valid."; start recomputed per the report's "`:1794`–`:1796` onward"); §0.45 item 2 `:2992` `view()` declaration `:777-780`→`:817`–`:820`.
- **C-4 (stale Phase 1 anchors) — applied: 4 instances, anchors recomputed against current P1 v14 bytes.** The report's offered coordinates do not carry the quoted content at current bytes (recomputed: P1 `:5497` is the `ReplayAssertions` row, `:5473` is §0.18's ordinary-foreign clarification, `:3326` sits inside `UniformService`), so each target was relocated by the correction's own content description and verified in place: §1.3 `:479` bindDefault warning `:4009`→`:5513` (§5 non-verbs row: "Minecraft's FBO is not `bindDefault`'s name 0"); §4.8 `:1988` and §0.45 item 1 `:2983` borrowed-depth service row `:4131`–`:4147`→`:5489` (§5.2 "§0.18/§0.19 framebuffer/depth additions" row: `borrowDepthAttachment`/`attachDepthStencil`/`initializeDepthTextureFromFramebuffer` facade shape plus "Phase 5 owns format, freshness, attachment/copy cadence, allocation/copy tier, restoration, and Minecraft lifetime"); §4.8 `:1992` forged/wrong-origin/wrong-device/stale service contract `:3038`–`:3058`→`:5494` (§5.2 "Opaque handle types" row: marker-implementation/wrong-device/wrong-origin rejected before GL, borrowed values legal only at `attachDepth`/`attachDepthStencil` with packed depth/stencil metadata). The claims themselves were confirmed true of current P1.

# Phase 7 - architecture review 47

**Date:** 2026-09-08 (Attempt-11 verification wave; fresh independent whole-owner review)
**Frozen target:** `docs/phase7/v1/PHASE_7_DOC.md` — SHA-256 `ac1ae83e76ca056086eb0ae9dca094844bc9631eca9fd65a9c428eb6fd3faf67`, recomputed at wave open and identical to the Attempt-11 registry freeze (`docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_11.json`). Gate passed; review proceeded.
**Verdict:** **PASS** — 0 blocking, 0 corrections, 2 notes. No build, test, gradle, GL, or network execution was performed; this is a documentary-only review. IR-01 and implementation gates remain open; this review does not clear implementation.
**§5 impact:** Interface changed: **no** — both notes are provenance/labeling observations; no §5 addition, removal, or semantic change is required by this review.

---

## 1. Review scope and method

Complete fresh read of the frozen Phase 7 document (all 4,778 lines: §0 header/receipts through §12 checklist and closing status). Governing inputs read: `docs/design/v2.0-RC3/DESIGN.md` Part I (§§G0–G12; Part I begins at `:92`, §G9 template at `:790`–`:827`, §G10 OQ ownership at `:829`–`:858`, Pintonium rules §G11) and the Phase 7 Part-II assignment (`:1805`–`:1953`, verified including the dependency list at `:1807`, the architecture-requirement seam at `:1935`–`:1939`, the doc gate at `:1941`–`:1945`, and the context budget at `:1950`–`:1953`); `docs/research/v1/RESEARCH.md` reading guide and decisions (ll. 11–107), §3.1/§3.3/§4.3/§4.4, §7.1, Appendix A.1, Appendix E, Appendix F.1/F.5/F.6, and the §11 OQ registry. Dependency verification read each consumed dependency's current §0 header/§1/§5 (and incorporated sections where cited): P1 v14, P2 v2, P3 v1, P4 v1, P5 v1, P6 v1, plus P13 §5 dependency-facing rows. Conformance-map rows were spot-checked against the cited RESEARCH/appendix text at recomputed coordinates, and a broad sample of dependency line-pin citations was byte-verified against current dependency bytes. No other `PHASE_*_REVIEW_*.md`, no `reference-src/**`, no chatlogs, and no root transcripts were read.

## 2. Doc-gate and template completeness

Every clause of the RC3 doc gate (`DESIGN.md:1941`–`:1945`) is satisfied by the frozen bytes:

1. **Hook needs 1–11 traced.** §3.3 maps each §7.1 hook need to its disposition against `RESEARCH.md:796`–`:820` (byte-verified: needs rows present, need 11 included); §4.10's catalog and §4.11's per-row dispositions carry owner phase and milestone tags for every Appendix-E deferral (rows 5–8 → Phase 10; rows 10–11 → Phase 13), matching the RC3 scope-out at `:1913`–`:1916`. Forge-event preferences are recorded with the "an event is not claimed to cover a moment its published API does not expose" discipline (§0.1, §4.11).
2. **v0.1 assembly narrative complete:** §9.1 gives the first end-to-end assembly in dependency order.
3. **Both spike specs complete with fallbacks.** §10.1 (OQ-3) and §10.2 (OQ-4) each carry the question verbatim from `RESEARCH.md:1009`/`:1010` (byte-verified), current-evidence framing, procedure, success criteria, and failure/fallback. RC3 §G10 assigns Phase 7 exactly OQ-3 and OQ-4 and no others; both are owned here and no unowned OQ is adopted.
4. **REV1 timeline adoption.** The Pintonium 7-row injection timeline is adopted per row with SRG targets (`func_175068_a`, `func_174976_a`, `field_175084_ae`, `func_174977_a` overload qualification) and Cleanroom-patch validation cited; rows with no working reference (sky/weather/clouds families) are explicitly flagged, and the reference-free fallback path is proven early in the assembly order.

Template completeness: all thirteen §G9 sections (0–12) are present against the skeleton anchor `DESIGN.md:790`–`:827` (byte-verified); §6 applies the degradation ladder case by case; §8–§9 and the §12 checklist (40 tagged rows with test hooks) are complete. Scope discipline: §1.1 owns exactly the two mandated parts; §1.2 states adjacent ownership for all thirteen neighbor phases; §1.3 states the Part-(a)/Part-(b) dumb-hook seam and expresses D-6 (Kirino-swappable hooks) as required by `DESIGN.md:1935`–`:1939`. D-7 (compatibility-profile baseline) is honored by the OQ-3 fallback posture.

## 3. Conformance-map and anchor audit

§3.1's ten RESEARCH frame-flow rows were checked against `RESEARCH.md` §4.4/§4.3 (ll. 530–570 region and 832–837): world-state sampling, fixed-buffer set, shadow-before-main ordering (including the RESEARCH-wins shadow-before-main-clear resolution recorded in §3.6 with `renderWorldPass` evidence), phase order, weather-before-translucent, lease/glint push-pop, composite ping-pong final, depth-copy moments, center depth, and identity fullscreens/mipmaps/scale — all mapped with accurate provenance. §3.2's Appendix A.1 rows match `RESEARCH.md:1091`–`:1141` (spot-verified rows 1, 2, 15, 17 and the preamble qualification at `:1390`–`:1392`). §3.5's engine-flag rows match `RESEARCH.md:1401`–`:1417` and Phase 3's §3.1 Appendix F.1 ownership map (`PHASE_3_DOC.md:1717`–`:1735`: `clouds`, `underwaterOverlay`, `sun`, `moon`, `vignette`, four `backFace.*`, `rain.depth`, `beacon.beam.depth`, `frustum.culling` — all owned by Phase 7 exactly as claimed), with the video-setting-precedence sentence inside the cited `:1442`–`:1448` range. §3.6's precipitation row matches P3 §3.2's F.6 row (`PHASE_3_DOC.md:1779`: PPT_NONE → none, ≥0.15 rain, else snow), and the jcpp-ordering row matches P3 §2.3's pipeline (catalog/default/session finalization before the properties parse and `MacroConfiguration` finalized before any shader jcpp, `PHASE_3_DOC.md:1639`–`:1672`). §3.7's files.txt claim is the honest 17-of-18 with the `Framebuffer` exception, cross-qualified by Appendix E's own preamble — mirrored in U7-3 rather than silently narrowed.

Dependency anchor spot-checks (all byte-exact against current bytes): P4 `:2140` ("no resolved program, selection or draw"), `:2144` ("resolve is detached handle-free inspection, not selection authority"), `:1810`–`:1818` (the exact `validateSelection` closed failure→result exposure incl. the Phase-5 mapping sentence), `:2168`–`:2169` (the §3.2 prohibition quote, verbatim); P5 `:785`–`:793` (protocol incl. `applyVirtualTransition(frameId, pass)`, `snapshot(PassDescriptor, ProgramBindingSelection)`, `discardPass`, `copyDepth` order), `:1014`–`:1017`, `:2721`–`:2740` and the verbatim "Bound alone transfers lease into closeable sixteen-row snapshot with BoundObject/Unused" row; P6 `:527`–`:539`, `:740`–`:750`, `:790`–`:799`, `:811`–`:812`, `:1065`–`:1077` (the `DUPLICATE`/`REJECTED_STALE_FRAME`/`REJECTED_GENERATION` reacquire duty), `:1813` (byte-equal eight-argument factory), `:1816` (FrameBegin row); P2 `:837`, `:1063`–`:1064`, `:1144`–`:1146`, `:1441`–`:1442`, `:2426`–`:2431`, `:2530`–`:2531`; P3 `:754`, `:844`, `:1029`, `:3359`, `:3405`–`:3408`, `:4211`–`:4265`; P1 `:3422` (`readDepthPixel` verb), `:5530`–`:5531`, `:5886`; P13 `:1727` (Frozen-inputs field order matches §5.2 exactly), `:1794`–`:1796`. Zero stale anchors found in current body text.

## 4. Interface honesty and dependency §5 verification

Every consumed interface exists in the owning dependency's current §5 as cited: Phase 5's snapshot/discard/mipmap/virtual-transition/draw-buffers-none lease/depth-copy/fixed-sampler policy and 5-argument shadow binding rows; Phase 4's selector/descriptor schemas, detached resolve, and validation exposure; Phase 6's eight-argument factory, required replay sink, retirement reasons (no `reset(CLOSE)`), event-sink methods, sample records, and FrameBegin ordering row; Phase 3's schema-23 gate, `MaterializedSource-v23`, nine trees, `captureInternalOptions` failure domain (P7's INVALID_STATE→INVALID_VALUE mapping is a receiver-side mapping of P3's closed enum, consistent with `PHASE_3_DOC.md:1029`–`:1052`, `:2166`–`:2169`, `:3421`), `CompanionOptionMacros`, `assets()`, and `canonicalString()`; Phase 2's `/4` capture-plan, run-manifest, run-timing, and world-generation schemas with D-P2-50/D-P2-55; Phase 1's `readDepthPixel`, pixel-transfer verbs, stated non-verbs, and frame-package homes. R7-10's adopted factory is byte-equal to Phase 6's §5.1 row; R7-11's reasons/results match Phase 6 §§0.24/4.14; R7-12's five-argument sixteen-row binding matches Phase 5's row. Phase 9 and Phase 8 are correctly held as downstream integration slots (`DEFERRED(P9)`/`NotInstalled`), not retroactive dependencies.

Exports are specified with receiver adoption on the other side: P13's dependency receipt cites this document's `:3923`–`:4015` for the active-tuple/lease-source/ten-step transaction; P8 §§0.7–0.8 migrates R7-12/13 with matching text; P2's consumer table lists Phase 7 for the run-manifest and capture-plan contracts; P12/P11 hand-offs in §5.5 name their receivers. §5.4's request ledger is honest: granted vs owner-designed/receiver-adopted/unverified statuses are distinguished, and ungranted changes are never assumed.

## 5. Identity, licensing, and decision-discipline audit

Current identities are correct against ground truth: `PackFrontEnd.CURRENT_SCHEMA_VERSION = 23` / `MaterializedSource-v23` (D-P7-57, §5.2 and §0.2 reconciliation); `RegistryFingerprint/profile-selection-v3` as opaque current owner identity (D-P7-67, §§5.1/5.2, checklist item 25a); `ShadowHookHealth/flattened-v3` with the complete 66/65/37-row catalogue (D-P7-58/68/74, §4.13.1, §8, checklist item 40); `TextureHookHealth/application-v2` with old application-v1 reports explicitly rejected as wrong-target evidence; `capture-plan/4` + `run-manifest/4` as the current Phase 2 wire (§1.1 item 8, D-P7-44/50); `projectionVersion1` and the nine-tree projection unchanged; Phase 2 v2 named as the current dependency artifact in §0.2. The historical receipt chain (schema19→20→21→22→23; `/3`→`/4`) is dated and explicitly superseded in the §5.2 receipt prose and §0.52, mirroring the accepted Phase 2 pattern. Licensing posture (§0.4) is compliant: GPL-3.0-or-later preserved; only the two assigned LGPL Pintonium files used as field evidence with no code copied; the Schlorbium digest used behavior-only; RESEARCH wins conflicts; no `glsl-transformation-lib`/vendored stareval usage. Pintonium mechanisms are adopted as evidence with §G11.4 decision references (e.g., D-P7-33's per-dimension pipeline-cache adoption of PD §3.1 "as a mechanism, not as dimension semantics", matching `DESIGN.md:1853`–`:1855`), and the U1 texture-sampling correction is adopted with no new suffix API. The §11.1 decision log, §11.3 open items, §11.4 contradiction rulings, and §11.5 upstream requests are coherent with the closing status: §5 changed in the attempt-10 fix-up, the document is unverified, and a fresh whole-document literal-PASS round (this review) is required before verified downstream consumption. No build/test/gradle/GL/network evidence is claimed anywhere in the document's gate accounting.

## 6. Findings and verdict

Blocking findings: none. Corrections: none.

- **N1 (note, provenance cosmetics).** The in-place §0 fix-up records §0.36 (`:308`) and §0.51 (`:485`) still state the §3.2 Phase 4 prohibition citation as `PHASE_4_DOC.md:2152`–`:2153`, while the current §3.2 body (`:799`) cites `:2168`–`:2169`. Both were byte-true at their respective fix-up times; the live body is correct against current Phase 4 bytes (verified), and §0.52's re-derivation policy explains the drift of frozen fix-up-time coordinates in concurrently growing dependency documents. No text change required; recorded so future readers take §0 notes as dated records, not current pins.
- **N2 (note, decision-log labeling).** Historical numeric receipts in three §11.1 rows (D-P7-33 schema20, D-P7-36 schema21, D-P7-48 schema22/`MaterializedSource-v22`, D-P7-35 `/3`) carry no per-row "historical" marker; supersession is carried by the §5.2 receipt prose (D-P7-57: "schema23/current-constant… MaterializedSource-v23"; D-P7-44/50 for `/4`) and §0.52's §0.2 reconciliation. This matches the accepted Phase 2 pattern (D-P2-32/38 historical, D-P2-55 current) and creates no current-identity error; noting for ledger hygiene only.

**Verdict: PASS** — 0 blocking, 0 corrections, 2 notes. Interface changed: no. The frozen Phase 7 document satisfies the RC3 doc gate, maps every in-scope contract row with accurate provenance, consumes only interfaces that exist in its dependencies' current §5 with receiver adoption recorded for its exports, holds correct current identities, and maintains licensing and decision discipline. No build, test, gradle, GL, or network execution was performed (documentary review only); IR-01, the final §G5.3 gate, and implementation clearance remain open elsewhere.

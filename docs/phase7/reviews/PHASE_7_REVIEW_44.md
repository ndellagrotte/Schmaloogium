# Phase 7 — architecture review 44

**Date:** 2026-09-08. **Frozen document:** `docs/phase7/v1/PHASE_7_DOC.md` SHA-256 `3458f5ae274383e5f2b5459d5aed997a41fbbd067f62f43a4f236cd482b6307e`. **Verdict:** PASS-WITH-CORRECTIONS (3 corrections, 2 notes). **§5 impact:** yes — Corrections 1 and 2 require §5.1 changes (public FrameState sequence and celestial-provider route text), so the interface region changes and a fresh verification round is required; Correction 3 is hook-catalog-only (§4.10) and leaves §5 untouched.

---

# Phase 7 Architecture Review — Round 44 (attempt8 retry, fresh whole-owner)

1. **Identity and scope.** Reviewed `docs/phase7/v1/PHASE_7_DOC.md` in full (4676 lines), SHA-256 `3458f5ae274383e5f2b5459d5aed997a41fbbd067f62f43a4f236cd482b6307e`, verified by checksum and matching `docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_8.json` (phase 7, reviewRound 44). The header-selected governing design is `docs/design/v2.0-RC3/DESIGN.md` with the Phase 7 assignment at `:1805–:1953` — the correct revision for the P3–P9 band. Read RC3 Part I governance, the assignment, RESEARCH §§0–1/4.4/4.5/7.1/11 and Appendices A.1/E/F.1 rows consumed by §3, current integration Resolutions (`docs/PHASE_INTEGRATION_REVIEW.md` incl. attempt7 follow-on), and the previous numbered review `docs/phase7/reviews/PHASE_7_REVIEW_43.md` as historical evidence only (its C1/Resolutions produced D-P7-72..75). Read dependency §5/incorporated contracts for P1 (`borrowedVertexList(String,VertexLayout,VertexInputPlan)`, fullscreenQuad R26), P2 (capture-plan/4, run-manifest/4, field grammar incl. `smoothingTimeTicks`/`entityCount`/`ownBuild`), P3 (CURRENT_SCHEMA_VERSION=23 at `:704`, MaterializedSource-v23 at `:581–582`), P4 (`RegistryFingerprint/profile-selection-v3` at `:1938`, RegistryBuildRequest order incl. `profileSelection` after configuration and `samplerPolicy` after `macroContribution`), P5 (D-P5-50 base-atlas currentness/refresh at `:2630–2645`, resize registration at `:2344–2375`), P6 (eight-argument factory `:437–449`, `retire(UNPUBLISHED_ABORT|REPLACEMENT|SHUTDOWN)` `:488–489`, D-P6-37 celestial receipt `:767–782`), P8 (`CelestialMath.angles/.sample` `:514–519`, D-P8-38 pure availability `:1034–1040`, D-P8-39 base-binding receiver `:1557–1580`, §4.13.1 flattened-v3 catalogue with group cardinalities OUTLINE=1,BLOB=1,CLOUD=1,ENTITY=13,FORGE=2,REBUILD=2,RESTORE=37,SLOT=1,TERRAIN=1,TRAVERSE=7,total 66, 65 non-CLOUD, six sort GET/SET rows after `147595-R-SET`), P10 (D-P10-30 H10-BRIGHTNESS-4 full v0.3/v0.5 only, `func_178962_a(IIII)V`), P12 (ReloadCoordinator/ReloadRequest merge algebra), P13 (three-argument `lease(expected,selection,AtlasBindingEvidence)` with INVALID_BASE_BINDING/STALE_BASE_BINDING `:351–352,367,1385–1390,1693`, phase13.parameters/v2 + phase13.sidecar/v1, TextureHookHealth/application-v2 eight rows, PreliminaryCompanionDemand, TextureSystemFactory.create). All mandated current identities are confirmed present and consistent with P7's receipts; no fabricated or stale identity was found in active §5 text.

2. **Mandated scrutiny areas.** (a) D-P7-72/73/74/75 receiving seams: the three-argument lease is used consistently in ordinary (§4.4 step 4), fullscreen (§4.6 step 5) and shadow (§5.1 ShadowBaseBindingReceiver) flows, with the documented close-binding-only/close-lease-only exception ownership matching P5/P8/P13; the 66/65/37 flattened-v3 receipt, full-only H10-BRIGHTNESS-4 completed-BLOCK authority, and application-v2 eight-row atlas report all match their owners. Two defects in this seam are reported below (C1, C2). (b) Ten-step transaction (§4.1/§5.3): step ordering, P4 Ready→generation adoption→P5 publish→P13 build/register→P9 publish→invalidation→atomic Active, and step-10 compensation ordering (texture retire → close caller-owned candidates/P8 → P9 reset/deactivate → P5 off → P4 ShadersOff, P6 retirement under R7-11) are internally consistent and match every consumed owner contract checked. (c) Reload paths: the P12 matrix translation, H-RESOURCE-01 pre-destructive gate (§4.8.1), resource-NONE retained-configuration branch and D-P7-60 profile-only REPUBLISH agree with P12/P13. (d) Retirement paths: R7-11 reasons/results, P13 quiescent retirement with lease-deferred deletion, P8 close-before-P5/P4-off match P6/P13/P8.

3. **Correction 1 (P1) — §4.3 sequences the main-estate bind/clear and GBUFFERS onset after the shadow slot, but vanilla 1.12.2 draws sky (and below-y=128 clouds) before `setupTerrain`.** Primary anchor `docs/phase7/v1/PHASE_7_DOC.md:1104–1117` (step 6 'After the shadow slot: bind the main estate and execute Phase 5's one clear plan … This retains RESEARCH's shadow-before-main-clear order' and step 7 'Start sky/terrain drawing only after every prior step'); compounding anchors `:1017–1021` (§2.2 state machine: `SHADOW_DONE … main estate clear/bind succeeds → GBUFFERS`), `:2564–2567` (public `FrameState` order), `:699` (§3.1 conformance row), `:1119–1123` (D-P7-72). Vanilla `EntityRenderer.renderWorldPass` (1.12.2 source) executes `GlStateManager.clear(16640)` (l.1345) → `setupCameraTransform` (l.1347) → `renderglobal.renderSky` (l.1367) → early `renderCloudsCheck` under `posY+eyeHeight<128` (l.1379) → `renderglobal.setupTerrain` (l.1387). H-FRAME-05 anchors the shadow slot at `func_174970_a` (setupTerrain, verified by MCP) AFTER the sky site. Failure paths: (i) the H-SKY-01/03 (and early H-CLOUD-01) scope entries arrive at state MATRICES_CAPTURED, before the D-P7-71 gate that 'only SUCCESS … permits entering GBUFFERS' — every sky/cloud scope is then WRONG_ORDER-rejected, permanently degrading those FEATURE/OBSERVER families; or (ii) if allowed, their draws land in the estate and step 6's fog-RGB clear erases them. Additionally, D-P7-72's H-SKY-02 dispatch requires a same-frame `ShadowFrameView`, but §4.3 step 5 constructs it only after setupTerrain completes (`mainTerrainFrameToken` captured there), so at the native sky moment no view exists — the §8.1 'real shadow delivers before activation and main sky reuses equal same-frame inputs' case encodes the same impossible order. The Pintonium evidence itself contradicts the step-6 claim: `MixinEntityRenderer_Shaders.java` binds/begins the main framebuffer at clear+AFTER (before sky) and only rebinds main after `renderShadows` at setupTerrain+AFTER ('Pintonium's earlier clear' is the workable shape, not the defect the sentence implies). Minimal owner fix: bind and clear the main estate with the fog-RGB clear plan before vanilla sky (immediately after step-4 matrix capture), reduce the post-shadow step to main-estate rebind, reorder/split the `FrameState` machine so scoped draws gate on the cleared estate rather than shadow completion, add the §3.6 ruling for the RESEARCH shadow-before-main-clear conflict this creates, and specify how the main H-SKY-02 route obtains its frame view (or receive a P8 overload on the retained frame identity) valid at the pre-setupTerrain sky moment.

4. **Correction 2 (P3) — §4.3:1076–1079 pins the provider's pre-camera shadowAngle to 'the ready plan's `celestialPolicy.sample(sunAngle)`', contradicting the plan-independent producer install.** §4.1 step 2 installs 'P8's public pure CelestialMath.angles in that provider independently of shadow planning result'; §5.1 states 'retained ShadowCelestialPolicy.sample delegates to it'; P6 D-P6-37 (`docs/phase6/v1/PHASE_6_DOC.md:767–771`) and P8 (`docs/phase8/v1/PHASE_8_DOC.md:1034–1036`, 'including NotRequested, Disabled, NotInstalled and unavailable-estate compositions') both require the angles route when no Ready plan exists — `ShadowPlanResult.NotRequested()` carries no plan. As written, the normative frame-begin sentence cannot supply `FrameUniformSample.shadowAngle` for zero-shadow-minima packs at v0.2+, the exact defect class R43 C1 corrected for H-SKY-02 now surviving in the provider path. Minimal fix: replace the phrase with the installed pure `CelestialMath.angles(sunAngle)` route (or its stateless retained delegate), keeping 'explicit absence' scoped to v0.1 staging.

5. **Correction 3 (P3) — H-SKY-02's redirect target matches two unconditional call sites inside `renderSky`.** Anchor `docs/phase7/v1/PHASE_7_DOC.md:1639`. Vanilla `RenderGlobal.renderSky` calls `World.getCelestialAngle(func_72826_c)` twice (sunrise-color computation and the sun/moon `rotate(angle*360)` site; SRG/descriptor confirmed by MCP). The row specifies a plain REDIRECT with no slice/ordinal discriminator, while §4.10.1 accepts ordinals only where proven and H-SKY-03's adjacent row explicitly requires slice bounding. Unqualified, the injector applies to both sites (actual=2 vs expect=1), degrading the OBSERVER per §4.10.1 and double-firing the celestial update — defeating the D-P7-72 dispatch the row exists to carry. Minimal fix: qualify the anchor to the sun/moon rotation invocation (slice-bounded or OQ-4-proven ordinal), excluding the sunrise-color site.

6. **Notes (optional, no contract change ordered).** N1 — stale §5.2 dependency coordinates: the 'frozen request at line 1150' citation for P13 (actual declarations at `docs/phase13/v1/PHASE_13_DOC.md:389` and `:1688`, field list itself exact) and the P5 'binding row at line 2363' (actual `docs/phase5/v1/PHASE_5_DOC.md:2729`; `:2344–2375` is the resize-registration contract). N2 — §0.2's dependency-inventory rows cite ancient review rounds (e.g. P2 'Review 15'); the substantive claim (all current dependency contracts unverified) is correct, but the historical framing lags the R38–R45 history.

7. **§5 grants sufficiency — explicit statement.** Current §5 grants are **architecturally sufficient but entirely unverified**. Every consumed shape has an owner disposition: R7-1..R7-7 granted/consumed; R7-8 granted/adopted; R7-9 granted with production gated on P3 reverification; R7-10/11 and R7-12/13 and Phase13 R1 owner-designed/receiver-adopted; P8 D-P8-38/39, P5 D-P5-50, P13 D-P13-42/43, P10 D-P10-30, P6 D-P6-37 and the identity set (schema23/MaterializedSource-v23, profile-selection-v3, flattened-v3 66/65/37, application-v2 eight rows, parameters/v2+sidecar/v1, capture-plan/4+run-manifest/4, three-argument lease with INVALID/STALE_BASE_BINDING) all resolve against their owners. However, no fresh whole-document PASS exists on the current bytes — the document's own closing status says §5 changed and is unverified — so none of it is consumable for implementation; this review does not change that standing. Corrections 1–2 require §5.1 changes (FrameState sequence and celestial route text) and therefore a fresh verification round; Correction 3 is catalog-only (§4.10).

8. **Limitations.** Read-only review: no edits, builds, tests, formatters, linters or validation commands were run. No runtime, GL, hook-application, OQ-3/OQ-4, capture or implementation evidence exists or was claimed; documentary/native-source checks are mappings and call-order corroboration, not runtime proof. The historical pins in §0.1 (`cleanroom-0.6.6-alpha`, `pintonium-9c2fcc1`) are absent from this checkout; order evidence was corroborated with `reference-src/Cleanroom-0.6.12-alpha` patches, `reference-src/Pintonium-main` (the same two assigned LGPL mixin files), MCP stable_39 resolution, and a public vanilla 1.12.2 source mirror (used by P10's own document as evidence). No forbidden source (Oculus transform pipeline, glsl-transformer, chatlogs, root transcripts, OptiFine decompile) was read. IR-01, final §G5.3 and implementation gates remain open.

9. **Verdict.** The owner document is coherent, exhaustive and faithful to its consumed contracts across the ten-step transaction, reload/retirement paths and all attempt8 emphasis seams, with every current identity verified. One substantive sequencing defect (C1) makes the documented frame-begin order unimplementable at the vanilla sky/cloud sites and breaks the D-P7-72 dispatch premise, plus two localized consistency defects (C2, C3). Structural rejection is not warranted.

**PASS-WITH-CORRECTIONS — corrections: 3, notes: 2, section 5 impact: yes.**

## Resolutions

All three corrections landed in `docs/phase7/v1/PHASE_7_DOC.md` with decision rows D-P7-76 (C1), D-P7-77 (C2) and D-P7-78 (C3) appended to §11.1; header §0.50 records the fix-up and the §5 change (public `FrameState` sequence and the `FrameBeginSignal` terrain-token carrier), so the fresh verification round this review requires remains open. R44 N1/N2 ordered no contract change and were skipped. Only the two named files were touched; no schemas or versioned identities changed.

- **C1 — resolved by D-P7-76.** §4.3 reordered so the main-estate bind and Phase 5's one fog-RGB clear execute immediately after matrix capture (step 4, D-P7-71 SUCCESS gating every gbuffers scope) before the native sky site, and the post-shadow step is reduced to the main-estate rebind — no second clear. Final frame-begin step order: (1) `renderWorldPass` HEAD — Phase 4 contexts, Phase 6 `beginFrame`, Phase 9 held hands, the plan-independent pure `CelestialMath.angles(sunAngle)` sample, and frame-begin construction of this frame's `ShadowFrameView` with `mainTerrainFrameToken` reserved from the hosting class's `field_175084_ae` counter (vanilla passes `this.frameCount++` at `EntityRenderer.java:1387` and touches the field nowhere else, `:192`; carried in `FrameBeginSignal` per the :1442 bridge sentence); (2) vanilla-state normalization before the ordinal-zero clear; (3) after that clear — `BufferRuntimeInputs`, main-depth refresh, Phase 5 `beginFrame`; (4) after `setupCameraTransform` — matrix capture, then main-estate bind + one clear plan (`ESTATE_CLEARED`); (5) around `setupTerrain` — token verification against the reserved identity, original invoked once, the shadow slot (R7-12 activation → select → bridge → P8 compute → P8 `releaseToFixedFunction`), then the main-estate rebind (`SHADOW_DONE`); (6) drawing order — vanilla sky and below-y=128 clouds draw between (4) and (5) exactly at the native sites, terrain and later scopes only after (5). §2.2 machine and §5.1 `FrameState` insert `ESTATE_CLEARED` between `MATRICES_CAPTURED` and `SHADOW_DONE`, redefine `SHADOW_DONE` as slot-return plus successful rebind, and add the scope-window gating sentence; §3.1 row updated; new §3.6 ruling records the RESEARCH ruling (main clear precedes sky; shadow pass still precedes main gbuffers draws) citing the vanilla order evidence from the finding (`EntityRenderer.java:1345/1347/1367/1379/1387`, mirror `KealJones/mc-1.12.2-source_files`, the same coordinates this review's finding cites); D-P7-72 text and the §8.1 real-shadow planned case synced; H-FRAME-01/H-FRAME-05 catalog rows carry the shadow-field read and verification. No new P8 API requested and none unavoidable.
- **C2 — resolved by D-P7-77.** §4.3 step 1 restated to the installed pure `CelestialMath.angles(sunAngle)` route through its stateless retained delegate, plan-independent including NotRequested, Disabled, NotInstalled and unavailable-estate compositions, matching §4.1 step 2, §5.1, P6 D-P6-37 and P8 D-P8-38; "explicit absence" is scoped to v0.1 producer-not-installed staging.
- **C3 — resolved by D-P7-78.** H-SKY-02's anchor is qualified to the sun/moon celestial-rotation `func_72826_c` site — slice-bounded to the same sun/moon slice H-SKY-03 bounds (`RenderGlobal.java:1396`, `GlStateManager.rotate(this.world.getCelestialAngle(partialTicks) * 360.0F, ...)`), excluding the sunrise-color site (`:1349`) — with an OQ-4-proven ordinal accepted as the alternative discriminator; expect exactly 1 application.

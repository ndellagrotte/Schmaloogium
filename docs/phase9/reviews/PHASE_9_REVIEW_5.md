# Phase 9 — architecture review 5

**Date:** 2026-09-08. **Frozen document:** `docs/phase9/v1/PHASE_9_DOC.md` SHA-256 `471d6547fc3a43a16062e2575833b72dc3e7325e482eaa7895d1d147c02e4b2e`. **Verdict:** PASS (0 corrections, 2 notes). **§5 impact:** none — current P3/P6/P7/P8/P10 §5 contracts cover every Phase 9 owned path, default, and exposed type; no §5 amendment is required

---

# Phase 9 — Independent Whole-Owner Architecture Review R5 (attempt8, round 5)

## 1. Frozen identity

- **Owner:** `docs/phase9/v1/PHASE_9_DOC.md`, all 1395 lines, §§0–12 read completely.
- **SHA256 verified by command:** `471d6547fc3a43a16062e2575833b72dc3e7325e482eaa7895d1d147c02e4b2e` — byte-identical to the attempt8 frozen entry (`docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_8.json`, phase 9, reviewRound 5) and to the hash reviewed by R4. Owner bytes are unchanged since R4's PASS; attempt8's infrastructure failure (`usage_limit_reached`, no reports produced) changed no verdict.
- The prior numbered review (`docs/phase9/reviews/PHASE_9_REVIEW_4.md`, PASS, 0 corrections, 2 notes) is treated as historical evidence only; this review re-derived every load-bearing boundary from the current dependency documents. No Resolutions are appended to that file; the operative Resolutions live in `docs/PHASE_INTEGRATION_REVIEW.md:1036–1345` and were read.

## 2. Governing design and authority

- The header (`docs/phase9/v1/PHASE_9_DOC.md:5–10`) selects `docs/design/v2.0-RC3/DESIGN.md` Part I §G0–§G12 plus the Phase 9 assignment at lines 2035–2125 — confirmed by direct read of `:2034–2128`. RC3 is the correct governing revision for P9 under the attempt8 allocation (P3–P9 → RC3); the header explicitly declines to change an earlier phase's revision. Assignment requirements (all §3.7 forms + per-mod merge + precedence in the conformance map; fully specified Phase 10 alias service; dual-spec alias table and 11300 re-parse as designed features with PD provenance; held-item/`entityColor` reference-free status acknowledged; rebuilt-never-mutated tables; G2.4 unknown-name degradation; headless + in-game impl gate) are each satisfied by §§3–4.9, §4.10, §4.6–4.8, §3 rows, D-P9-1, §6, §8.3 respectively.
- Key RESEARCH anchors re-verified in the current file: entries-before-tags/ore-dictionary shim (`docs/research/v1/RESEARCH.md:447`), §3.7 file forms (`:453–462`), Phase 10 payload `renderType<<16 | aliased-block-id` + metadata (`:1276–1305` App C.2), App D.1/D.4 held + per-draw inventories (`:1318–1378`), App E rows 13–14 incl. the historical `func_147549_a` candidate (`:1405–1406`). P9 §4.12 correctly demotes the App E row-14 convenience overload to historical evidence and targets the actual lower dispatch.

## 3. Scope / §5 audit — current contracts vs every owned path

Each Phase 9 owned path was traced to its current receiving branch:

1. **Selector/schema consumption (P3).** Current P3 publishes `PackFrontEnd.CURRENT_SCHEMA_VERSION = 23` (`docs/phase3/v1/PHASE_3_DOC.md:704`, binding rule `:4154–4156`), `MaterializedSource-v23` (`:3302`, `:4205`), `projectionVersion=1` + nine source-free trees (`:4034`, `:4208`), D-P3-72 BLOCK/ENTITY dual-era alternates (`:561–564`, `:3055–3058`), and D-P3-73 typed metadata/interval constraints (`:578–583`, §5.1 row `:3354`). P9's receipts D-P9-22/§5.2 and D-P9-23/§4.5 consume exactly these surfaces with PRESENT_EMPTY/nonempty-alternate-only selection — identical wording on both sides. P3's receiver handoff (`:5252–5268`) demanded the P9 amendment P9 now records; no reparse, range expansion, or heuristic is introduced.
2. **Hand-light policy (P3/P6).** P9 §4.11's user→pack→true rule and `MC_OLD_HAND_LIGHT` emission claim matches current P3 D-P3-64 (`docs/phase3/v1/PHASE_3_DOC.md:2338–2341`, `:3484–3486`): explicit user, else parsed pack, else true, emitted iff true, after the single A–G-only Properties parse (the earlier IR-24-resolution-era "explicit user true only" wording was superseded by the current P3 text; P9 tracks the current rule). P6's `HeldItemSample` four-int tuple and old-mode `(max(main,off), off)` mapping (`docs/phase6/v1/PHASE_6_DOC.md:796–798`, `:808–812`) match P9 §4.11 exactly; sink methods `updateEntityId/BlockEntityId/EntityColor/updateHeldItems` exist at `:529–534`; P6 §5.1 names P7 as sole `entityColor` writer with P9 as consumer (`:1827` row "Governing `entityColor` producer", consumers "Phases 7/9").
3. **Entity/TE/color hooks (P7).** Current P7 catalog rows match P9's ledger verbatim in substance: H9-HELD-01 (`docs/phase7/v1/PHASE_7_DOC.md:1620`), H-ENTITY-02/H9-ENTITY-ID-01 both RenderManager descriptors (`:1655`), H-ENTITY-03/H9-BLOCK-ENTITY-ID-01 retargeted to `func_192854_a(TileEntity;DDDFIF)V` AROUND/finally with "no duplicate convenience-overload hook" (`:1656`) — exactly P9 §5.6's C2 amendment (D-P9-20) — and H9-COLOR-01/02 as P7-owned v0.1 OBSERVER rows with exact-operand capture (`:1662–1663`, `:1686–1691`, `:1944–1947`), matching P9 §4.13's no-second-writer stance.
4. **Draw-scope admission (P7/P8).** P7 §5.1 publishes `IdScopeAdmission` opaque, `enterEntity/enterBlockEntity → Entered(IdScopeToken) | Rejected(STALE_ADMISSION|STALE_GENERATION|WRONG_THREAD|STACK_LIMIT)`, LIFO `leave`, main-or-`ShadowExecutionBridge.validate==Valid` issuance (`:3552`, `:3580–3587`) — the exact enum and admission kinds of P9 §4.12. P8's receiving side mirrors it, including restoration-before-shadow-release and the full rejection domain (`docs/phase8/v1/PHASE_8_DOC.md:1392–1402`, `:1781`, `:1980`; D-P8-14 `:2523`). Shadow TE IDs never require a main gbuffers scope on either side.
5. **Held/reset slot and publication (P7).** `IdDynamicsFrameSlot.afterUniformFrameAccepted/resetFrame` with `Completed/HeldFeatureDisabled/Rejected/Failed` and `Reset/AlreadyNeutral` (`:2616–2626`, `:3061–3070`) supports P9 §5.1's `PerDrawDynamics` hand-off; §5.3's ten-step transaction (`:3868–3947`) publishes Phase 9 after Phase 13 (step 9), requires `IdDependentGeometryInvalidator` Completed before admission, and converges every failure off (step 10) — P9 §5.3's five-step paraphrase and §4.1/§6 failure table restate this without contradiction. All six types P7 §5.3 demands from a verified P9 artifact (`IdRuntimeBuilder/Candidate/Publisher/PublishedIdRuntime/RenderLayerLookup/PerDrawDynamics`) are exposed by P9 §5.1. `RenderLayerLookup` consumption exists in current P7 (`:1224–1231`: exact ordinal → four-value decision, absence preserves vanilla, generation stamped into the compiled product).
6. **FastTESR deferred batch (P7/P10).** P9 §4.12.1/§5.6's C3 amendment is now fully received: P7 rows H7-TE-BATCH-BEGIN/FAST/DRAW (`:1664–1666`) and D-P7-54 (`:1842–1845`, `:4473`, drain join at `:3880`); P10 rows H10-TE-BATCH-OWNER-{END,ARRAY,BULK,SAVE,RESTORE,BEGIN,RESET} on exactly P9's seven named subtargets, H10-TE-BATCH-SORT at `func_181674_a(FFF)V`, H10-TE-BATCH-RANGES in `WorldVertexBufferUploader.func_181679_a` after preDraw/before postDraw (`docs/phase10/v1/PHASE_10_DOC.md:1662–1670`), with D-P10-26 (`:1229–1239`, `:2244`) and §5.3 ledger row "P9 deferred native ranges… Coordinated adoption, unverified" (`:1839`). P9's per-quad complete-owner rule, sorted-permutation bijection, pass>0 single full-batch sort, A0…A(N−1),B0…B(N−1) adjacency, once-only Forge pre/postDraw, and primitive-ID split disclosure all have matching receiving text; P9 invents no receiver IDs beyond those now published.
7. **Alias service (P10).** P10 §5.2 consumes exactly `AliasLookup.generation()`, `mcEntity(int)`, `BlockStampResult`, one immutable lookup per task, two words, no alias re-resolution (`:1800`, `:294–296`, `:665–668`), and stamps `mc_Entity.x/y/z` from P9's packed word + metadata (`:377–379`) — bit-for-bit P9 §4.10. P10 D-P10-28 adopts schema23/MaterializedSource-v23 through P7 with "P9 alone resolves registries" (`:2246`), matching P9's role boundary.
8. **Identity spot-checks.** ShadowHookHealth `flattened-v3` 66 rows/65 non-CLOUD with RESTORE framing (`docs/phase7/v1/PHASE_7_DOC.md:1806–1810`, D-P7-74 `:4492`); TextureHookHealth `application-v2` with eight retained rows (`:1890–1894`, D-P7-69 `:4487`); P13 `phase13.parameters/v2` and the three-argument lease with `INVALID_BASE_BINDING/STALE_BASE_BINDING` (`docs/phase13/v1/PHASE_13_DOC.md:1680`, `:1693–1694`) — P9 touches neither boundary and makes no contrary claim. P9's only P4 `RegistryFingerprint/own-build-v1` mention sits inside the schema21 receipt paragraph that D-P9-22 explicitly marks historical; current composition treats P4's fingerprint (now profile-selection-v3) as opaque, which is precisely the disposition that paragraph states, and P9's own `IdRegistrySnapshot.RegistryFingerprint` (line 321) is its distinct live ID-registry identity by design.

No producing-side value, variant, or enum crosses a boundary without an explicit current consumer branch: every P9-exposed type in §5.1 maps to a current consumer row (P3 `:3354`; P6 `:1823` sink row; P7 `:3594–3608`; P8 `:1781`; P10 `:1800`), and no silent drop, no-op, or discard was found.

## 4. Corrections

None. No current Phase 9 contract defect was established: the owner is internally consistent, matches its governing RC3 assignment, and every owned path and default (absence → warn-once/neutral, legacy fallback only on pack-absent `block.properties`, DEFAULT→true hand-light, dynamic-light suppression-only) is supported by current dependency §5 text on both endpoints. P9 invents no IDs or scopes: all catalog spellings it names (H9-*, H7-TE-BATCH-*, H10-TE-BATCH-*) exist verbatim in the current owner catalogs.

## 5. Notes (optional; no contract change ordered)

1. **Historical reference pins remain unavailable (verification limit).** `reference-src/pintonium-9c2fcc1` and `reference-src/cleanroom-0.6.6-alpha` are absent from this checkout (confirmed by directory listing). P9 §0.3's correction-evidence paragraph honestly disclaims byte identity for the historical pins, and the load-bearing mechanisms are corroborated in the available trees: `putIfAbsent`, symmetric water/lava expansion and the modern-name table in `reference-src/Pintonium-main/forge122/.../VintageBlockMaterialMapping.java:58–94`, and the entity-empty retry/fallback shape in `IdMap.java`; Cleanroom 0.6.12-alpha's `RenderGlobal.java.patch` and `FastTESR.java:34–67` exist as cited. Same posture as R4 N1; owner bytes unchanged.
2. **§5.4 R9-2 gate wording is conservative, not wrong.** The "Remaining gate" cell still names "receiving-owner integration and fresh Phase 7/9 review", while the receiving-side integration has since landed (P7 D-P7-54 + H7-TE-BATCH rows; P10 D-P10-26 + H10-TE-BATCH rows), as §5.6's closing paragraph itself records. The genuinely open item — fresh owner/receiver verification — is unaffected; a future tidy-up may drop the completed half of that cell. No behavioral consequence.

## 6. §5 grant sufficiency

**Current §5 grants are sufficient.** Declared dependencies P3/P6/P7 publish every contract P9 §5.2 consumes (schema23 admission, typed selector graph, dual-era alternates, engine flags/codec, sink/value records, frame/scope tokens, reload/transaction, hook handoff, admission); the reverse dependency P10 (R10-3/D-P10-26) and the shadow-side P8 receiver close the remaining owned paths. R9-1/R9-2/R10-3 are correctly recorded as owner-designed/receiver-adopted **unverified** with fresh reviews as the implementation gate — an honest deferral, which this review does not convert into runtime or implementation clearance (none exists, per the shared authority).

## 7. Limitations

Architecture inspection only: no build, test, formatter, linter, or validation command was run; no file was edited. I did not certify native rendering behavior, hook application, Mixin feasibility, license-compliant incorporation, sibling-phase correctness, or any historical review's conclusions. Unavailable historical source trees are covered by note 1. Documentary/model checks are not native or runtime proof.

## 8. Final verdict

**PASS — 0 corrections, 2 notes, §5 impact: none.** The frozen owner document, unchanged since R4, remains fully supported by the current dependency contracts; every §5.6 coordinated amendment now has a published receiving branch, and no new correction is required.

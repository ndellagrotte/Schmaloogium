# Phase 9 — architecture review 8

**Date:** 2026-09-08
**Frozen target:** `docs/phase9/v1/PHASE_9_DOC.md` — SHA-256 `6a52c47ab5862ce199aa1d1beadaab7b99ace62f368ccd363abf3c20455df071`, recomputed at wave open and matching `docs/build/reviews/ARCHITECTURE_REVIEW_ATTEMPT_11.json` (phase 9 entry, review round 8). Gate passed; review proceeded.
**Verdict:** PASS — 0 blocking, 0 corrections, 3 notes.
**§5 impact:** none — no finding requires a change to the reviewed document's §4 semantics or §5 cross-phase interfaces, so no §G1.3 re-verification round is triggered.

---

## 1. Verification scope and method

Fresh independent whole-owner verify session per §G1.2 (refutation posture, no author context). Read in the mandated order: governing design `docs/design/v2.0-RC3/DESIGN.md` Part I §G0–§G12 (ll. 92–1109) and the Phase 9 assignment (ll. 2035–2125); `docs/research/v1/RESEARCH.md` §0–§1 (ll. 11–107, decision log D-1…D-10 at ll. 94–104) plus every RESEARCH region the reviewed document cites; dependency docs `docs/phase3/v1/PHASE_3_DOC.md`, `docs/phase6/v1/PHASE_6_DOC.md`, `docs/phase7/v1/PHASE_7_DOC.md` at their §0 current-state header receipts, §1, §5, and the §11 decision rows this document names; and the receiver-side citations of `docs/phase10/v1/PHASE_10_DOC.md` and `docs/MOVES.md` needed to test the §5.6 receiver-adoption claims. The reviewed document was read complete (1415 lines).

Not done, by boundary: no `reference-src/**` read (all Pintonium/Cleanroom/Schlorbium coordinates in the reviewed document are accepted under its declared §0.2 checkout-rename alias policy and were not independently re-derived); no other `PHASE_*_REVIEW_*.md`, no `docs/**/chatlogs/`, no root `*.txt`, no integration-review file; no build, test, Gradle, GL, capture, or network command was executed — this is documentary verification only. All line numbers below are recomputed against current bytes.

## 2. Doc gate

Every literal criterion in the assignment's **Doc gate** (`docs/design/v2.0-RC3/DESIGN.md:2114`–`:2117`) is met:

- **Every §3.7 form in the conformance map** — `docs/phase9/v1/PHASE_9_DOC.md` §3 (ll. 380–414) maps short names, namespaced names, property-matched forms, legacy `id:meta`, per-mod files, pack-over-mod precedence, first-writer-wins, metadata/property predicates, entries-before-tags, tag expansion, still↔flowing fluids, the modern→1.12 alias table, the 11300 retry, the OF-legacy numeric fallback, custom render layers, held-item IDs/lights, `dynamicHandLight`, `entityId`, `blockEntityId`, `entityColor`, per-draw refresh, the Phase 10 alias service, unknown-name degradation, and rebuild-never-mutate. Zero unmapped in-scope rows found.
- **Per-mod merge and precedence rules documented** — §4.3 (bounded per-mod acquisition, exact `assets/<modid>/shaders/{block,item,entity}.properties` paths, deterministic code-point mod ordering) and §4.4 (four-tier algebra, assign-if-absent, attributed conflict diagnostics); pack-wins cites the assignment at `docs/design/v2.0-RC3/DESIGN.md:2069`–`:2071`, whose text ("define precedence: pack wins over mod, document it") matches.
- **Alias-service interface fully specified for Phase 10** — §2.2 signatures plus §4.10's exact bit contract (`lowId = aliasedBlockId & 0xffff`, `packedWord = (highType << 16) | lowId`, metadata word), representable-domain and `Unrepresentable`/`Absent` semantics, generation equality with the Phase 10 build context, and O(1) hot-path posture. `docs/phase10/v1/PHASE_10_DOC.md` confirms consumption (D-P10-26 at :1230 and :2246; native rows :1663–1671).
- **REV1 dual-spec features with PD provenance** — the alias-table mechanism (§4.6, seeds identical to `DESIGN.md:2059`–`:2062`) and the 11300 re-parse trick (§4.7) appear as designed features carrying `[V:observed — Pintonium …]` tags in §3 and D-P9-3/D-P9-4 contract checks; the PD tag claim is explicitly rejected as 1.12.2 evidence (§3 tail ll. 412–414, §11.2 item 1).
- **Held-item/`entityColor` reference-free status acknowledged** — §3 row ll. 401 ("no Pintonium 1.12 producer inherited"), §4.13 ("Pintonium has no 1.12 producer"), D-P9-7; consistent with §G11.5 REV3's "no help available" tier (`DESIGN.md:956`–`:958`).

## 3. Conformance-map audit and anchor spot-checks

Spot-checked mapped rows against the cited text at recomputed coordinates. All verified rows are semantically faithful:

- `docs/research/v1/RESEARCH.md:448` — tag-based ID files "don't exist on 1.12.2; would need an ore-dictionary-style shim + Iris's entries-beat-tags priority rule": exactly the shim §4.7 designs and the precedence §4.4 records; the row correctly refuses PD as 1.12 tag proof.
- `docs/research/v1/RESEARCH.md:455`–`:460` (row) and `:463` (macros row) — §3.7's file forms, mod extension point, and "standard macros A–G, no option macros" all match; the no-option-macro posture is preserved by §4.11's "option-macro-free Properties parse" division of labor.
- `docs/research/v1/RESEARCH.md:1297`–`:1305` — App C.1/C.2 `mc_Entity` value "`renderType<<16 | aliased-block-id`, metadata" matches the §4.10 packing exactly.
- `docs/research/v1/RESEARCH.md:1323`–`:1328` and `:1329` — D.1 rows for `heldItemId`/`heldItemId2` and "old-hand-light mode: brighter hand wins" match §4.11's `(max(main,off), off)` old mode.
- `docs/research/v1/RESEARCH.md:1373`–`:1375`, `:1413`, `:1414` — D.4 `entityId`/`blockEntityId` rows and App E rows 13/14 (`RenderManager.func_188388_a`/`func_188391_a`; `TileEntityRendererDispatcher.func_147549_a`) support §4.12; the document correctly demotes row 14's convenience overload to historical candidate evidence with `func_192854_a` as the actual target.
- `docs/research/v1/RESEARCH.md:1369`–`:1382` — D.4 title "excluded from custom-uniform expressions" and cadence "per-draw dynamics at their hooks" support the per-draw-refresh row.
- `docs/research/v1/RESEARCH.md:1434`–`:1445` — F.1 lists `oldHandLight` · `dynamicHandLight` as tri-state engine flags; §4.11's user-over-pack resolution matches F.1's "corresponding in-game video settings win where both exist" (ll. 1447–1448).
- `docs/design/v2.0-RC3/DESIGN.md:2110`–`:2112` — "rebuilt, never mutated in place" and "unknown names degrade per G2.4 (warn once, id absent)" both cited correctly (§3 rows ll. 409–410; §6 ladder mapping uses rungs 2a/4/5 per §G2.4 REV2).
- `docs/design/v2.0-RC3/DESIGN.md:2119`–`:2121` — impl gate quoted honestly in §8.3, including the disclosure that `blockEntityId`/`entityColor`/layer/off-hand assertions are added beyond the two named uniforms.
- `docs/research/v1/RESEARCH.md:72`–`:80` (§1.2 non-goals) — the dynamic-lights non-goal citation in §1.2/§4.11 is accurate (MCPatcher row includes "dynamic lights…").

No semantic-fidelity error of the depthtex1-unit class was found. Reference-src coordinates (shaders.txt, Pintonium `.java`, Cleanroom sources) were not re-derived — out of bounds for this session; their disposition rests on the document's own §0.2 alias declaration and the wave's Phase-C re-derivation, and is recorded as such rather than verified.

## 4. Interface honesty and receiver adoption

**Consumed interfaces all exist in dependency §5s:**

- Phase 3 — `PackFrontEnd.CURRENT_SCHEMA_VERSION` = 23 (`docs/phase3/v1/PHASE_3_DOC.md:4211`–`:4213`); exact `schemaVersion == CURRENT_SCHEMA_VERSION` gating (:4258); `MaterializedSource-v23` domain (:4262); nested `IdMappingInput.schemaVersion` equality with "Phase 9 rejects a mismatch before parsing mod bytes or building aliases" (:4322); `IdMappingParser.parse(IdMappingParseRequest)` with kind/`Optional<ImmutableBytes>`/origin/`IdMappingMacroEnvironment`/`DiagnosticReporter` (:1333–1342); `MappingFileState {ABSENT, PRESENT_EMPTY, PRESENT_RULES}` and `MappingEra {CLASSIC, MODERN}` (:1391–1393); `forced11300Rules` with the D-P3-72 BLOCK/ENTITY grant and ITEM/LAYER exclusion (:1311–1313); the §5.1 ID-mapping row naming `MetadataConstraint`/`IntegerRange`/`PropertyValueConstraint`/`PropertyPredicate` and "P9 alone selects PRESENT_EMPTY/nonempty alternate … never reparses selectors" (:3411); `PackConfiguration.idMappings()` (:747); load-time `MC_OLD_HAND_LIGHT` projection (:2396, :3542); producer grants §0.62–§0.65 (ll. 539–600) and the current §G1.3 status §0.67 (ll. 621–643). P9's §5.2 Phase 3 rows and its D-P9-22 schema23 receiver receipt match P3's current surface exactly; the older numeric receipts (D-P9-16…19) are demoted to history by the document itself.
- Phase 6 — `updateEntityColor`/`updateEntityId`/`updateBlockEntityId`/`updateHeldItems` (`docs/phase6/v1/PHASE_6_DOC.md:533`–`:538`); `HeldItemSample` (:800–802) and the old-mode mapping "disabled → `(mainLight, offHandLight)`; enabled → `(max(mainLight, offHandLight), offHandLight)`" (:809–817), exactly matching P9 §4.11's "exactly matching Phase 6's binding contract" claim and its §3 citation of :800–:802 plus :811–:815; §5 rows for `UniformRuntime.events() -> UniformEventSink` (:1814), the `UniformEventSink` schema row listing Phases 7/8/9/13 (:1819), and the governing v0.1 `entityColor` producer "not deferred to Phase 9/v0.3" (:1828, :1469) — P9 §4.13's reuse posture is the consumer side of exactly this contract.
- Phase 7 — `FrameToken`/`ScopeToken`/`FrameBeginSignal` (`docs/phase7/v1/PHASE_7_DOC.md:2632`–`:2647`); `HookApplicationReport` (:2517); `ShaderReloadController` (:2951) and `UniformSignalBridge` (:3027, integration row :3700 "P7 v0.1 color/P9 v0.3 IDs unchanged"); the ten-step transaction (§4.1 :888; step placement :2440–2475 — "At step9, after P13 and P9 acceptance" confirms P9 §5.3's publish-after-textures ordering, and step1/step10 drain/off-compensation matches P9 §5.3's failure convergence); `ShadowExecutionBridge.validate(...)` returning `Valid` (:2764–:2782); H-ENTITY-02/H9-ENTITY-ID-01 (:1746) and H-ENTITY-03/H9-BLOCK-ENTITY-ID-01 (:1748) as the rows P9 augments; H9-HELD-01 at the accepted-frame boundary (:1712, :806); H9-COLOR-01/02 owner-P7 v0.1 rows (:1754–1755).

**§5.6 receiver-adoption claims verified against the receivers' current bytes:** `docs/phase7/v1/PHASE_7_DOC.md:1748` carries exactly the one-AROUND/finally `func_192854_a(Lnet/minecraft/tileentity/TileEntity;DDDFIF)V` row with "no duplicate convenience-overload hook" that P9 §5.6 describes; :1756–:1758 carry `H7-TE-BATCH-BEGIN` (`preDrawBatch()V`), `H7-TE-BATCH-FAST` (single `renderTileEntityFast` invocation, lending the current P9 ordinal), and `H7-TE-BATCH-DRAW` (`drawBatch(I)V`, sorted ranges with fresh P9 IDs) with the semantics P9 attributes to them; `docs/phase10/v1/PHASE_10_DOC.md` D-P10-26 (:1230, :2246, :1841) receives the same protocol with the seven owner subrows (:1663–1669), the sort-permutation row on `BufferBuilder.func_181674_a(FFF)V` (:1670), and the native-range row in `WorldVertexBufferUploader.func_181679_a` (:1671) — all matching P9 §4.12.1/§5.6's specification, including the seventh bulk-append subrow P9 demands. Receiver adoption is real, symmetric, and unverified-by-design ("fresh owner/receiver verification" gates everywhere); no absent API is invoked as if granted.

**Exports:** every §5.1 export is specified in §2/§4 with concrete semantics (no gestures), and its consumers are real: Phase 10 consumes `AliasLookup.mcEntity` + generation (P10 :1709, :1841); Phase 7 consumes `PublishedIdRuntime` (P7 :2710–2712 id-participant signatures); Phase 12 supplies the higher-priority decoded `oldHandLight` (P6/P7 rows consistent); Phase 2 records fingerprints only.

**Governance:** the header's RC3 adoption is recorded in `docs/MOVES.md` (:89 "Phases 3–9 deliberately adopt RC3 from their initial builds", :101, :121 "governs Phases 3–9", :184–186), so the declared governing design is the maintained truth, and the dependency set (3, 6, 7) matches `DESIGN.md:594`/§G5.1.

## 5. Scope discipline, template completeness, decisions, licensing

- **Scope discipline.** Everything in the assignment's Scope—in (ll. 2050–2092) is designed: alias resolution (§4.4–4.5), both dual-spec mechanisms (§4.6–4.7), per-mod merge (§4.3), custom layers with the Phase 7 dispatch cross-reference (§4.9), held-item tracking including both flags (§4.11), per-draw dynamics with real `entityColor` delivery and the fixed-function-observation prohibition honored (§4.12–4.13, D-P9-7), and the Phase 10 alias service (§4.10). Scope—out (vertex stamping, uniform upload, grammar parsing) is explicitly assigned in §1.2. The one apparent extension — generalizing the entity-only 11300 bridge to BLOCK — is not silent scope creep: it is recorded as correction C1/D-P9-19, and the producer grant exists on the dependency side (`docs/phase3/v1/PHASE_3_DOC.md:566`–`:581`, "resolves the producer gap identified by P9 review 1 C1", schema 22, superseded by D-P3-73's schema 23), i.e. the §G1.1 flag-then-request path was followed.
- **Template completeness.** All thirteen §G9 sections (`DESIGN.md:794`–`:827`) present and substantive; §10 correctly carries no spike since §G5.1/§G10 assign Phase 9 no OQ; §9 milestone tags are exclusive and the final row is data growth, not deferred rewrite (§G0.3/§G4.3 posture).
- **Decisions D-1…D-10.** No contradiction: D-1 (Cleanroom-only glue), D-2 (dynamic lights stay a non-goal with suppression-only interop), D-5 (Mixin hooks, dumb forwarders, policy in glue/engine), D-6 (`:engine` MC-free; D-6 forbidden-type test in §8.2), D-7/D-8 (§0.3 licensing posture), D-9 (n/a, no GL redesign), D-10 (§8.3 T0/T1 harness gate, no pack images committed). §11.2 records four input contradictions with rulings; none silently overturns RESEARCH.
- **Licensing/Pintonium compliance.** Every PD-derived conformance row carries `[V:observed — Pintonium <path>]`; contract-visible adoptions (putIfAbsent mechanics, alias table, 11300 trick, fallback re-derivation) each have a D-P9 decision with a contract check per §G11.4; the §G11.5 "reuse structure, re-derive values" tier is honored (live-registry re-derivation instead of Pintonium's curated fallback, D-P9-6); the AGPL transformation boundary was neither followed nor used (§0.2 import discipline, §0.3); stubbed/dead PD §17 features (held-item, `blockEntityId` producers) are not inherited as producers. Historical dependency-PASS citations in §0 are framed as "at initial authorship" provenance, not current claims; current-state receipts (schema23/MaterializedSource-v23, projectionVersion=1 + nine source-free trees) match the known current identities.

## 6. Findings and verdict

Findings, each with location / claim / evidence / severity:

1. **N8-1 (note) — §0.1 declared RESEARCH reading ranges are off by one line at two points.**
   Location: `docs/phase9/v1/PHASE_9_DOC.md:44` (declares "§3.7, lines 453–462") and `:47`–`:48` (declares "Appendix D.4, lines 1369–1382").
   Claim/evidence: §3.7 actually spans `docs/research/v1/RESEARCH.md:454`–`:463` (the closing "standard macros A–G, no option macros" sentence, which the document itself relies on, is at :463, outside the declared range), and D.4's content runs through :1383. The document's own functional anchors (`:463`, `:1370`, `:1382`) cited in §3 are correct, so no semantic claim is affected.
   Severity: note — a fix-up may widen the two declared ranges; no contract surface changes.

2. **N8-2 (note) — the assignment's PD §8.2 `blockEntityId` REV1 warning has no explicit disposition row.**
   Location: `docs/phase9/v1/PHASE_9_DOC.md` §3 (ll. 405), §4.12 (ll. 784–797), §11.2 (ll. 1334–1348).
   Claim/evidence: `docs/design/v2.0-RC3/DESIGN.md:2084`–`:2089` warns that blockEntityId stamping "has no 1.12.2 caller in the reference". The substance is answered — §4.12's "Actual dispatch coverage" re-derives the real vanilla caller chain (`func_180546_a` → `func_192854_a`, normal/global/damaged/direct) rather than inheriting the reference gap — and held-item ("no Pintonium 1.12 producer inherited", §3 ll. 401) and `entityColor` ("Pintonium has no 1.12 producer", §4.13 ll. 871) each carry an explicit acknowledgment, but `blockEntityId` has no parallel sentence. Adding one clause to §11.2 would make the acknowledgment uniform.
   Severity: note — doc-gate literally requires only the held-item/`entityColor` acknowledgment, which is present; this is a uniformity suggestion.

3. **N8-3 (note) — superseded P4 identity token inside a demoted historical receipt.**
   Location: `docs/phase9/v1/PHASE_9_DOC.md:976` (D-P9-17 block): "P4's `RegistryFingerprint/own-build-v1`".
   Claim/evidence: the containing schema20 receipt is expressly "history only" per D-P9-18 (ll. 967–980) and D-P9-22 (ll. 1331), so the token is not cited as current — the current-identity rule is not violated, and Phase 4 is not a declared dependency. A "(historical)" qualifier on the token would prevent any misreading now that the current P4 identity generation is `profile-selection-v3` (cf. `docs/phase7/v1/PHASE_7_DOC.md:4756`).
   Severity: note.

No blocking and no correction-level finding was raised: the doc gate is literally met, the conformance map is faithful at every checked anchor, all consumed interfaces exist on the producers' current §5 surfaces, the §5.6 receiver-adoption claims are true against current P7/P10 bytes, scope and template are complete, D-1…D-10 stand, and the licensing posture is compliant. Consequently the §5-impact statement in the header applies (no re-verification trigger) and no Resolutions section follows this review.

No build, test, Gradle, GL, capture, or network evidence was used or executed in this review.

# Phase 9 — architecture review 7

**Date:** 2026-09-08 · **Frozen document:** `docs/phase9/v1/PHASE_9_DOC.md` (1415 lines), SHA-256 `f1a18d4281efd364b2e60096306d1418308ddced66f526a14d4fbddae3724593` — recomputed at review start, exact match, gate passed. **Verdict: PASS-WITH-CORRECTIONS — 3 corrections, 2 notes, 0 blocking.** §5 impact: all three corrections are citation-coordinate (line-pin) fixes inside §3/§5/§11 prose; every §5 contract semantic, including the C2 receiver-adopted claims against current P7/P10 bytes, was independently re-verified TRUE and is unchanged by the fixes — no interface change, no re-derivation of receiver adoption, no re-opened §5 verification beyond the pin fix-up itself. No build, test, verify loop, gradle, GL, or network fetch was executed; this is document verification only (web URLs already recorded in the frozen doc were not re-fetched).

---

## 1. Scope, identity and authority actually verified

- **Hash gate:** `sha256sum docs/phase9/v1/PHASE_9_DOC.md` → `f1a18d4281efd364b2e60096306d1418308ddced66f526a14d4fbddae3724593`, identical to the frozen hash. Line count 1415 as declared.
- **Read completely:** `docs/phase9/v1/PHASE_9_DOC.md` lines 1–1415 (all sections §0–§12).
- **Governing design:** `docs/design/v2.0-RC3/DESIGN.md` Part I §G0–§G12 (lines 92–1109, read in full) and the Phase 9 assignment at lines 2035–2125 (line 2126 is the `---` separator; the doc's "2035–2125" pin is exact). Phase/dependency table region 580–608 confirmed as claimed in doc §0.1.
- **RESEARCH.md** (`docs/research/v1/RESEARCH.md`, 1587 lines): mandated window §0–§1 lines 11–107 (D-1…D-10 at :95–:104), plus the cited windows :438–:462 (§3.6.8/§3.7), :600–:618 (§4.7), :1277–:1305 (App C.2), :1318–:1337 (App D.1), :1369–:1383 (App D.4), :1387–:1414 (App E rows 13–14), :1424–:1448 (App E.2/F.1 policy flags).
- **Dependency docs (current bytes):** P3 `docs/phase3/v1/PHASE_3_DOC.md` (§0.65/D-P3-72/D-P3-73 regions, §4.9/§5 contract rows, `PackFrontEnd.CURRENT_SCHEMA_VERSION = 23` at :723, `MaterializedSource-v23` at :3328/:4231, `SelectorKind{ENTRY,TAG}`/`MappingEra{CLASSIC,MODERN}` at :1361–:1362, `forced11300Rules` at :1280, era semantics at :3052–:3054, BLOCK selection law at :3081–:3084, receiver hand-offs at :5269–:5298); P6 `docs/phase6/v1/PHASE_6_DOC.md` (:525–:537 `UniformEventSink` incl. `updateEntityId/updateBlockEntityId/updateEntityColor/updateHeldItems`, :472 `events()`, :798–:800 `HeldItemSample`, :807–:815 old-mode tuple law); P7 `docs/phase7/v1/PHASE_7_DOC.md` (:1726–:1737 hook rows, :1913/:4547 D-P7-54, :2930/:3674 `ShaderReloadController`, :2986/:3678 `HookApplicationReport`, :3006/:3679 `UniformSignalBridge`, :3667–:3668 `FrameToken`/`FrameBeginSignal`, :3675 `IdDependentGeometryInvalidator`, :390 ten-step transaction); P10 `docs/phase10/v1/PHASE_10_DOC.md` (:1230/:1841/:2246 D-P10-26, :1663–:1671 `H10-TE-BATCH-*` rows, :1700/:2425 invalidator implementation, :1801 `AliasLookup` consumer row, :223 P9 ownership row).
- **PD:** `docs/reference/pintonium/v1.0/PINTONIUM_DESIGN.md` §8 heading map — §8 spans 490–537; the doc's §0.1 pin "§8, lines 490–535" covers the entire section content (536 blank, 537 separator), satisfying the spec's "PD §8 (whole)".
- **Identity discipline re-verified against current producers:** schema23/`MaterializedSource-v23` (P3 :723/:3328/:4224–:4234) matches doc §5.2 D-P9-22; `projectionVersion=1` + nine source-free trees (P3 :583/:4234) matches doc §4.7/:962; CLASSIC/MODERN alternate-era semantics (P3 :3052–:3054) matches doc §4.6/:593–:595; D-P9-23's typed selector receipt matches P3 D-P3-73 (:578–:583, :5096, :3380); D-P9-19/D-P9-22's historical numeric receipts are correctly framed as history. No foreign identity string (profile-selection-v3, flattened-v3 counts, capture-plan/run-manifest, phase13 codecs) is claimed by this doc.
- **Out-of-bounds by review constraint (not re-verified, per §G1.2/hard-boundary rules):** everything under `reference-src/**` (schlorbium/Pintonium/Cleanroom pinned ranges), `docs/**/chatlogs/`, root `*.txt`, other `PHASE_*_REVIEW_*.md` (so the historical PASS receipts cited at doc :28–:33 were not re-opened), and `docs/build/**`.

## 2. Scope and §5 audit

- **Doc gate (spec :2114–:2117): met.** Every §3.7 form appears as a conformance-map row (files/forms :384–:388, per-mod merge :389, precedence :390–:393, tags :394, fluids/modern alias :395–:396, 11300 retry :397, fallback :398, layers :399–:400, held :401–:403, per-draw :404–:407, P10 service :408, degradation/rebuild :409–:410) — zero unmapped in-scope rows. Alias-service interface fully specified (§4.10 bit-exact `mcEntity`, §5.1 exact Phase 10 contract). Dual-spec mechanisms carry PD provenance tags (`[V:observed — Pintonium …]`) and recorded decisions (D-P9-3/D-P9-4). Held-item/`entityColor` reference-free status acknowledged (:401, :406; PD §8.2 gaps at PD :528–:531).
- **Interface honesty: verified against current dependency §5s.** Everything §5.2 consumes exists: P3 schema23 admission, typed selector constraints, `IdMappingParser` mod-byte acceptance with `MappingOrigin` (P3 :1841), `EngineOptionData`/`ShaderPropertiesModel.engineFlags` surfaces; P6 sink methods and `HeldItemSample` tuple; P7 tokens/bridges/controller/report/invalidator. Everything §5.1 promises is consumed downstream: P10 :1801 mirrors the `AliasLookup.generation()`/`mcEntity`/`BlockStampResult` contract verbatim; P7 :3678 carries the three P9 rows and two P7 color rows with stable H9 spellings exactly as §4.13 claims. The §5.6 coordinated amendments are genuinely receiver-adopted: P7 :1727 (H-ENTITY-03 lower dispatch, "no duplicate convenience-overload hook"), P7 :1735–:1737 (`H7-TE-BATCH-BEGIN/FAST/DRAW`), P7 D-P7-54 (:1913, :4547), P10 D-P10-26 with the seven owner subrows plus SORT/RANGES (:1663–:1671) whose targets and semantics match §5.6's enumeration one-for-one.
- **Scope discipline: clean.** Scope—out items (vertex stamping, upload mechanics, grammar parsing) are each assigned to their owners in §1.2 and nowhere designed here (§4.10 computes only; §4.11 routes through the P6 sink; D-P9-23 forbids reparse). No Scope—in item dropped; the BLOCK extension of the 11300 mechanism is the D-P3-72 producer grant (P3 :561, :3081–:3084) that the spec's modern-`grass` special case requires, correctly received rather than invented.
- **Template completeness:** all thirteen §G9 sections present and substantive; no assigned OQ (spec :2037 "OQs: —"), so no spike specs required (§10 correctly states none).

## 3. Required corrections

### C7-1 — P7 receiver-row line pins are stale; cited coordinates contain unrelated content (severity: correction)

- **Location / claim:** `docs/phase9/v1/PHASE_9_DOC.md` §5.4 R9-2 row (:1058), §5.6 preamble (:1073–:1074), §5.6 H-ENTITY-03 bullet (:1078), §5.6 batch-rows bullet (:1085), and §11.4 item 3 (:1371–:1372) all cite `PHASE_7_DOC.md:1710` for the H-ENTITY-03 lower-dispatch row and `:1718`–`:1720` for the `H7-TE-BATCH-*` rows.
- **Evidence:** in current `docs/phase7/v1/PHASE_7_DOC.md` the H-ENTITY-03 row is at **:1727** and `H7-TE-BATCH-BEGIN/FAST/DRAW` are at **:1735/:1736/:1737**. Line :1710 is the H-SKY-02 celestial-redirect row; :1718–:1720 are the `func_174982_a`/U7-1 note — neither cited range contains the claimed content. The *semantic* claims were independently re-verified true at the correct coordinates: :1727 carries one AROUND/finally on `func_192854_a(Lnet/minecraft/tileentity/TileEntity;DDDFIF)V` with the exact "no duplicate convenience-overload hook" phrasing and the "not a fixed runtime call count" health clause; :1735–:1737 carry BEGIN on `preDrawBatch()V`, FAST around the single `renderTileEntityFast` invocation, DRAW around `drawBatch(I)V`, each "one application", with the borrow/ordinal/admission semantics §5.6 restates.
- **Failure path:** a verification session following the stated pins lands on sky/terrain rows, concludes the receiver rows are absent, and falsely fails the receiver-adopted claim — the exact failure the wave's anchor re-derivation was meant to preclude.
- **Owner + receivers:** owner Phase 9 doc; no receiver change (P7/P10 bytes are correct and untouched).
- **Minimal resolution:** repoint the five sites to `PHASE_7_DOC.md:1727` and `:1735`–`:1737` (P10 `D-P10-26` pointer is correct as written). No wording change beyond the coordinates.

### C7-2 — conformance row :402: both citation pins miss the claimed content (severity: correction)

- **Location / claim:** §3 row for `heldBlockLightValue`/`2` (:402) cites `docs/research/v1/RESEARCH.md:1327`–`:1328` and `docs/phase6/v1/PHASE_6_DOC.md:796`–`:798` for "static held-state light and exact Phase 6 tuple".
- **Evidence:** the RESEARCH `heldBlockLightValue` row is at **:1329** (:1327 is the D.1 table separator, :1328 the `heldItemId` row — that pin belongs to row :401). The P6 `HeldItemSample` record spans **:798–:800** (:796–:797 are `BlendSample`'s tail lines; the four-int tuple fields sit on :799–:800). The companion pin `:809`–`:813` is correct — the brighter-hand-wins construction law is at P6 :811–:813 — so only the first pin of each pair drifts.
- **Failure path:** a reader auditing "exact Phase 6 tuple" against :796–:798 sees `srcRgb/dstRgb/srcAlpha/dstAlpha` and the record header only, and cannot confirm the tuple; the RESEARCH pin surfaces the wrong uniform's row.
- **Owner + receivers:** owner Phase 9 doc; none.
- **Minimal resolution:** repoint RESEARCH to `:1329` (or `:1328`–`:1329` if the heldItemId context is wanted) and P6 to `:798`–`:800`.

### C7-3 — conformance row :388: A–G macro pin off by one (severity: correction)

- **Location / claim:** §3 row "standard A–G macros, no option macros" (:388) cites `docs/research/v1/RESEARCH.md:460`–`:462`.
- **Evidence:** the macro sentence — "All these files are macro-preprocessed (standard macros A–G, no option macros)" — is at RESEARCH **:463**; :460–:462 carry the mod-extension and custom-render-layer sentences (already correctly cited by rows :384/:399).
- **Failure path:** the pinned range does not contain the contract basis for the row's claim; an auditor would mark the row unevidenced.
- **Owner + receivers:** owner Phase 9 doc; none.
- **Minimal resolution:** repoint to `:463` (or extend row :384's `:455`–`:460` style to `:456`–`:463` on this row).

## 4. Notes

1. **§5.3 step count (no action required):** the section calls P7's transaction "ten-step" and then lists five P9-relevant steps. P7 genuinely has a ten-step transaction (`PHASE_7_DOC.md:390`, D-P7-26 at :4518), the condensation says "follow Phase 7", and P10's step-9 wording (:1310–:1311) matches §5.3's publish-after-textures ordering — honest, but a reader may miscount the excerpt as the whole transaction.
2. **Unverifiable-by-constraint pins:** all `reference-src/**` ranges (schlorbium doc pins, Pintonium file pins, Cleanroom registry/FML pins) and the historical dependency PASS receipts (:28–:33) sit outside this review's permitted reads. The C3 checkout-rename ruling (§0.2 :118–:124) explicitly declares those line numbers historical readings that are "not re-derived here", and §0.3 (:155) disclaims byte-identity for the 0.6.6-alpha paths — so the doc makes no claim this reviewer could not accept on its own terms. Wave items verified within bounds: C1 RESEARCH repoints :448/:1413/:1414 exact (tag-shim+entries-beat-tags rule at :448; App E rows 13/14 at :1413/:1414); C2 semantics true (see §2); C3 internally consistent and licensing-clean; C4 consistent (see §5).

## 5. Areas audited and found sound

- **C4 exception sentence (§4.5 :533–:536) vs §4.6 mapping semantics: consistent and correctly scoped.** §4.6 :589–:598 states the ambiguous tokens (`minecraft:grass`, `minecraft:redstone_lamp`) retain exact 1.12 meaning unless P3 supplies `MappingEra.MODERN` provenance, mapping grass→tallgrass and lamp→lit block; the §4.5 sentence states the same rule as a per-selector replacement of exact lookup, keeps the CLASSIC/neutral path exact, and preserves exact-before-expansion for every other selector — no scope leak, no second ambiguity source. The sole-producer constraint (§4.6 :593–:595, §4.7 :631–:636) matches P3's current law (:3052–:3054 CLASSIC/MODERN split; :3081–:3084 PRESENT_EMPTY+nonempty-alternate-only selection) exactly.
- **C3 checkout-rename alias ruling (§0.2 :118–:124): sound.** It covers every occurrence class it must (§0.1 input list, §3 provenance tags, §4.2/§4.3/§4.14 citations), declares the old tree names aliases of the current `Pintonium-main/`/`Cleanroom-0.6.12-alpha/` checkouts, preserves historical line numbers without re-mining, and is corroborated by §0.3's separate, current-tree 0.6.12-alpha evidence (RenderGlobal/dispatcher patches, `FastTESR.java:34–67`) with the explicit "not claimed recovered or byte-identical" disclaimer. Consistent with DESIGN G0.2's alias-table framing; no licensing posture change (no new LGPL content adopted; new platform evidence is patch-behavioral + web-mirror with recorded URLs).
- **Conformance-map semantic fidelity (in-bounds rows):** pack-wins (DESIGN :2069–:2071), rebuild/unknown-names (DESIGN :2110–:2112), impl-gate (DESIGN :2119–:2121), §3.7 forms (RESEARCH :456–:460), entries-beat-tags/tag-shim (:448), per-draw cadence (:1369–:1383), held IDs (:1323–:1328 → :1328), P10 bit layout (:1297–:1305 vs §4.10's exact `renderType<<16 | lowId` and 16-bit domain), entityId/blockEntityId rows (:1373–:1375, :1413/:1414) — all semantically faithful to the cited text.
- **§4.12/§4.12.1 hook design:** H9-ENTITY-ID dual-entry HEAD/RETURN with LIFO tokens and frame-drain backstop; H9-BLOCK-ENTITY-ID on the actual lower dispatch `func_192854_a(TileEntity,DDDFIF)V` with App E row 14 explicitly demoted to historical candidate (:796–:797); the FastTESR deferred route (owner sidecar, observed `func_181674_a(FFF)V` permutation, contiguous homogeneous ranges, fresh admission per range, primitive-ID boundary disclosure) is coherent, failure-contained per §6 rung 4, and its §5.6 receiver specification matches current P7/P10 rows one-for-one.
- **§4.13 entityColor:** P7-owned v0.1 producer with operand capture at the `GL_TEXTURE_ENV_COLOR` invocation, no TexEnv state query, no second writer — matches P7 :1733–:1734 and PD §8.2's gap warning; the RC3 Phase 6 full-inventory reconciliation is recorded as ruling 3 in §11.2.
- **D-1…D-10 / §G11 discipline:** no decision-log contradiction found (dynamic lights remain a non-goal with suppression-only disposition; D-5/D-6 respected by the §1.3 seam and dumb-mixin rules); every Pintonium adoption carries provenance and a contract-checked decision (D-P9-2/3/4/6/7); PD's "working in production" tag claim is properly rejected for 1.12.2 (:412–:414) with the §11.4 item-4 upstream request; AGPL transformation material explicitly untouched (:126–:137).
- **§6/§7/§8/§9:** G2.4 ladder mapping uses only defined rungs (2a/2/3/4/5 + deliberate fallback); threading model (render-thread publication/scopes, off-thread build) matches §G2.3; §8's test plan covers every §4 mechanism including the two-renderer native-range assertion and P3-origin typed-range cases; §9 milestone tags are complete with the final row correctly framed as data growth.

## 6. Verdict

**PASS-WITH-CORRECTIONS** — 3 corrections (C7-1 stale P7 pins across five sites; C7-2 dual-pin drift in conformance row :402; C7-3 off-by-one macro pin in row :388), 2 notes, 0 blocking. All findings are anchor-hygiene; the wave's four change classes verify as semantically correct (C1 four of five repoints exact; C2 receiver-adopted claims true against current P7/P10 bytes with D-P7-54/D-P10-26 confirmed; C3 and C4 fully consistent). No structural defect; no §5 contract semantic change required. Per §G1.3 the pin fix-up does not alter cross-phase interface content, so the corrected doc closes without re-verification of §5 semantics. No build, test, verify loop, gradle, GL, or network fetch was executed.

## Resolutions

2026-09-08 fix-up (attempt-10 wave):

Doc bytes drifted post-freeze (SHA-256 now `ea72e865…e7dae` vs frozen `f1a18d42…4593`; 1415 lines unchanged — pin-digit swaps only). Corrections were found already materialized in current bytes and were re-verified against current producer bytes rather than re-edited; exhaustive greps show zero residual stale pins.

- **C7-1:** applied — all five named sites (§5.4 R9-2 row, §5.6 preamble, §5.6 H-ENTITY-03 bullet, §5.6 batch-rows bullet, §11.4 item 3; current doc :1058, :1073–:1074, :1078, :1085, :1371–:1372) cite `PHASE_7_DOC.md:1727` and `:1735`–`:1737`; coordinates re-derived in current P7 bytes (:1710 = H-SKY-02 celestial redirect, :1717–:1719 = `func_174982_a`/U7-1 note; :1727 = `func_192854_a(Lnet/minecraft/tileentity/TileEntity;DDDFIF)V` AROUND/finally with "no duplicate convenience-overload hook" and "not a fixed runtime call count"; :1735–:1737 = `preDrawBatch()V` / single `renderTileEntityFast` / `drawBatch(I)V`). P10 D-P10-26 pointer untouched as prescribed.
- **C7-2:** applied — row :402 cites `RESEARCH.md:1329` and `PHASE_6_DOC.md:798`–`:800` (verified: RESEARCH :1327 = D.1 separator, :1328 = `heldItemId` row, :1329 = `heldBlockLightValue` row; P6 `HeldItemSample` four-int tuple at :799–:800); companion `:809`–`:813` pin unchanged and correct (brighter-hand-wins law at :811–:813). Row :401's `:1323`–`:1328` left as-is (correctly belongs to `heldItemId`).
- **C7-3:** applied — row :388 cites `RESEARCH.md:463` (verified: "All these files are macro-preprocessed (standard macros A–G, no option macros)" at :463; :460–:462 carry the mod-extension/custom-render-layer sentences). Row :384's `:455`–`:460` left as-is per review.
- **Note 1:** note — accepted; §5.3's five-step P9-relevant excerpt with "follow Phase 7" is an honest condensation of P7's ten-step transaction; no action.
- **Note 2:** note — accepted; out-of-bounds reference-src/historical pins stand on their own recorded terms (§0.2 :118–:124 alias ruling, §0.3 :155 byte-identity disclaimer).
- **Sweep 1 (doc :115 `verification/targets/phase-8.json`):** note — accepted recommended disposition: provenance-only §0.2 cite of an input actually inspected before Phase 9 scope was known (verification/ tree retired and deleted 2026-08-08 per docs/MOVES.md); left untouched — a rewrite would falsify the record of what the build session read.
